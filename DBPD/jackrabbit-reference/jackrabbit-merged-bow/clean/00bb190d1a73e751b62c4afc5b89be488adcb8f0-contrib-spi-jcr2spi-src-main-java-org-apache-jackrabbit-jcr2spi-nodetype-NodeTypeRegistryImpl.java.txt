JCR-1150: use "Integer.valueOf" instead of "new Integer", uses entry sets instead of key sets when value is needed as well
 

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@580332 13f79535-47bb-0310-9956-ffa450edef68

-        Iterator it = defMap.keySet().iterator();
-        while (it.hasNext()) {
-            QNodeTypeDefinition ntd = (QNodeTypeDefinition)it.next();
-            internalRegister(ntd, (EffectiveNodeTypeImpl)defMap.get(ntd));
+        for (Iterator it = defMap.entrySet().iterator(); it.hasNext(); ) {
+            Map.Entry entry = (Map.Entry)it.next();
+            QNodeTypeDefinition ntd = (QNodeTypeDefinition)entry.getKey();
+            internalRegister(ntd, (EffectiveNodeTypeImpl)entry.getValue());

INS24 INS58 MOV32 MOV8 MOV43 INS59 INS60 MOV42 MOV32 INS43 MOV43 INS59 INS40 UPD42 INS42 INS11 UPD42 INS43 MOV43 INS32 INS40 INS42 INS42 UPD42 UPD42 DEL42 DEL59 DEL60 DEL61