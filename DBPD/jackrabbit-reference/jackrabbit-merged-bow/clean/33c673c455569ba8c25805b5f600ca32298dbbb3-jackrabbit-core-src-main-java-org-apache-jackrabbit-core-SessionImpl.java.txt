JCR-2084 DataStore: garbage collection fails if a workspace is not initialized

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@769319 13f79535-47bb-0310-9956-ffa450edef68

-            wspInfo.getSystemSession();
+            // the workspace could be disposed again, so re-initialize if required
+            // afterwards it will not be disposed because a session is registered
+            wspInfo.initialize();

MOV21 UPD42