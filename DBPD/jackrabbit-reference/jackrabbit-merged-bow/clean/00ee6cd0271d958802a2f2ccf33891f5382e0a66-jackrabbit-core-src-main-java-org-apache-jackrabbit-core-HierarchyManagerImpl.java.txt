JCR-2087 Upgrade to Java 5 as the base platform

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@785116 13f79535-47bb-0310-9956-ffa450edef68

-import java.util.Iterator;
-    protected Set getParentIds(ItemState state, boolean useOverlayed) {
+    protected Set<NodeId> getParentIds(ItemState state, boolean useOverlayed) {
-            Set s = ns.getSharedSet();
+            Set<NodeId> s = ns.getSharedSet();
-            LinkedHashSet s = new LinkedHashSet();
+            LinkedHashSet<NodeId> s = new LinkedHashSet<NodeId>();
-        return Collections.EMPTY_SET;
+        return Collections.emptySet();
-            Set parentIds = getParentIds(state, false);
+            Set<NodeId> parentIds = getParentIds(state, false);
-                Set grandparentIds = new LinkedHashSet();
-                Iterator iter = parentIds.iterator();
-                while (iter.hasNext()) {
-                    NodeId parentId = (NodeId) iter.next();
+                Set<NodeId> grandparentIds = new LinkedHashSet<NodeId>();
+                for (NodeId parentId : parentIds) {
-            Set parentIds = getParentIds(state, true);
+            Set<NodeId> parentIds = getParentIds(state, true);
-                Set grandparentIds = new LinkedHashSet();
-                Iterator iter = parentIds.iterator();
-                while (iter.hasNext()) {
-                    NodeId parentId = (NodeId) iter.next();
+                Set<NodeId> grandparentIds = new LinkedHashSet<NodeId>();
+                for (NodeId parentId : parentIds) {

INS74 MOV43 INS43 INS42 INS32 INS42 INS42 INS74 INS74 INS74 INS74 MOV43 INS43 MOV43 INS43 MOV43 INS43 INS70 MOV43 INS43 INS70 INS42 INS42 INS74 INS42 INS42 INS74 INS44 INS42 MOV8 INS42 INS74 INS44 INS42 MOV8 MOV43 INS43 MOV43 MOV43 MOV43 INS42 MOV43 INS43 MOV43 INS42 INS42 UPD42 MOV42 INS74 INS42 INS74 MOV43 INS43 MOV43 MOV43 INS42 DEL40 DEL26 DEL40 DEL42 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL61 DEL42 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL61