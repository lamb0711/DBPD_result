JCR-2157 SQL2 parser: Support CAST

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@785589 13f79535-47bb-0310-9956-ffa450edef68

-            // TODO getBinary
-            return valueFactory.createValue(value.getString());
+            return valueFactory.createValue(value.getBinary());
-            // TODO getDate
-            return valueFactory.createValue(value.getString());
+            return valueFactory.createValue(value.getDate());
-            // TODO getLong
-            return valueFactory.createValue(value.getString());
+            return valueFactory.createValue(value.getLong());
-            // TODO getDouble
-            return valueFactory.createValue(value.getString());
+            return valueFactory.createValue(value.getDouble());
-            // TODO getDecimal
-            return valueFactory.createValue(value.getString());
+            return valueFactory.createValue(value.getDecimal());

MOV25 MOV32 MOV8 INS25 MOV32 MOV8 MOV25 UPD42 INS8 UPD42 INS41 UPD42 INS32 UPD42 INS42 INS42 INS32 INS42 INS42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL41 DEL8 DEL25