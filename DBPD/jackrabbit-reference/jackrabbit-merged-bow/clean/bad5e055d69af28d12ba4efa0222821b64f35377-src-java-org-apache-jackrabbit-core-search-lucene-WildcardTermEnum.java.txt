Upgraded to Lucene 1.4.3 and implemented order by in queries

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@148868 13f79535-47bb-0310-9956-ffa450edef68

-        super(reader, term);
-        // FIXME optimize term enum. find start term text
-        setEnum(reader.terms(new Term(term.field(), "")));
+        int idx = 0;
+        while (idx < term.text().length()
+                && Character.isLetterOrDigit(term.text().charAt(idx))) {
+            idx++;
+        }
+        // because IndexReader.terms() starts with the term after the given
+        // one start with idx - 1
+        if (idx > 0) {
+            idx--;
+        }
+        setEnum(reader.terms(new Term(term.field(), term.text().substring(0, idx))));

INS60 INS61 INS25 INS39 INS59 INS27 INS8 INS27 INS8 INS42 INS34 INS27 INS32 INS21 INS42 INS34 INS21 INS42 INS32 INS42 INS42 INS32 INS37 INS37 INS32 INS42 INS32 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS32 INS42 INS34 INS42 INS42 INS42 DEL42 DEL42 DEL46 DEL45