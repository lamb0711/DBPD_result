Use object arrays internally for better type safety (JCR-54)
 - suggested by Angela Schreiber

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@158757 13f79535-47bb-0310-9956-ffa450edef68

-import java.util.Collection;
-import java.util.Iterator;
-        Collection types =
+        String[] types =
-
-            Iterator iterator = types.iterator();
-            while (iterator.hasNext()) {
-                vector.add(fromJCRName((String) iterator.next()));
+            for (int i = 0; i < types.length; i++) {
+                vector.add(fromJCRName(types[i]));
-
-        Iterator iterator = getChildElements(PROPERTYDEF_ELEMENT);
-        while (iterator.hasNext()) {
+        Element[] elements = getChildElements(PROPERTYDEF_ELEMENT);
+        for (int i = 0; i < elements.length; i++) {
-            Element element = (Element) iterator.next();
-            PropDefFormat format =
-                new PropDefFormat(getNamespaceResolver(), element, property);
+            PropDefFormat format = new PropDefFormat(
+                    getNamespaceResolver(), elements[i], property);
-        Iterator iterator = getChildElements(CHILDNODEDEF_ELEMENT);
-        while (iterator.hasNext()) {
+        Element[] elements = getChildElements(CHILDNODEDEF_ELEMENT);
+        for (int i = 0; i < elements.length; i++) {
-            Element element = (Element) iterator.next();
-                new NodeDefFormat(getNamespaceResolver(), element, node);
+                new NodeDefFormat(getNamespaceResolver(), elements[i], node);

INS24 INS24 INS5 INS5 INS58 INS27 INS37 MOV8 INS5 INS58 INS27 INS37 MOV8 MOV43 INS85 INS24 MOV43 INS85 UPD42 INS39 INS59 INS42 INS40 INS42 MOV43 INS85 UPD42 INS39 INS59 INS42 INS40 INS42 INS58 INS27 INS37 INS8 UPD42 MOV42 INS42 INS34 MOV43 UPD42 MOV42 INS42 INS34 MOV43 INS39 INS59 INS42 INS40 INS42 MOV21 UPD42 MOV42 INS34 MOV43 INS2 MOV43 INS2 INS42 INS42 INS42 INS42 INS2 INS42 INS42 DEL40 DEL26 DEL40 DEL26 DEL43 DEL42 DEL42 DEL32 DEL11 DEL42 DEL43 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL8 DEL61 DEL43 DEL42 DEL42 DEL43 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL42 DEL42 DEL42 DEL32 DEL61 DEL43 DEL42 DEL42 DEL43 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL42 DEL42 DEL42 DEL32 DEL61