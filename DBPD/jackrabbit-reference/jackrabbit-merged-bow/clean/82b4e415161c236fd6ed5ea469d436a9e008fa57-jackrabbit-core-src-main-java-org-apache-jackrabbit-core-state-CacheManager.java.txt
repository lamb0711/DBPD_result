JCR-1742 CacheManager resizeAll is slow

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@696672 13f79535-47bb-0310-9956-ffa450edef68

-        log.debug("resizeAll size=" + caches.size());
+        if (log.isDebugEnabled()) {
+            log.debug("resizeAll size=" + caches.size());
+        }
-            log.debug(cache + " now:" + cache.getMaxMemorySize() + " used:"
-                    + info.getMemoryUsed() + " access:" + info.getAccessCount()
-                    + " new:" + info.getMemory());
+            if (log.isDebugEnabled()) {
+                log.debug(cache + " now:" + cache.getMaxMemorySize() + " used:"
+                        + info.getMemoryUsed() + " access:" + info.getAccessCount()
+                        + " new:" + info.getMemory());
+            }

INS25 INS32 INS8 INS42 INS42 MOV21 INS25 INS32 INS8 INS42 INS42 MOV21