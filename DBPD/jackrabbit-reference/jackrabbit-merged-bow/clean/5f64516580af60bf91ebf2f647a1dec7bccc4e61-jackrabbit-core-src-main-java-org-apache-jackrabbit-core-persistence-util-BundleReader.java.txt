JCR-2762: Optimize bundle serialization

Add common names used by the access control features. Reorder names by popularity.

Rename read/writeInt() to read/writeVarInt() as suggested by Thomas. Remove recursion from writeVarInt().

Minor optimizations in read/writeName(), including a fix to the case where the local name is more than 15 characters long.

Extend the use of read/writeVarInt() and read/writeString() to more places in the code.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1022488 13f79535-47bb-0310-9956-ffa450edef68

-            bundle.setModCount((short) readInt());
+            bundle.setModCount((short) readVarInt());
-                    count = readInt() + 0x0f - 1;
+                    count = readVarInt() + 0x0f - 1;
-            entry.setModCount((short) readInt());
+            entry.setModCount((short) readVarInt());
-                        val = InternalValue.create(binding.dataStore, in.readUTF());
+                        val = InternalValue.create(binding.dataStore, readString());
-                        blobIds[i] = in.readUTF();
+                        blobIds[i] = readString();
-                    // because writeUTF(String) has a size limit of 64k,
-                    // Strings are serialized as <length><byte[]>
-                    int len;
-                        len = readInt();
+                        val = InternalValue.valueOf(
+                                readString(), entry.getType());
-                        len = in.readInt();
+                        // because writeUTF(String) has a size limit of 64k,
+                        // Strings are serialized as <length><byte[]>
+                        int len = in.readInt();
+                        byte[] bytes = new byte[len];
+                        in.readFully(bytes);
+                        val = InternalValue.valueOf(
+                                new String(bytes, "UTF-8"), entry.getType());
-                    byte[] bytes = new byte[len];
-                    in.readFully(bytes);
-                    val = InternalValue.valueOf(
-                            new String(bytes, "UTF-8"), entry.getType());
-            return new BigDecimal(in.readUTF());
+            return new BigDecimal(readString());
-                uri = in.readUTF();
+                uri = readString();
-            String local;
-            int len = b & 0x0f;
-            if (b != 0x0f) {
-                byte[] buffer = new byte[len + 1];
-                in.readFully(buffer);
-                local = new String(buffer, "UTF-8");
-            } else {
-                local = in.readUTF();
-            }
+            String local = new String(readBytes((b & 0x0f) + 1, 0x10), "UTF-8");
-    private int readInt() throws IOException {
+    private int readVarInt() throws IOException {
-            return readInt() << 7 | b & 0x7f;
+            return readVarInt() << 7 | b & 0x7f;
+    private String readString() throws IOException {
+        if (version >= BundleBinding.VERSION_3) {
+            return new String(readBytes(0, 0), "UTF-8");
+        } else {
+            return in.readUTF();
+        }
+    }
+
+    private byte[] readBytes(int len, int base) throws IOException {
+        if (len == base) {
+            len += readVarInt();
+        }
+        byte[] bytes = new byte[len];
+        in.readFully(bytes);
+        return bytes;
+    }
+

INS31 INS31 UPD42 INS83 INS43 INS42 INS43 INS8 INS83 MOV5 INS42 INS44 INS44 INS43 INS8 INS42 INS42 INS25 INS39 INS42 INS39 INS42 INS42 INS25 INS60 INS21 INS41 INS27 INS8 MOV8 INS27 INS8 INS5 INS59 INS32 INS42 INS60 INS42 INS40 INS41 INS41 INS42 INS42 INS21 INS39 INS85 INS42 INS3 INS42 INS42 INS42 MOV43 INS59 INS14 MOV32 INS7 INS5 INS42 MOV42 INS14 INS43 INS32 INS45 INS42 INS32 INS39 INS85 INS60 MOV60 MOV21 MOV21 UPD42 MOV43 INS32 MOV45 INS42 INS42 INS34 INS34 INS42 UPD42 UPD42 INS39 INS59 INS42 INS27 INS34 UPD42 UPD42 INS32 INS42 MOV32 UPD42 INS36 MOV34 INS42 INS42 INS32 INS32 MOV27 UPD42 UPD42 UPD42 MOV42 INS42 INS42 UPD42 DEL42 DEL42 DEL39 DEL42 DEL59 DEL60 DEL32 DEL42 DEL7 DEL21 DEL42 DEL42 DEL59 DEL60 DEL39 DEL42 DEL59 DEL60 DEL42 DEL34 DEL27 DEL39 DEL85 DEL5 DEL42 DEL42 DEL27 DEL3 DEL59 DEL60 DEL42 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL14 DEL7 DEL21 DEL8 DEL25 DEL42 DEL7 DEL21