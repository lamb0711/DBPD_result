fixed log level as suggested by claus koell on dev list

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@522460 13f79535-47bb-0310-9956-ffa450edef68

-            if (numWrites++%10000 == 0 && log.isDebugEnabled()) {
-                log.info(this + " size=" + cache.size() + ", " + totalMem + "/" + maxMem);
+            if (numWrites++ % 10000 == 0 && log.isDebugEnabled()) {
+                log.debug(this + " size=" + cache.size() + ", " + totalMem + "/" + maxMem);

UPD42