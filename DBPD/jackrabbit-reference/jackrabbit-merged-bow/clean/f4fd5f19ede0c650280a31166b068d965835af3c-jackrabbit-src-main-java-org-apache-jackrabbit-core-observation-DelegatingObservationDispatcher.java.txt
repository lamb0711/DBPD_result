JCR-453: add/remove dispatchers from DelegatingObservationDispatcher is not synchronized


git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@412344 13f79535-47bb-0310-9956-ffa450edef68

+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
-import java.util.Iterator;
+     * Logger instance.
+     */
+    private static Logger log = LoggerFactory.getLogger(DelegatingObservationDispatcher.class);
+
+    /**
-        dispatchers.add(disp);
+        synchronized (dispatchers) {
+            dispatchers.add(disp);
+        }
-        dispatchers.remove(disp);
+        synchronized (dispatchers) {
+            dispatchers.remove(disp);
+        }
-        Iterator iter = dispatchers.iterator();
-        while (iter.hasNext()) {
-            ObservationManagerFactory fac = (ObservationManagerFactory) iter.next();
-            EventStateCollection events = new EventStateCollection(fac, session, pathPrefix);
-            events.addAll(eventList);
-            events.prepare();
-            events.dispatch();
+        ObservationManagerFactory[] disp;
+        synchronized (dispatchers) {
+            disp = (ObservationManagerFactory[]) dispatchers.toArray(
+                    new ObservationManagerFactory[dispatchers.size()]);
+        }
+        for (int i=0; i< disp.length; i++) {
+            EventStateCollection events =
+                    new EventStateCollection(disp[i], session, pathPrefix);
+            try {
+                events.addAll(eventList);
+                events.prepare();
+                events.dispatch();
+            } catch (Exception e) {
+                log.error("Error while dispatching events.", e);
+            }

MOV26 INS26 UPD40 INS40 INS23 INS29 INS83 INS83 INS43 INS59 INS8 INS8 INS65 INS42 INS42 INS32 INS51 INS51 INS60 INS51 INS24 INS66 INS42 INS42 INS57 INS42 MOV8 INS42 MOV8 INS5 INS59 INS42 INS8 INS58 INS27 INS37 INS8 INS43 MOV43 INS85 INS42 INS21 INS39 INS59 INS42 INS40 INS42 INS60 INS54 INS42 INS7 INS42 INS34 MOV43 MOV59 MOV8 INS12 INS42 INS11 UPD42 INS14 INS44 INS8 INS5 INS32 MOV43 INS2 INS42 INS42 INS43 INS42 INS21 MOV43 INS85 INS42 INS42 INS3 INS42 INS42 INS42 INS32 INS5 INS32 INS42 UPD42 MOV42 INS45 UPD42 MOV42 INS43 INS85 INS42 INS42 UPD42 MOV42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL42 DEL42 DEL42 DEL42 DEL14 DEL59 DEL60 DEL43 DEL60 DEL32 DEL61