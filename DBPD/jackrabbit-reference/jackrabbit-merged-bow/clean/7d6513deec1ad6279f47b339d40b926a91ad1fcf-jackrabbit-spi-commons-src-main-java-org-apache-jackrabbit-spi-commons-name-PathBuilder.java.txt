- add missing @Override annotation
- parametrize generic types
- remove unnecessary unboxing


git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@915837 13f79535-47bb-0310-9956-ffa450edef68

-    private final LinkedList queue;
+    private final LinkedList<Path.Element> queue;
-        queue = new LinkedList();
+        queue = new LinkedList<Path.Element>();
-        for (int i = 0; i < elements.length; i++) {
-            addLast(elements[i]);
+        for (Path.Element element : elements) {
+            addLast(element);
-        Path.Element[] elements = (Path.Element[]) queue.toArray(new Path.Element[queue.size()]);
+        Path.Element[] elements = queue.toArray(new Path.Element[queue.size()]);

INS74 MOV43 INS43 INS70 INS40 INS44 INS42 INS8 MOV5 INS43 INS42 MOV21 MOV32 INS74 INS40 MOV43 INS43 INS42 INS40 DEL42 DEL42 DEL2 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL8 DEL24 DEL40 DEL43 DEL85 DEL5 DEL11