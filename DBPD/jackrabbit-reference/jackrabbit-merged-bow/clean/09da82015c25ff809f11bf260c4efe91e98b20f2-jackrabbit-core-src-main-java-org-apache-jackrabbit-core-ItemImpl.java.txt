JCR-1538: [patch] add toString for NodeImpl and PropertyImpl 

Include a "node" or "property" prefix in the ItemImpl.toString() output.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@691305 13f79535-47bb-0310-9956-ffa450edef68

-     * Returns the ({@link #safeGetJCRPath() safe}) path of this item
-     * for use in diagnostic output.
+     * Returns a string with the type and ({@link #safeGetJCRPath() safe}) path
+     * of this item for use in diagnostic output.
-        return safeGetJCRPath();
+        if (isNode()) {
+            return "node " + safeGetJCRPath();
+        } else {
+            return "property " + safeGetJCRPath();
+        }

INS8 INS25 UPD66 UPD66 UPD66 INS32 MOV8 INS8 INS42 INS41 INS27 INS27 INS45 MOV32 INS45 INS32 INS42