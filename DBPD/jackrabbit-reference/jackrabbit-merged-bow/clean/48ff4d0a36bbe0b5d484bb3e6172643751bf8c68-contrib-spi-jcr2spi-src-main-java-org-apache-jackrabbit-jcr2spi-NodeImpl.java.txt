work in progress

- simplify: don't check for supported-option (locking, versioning) on the client.


git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@479646 13f79535-47bb-0310-9956-ffa450edef68

-        checkSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED);
-        if (!isSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED)) {
-            return true;
-        }
-        checkSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED);
-        checkSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED);
-
-        checkSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED);
-        checkSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED);
-        checkSupportedOption(Repository.OPTION_LOCKING_SUPPORTED);
-
-        checkSupportedOption(Repository.OPTION_LOCKING_SUPPORTED);
-
-        checkSupportedOption(Repository.OPTION_LOCKING_SUPPORTED);
-
-        checkSupportedOption(Repository.OPTION_LOCKING_SUPPORTED);
-        if (!isSupportedOption(Repository.OPTION_LOCKING_SUPPORTED) || isNew()) {
-            // if locking is not support at all or if this node is new, no
-            // checks must be performed.
+        if (isNew()) {
+            // if this node is new, no checks must be performed.
-        checkSupportedOption(Repository.OPTION_VERSIONING_SUPPORTED);

MOV32 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL38 DEL9 DEL41 DEL8 DEL25 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL21 DEL42 DEL40 DEL32 DEL38 DEL27 DEL42 DEL40 DEL32 DEL21