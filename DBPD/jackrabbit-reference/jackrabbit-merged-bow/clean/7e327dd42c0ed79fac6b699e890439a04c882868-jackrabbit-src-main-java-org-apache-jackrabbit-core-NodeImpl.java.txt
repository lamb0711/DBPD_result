Make locking part of XAResource's transaction support

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@354456 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.jackrabbit.core.lock.LockManager;
-        LockManager lockMgr = ((WorkspaceImpl) session.getWorkspace()).getLockManager();
-        return lockMgr.lock(this, isDeep, isSessionScoped);
+        return session.getLockManager().lock(this, isDeep, isSessionScoped);
-        // todo: fixme for transactions
-        if (isTransactionalNew()) {
+        if (isNew()) {
-
-        LockManager lockMgr = ((WorkspaceImpl) session.getWorkspace()).getLockManager();
-        return lockMgr.getLock(this);
+        return session.getLockManager().getLock(this);
-
-        LockManager lockMgr = ((WorkspaceImpl) session.getWorkspace()).getLockManager();
-        lockMgr.unlock(this);
+        session.getLockManager().unlock(this);
-        // todo: fixme for transactions
-        if (!isNodeType(QName.MIX_LOCKABLE) || isTransactionalNew()) {
+        if (!isNodeType(QName.MIX_LOCKABLE) || isNew()) {
-
-        LockManager lockMgr = ((WorkspaceImpl) session.getWorkspace()).getLockManager();
-        return lockMgr.holdsLock(this);
+        return session.getLockManager().holdsLock(this);
-        // todo: fixme for transactions
-        if (isTransactionalNew()) {
+        if (isNew()) {
-
-        LockManager lockMgr = ((WorkspaceImpl) session.getWorkspace()).getLockManager();
-        return lockMgr.isLocked(this);
+        return session.getLockManager().isLocked(this);
-        // todo: fixme for transactions
-        if (isTransactionalNew()) {
-            // a new node must not be checked
+        if (isNew()) {
+            // a new node needs no check
-
-        LockManager lockMgr = ((WorkspaceImpl) session.getWorkspace()).getLockManager();
-        lockMgr.checkLock(this);
+        session.getLockManager().checkLock(this);

INS41 INS41 INS21 INS41 INS41 INS21 INS32 INS32 INS32 INS32 INS32 INS32 INS32 MOV42 MOV52 MOV42 MOV42 UPD42 INS32 MOV42 MOV52 INS32 MOV42 MOV52 INS32 MOV42 MOV52 UPD42 INS32 MOV42 MOV52 UPD42 INS32 MOV42 MOV52 MOV42 MOV42 MOV42 MOV42 MOV42 MOV42 UPD42 MOV42 MOV42 MOV42 MOV42 MOV42 MOV42 DEL40 DEL26 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL32 DEL11 DEL36 DEL32 DEL59 DEL60 DEL42 DEL32 DEL41 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL32 DEL11 DEL36 DEL32 DEL59 DEL60 DEL42 DEL32 DEL41 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL32 DEL11 DEL36 DEL32 DEL59 DEL60 DEL42 DEL32 DEL21 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL32 DEL11 DEL36 DEL32 DEL59 DEL60 DEL42 DEL32 DEL41 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL32 DEL11 DEL36 DEL32 DEL59 DEL60 DEL42 DEL32 DEL41 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL32 DEL11 DEL36 DEL32 DEL59 DEL60 DEL42 DEL32 DEL21