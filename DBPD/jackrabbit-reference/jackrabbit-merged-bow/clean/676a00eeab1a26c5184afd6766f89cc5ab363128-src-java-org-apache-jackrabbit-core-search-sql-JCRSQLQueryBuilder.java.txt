Implement location step with context position index.

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@156923 13f79535-47bb-0310-9956-ffa450edef68

-                int index = 0;
+                int index = LocationStepQueryNode.NONE;
-                        index = 0;
+                        index = LocationStepQueryNode.NONE;
+                    if (name.equals("%")) {
+                        name = null;
+                    }
-                    // no index
+                    // no index specified
+                    // - index defaults to 1 if there is an explicit name test
+                    // - index defaults to NONE if name test is %
-                    // in SQL this means index 1
-                    index = 1;
-                }
-                if (name.equals("%")) {
-                    name = null;
+                    if (name.equals("%")) {
+                        name = null;
+                    } else {
+                        index = 1;
+                    }

INS40 MOV25 INS25 INS32 INS8 INS8 INS42 INS42 INS45 INS21 MOV21 INS7 INS40 INS42 INS33 DEL34 DEL34