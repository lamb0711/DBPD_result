JCR-1104: JSR 283 support

Upgraded to the latest pre-release version of the JCR 2.0 API jar. Adjusted sources accordingly (see JCR-2200 and JCR-2201 for the missing pieces).

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@792508 13f79535-47bb-0310-9956-ffa450edef68

-                String s = readString();
+                Value value = readString();
-                return s;
+                return value.getString();
-                    c = factory.fullTextSearch(name, null, readString());
+                    c = factory.fullTextSearch(
+                            name, null, factory.literal(readString()));
-                    c = factory.fullTextSearch(selector, name, readString());
+                    c = factory.fullTextSearch(
+                            selector, name, factory.literal(readString()));
-                c = factory.fullTextSearch(getOnlySelectorName(), name, readString());
+                c = factory.fullTextSearch(
+                        getOnlySelectorName(), name,
+                        factory.literal(readString()));
-    private String readString() throws RepositoryException {
+    private Value readString() throws RepositoryException {
-        String s = currentValue.getString();
+        Value value = currentValue;
-        return s;
+        return value;

UPD43 UPD42 UPD43 UPD42 UPD42 UPD42 INS42 UPD43 INS32 UPD42 UPD42 INS42 INS42 INS32 INS42 INS42 MOV32 INS32 INS32 INS42 INS42 MOV32 INS42 INS42 MOV32 DEL42 DEL42 DEL42 DEL32