JCR-3358: Allow null type in JcrUtils.getOrAddNode(parent, name, type)

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1353450 13f79535-47bb-0310-9956-ffa450edef68

-        if (parent.hasNode(name)) {
-            return parent.getNode(name);
-        } else {
-            return parent.addNode(name);
-        }
+        return getOrAddNode(parent, name, null);
-     * @param type type of the child node, ignored if the child already exists
+     * @param type type of the child node or {@code null},
+     *             ignored if the child already exists
-        } else {
+        } else if (type != null) {
+        } else {
+            return parent.addNode(name);

INS8 MOV41 INS66 INS65 INS66 UPD66 INS25 INS42 INS33 INS66 INS27 MOV8 MOV8 INS42 INS33 DEL42 DEL42 DEL42 DEL42 DEL32 DEL8 DEL25 DEL8