JCR-2509 : Reduce number of compiler warning by adding @Override and generics where appropriate [step 1: jackrabbit-webdav]

in addition:
- fix indention
- minor improvements in comments
- use foreach loops where possible
- introduce PropEntry marker interface as common base for DavProperty and DavPropertyName
- add o.a.j.webdav.lock.Type.hashCode

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@915333 13f79535-47bb-0310-9956-ffa450edef68

-            List hrefList = new ArrayList();
+            List<String> hrefList = new ArrayList<String>();
-            source = (String[])hrefList.toArray(new String[hrefList.size()]);
+            source = hrefList.toArray(new String[hrefList.size()]);
-                for (int i = 0; i < updateSource.length; i++) {
-                    vE.appendChild(DomUtil.hrefToXml(updateSource[i], factory));
+                for (String source : updateSource) {
+                    vE.appendChild(DomUtil.hrefToXml(source, factory));
-}
+}

INS70 INS44 INS42 INS8 INS74 MOV43 INS42 MOV21 MOV43 INS43 MOV32 INS42 INS74 MOV43 INS43 INS42 INS42 DEL85 DEL5 DEL11 DEL42 DEL42 DEL2 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL8 DEL24