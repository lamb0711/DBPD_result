JCR-2165: Node.getReferences(String) and Node.getWeakReferences(String) issues

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@786513 13f79535-47bb-0310-9956-ffa450edef68

-                    for (PropertyId propId : filteredList) {
+                    for (PropertyId propId : idList) {
-                    if (p.getType() == PropertyType.WEAKREFERENCE
-                            && p.getString().equals(getIdentifier())) {
-                        if (name != null) {
-                            if (name.equals(p.getName())) {
-                                l.add(p);
+                    if (name != null && !name.equals(p.getName())) {
+                        continue;
+                    }
+                    if (p.getType() == PropertyType.WEAKREFERENCE) {
+                        boolean containsId = false;
+                        if (p.isMultiple()) {
+                            // multi-valued
+                            Value[] v = p.getValues();
+                            for (int i = 0; i < v.length; i++) {
+                                if (getIdentifier().equals(v[i].getString())) {
+                                    containsId = true;
+                                    break;
+                                }
+                            // single-valued
+                            if (getIdentifier().equals(p.getString())) {
+                                containsId = true;
+                            }
+                        }
+                        if (containsId) {

MOV25 INS25 UPD42 INS27 MOV27 INS8 MOV27 INS38 INS18 INS60 MOV25 MOV25 MOV32 INS39 INS59 INS32 INS8 INS8 INS42 INS42 INS9 INS42 INS42 INS60 INS24 INS25 INS5 INS59 INS58 INS27 INS37 INS8 MOV32 MOV32 MOV42 INS8 INS43 INS85 INS42 INS32 INS39 INS59 INS42 INS40 INS42 INS25 INS21 INS42 INS42 INS42 INS42 INS34 INS32 MOV8 INS7 INS32 INS42 INS32 INS21 INS10 INS42 INS9 INS42 INS2 INS42 INS7 INS42 INS42 INS42 INS9 DEL42 DEL42 DEL42 DEL32 DEL21 DEL8 DEL27