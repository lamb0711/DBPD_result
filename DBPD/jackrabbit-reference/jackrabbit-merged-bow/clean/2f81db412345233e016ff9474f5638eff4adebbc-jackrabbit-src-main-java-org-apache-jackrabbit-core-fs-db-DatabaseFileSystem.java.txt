JCR-97: Various checkstyle conformance improvements.

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@386994 13f79535-47bb-0310-9956-ffa450edef68

-                File f = tmpFile;
-                                long length = f.length();
-                                in = new FileInputStream(f);
+                                long length = tmpFile.length();
+                                in = new FileInputStream(tmpFile);
-                            long length = f.length();
-                            in = new FileInputStream(f);
+                            long length = tmpFile.length();
+                            in = new FileInputStream(tmpFile);
-                        f.delete();
-                        f = null;
+                        tmpFile.delete();
-                File f = tmpFile;
-                RandomAccessFile raf = new RandomAccessFile(f, "rw");
+                private final RandomAccessFile raf =
+                    new RandomAccessFile(tmpFile, "rw");
-                                long length = f.length();
-                                in = new FileInputStream(f);
+                                long length = tmpFile.length();
+                                in = new FileInputStream(tmpFile);
-                            long length = f.length();
-                            in = new FileInputStream(f);
+                            long length = tmpFile.length();
+                            in = new FileInputStream(tmpFile);
-                        f.delete();
-                        f = null;
-                        raf = null;
+                        tmpFile.delete();
-                public void write(byte b[]) throws IOException {
+                public void write(byte[] b) throws IOException {
-                public void write(byte b[], int off, int len) throws IOException {
+                public void write(byte[] b, int off, int len) throws IOException {
-     * Initializes the database connection used by this file system. 
+     * Initializes the database connection used by this file system.
-    
+
-     * 
+     *
-            String src = (FileSystemPathUtil.denotesRoot(srcPath) ?
-                    srcPath + names[i] : srcPath + FileSystem.SEPARATOR + names[i]);
-            String dest = (FileSystemPathUtil.denotesRoot(destPath) ?
-                    destPath + names[i] : destPath + FileSystem.SEPARATOR + names[i]);
+            String src = (FileSystemPathUtil.denotesRoot(srcPath)
+                    ? srcPath + names[i] : srcPath + FileSystem.SEPARATOR + names[i]);
+            String dest = (FileSystemPathUtil.denotesRoot(destPath)
+                    ? destPath + names[i] : destPath + FileSystem.SEPARATOR + names[i]);

UPD66 INS83 INS83 MOV43 INS5 INS5 MOV43 UPD42 INS39 INS85 INS39 INS85 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 DEL42 DEL43 DEL42 DEL42 DEL59 DEL23 DEL42 DEL33 DEL7 DEL21 DEL42 DEL43 DEL42 DEL42 DEL59 DEL23 DEL42 DEL33 DEL7 DEL21 DEL42 DEL33 DEL7 DEL21 DEL39 DEL85 DEL39 DEL85