JCR-3178 Improve error messages for index aggregates

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1213249 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.jackrabbit.core.state.NoSuchItemStateException;
+            } catch (NoSuchItemStateException e) {
+                // do not fail if aggregate cannot be created
+                log.info(
+                        "Exception while building indexing aggregate for {}. Node is not available {}.",
+                        state.getNodeId(), e.getMessage());
-                log.warn("Exception while building indexing aggregate for"
-                        + " node with id: " + state.getNodeId(), e);
+                log.warn("Exception while building indexing aggregate for "
+                        + state.getNodeId(), e);
+        } catch (NoSuchItemStateException e) {
+            log.info(
+                    "Exception while retrieving aggregate roots. Node is not available {}.",
+                    e.getMessage());

INS26 INS40 INS12 INS44 INS8 INS12 INS43 INS42 INS21 INS44 INS8 INS42 INS32 INS43 INS42 INS21 INS42 INS42 INS45 INS32 INS42 INS32 INS42 INS42 INS42 INS42 INS45 MOV32 INS32 INS42 INS42 UPD45 INS32 INS42 INS42 DEL45