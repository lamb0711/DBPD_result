JCR-2087: Upgrade to Java 5 as the base platform

Generify MultiIndex.update()

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@777541 13f79535-47bb-0310-9956-ffa450edef68

-    synchronized void update(Collection remove, Collection add) throws IOException {
+    synchronized void update(
+            Collection<UUID> remove, Collection<Document> add)
+            throws IOException {
-            for (Iterator it = remove.iterator(); it.hasNext(); ) {
-                executeAndLog(new DeleteNode(transactionId, (UUID) it.next()));
+
+            for (UUID uuid : remove) {
+                executeAndLog(new DeleteNode(transactionId, uuid));
-            for (Iterator it = add.iterator(); it.hasNext(); ) {
-                Document doc = (Document) it.next();
-                if (doc != null) {
-                    executeAndLog(new AddNode(transactionId, doc));
+
+            for (Document document : add) {
+                if (document != null) {
+                    executeAndLog(new AddNode(transactionId, document));
-        update(Collections.EMPTY_LIST, Arrays.asList(new Document[]{doc}));
+        Collection<UUID> empty = Collections.emptyList();
+        update(empty, Collections.singleton(doc));
-        update(Arrays.asList(new UUID[]{uuid}), Collections.EMPTY_LIST);
+        Collection<Document> empty = Collections.emptyList();
+        update(Collections.singleton(uuid), empty);
-        Document[] docs = indexingQueue.getFinishedDocuments();
-        Map finished = new HashMap();
-        for (int i = 0; i < docs.length; i++) {
-            String uuid = docs[i].get(FieldNames.UUID);
-            finished.put(UUID.fromString(uuid), docs[i]);
+        Map<UUID, Document> finished = new HashMap<UUID, Document>();
+        for (Document document : indexingQueue.getFinishedDocuments()) {
+            UUID uuid = UUID.fromString(document.get(FieldNames.UUID));
+            finished.put(uuid, document);
-            for (Iterator it = finished.keySet().iterator(); it.hasNext(); ) {
-                indexingQueue.removeDocument(it.next().toString());
+            for (UUID uuid : finished.keySet()) {
+                indexingQueue.removeDocument(uuid.toString());
-                    for (Iterator it = finished.keySet().iterator(); it.hasNext(); ) {
-                        executeAndLog(new DeleteNode(getTransactionId(), (UUID) it.next()));
+                    for (UUID uuid : finished.keySet()) {
+                        executeAndLog(new DeleteNode(getTransactionId(), uuid));
-                    for (Iterator it = finished.values().iterator(); it.hasNext(); ) {
-                        executeAndLog(new AddNode(
-                                getTransactionId(), (Document) it.next()));
+                    for (Document document : finished.values()) {
+                        executeAndLog(new AddNode(getTransactionId(), document));

INS74 INS74 INS60 INS60 INS60 INS70 MOV43 INS43 MOV43 MOV43 INS74 INS59 INS74 INS59 INS74 MOV59 INS44 MOV32 INS8 INS42 INS70 INS70 INS43 INS43 INS42 INS32 INS42 INS43 MOV43 INS42 INS32 INS42 MOV43 MOV43 INS43 MOV43 INS42 MOV60 MOV21 INS70 INS44 INS42 INS8 INS44 INS42 MOV8 INS42 INS42 INS42 INS42 UPD42 UPD42 INS42 INS42 INS42 INS42 UPD42 UPD42 INS42 INS42 INS74 MOV43 INS44 MOV32 INS8 INS43 INS42 MOV21 MOV43 INS42 MOV43 MOV43 INS43 MOV42 INS42 INS43 INS42 MOV21 UPD42 MOV42 INS42 INS42 INS42 INS32 UPD42 MOV42 UPD42 INS42 MOV42 MOV40 INS70 INS70 INS42 UPD42 MOV42 INS44 MOV32 INS8 INS44 MOV32 INS8 INS43 INS42 MOV21 MOV43 INS42 MOV21 UPD42 UPD42 MOV42 MOV32 INS42 MOV32 INS42 DEL42 DEL42 DEL32 DEL11 DEL42 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL58 DEL42 DEL42 DEL32 DEL8 DEL24 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL58 DEL42 DEL42 DEL32 DEL24 DEL40 DEL85 DEL5 DEL42 DEL4 DEL3 DEL85 DEL5 DEL42 DEL4 DEL3 DEL40 DEL42 DEL43 DEL42 DEL42 DEL2 DEL42 DEL42 DEL32 DEL42 DEL42 DEL2 DEL85 DEL5 DEL42 DEL59 DEL60 DEL60 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL8 DEL24 DEL42 DEL32 DEL43 DEL42 DEL42 DEL32 DEL59 DEL58 DEL42 DEL42 DEL32 DEL8 DEL24 DEL42 DEL42 DEL32 DEL11 DEL42 DEL42 DEL32 DEL11 DEL43 DEL42 DEL42 DEL32 DEL59 DEL58 DEL42 DEL42 DEL32 DEL8 DEL24 DEL42 DEL43 DEL42 DEL42 DEL32 DEL59 DEL58 DEL42 DEL42 DEL32 DEL8 DEL24