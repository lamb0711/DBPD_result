JCR-573: Added a number of missing license headers.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@453110 13f79535-47bb-0310-9956-ffa450edef68

-                    // replace prefix variable
-                    sql = Text.replace(sql, SCHEMA_OBJECT_PREFIX_VARIABLE, schemaObjectPrefix);
-                    // execute sql stmt
-                    stmt.executeUpdate(sql);
+                    // Skip comments and empty lines
+                    if (!sql.startsWith("#") && sql.length() > 0) {
+                        // replace prefix variable
+                        sql = Text.replace(sql, SCHEMA_OBJECT_PREFIX_VARIABLE, schemaObjectPrefix);
+                        // execute sql stmt
+                        stmt.executeUpdate(sql);
+                    }

INS8 INS25 MOV21 INS27 MOV8 INS38 INS27 INS32 INS32 INS34 INS42 INS42 INS45 INS42 INS42