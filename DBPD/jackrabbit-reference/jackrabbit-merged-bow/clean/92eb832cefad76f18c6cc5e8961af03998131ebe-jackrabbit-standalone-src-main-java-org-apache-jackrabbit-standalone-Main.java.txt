JCR-422: Implement a backup tool

Add a backup mode to Jackrabbit standalone.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@776356 13f79535-47bb-0310-9956-ffa450edef68

-import java.net.BindException;
+import org.apache.jackrabbit.core.RepositoryCopier;
+import org.apache.jackrabbit.core.config.RepositoryConfig;
+        options.addOption(
+                "b", "backup", false, "create a backup of the repository");
+        options.addOption(
+                "R", "backup-repo", true,
+                "backup repository directory (jackrabbit-backupN)");
+        options.addOption(
+                "C", "backup-conf", true,
+                "backup repository configuration file");
-            message("Starting the server...");
-            prepareWebapp(file, repository, tmp);
-            accessLog.setHandler(webapp);
-            prepareAccessLog(log);
-            server.setHandler(accessLog);
-            prepareConnector();
-            server.addConnector(connector);
-            prepareShutdown();
+            if (command.hasOption("backup")) {
+                backup(repository);
+            } else {
+                message("Starting the server...");
+                prepareWebapp(file, repository, tmp);
+                accessLog.setHandler(webapp);
+                prepareAccessLog(log);
+                server.setHandler(accessLog);
+                prepareConnector();
+                server.addConnector(connector);
+                prepareShutdown();
-            try {
-                server.start();
+                try {
+                    server.start();
-                String host = connector.getHost();
-                if (host == null) {
-                    host = "localhost";
+                    String host = connector.getHost();
+                    if (host == null) {
+                        host = "localhost";
+                    }
+                    message("Apache Jackrabbit is now running at "
+                            +"http://" + host + ":" + connector.getPort() + "/");
+                } catch (Throwable t) {
+                    System.err.println(
+                            "Unable to start the server: " + t.getMessage());
+                    System.exit(1);
-                message("Apache Jackrabbit is now running at "
-                        +"http://" + host + ":" + connector.getPort() + "/");
-            } catch (Throwable t) {
-                System.err.println(
-                        "Unable to start the server: " + t.getMessage());
-                System.exit(1);
+    private void backup(File sourceDir) throws Exception {
+        RepositoryConfig source;
+        if (command.hasOption("conf")) {
+            source = RepositoryConfig.create(
+                    new File(command.getOptionValue("conf")), sourceDir);
+        } else {
+            source = RepositoryConfig.create(sourceDir);
+        }
+
+        File targetDir;
+        if (command.hasOption("backup-repo")) {
+            targetDir = new File(command.getOptionValue("backup-repo"));
+        } else {
+            int i = 1;
+            do {
+                targetDir = new File("jackrabbit-backup" + i++);
+            } while (targetDir.exists());
+        }
+
+        RepositoryConfig target;
+        if (command.hasOption("backup-conf")) {
+            target = RepositoryConfig.install(
+                    new File(command.getOptionValue("backup-conf")), targetDir);
+        } else {
+            target = RepositoryConfig.install(targetDir);
+        }
+
+        message("Creating a repository copy in " + targetDir);
+
+        RepositoryCopier copier = new RepositoryCopier(source, target);
+        copier.copy();
+
+        message("The repository has been successfully copied.");
+    }
+

MOV26 INS26 UPD40 INS40 INS31 INS83 INS39 INS42 INS44 INS43 INS8 INS21 INS21 INS21 INS43 INS42 INS42 INS60 INS25 INS60 INS25 INS60 INS25 INS21 INS60 INS21 INS21 INS32 INS32 INS32 INS42 INS43 INS59 INS32 INS8 INS8 INS43 INS59 INS32 INS8 INS8 INS43 INS59 INS32 INS8 INS8 INS32 INS43 INS59 INS32 INS32 INS42 INS42 INS45 INS45 INS9 INS45 INS42 INS42 INS45 INS45 INS9 INS45 INS42 INS42 INS45 INS45 INS9 INS45 INS42 INS42 INS42 INS42 INS45 INS21 INS21 INS42 INS42 INS42 INS42 INS45 INS21 INS60 INS19 INS42 INS42 INS42 INS42 INS45 INS21 INS21 INS42 INS27 INS42 INS42 INS14 INS42 INS42 INS42 INS45 INS7 INS7 INS7 INS39 INS59 INS8 INS32 INS7 INS7 INS45 INS42 INS43 INS42 INS42 INS25 INS42 INS32 INS42 INS32 INS42 INS14 INS42 INS34 INS21 INS42 INS42 INS42 INS32 INS42 INS32 INS42 INS32 INS8 INS8 INS42 INS42 INS14 INS42 INS42 INS42 INS42 INS43 INS32 INS7 INS42 INS42 INS14 INS42 INS42 INS42 INS42 INS42 INS42 INS45 INS21 MOV21 MOV21 MOV21 MOV21 MOV21 MOV21 MOV21 MOV21 MOV54 INS43 INS32 INS42 INS42 INS42 INS45 INS42 INS14 INS43 INS32 INS32 INS42 INS42 INS42 INS45 INS43 INS27 INS42 INS42 INS42 INS45 INS42 INS42 INS42 INS45 INS37 INS42