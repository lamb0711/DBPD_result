JCR-468: the code in ItemImpl.isSame() only compares the item id, but not the source workspace.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@429293 13f79535-47bb-0310-9956-ffa450edef68

-            return id.equals(other.id);
+            return id.equals(other.id) 
+                    && session.getWorkspace().getName().equals(
+                            other.getSession().getWorkspace().getName());

INS27 MOV32 INS32 INS32 INS42 INS32 INS32 INS42 INS32 INS42 INS42 INS42 INS32 INS42 INS42 INS42