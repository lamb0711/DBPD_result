JCR-1227: Restore of empty multivalue property always changes property type to string
    - Use the actual instead of defined property type in
      checkin() and restore()
    - Added a test case

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@628281 13f79535-47bb-0310-9956-ffa450edef68

-            int type;
-            if (values.length > 0) {
-                type = values[0].getType();
-            } else {
-                type = prop.getDefinition().getRequiredType();
-            }
-            setPropertyValues(prop.getQName(), type, copiedValues);
+            setPropertyValues(prop.getQName(), prop.getType(), copiedValues);

INS21 INS32 INS42 MOV32 INS32 MOV42 MOV42 UPD42 MOV42 DEL39 DEL42 DEL59 DEL60 DEL40 DEL34 DEL27 DEL42 DEL42 DEL34 DEL2 DEL42 DEL32 DEL7 DEL21 DEL8 DEL42 DEL42 DEL32 DEL32 DEL7 DEL21 DEL8 DEL25 DEL42 DEL42 DEL32 DEL21