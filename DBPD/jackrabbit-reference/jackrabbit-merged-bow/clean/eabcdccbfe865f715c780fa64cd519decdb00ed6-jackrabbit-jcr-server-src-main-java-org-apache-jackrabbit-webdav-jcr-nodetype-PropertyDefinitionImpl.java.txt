JCR-2509 : Reduce number of compiler warning by adding @Override and generics where appropriate [step 2: jackrabbit-jcr-server]

in addition:
- minor improvements in comments, typos
- use foreach loops where possible
- remove redundant Tika imports in server/io package
- remove redundant casts
- add missing index increment in SubscriptionManagerImpl.SubscriptionMap

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@915339 13f79535-47bb-0310-9956-ffa450edef68

+    @Override
-            for (int i = 0; i < values.length; i++) {
+            for (Value value : values) {
-                    DomUtil.setText(valElem, values[i].getString());
+                    DomUtil.setText(valElem, value.getString());
-        String[] constraints = getValueConstraints();
-        for (int i = 0; i < constraints.length; i++) {
+        for (String constraint : getValueConstraints()) {
-            DomUtil.setText(vcElem, constraints[i]);
+            DomUtil.setText(vcElem, constraint);
-        String[] qops = getAvailableQueryOperators();
-        for (int i = 0; i < qops.length; i++) {
+        for (String qop : getAvailableQueryOperators()) {
-            DomUtil.setText(opElem, qops[i]);
+            DomUtil.setText(opElem, qop);
+    @Override
-}
+}

INS78 INS78 INS42 INS70 INS70 INS42 INS44 MOV32 MOV8 INS44 MOV32 MOV8 INS70 MOV43 INS42 MOV43 INS42 INS44 INS42 MOV8 INS43 INS42 INS42 INS42 INS42 INS42 DEL42 DEL42 DEL2 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL24 DEL42 DEL42 DEL2 DEL85 DEL5 DEL42 DEL59 DEL60 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL24 DEL42 DEL42 DEL2 DEL85 DEL5 DEL42 DEL59 DEL60 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL24