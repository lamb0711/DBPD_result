JCR-2836: Unclosed threads in Jackrabbit

Remove the use of ThreadLocal variables in PerQueryCache to avoid leaking memory in container environments.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1055117 13f79535-47bb-0310-9956-ffa450edef68

-    MatchAllScorer(IndexReader reader, String field)
+    MatchAllScorer(IndexReader reader, String field, PerQueryCache cache)
-        calculateDocFilter();
+        calculateDocFilter(cache);
-    private void calculateDocFilter() throws IOException {
-        PerQueryCache cache = PerQueryCache.getInstance();
+    private void calculateDocFilter(PerQueryCache cache) throws IOException {

INS44 INS44 INS43 INS42 MOV43 INS42 INS42 INS42 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60