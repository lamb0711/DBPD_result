JCR-2509 : Reduce number of compiler warning by adding @Override and generics where appropriate [step 1: jackrabbit-webdav]

in addition:
- fix indention
- minor improvements in comments
- use foreach loops where possible
- introduce PropEntry marker interface as common base for DavProperty and DavPropertyName
- add o.a.j.webdav.lock.Type.hashCode

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@915333 13f79535-47bb-0310-9956-ffa450edef68

-import java.util.Iterator;
-    private final Map entries = new HashMap();
+    private final Map<String, Entry> entries = new HashMap<String, Entry>();
-            return ((Entry)entries.get(key)).hrefs;
+            return entries.get(key).hrefs;
-        Iterator it = entries.values().iterator();
-        while (it.hasNext()) {
-            Entry entry = (Entry)it.next();
+        for (Entry entry : entries.values()) {
-            for (int i = 0; i < entry.hrefs.length; i++) {
-                elem.appendChild(DomUtil.hrefToXml(entry.hrefs[i], document));
+            for (String href : entry.hrefs) {
+                elem.appendChild(DomUtil.hrefToXml(href, document));
-            List hrefs = new ArrayList();
+            List<String> hrefs = new ArrayList<String>();
-            oResponse.addEntry(el.getLocalName(), DomUtil.getNamespace(el), (String[])hrefs.toArray(new String[hrefs.size()]));
+            oResponse.addEntry(el.getLocalName(), DomUtil.getNamespace(el), hrefs.toArray(new String[hrefs.size()]));
-}
+}

INS74 MOV43 INS43 MOV43 INS70 INS42 INS74 INS44 MOV32 INS8 MOV43 INS43 MOV43 MOV43 INS42 MOV60 INS70 INS42 INS44 INS40 INS8 INS74 MOV32 INS43 INS42 MOV21 MOV43 INS43 MOV32 INS42 INS42 INS74 MOV43 INS43 INS42 INS42 DEL40 DEL26 DEL11 DEL36 DEL40 DEL42 DEL2 DEL42 DEL43 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL42 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL8 DEL24 DEL8 DEL61 DEL42 DEL43 DEL85 DEL5 DEL11