JCR-2747: ConcurrentModificationException in IndexMerger

Synchronize access to the busyMergers list

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@996810 13f79535-47bb-0310-9956-ffa450edef68

-        for (Worker worker : busyMergers) {
-            worker.unblock();
+        synchronized (busyMergers) {
+            for (Worker worker : busyMergers) {
+                worker.unblock();
+            }

INS51 INS42 INS8 MOV70