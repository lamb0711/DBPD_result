Performance improvement
- Remove redundant check in getName(). Most calls to hasItemState() will succeed anyway and the case where the item does not exist is also covered in getItemState().

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@292693 13f79535-47bb-0310-9956-ffa450edef68

-            if (!hasItemState(nodeId)) {
-                String msg = "failed to resolve name of " + nodeId;
-                log.debug(msg);
-                throw new ItemNotFoundException(nodeId.toString());
-            }
+            } catch (NoSuchItemStateException nsis) {
+                String msg = "failed to resolve name of " + nodeId;
+                log.debug(msg);
+                throw new ItemNotFoundException(nodeId.toString());

INS12 INS44 MOV8 INS43 INS42 INS42 DEL42 DEL42 DEL32 DEL38 DEL25