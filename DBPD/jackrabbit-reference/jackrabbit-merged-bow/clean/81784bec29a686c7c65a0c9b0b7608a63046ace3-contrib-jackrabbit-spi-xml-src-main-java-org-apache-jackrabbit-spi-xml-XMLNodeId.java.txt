contrib/jackrabbit-spi-xml: Ignore text elements (etc.) for now.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@498220 13f79535-47bb-0310-9956-ffa450edef68

+import java.util.ArrayList;
+import java.util.Collection;
-        return new XMLNodeIdIterator(node.getChildNodes());
+        Collection infos = new ArrayList();
+        NodeList nodes = node.getChildNodes();
+        for (int i = 0; i < nodes.getLength(); i++) {
+            Node child = nodes.item(i);
+            if (child.getNodeType() == Node.ELEMENT_NODE) {
+                infos.add(new XMLNodeId(child));
+            }
+        }
+        return infos.iterator();
-                    if (name.equals(new XMLNodeId(child).getName())) {
+                    if (child.getNodeType() == Node.ELEMENT_NODE
+                            && name.equals(new XMLNodeId(child).getName())) {
-                if (current == node) {
-                    index = i + 1;
-                } else if (node.getNamespaceURI().equals(current.getNamespaceURI())
-                        && node.getLocalName().equals(current.getLocalName())) {
+                if (current.getNodeType() != Node.ELEMENT_NODE) {
+                    // skip
+                } else if (current == node) {
+                    index = count + 1;
+                } else if (getName().equals(new XMLNodeId(current).getName())) {

INS26 INS26 INS40 INS40 INS8 INS60 INS60 INS24 INS41 INS43 INS59 INS43 INS59 INS58 INS27 INS37 INS8 INS32 INS42 INS42 INS14 INS42 INS42 MOV32 INS39 INS59 INS42 INS32 INS42 INS60 INS25 INS42 INS42 INS43 INS42 INS34 INS42 INS42 INS43 INS59 INS27 INS8 INS42 INS42 INS42 INS32 INS32 INS40 INS21 INS25 INS42 INS42 INS42 INS42 INS42 INS32 INS27 INS8 INS25 INS42 INS42 INS14 INS32 INS40 MOV27 MOV8 MOV25 UPD43 MOV43 INS42 INS42 INS42 INS32 UPD42 MOV32 MOV42 INS32 INS27 UPD42 INS14 UPD42 MOV42 INS27 MOV32 UPD42 INS43 INS42 INS32 INS40 INS42 INS42 INS42 DEL14 DEL41 DEL8 DEL42 DEL42 DEL42 DEL32 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL32 DEL27 DEL25