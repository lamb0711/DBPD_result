minor improvement

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1182309 13f79535-47bb-0310-9956-ffa450edef68

-                String[] includes =
-                        webdavRequest.getParameterValues(PARAM_INCLUDE);
+                String[] includes = webdavRequest.getParameterValues(PARAM_INCLUDE);
-                    for (int i = 0; i < includes.length; i++) {
+                    for (String include : includes) {
-                            if (includes[i].startsWith("/")) {
-                                n = session.getNode(includes[i]);
+                            if (include.startsWith("/")) {
+                                n = session.getNode(include);
-                                n = node.getNode(includes[i]);
+                                n = node.getNode(include);
-                            if (!alreadyAdded.contains(n.getPath())) {
+                            String np = n.getPath();
+                            if (!alreadyAdded.contains(np)) {
-                                alreadyAdded.add(n.getPath());
+                                alreadyAdded.add(np);

INS70 INS44 INS42 INS8 INS43 INS42 MOV54 INS42 INS60 INS43 INS59 INS42 INS42 INS42 MOV32 INS42 UPD42 MOV42 INS42 INS42 DEL42 DEL42 DEL2 DEL42 DEL42 DEL2 DEL42 DEL42 DEL2 DEL42 DEL32 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL8 DEL24