jcr-commands 
 - naming and packaging reviewed
 - now commands only read parameters from the context because commands shouldn't have a state of their own but use only the context.
 - i18n reorganized in one single resources file
 - batch mode added
 - cover most of jcr operations (versioning, locking, etc.)
 - commands simplified. Most commands don't have default values.
 - logging added

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@294914 13f79535-47bb-0310-9956-ffa450edef68

-package org.apache.jackrabbit.chain.command.info;
+package org.apache.jackrabbit.command.info;
+import javax.jcr.Node;
-import org.apache.jackrabbit.chain.CtxHelper;
-import org.apache.jackrabbit.chain.JcrCommandException;
+import org.apache.jackrabbit.command.CommandHelper;
+import org.apache.jackrabbit.command.CommandException;
-    /** property name pattern */
-    private String pattern;
-    private String patternKey;
+    private String patternKey = "pattern";
-    protected Iterator getProperties(Context ctx) throws JcrCommandException,
+    protected Iterator getProperties(Context ctx) throws CommandException,
-        String pattern = CtxHelper.getAttr(this.pattern, patternKey, "*", ctx);
-        return CtxHelper.getProperties(ctx, pattern);
-    }
-
-    /**
-     * @return Returns the pattern.
-     */
-    public String getPattern()
-    {
-        return pattern;
-    }
-
-    /**
-     * @param pattern
-     *            The pattern to set.
-     */
-    public void setPattern(String pattern)
-    {
-        this.pattern = pattern;
+        String pattern = (String) ctx.get(this.patternKey) ;
+        Node n = CommandHelper.getCurrentNode(ctx) ;
+        return CommandHelper.getProperties(ctx, n, pattern);

INS26 UPD40 INS40 UPD40 UPD40 INS31 MOV29 MOV83 MOV43 MOV42 MOV44 UPD43 MOV43 MOV43 INS8 INS45 UPD42 MOV60 INS60 INS41 MOV43 INS43 INS59 INS32 INS11 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42 MOV43 INS32 UPD42 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 UPD42 MOV42 MOV22 UPD42 DEL66 DEL65 DEL29 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL42 DEL45 DEL42 DEL32 DEL42 DEL32 DEL41 DEL8 DEL31 DEL66 DEL65 DEL29 DEL83 DEL42 DEL43 DEL42 DEL42 DEL41 DEL8 DEL31 DEL42 DEL66 DEL65 DEL29 DEL83 DEL39 DEL42 DEL42 DEL44 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL8 DEL31