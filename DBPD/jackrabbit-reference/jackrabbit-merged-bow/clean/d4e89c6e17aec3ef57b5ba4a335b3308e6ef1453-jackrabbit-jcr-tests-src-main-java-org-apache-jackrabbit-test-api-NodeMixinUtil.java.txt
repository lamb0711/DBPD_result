JCR-3499: Test cases should not rely on equality of node types

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1437933 13f79535-47bb-0310-9956-ffa450edef68

-import java.util.Arrays;
-import java.util.List;
+import java.util.HashSet;
+import java.util.Set;
-        List<NodeType> existingMixins = Arrays.asList(node.getMixinNodeTypes());
+
+        Set<String> existingMixins = new HashSet<String>();
+        for (NodeType nt : node.getMixinNodeTypes()) {
+            existingMixins.add(nt.getName());
+        }
-            NodeType nt = mixins.nextNodeType();
-            if (!existingMixins.contains(nt)) {
-                return nt.getName();
+            String ntName = mixins.nextNodeType().getName();
+            if (!existingMixins.contains(ntName)) {
+                return ntName;

UPD40 UPD40 INS70 UPD74 INS44 MOV32 MOV8 UPD43 UPD43 INS14 MOV43 INS42 INS21 UPD42 UPD42 INS74 INS32 INS43 INS8 INS43 INS43 INS42 INS42 MOV32 INS42 UPD42 INS32 INS41 INS42 INS42 MOV32 INS42 UPD42 INS42 DEL42 DEL42 DEL32 DEL41