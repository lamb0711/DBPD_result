JCR-1910: RMIRemoteBindingServlet fails to initialize if the RMI registry is not available

Hmm, in fact we can also ignore the MalformedURLException as we already logged that during init().


git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@727397 13f79535-47bb-0310-9956-ffa450edef68

-            log("Invalid RMI URL: " + url, e);
+            // Ignore, we already logged a warning about this during init()

DEL42 DEL45 DEL42 DEL27 DEL42 DEL32 DEL21