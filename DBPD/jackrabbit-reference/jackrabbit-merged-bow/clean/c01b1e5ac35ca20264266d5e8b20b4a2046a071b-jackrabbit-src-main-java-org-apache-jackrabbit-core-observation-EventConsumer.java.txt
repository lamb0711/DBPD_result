JCR-279: IllegalStateException thrown when consuming events

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@348920 13f79535-47bb-0310-9956-ffa450edef68

-        for (Iterator it = events.iterator(); it.hasNext();) {
+        for (Iterator it = events.iterator(); it.hasNext() && session.isLive();) {
+        // only deliver if session is still live
+        if (!session.isLive()) {
+            return;
+        }

INS25 INS27 INS38 INS8 MOV32 INS32 INS32 INS41 INS42 INS42 INS42 INS42