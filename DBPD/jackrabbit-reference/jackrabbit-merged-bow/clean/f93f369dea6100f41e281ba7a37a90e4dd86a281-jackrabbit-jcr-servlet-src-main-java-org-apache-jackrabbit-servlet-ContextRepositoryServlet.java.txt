JCR-956: Use the existing RepositoryFactory interface instead of recreating it as an abstract factory method of AbstractRepositoryServlet. As a bonus this automatically makes all repository binding servlets use lazy loading for the repository.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@606928 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.jackrabbit.commons.repository.ProxyRepository;
-     * Creates and returns a repository proxy for accessing a repository
+     * Creates and returns a repository factory for accessing a repository
-     * @return repository proxy
+     * @return repository factory
-    protected Repository getRepository() throws ServletException {
+    protected RepositoryFactory getRepositoryFactory()
+            throws ServletException {
-        RepositoryFactory factory;
-        if (context != null) {
-            factory = new CrossContextRepositoryFactory(context, path, name);
+        if (path != null) {
+            return new CrossContextRepositoryFactory(context, path, name);
-            factory = new ContextRepositoryFactory(context, name);
+            return new ContextRepositoryFactory(context, name);
-        return new ProxyRepository(factory);

MOV43 UPD42 UPD66 UPD66 UPD42 INS41 INS41 MOV14 MOV14 DEL40 DEL26 DEL42 DEL43 DEL42 DEL59 DEL60 DEL42 DEL7 DEL21 DEL42 DEL7 DEL21 DEL42 DEL43 DEL42 DEL14 DEL41