Refactoring step 8 (JCR-53)
 - Fixed createWorkspaceProblem() (not adding the created configuration)
 - Added package documentation and a simple class diagram
 - Simplified workspace template handling
 - Javadocs and cleanups in RepositoryConfig


git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@156506 13f79535-47bb-0310-9956-ffa450edef68

+ * <p>
+ * Note that the configuration objects returned by this parser are not
+ * initialized. The caller needs to initialize the configuration objects
+ * before using them.
+     * <p>
+     * Note that the returned repository configuration object has not been
+     * initialized.
+        // Workspace configuration template
+        Element template = getElement(root, WORKSPACE_ELEMENT);
+
-        return new RepositoryConfig(
-                root, this, home, appName, fsc, workspaceDirectory,
-                defaultWorkspace, amc, vc);
+        return new RepositoryConfig(home, appName, amc, fsc,
+                workspaceDirectory, defaultWorkspace, template, vc);
+     * <p>
+     * Note that the returned workspace configuration object has not been
+     * initialized.

INS66 INS66 INS66 INS66 INS60 INS41 INS66 INS66 INS66 INS43 INS59 INS14 INS66 INS66 INS66 INS42 INS42 INS32 MOV43 MOV42 MOV42 INS42 MOV42 MOV42 MOV42 UPD42 MOV42 MOV42 INS42 INS42 INS42 DEL42 DEL52 DEL14 DEL41