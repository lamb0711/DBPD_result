JCR-1150: use "Integer.valueOf" instead of "new Integer", uses entry sets instead of key sets when value is needed as well
 

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@580332 13f79535-47bb-0310-9956-ffa450edef68

-        for (Iterator it = activeListeners.keySet().iterator(); it.hasNext(); ) {
-            EventListener listener = (EventListener) it.next();
-            EventFilter filter = (EventFilter) activeListeners.get(listener);
+        for (Iterator it = activeListeners.entrySet().iterator(); it.hasNext(); ) {
+            Map.Entry entry = (Map.Entry) it.next();
+            EventListener listener = (EventListener) entry.getKey();
+            EventFilter filter = (EventFilter) entry.getValue();

INS60 INS43 MOV43 INS59 MOV43 INS40 UPD42 INS42 INS11 UPD42 INS43 MOV43 INS32 MOV43 INS40 INS42 INS42 UPD42 UPD42 DEL42