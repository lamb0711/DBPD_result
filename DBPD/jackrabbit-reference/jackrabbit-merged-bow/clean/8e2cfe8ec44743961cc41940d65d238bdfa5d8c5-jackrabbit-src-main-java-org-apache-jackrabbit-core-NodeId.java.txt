- clean up of some hashCode stuff

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@378546 13f79535-47bb-0310-9956-ffa450edef68

+    /** the precalculated hashcode */
+    private final int hashCode;
+
+        this.hashCode = uuid.hashCode();
-    //-------------------------------------------< java.lang.Object overrides >
+    //-----------------------------------------< java.lang.Object overrides >---
-     * Compares node identifiers for equality.
-     *
-     * @param obj other object
-     * @return <code>true</code> if the given object is a node identifier
-     *         instance that identifies the same node as this identifier,
-     *         <code>false</code> otherwise
-     * @see Object#equals(Object)
+     * {@inheritDoc}
-     * Returns the node UUID.
+     * {@inheritDoc}
-     * @return node UUID
-     * @see Object#toString()
+     * Returns the same as <code>this.getUUID().toString()</code>
-     * Returns the hash code of the node UUID. The computed hash code
-     * is memorized for better performance.
+     * {@inheritDoc}
-     * @return hash code
-     * @see Object#hashCode()
+     * Returns the same as <code>this.getUUID().hashCode()</code>
-        // NodeId is immutable, we can store the computed hash code value
-        if (hash == 0) {
-            hash = uuid.hashCode();
-        }
-        return hash;
+        return hashCode;

INS23 INS29 INS83 INS83 INS39 INS59 INS65 INS42 INS21 INS65 INS66 INS7 INS65 INS65 UPD66 INS65 UPD66 UPD42 INS22 MOV32 INS52 INS42 DEL66 DEL65 DEL42 DEL66 DEL65 DEL66 DEL66 DEL66 DEL65 DEL42 DEL42 DEL42 DEL43 DEL69 DEL68 DEL65 DEL66 DEL65 DEL42 DEL42 DEL68 DEL65 DEL66 DEL66 DEL65 DEL42 DEL42 DEL68 DEL65 DEL42 DEL34 DEL27 DEL42 DEL7 DEL21 DEL8 DEL25