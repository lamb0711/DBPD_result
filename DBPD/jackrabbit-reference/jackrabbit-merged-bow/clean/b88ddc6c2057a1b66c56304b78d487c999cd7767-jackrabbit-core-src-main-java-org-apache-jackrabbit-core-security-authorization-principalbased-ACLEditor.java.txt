JCR-1588: JSR 283 Access Control

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@713975 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.jackrabbit.api.security.principal.NoSuchPrincipalException;
-        Principal principal;
+        Principal principal = null;
-            principal = pMgr.getPrincipal(principalName);
-        } else {
+            try {
+                principal = pMgr.getPrincipal(principalName);
+            } catch (NoSuchPrincipalException e) {
+                // should not get here. 
+            }
+        }
+        if (principal == null) {
-            // TODO: rather throw?

INS26 INS40 INS25 MOV32 INS8 INS27 INS33 INS54 INS42 INS33 MOV8 INS12 INS44 INS8 INS43 INS42 INS42