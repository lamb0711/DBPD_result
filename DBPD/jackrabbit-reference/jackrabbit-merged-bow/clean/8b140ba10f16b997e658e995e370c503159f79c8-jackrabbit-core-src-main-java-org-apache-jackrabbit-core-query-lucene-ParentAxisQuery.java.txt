JCR-1427: XPath query with child axis predicates

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@631619 13f79535-47bb-0310-9956-ffa450edef68

-                                hits.clear(i);
+                                // no more name tests, clear remaining
+                                hits.clear(i, hits.length());
+                            } else {
+                                // assert doc >= i
+                                int doc = tDocs.doc();
+                                if (doc > i) {
+                                    // clear hits
+                                    hits.clear(i, doc);
+                                    i = doc;
+                                }

INS8 INS25 MOV38 INS8 INS8 INS21 INS60 MOV25 INS32 INS39 INS59 INS27 INS42 INS42 INS42 INS32 INS42 INS32 INS42 INS42 INS21 INS42 INS42 INS42 INS42 INS7 INS42 INS42 INS42 DEL8