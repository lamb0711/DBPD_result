JCR-415: Enhance indexing of binary content
- Remove workaround with TextExtractorReader. Binary content is now indexed (text extracted) when really needed.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@489112 13f79535-47bb-0310-9956-ffa450edef68

-                    Document d = index.createDocument(n);
+                    Document d = index.createNodeIndexer(n).createDoc();
-                Document d = index.createDocument(node);
+                Document d = index.createNodeIndexer(node).createDoc();

INS32 INS32 INS42 INS32 MOV42 UPD42 MOV42 MOV42 INS32 INS42 MOV42 UPD42 MOV42 MOV42 DEL32 DEL32