JCR-391 WebDAV method invocation trying to create a new resource should fail with 409 (Conflict) if parent resource does not exist

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@393731 13f79535-47bb-0310-9956-ffa450edef68

-        // TODO: support shallow and deep copy
-        if (shallow) {
+        if (shallow && isCollection()) {
+            // TODO: currently no support for shallow copy; however this is
+            // only relevant if the source resource is a collection, because
+            // otherwise it doesn't make a difference

INS27 INS42 INS32 INS42 DEL42