[JCR-317] replace UUID strings by UUID classes in NodeId, etc..

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@378221 13f79535-47bb-0310-9956-ffa450edef68

-    private final String rootNodeId;
+    private final NodeId rootNodeId;
-    private final String parentId;
+    private final NodeId parentId;
-            String rootNodeId, String parentId) {
+            NodeId rootNodeId, NodeId parentId) {
-     * @return
+     * @return the virtual item state provider
-                NodeImpl root = (NodeImpl) systemSession.getItemManager().getItem(new NodeId(rootNodeId));
+                NodeImpl root = (NodeImpl) systemSession.getItemManager().getItem(rootNodeId);
-                NodeImpl root = (NodeImpl) systemSession.getItemManager().getItem(new NodeId(rootNodeId));
+                NodeImpl root = (NodeImpl) systemSession.getItemManager().getItem(rootNodeId);
-                parent.internalGetUUID(),
+                parent.getNodeId(),
-                node.internalGetUUID(),
+                node.getNodeId(),
-                    node.internalGetUUID(),
+                    (NodeId) node.getId(),
-                parent.internalGetUUID(),
+                parent.getNodeId(),
-                node.internalGetUUID(),
+                node.getNodeId(),

UPD43 UPD43 UPD42 UPD42 UPD43 MOV43 UPD42 UPD42 MOV42 INS66 UPD42 UPD42 UPD42 UPD42 MOV43 INS11 MOV43 INS32 MOV43 MOV42 UPD42 MOV42 INS42 INS42 DEL42 DEL14 DEL42 DEL14 DEL32