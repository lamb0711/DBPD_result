JCR-1323: Fixed and test case added

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@613221 13f79535-47bb-0310-9956-ffa450edef68

-            int totalSize = getTotalSize();
-            if (limit > 0 && totalSize > limit) {
+            long size = getTotalSize() - offset;
+            if (limit > 0 && size > limit) {
-                return totalSize - offset;
+                return size;

UPD39 UPD42 INS27 MOV32 INS42 UPD42 INS42 DEL42 DEL42 DEL27