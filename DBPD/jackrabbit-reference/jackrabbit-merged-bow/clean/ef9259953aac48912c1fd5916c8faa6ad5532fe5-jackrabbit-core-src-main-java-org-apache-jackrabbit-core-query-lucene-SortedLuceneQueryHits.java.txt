JCR-2374: Use FieldSelector in Sorted/LuceneQueryHits when reading UUID

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@831844 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.lucene.search.Sort;
-import org.apache.lucene.search.Query;
-import org.apache.lucene.search.TopFieldDocCollector;
-import org.apache.lucene.search.ScoreDoc;
-import org.apache.lucene.index.IndexReader;
-import org.apache.jackrabbit.core.id.NodeId;
-import org.slf4j.LoggerFactory;
-import org.slf4j.Logger;
-
-import java.util.List;
+import java.util.List;
+
+import org.apache.jackrabbit.core.id.NodeId;
+import org.apache.lucene.index.IndexReader;
+import org.apache.lucene.search.Query;
+import org.apache.lucene.search.ScoreDoc;
+import org.apache.lucene.search.Sort;
+import org.apache.lucene.search.TopFieldDocCollector;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
-            String uuid = reader.document(docs[i].doc).get(FieldNames.UUID);
+            String uuid = reader.document(docs[i].doc,
+                    FieldSelectors.UUID).get(FieldNames.UUID);

MOV26 MOV26 MOV26 MOV26 MOV26 MOV26 MOV26 MOV26 INS40