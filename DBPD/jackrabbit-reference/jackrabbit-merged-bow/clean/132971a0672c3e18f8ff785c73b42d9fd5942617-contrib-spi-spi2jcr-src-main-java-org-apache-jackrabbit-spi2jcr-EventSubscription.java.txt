JCR-1153: remove EventBundle.getId() and adjust EventBundleImpl accordingly.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@581837 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.jackrabbit.uuid.UUID;
-            bundles[i] = new EventBundleImpl(filteredEvents,
-                    bundles[i].isLocal(), bundles[i].getBundleId());
+            bundles[i] = new EventBundleImpl(filteredEvents, bundles[i].isLocal());
-        String bundleId = UUID.randomUUID().toString();
-        EventBundle bundle = new EventBundleImpl(spiEvents, isLocal, bundleId);
+        EventBundle bundle = new EventBundleImpl(spiEvents, isLocal);

DEL40 DEL26 DEL42 DEL42 DEL2 DEL42 DEL32 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL42 DEL32 DEL59 DEL60 DEL42