JCR-1357: Create "quick start" developer bundles for model 1,2,3 deployment

Use the maven-dependency-plugin instead of the maven-assembly-plugin to generate the standalone jar file. This way we only get one build artifact.

Set the Main-Class attribute in the manifest.

Automatically detect the location of the jar file using Class.getProtectionDomain().

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@720524 13f79535-47bb-0310-9956-ffa450edef68

+import java.net.URL;
+        URL location =
+            Main.class.getProtectionDomain().getCodeSource().getLocation();
+
-        webapp.setWar("target/jackrabbit-standalone-SNAPSHOT-jar-with-dependencies.jar");
+        webapp.setWar(location.toURI().toString());
-        Server server = new Server(8080);
+        Server server = new Server(1234);
+        server.setStopAtShutdown(true);

INS26 INS40 INS60 INS21 INS43 INS59 MOV43 INS32 INS42 INS42 INS32 INS32 INS42 INS42 INS9 INS32 INS42 INS32 INS42 MOV43 UPD34 INS32 INS42 INS42 INS42 INS57 INS42 INS43 INS42 DEL45