JCR-2343: Utility class to tranform JCR-SQL2 to/from JCR-JQOM

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@825633 13f79535-47bb-0310-9956-ffa450edef68

-package org.apache.jackrabbit.spi.commons.query;
+package org.apache.jackrabbit.commons.query;
-import org.slf4j.Logger;
-import org.slf4j.LoggerFactory;
-
-     * Logger instance for this class.
-     */
-    private static final Logger log = LoggerFactory.getLogger(QueryObjectModelBuilderRegistry.class);
-
-    /**
-        try {
-            Iterator<QueryObjectModelBuilder> it = ServiceRegistry.lookupProviders(QueryObjectModelBuilder.class,
-                    QueryObjectModelBuilder.class.getClassLoader());
-            while (it.hasNext()) {
-                QueryObjectModelBuilder builder = it.next();
-                BUILDERS.add(builder);
-                languages.addAll(Arrays.asList(builder.getSupportedLanguages()));
-            }
-        } catch (Error e) {
-            log.warn("Unable to load providers for QueryObjectModelBuilder: " + e);
+        Iterator<QueryObjectModelBuilder> it = ServiceRegistry.lookupProviders(QueryObjectModelBuilder.class,
+                QueryObjectModelBuilder.class.getClassLoader());
+        while (it.hasNext()) {
+            QueryObjectModelBuilder builder = it.next();
+            BUILDERS.add(builder);
+            languages.addAll(Arrays.asList(builder.getSupportedLanguages()));

UPD40 MOV8 MOV60 MOV21 DEL40 DEL26 DEL40 DEL26 DEL66 DEL65 DEL29 DEL83 DEL83 DEL83 DEL42 DEL43 DEL42 DEL42 DEL42 DEL42 DEL43 DEL57 DEL32 DEL59 DEL23 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL45 DEL42 DEL27 DEL32 DEL21 DEL8 DEL12 DEL54 DEL8