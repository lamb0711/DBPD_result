JCR-973 XML import using MacOS X WebDAV client does not work


git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@548395 13f79535-47bb-0310-9956-ffa450edef68

-        return isXmlMimeType && context.hasStream() && super.canImport(context, isCollection);
+        return isXmlMimeType
+                && context.hasStream()
+                && context.getContentLength() > 0
+                && super.canImport(context, isCollection);

INS27 MOV42 MOV32 INS27 INS32 INS34 INS42 INS42