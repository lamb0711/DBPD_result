[JCR-317] replace UUID strings by UUID classes in NodeId, etc..

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@378221 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.jackrabbit.core.NodeId;
+import org.apache.jackrabbit.core.SessionImpl;
+
-    private final Session session;
+    private final SessionImpl session;
-        this.session = session;
+        this.session = (SessionImpl) session;
-        String id = (String) versions.removeFirst();
+        NodeId id = (NodeId) versions.removeFirst();
-            return (Version) session.getNodeByUUID(id);
+            return (Version) session.getNodeById(id);
-        String id = v.getId();
+        NodeId id = v.getId();

INS26 INS26 INS40 INS40 UPD43 UPD42 UPD43 UPD43 INS11 UPD42 UPD42 INS43 INS42 UPD43 INS42 UPD42 UPD42 DEL42