#JCR-395 Using transactions leads to memory leak


git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@394937 13f79535-47bb-0310-9956-ffa450edef68

-        if (flags == TMSUCCESS || flags == TMFAIL) {
-            associate(null);
-            txGlobal.remove(xid);
-        } else if (flags == TMSUSPEND) {
+        if (flags == TMSUCCESS || flags == TMFAIL || flags == TMSUSPEND) {
+
+        txGlobal.remove(xid);
+
+        txGlobal.remove(xid);

MOV25 INS21 MOV21 INS27 INS32 MOV27 MOV27 INS42 INS42 INS42 DEL42 DEL33 DEL32 DEL21 DEL8 DEL25