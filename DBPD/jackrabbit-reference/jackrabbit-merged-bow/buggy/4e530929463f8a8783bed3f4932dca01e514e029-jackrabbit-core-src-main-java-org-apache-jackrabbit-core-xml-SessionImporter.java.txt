JCR-1104 - JSR 283 support
- shareble nodes (work in progress)
- workspace and session import of conflicting shareable nodes

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@644747 13f79535-47bb-0310-9956-ffa450edef68

+            // if conflicting node is shareable, then clone it
+            if (conflicting.isNodeType(NameConstants.MIX_SHAREABLE)) {
+                parent.clone(conflicting, nodeInfo.getName());
+                return null;
+            }
+                    if (node == null) {
+                        // no new node has been created, so skip this node
+                        parents.push(null); // push null onto stack for skipped node
+                        log.debug("skipping existing node " + nodeInfo.getName());
+                        return;
+                    }

INS25 INS32 INS8 INS42 INS42 INS40 INS21 INS41 INS8 INS32 INS33 MOV21 INS25 INS42 INS42 INS42 INS32 INS27 MOV8 INS42 INS42 INS42 INS33 INS21 INS21 INS41 INS32 INS32 INS42 INS42 INS33 INS42 INS42 INS27 INS45 INS32 INS42 INS42