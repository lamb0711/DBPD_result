Removed o.a.j.Constants and moved all name constants to o.a.j.name.QName in order to simplify package dependencies. Also added two new constants: QName.EMPTY and QName.WILDCARD.

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@293034 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.jackrabbit.Constants;
+import org.apache.jackrabbit.name.QName;
-            systemViewXML = Constants.NS_SV_URI.equals(namespaceURI);
+            systemViewXML = QName.NS_SV_URI.equals(namespaceURI);
-        private static final String DEFAULT_URI = Constants.NS_DEFAULT_URI;
-
-        private static final String EMPTY_PREFIX = "";
-            if (DEFAULT_URI.equals(uri)) {
+            if (QName.NS_DEFAULT_URI.equals(uri)) {
-                return DEFAULT_URI;
+                return QName.NS_DEFAULT_URI;
-            if (DEFAULT_URI.equals(uri)) {
+            if (QName.NS_DEFAULT_URI.equals(uri)) {
-                if (uri.equals(nsContext.getURI(EMPTY_PREFIX))) {
-                    return EMPTY_PREFIX;
+                if (uri.equals(nsContext.getURI(QName.NS_EMPTY_PREFIX))) {
+                    return QName.NS_EMPTY_PREFIX;

MOV26 UPD40 INS40 INS40 UPD40 INS40 INS40 INS40 DEL83 DEL83 DEL83 DEL42 DEL43 DEL42 DEL40 DEL59 DEL23 DEL83 DEL83 DEL83 DEL42 DEL43 DEL42 DEL45 DEL59 DEL23 DEL42 DEL42 DEL42 DEL42 DEL42