JCR-258 - remove JDOM dependency from jcr-server contribution
JCR-295 - usage of Cache-Control header 
JCR-297 - log output while parsing xml
minor consistency issues and improvements

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@368683 13f79535-47bb-0310-9956-ffa450edef68

+    private OutputStream outStream;
-                return new FileOutputStream(outFile);
+                // clean up the stream retrieved by the preceeding handler, that
+                // did not behave properly and failed to export although initially
+                // willing to handle the export.
+                if (outStream != null) {
+                    outStream.close();
+                }
+                outStream = new FileOutputStream(outFile);
+                return outStream;
+        // make sure the outputStream gets closed (and don't assume the handlers
+        // took care of this.
+        if (outStream != null) {
+            try {
+                outStream.close();
+            } catch (IOException e) {
+                // ignore
+            }
+        }
+                boolean hasContentLength = false;
+                    // check for content-length
+                    hasContentLength = DavConstants.HEADER_CONTENT_LENGTH.equals(name);
+                        // make sure the content-length is set
+                        if (!hasContentLength) {
+                            outputCtx.setContentLength(outFile.length());
+                        }

INS23 INS83 INS43 INS59 INS42 INS42 INS25 INS27 INS8 INS42 INS33 INS54 INS8 INS12 INS25 INS21 INS21 INS44 INS8 INS60 INS27 INS8 INS7 INS42 INS32 INS43 INS42 INS39 INS59 INS42 INS33 INS21 INS42 MOV14 INS42 INS42 INS42 INS42 INS9 INS21 INS32 INS7 INS42 INS42 INS42 INS32 INS25 INS40 INS42 INS42 INS38 INS8 INS42 INS21 INS32 INS42 INS42 INS32 INS42 INS42