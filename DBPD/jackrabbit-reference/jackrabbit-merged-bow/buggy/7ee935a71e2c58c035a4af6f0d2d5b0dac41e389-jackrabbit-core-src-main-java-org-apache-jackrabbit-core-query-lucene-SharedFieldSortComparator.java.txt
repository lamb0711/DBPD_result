JCR-97: Improve Checkstyle conformance
    - Various Checkstyle improvements

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@615680 13f79535-47bb-0310-9956-ffa450edef68

-                if (iTerm == jTerm) return 0;
-                if (iTerm == null) return -1;
-                if (jTerm == null) return 1;
-
-                return iTerm.compareTo(jTerm);
+                if (iTerm == jTerm) {
+                    return 0;
+                } else if (iTerm == null) {
+                    return -1;
+                } else if (jTerm == null) {
+                    return 1;
+                } else {
+                    return iTerm.compareTo(jTerm);
+                }

INS8 MOV25 MOV41 INS8 INS25 MOV41 MOV27 INS8 INS8 MOV41 MOV41 DEL25