JCR-1357: Create "quick start" developer bundles for model 1,2,3 deployment

Graceful handling of startup errors.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@721191 13f79535-47bb-0310-9956-ffa450edef68

+import java.net.BindException;
-            server.start();
-            String host = connector.getHost();
-            if (host == null) {
-                host = "localhost";
+            try {
+                server.start();
+
+                String host = connector.getHost();
+                if (host == null) {
+                    host = "localhost";
+                }
+                message("Apache Jackrabbit is now running at "
+                        +"http://" + host + ":" + connector.getPort() + "/");
+            } catch (Throwable t) {
+                System.err.println(
+                        "Unable to start the server: " + t.getMessage());
+                System.exit(1);
-            message("Apache Jackrabbit is now running at "
-                    +"http://" + host + ":" + connector.getPort() + "/");

INS26 INS40 INS54 INS8 INS12 MOV21 MOV60 MOV25 MOV21 INS44 INS8 INS43 INS42 INS21 INS21 INS42 INS32 INS32 INS40 INS42 INS27 INS42 INS42 INS34 INS45 INS32 INS42 INS42