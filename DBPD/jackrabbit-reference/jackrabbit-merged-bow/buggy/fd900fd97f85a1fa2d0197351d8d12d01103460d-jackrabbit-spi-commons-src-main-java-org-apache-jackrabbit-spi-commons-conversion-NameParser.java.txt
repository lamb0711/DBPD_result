JCR-1712: JCR Names (jsr 283)

- NameParser#checkFormat(String) failed for expanded names (missing check)
- some more tests

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@756405 13f79535-47bb-0310-9956-ffa450edef68

+        boolean checkFormat = (resolver == null);
-                        resolver.getPrefix(tmp);
+                        if (!checkFormat) {
+                            resolver.getPrefix(tmp);
+                        }
-        if (resolver == null) {
+        if (checkFormat) {

INS60 INS39 INS59 INS42 INS42 INS36 MOV27 INS25 INS38 INS8 INS42 MOV21