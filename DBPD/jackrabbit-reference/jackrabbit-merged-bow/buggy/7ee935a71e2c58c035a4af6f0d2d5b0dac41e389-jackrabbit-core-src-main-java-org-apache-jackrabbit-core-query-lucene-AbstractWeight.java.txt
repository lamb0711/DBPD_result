JCR-97: Improve Checkstyle conformance
    - Various Checkstyle improvements

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@615680 13f79535-47bb-0310-9956-ffa450edef68

-            IndexReader readers[] = mir.getIndexReaders();
-            int starts[] = new int[readers.length + 1];
+            IndexReader[] readers = mir.getIndexReaders();
+            int[] starts = new int[readers.length + 1];
-            Scorer scorers[] = new Scorer[readers.length];
-            for (int i = 0; i < readers.length; i++)
+            Scorer[] scorers = new Scorer[readers.length];
+            for (int i = 0; i < readers.length; i++) {
+            }

INS5 INS5 INS5 INS8 MOV43 INS85 INS39 INS85 MOV43 INS85 MOV21 DEL85 DEL39 DEL85 DEL85