JCR-1910: RMIRemoteBindingServlet fails to initialize if the RMI registry is not available

Just log a warning instead of failing with an exception when the repository can't be bound to the RMI registry.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@727388 13f79535-47bb-0310-9956-ffa450edef68

-            throw new ServletException("Invalid RMI URL: " + url, e);
+            log("Invalid RMI URL: " + url, e);
-            throw new ServletException(
-                    "RMI URL is already bound: " + url, e);
+            log("RMI URL is already bound: " + url, e);
-            throw new ServletException(
-                    "Failed to bind repository to RMI: " + url, e);
+            log("Failed to bind repository to RMI: " + url, e);

INS21 INS21 INS21 INS32 INS32 INS32 INS42 MOV27 INS42 INS42 MOV27 INS42 INS42 MOV27 INS42 DEL42 DEL43 DEL42 DEL14 DEL53 DEL42 DEL43 DEL42 DEL14 DEL53 DEL42 DEL43 DEL42 DEL14 DEL53