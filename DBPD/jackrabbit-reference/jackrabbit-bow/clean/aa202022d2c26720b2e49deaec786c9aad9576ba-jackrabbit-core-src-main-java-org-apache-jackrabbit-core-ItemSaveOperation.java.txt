only log full stack trace if debug is enabled

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1239824 13f79535-47bb-0310-9956-ffa450edef68

-                log.warn(msg, re);
+                if (log.isDebugEnabled()) {
+                    log.warn(msg, re);
+                } else {
+                    log.warn(msg);
+                }
