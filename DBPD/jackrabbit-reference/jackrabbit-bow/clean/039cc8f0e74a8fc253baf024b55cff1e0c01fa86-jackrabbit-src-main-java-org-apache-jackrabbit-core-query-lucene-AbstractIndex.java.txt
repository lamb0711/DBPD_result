Provide a new configuration parameter for SearchIndex: maxFieldLength. Corresponds to the lucene property maxFieldLength.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@394959 13f79535-47bb-0310-9956-ffa450edef68

+    /** maxFieldLength config parameter */
+    private int maxFieldLength = SearchIndex.DEFAULT_MAX_FIELD_LENGTH;
+
+            indexWriter.maxFieldLength = maxFieldLength;
+     * The lucene index writer property: maxFieldLength
+     */
+    void setMaxFieldLength(int maxFieldLength) {
+        this.maxFieldLength = maxFieldLength;
+        if (indexWriter != null) {
+            indexWriter.maxFieldLength = maxFieldLength;
+        }
+    }
+
+    /**
