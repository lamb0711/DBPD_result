JCR-3266 JCR-SQL2 query with multiple columns in result only returns last column when using Row.getValues()

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1303438 13f79535-47bb-0310-9956-ffa450edef68

-        for (Operand operand : columns.values()) {
-            values = evaluator.getValues(operand, this);
+        int i = 0;
+        for (String columnName : columns.keySet()) {
+            values[i++] = getValue(columnName);
