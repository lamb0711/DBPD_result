minor improvement

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1213722 13f79535-47bb-0310-9956-ffa450edef68

-    private static Logger log = LoggerFactory
-            .getLogger(LinkHeaderFieldParser.class);
+    private static Logger log = LoggerFactory.getLogger(LinkHeaderFieldParser.class);
-
-
-        for (String s : fieldValues) {
-            addFields(tmp, s);
+        if (fieldValues != null) {
+            for (String value : fieldValues) {
+                addFields(tmp, value);
+            }
-
-
-        if (en.hasMoreElements()) {
+        if (en != null && en.hasMoreElements()) {
-
