calculate initial size of the VersionIterator, since the size of the list is decreased in every iteration by calling LinkedList#removeFirst.

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@379191 13f79535-47bb-0310-9956-ffa450edef68

+     * The number of versions available.
+     */
+    private final long size;
+
+    /**
+        // retrieve inital size, since size of the list is not stable
+        size = versions.size();
-        return versions.size();
+        return size;
