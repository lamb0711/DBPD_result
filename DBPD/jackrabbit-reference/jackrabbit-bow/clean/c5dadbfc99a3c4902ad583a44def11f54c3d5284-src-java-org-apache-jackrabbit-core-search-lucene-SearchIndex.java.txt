Fixed order of jcr:score. Ascending now lists lower jcr:score values first. That is, less relevant first.

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@154145 13f79535-47bb-0310-9956-ffa450edef68

-                sortFields[i] = new SortField(null, SortField.SCORE, !orderSpecs[i]);
+                // order on jcr:score does not use the natural order as
+                // implemented in lucene. score ascending in lucene means that
+                // higher scores are first. JCR specs that lower score values
+                // are first.
+                sortFields[i] = new SortField(null, SortField.SCORE, orderSpecs[i]);
