JCR-1333: jackrabbit-jcr-rmi: Supplied javax.transaction.xa.Xid is assumed serializable, but is not on some environments
    - Added the SerializableXid class and adapted XAResources to use it

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@628289 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.jackrabbit.rmi.remote.SerializableXid;
-            return resource.recover(flag);
+            Xid[] xids = resource.recover(flag);
+            for (int i = 0; i < xids.length; i++) {
+                xids[i] = new SerializableXid(xids[i]);
+            }
+            return xids;
