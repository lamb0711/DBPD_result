JCR-574: MsExcelTextFilter throws Exception. Repository is not startable
- also catch runtime exceptions when initializing the reader. Some text filter implementations or 3rd party libraries may not behave properly.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@454360 13f79535-47bb-0310-9956-ffa450edef68

-            } catch (IOException e) {
+            } catch (Throwable t) {
-                        getClass().getName() + ": " + e);
-                log.debug("Dump: ", e);
+                        getClass().getName() + ": " + t);
+                log.debug("Dump: ", t);
