JCR-3617: Inconsistent CachingHierarchyManager under concurrent access
- Preliminary fix

This does not fix the root cause, but rather detects the inconsistent state and works around it after it happened.

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1498850 13f79535-47bb-0310-9956-ffa450edef68

-            throw new RepositoryException(msg, e);
+            log.debug(msg);
+        // JCR-3617: fall back to super class in case of ItemStateException
+        return super.resolvePath(path, typesAllowed);
