JCR-2313 - Improvements to user management (2) [work in progress] 

- fix query-statement in case of searchRoot equals the root-node's path

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@881201 13f79535-47bb-0310-9956-ffa450edef68

-        stmt.append(getSearchRoot(ntName));
+        String searchRoot = getSearchRoot(ntName);
+        if (!"/".equals(searchRoot)) {
+            stmt.append(searchRoot);
+        }
