Fix invalid UUID URN in Link header (introduced for JCR-2542)

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@1230507 13f79535-47bb-0310-9956-ffa450edef68

-    // a globally unique URI identifiying this session
-    private final String sessionIdentifier = "urn:uuid" + UUID.randomUUID();
+    // a globally unique URI identifying this session
+    private final String sessionIdentifier = "urn:uuid:" + UUID.randomUUID();
