JCR-1650: XPathQueryBuilder may not handle multiple jcr:deref correctly

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@668086 13f79535-47bb-0310-9956-ffa450edef68

-        return operands.remove(operand);
+        // JCR-1650 search the operand without relying on Object#equals(Object)
+        Iterator it = operands.iterator();
+        while (it.hasNext()) {
+            if (it.next() == operand) {
+                it.remove();
+                return true;
+            }
+        }
+        return false;
