JCR-2004: Update SPI locking to match JCR 2.0
JCR-2097: Broke lock tests in jcr2spi after the JCR 2.0 upgrade

- adjusted spi2jcr LockInfo implementation. that doesn't yet fix all broken tests... but those related to lock ownership
- deprecated the old LockInfoImpl constructor in spi-commons

git-svn-id: https://svn.apache.org/repos/asf/jackrabbit/trunk@772900 13f79535-47bb-0310-9956-ffa450edef68

-        super(lock.getLockToken(), lock.getLockOwner(),
-                lock.isDeep(), lock.isSessionScoped(),
+        super(lock.getLockToken(), lock.getLockOwner(), lock.isDeep(),
+                lock.isSessionScoped(), lock.getSecondsRemaining(), lock.isLockOwningSession(), 
