JCR-196: jcr:like() does not scale well on large value ranges
- do not use 'enum' as variable name

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@239418 13f79535-47bb-0310-9956-ffa450edef68

-            TermEnum enum = new WildcardTermEnum(reader, field, propName, pattern);
+            TermEnum terms = new WildcardTermEnum(reader, field, propName, pattern);
-                    while (enum.term() != null) {
-                        docs.seek(enum);
+                    while (terms.term() != null) {
+                        docs.seek(terms);
-                        if (!enum.next()) {
+                        if (!terms.next()) {
-                enum.close();
+                terms.close();
