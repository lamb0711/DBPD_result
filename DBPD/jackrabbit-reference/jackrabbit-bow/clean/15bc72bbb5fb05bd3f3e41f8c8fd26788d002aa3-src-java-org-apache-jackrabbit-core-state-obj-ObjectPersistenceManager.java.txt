inputstreams were not closed while storing blobs

git-svn-id: https://svn.apache.org/repos/asf/incubator/jackrabbit/trunk@124974 13f79535-47bb-0310-9956-ffa450edef68

-                String blobId = blobStore.put((PropertyId) state.getId(), i, blobVal.getStream(), blobVal.getLength());
+                InputStream in = blobVal.getStream();
+                String blobId;
+                try {
+                    blobId = blobStore.put((PropertyId) state.getId(), i, in,
+                            blobVal.getLength());
+                } finally {
+                    try {
+                        in.close();
+                    } catch (IOException e) {
+                        // ignore
+                    }
+                }
