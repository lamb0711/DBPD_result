HADOOP-16962. Making `getBoolean` log warning message for unrecognized value. Contributed by Ctest.

-    else return defaultValue;
+    else {
+      LOG.warn("Invalid value for boolean: " + valueString +
+               ", choose default value: " + defaultValue + " for " + name);
+      return defaultValue;
+    }
