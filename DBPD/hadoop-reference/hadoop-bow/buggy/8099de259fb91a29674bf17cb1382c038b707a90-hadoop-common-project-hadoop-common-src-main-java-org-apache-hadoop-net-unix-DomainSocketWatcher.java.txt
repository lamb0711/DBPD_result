HADOOP-10404. Some accesses to DomainSocketWatcher#closed are not protected by the lock (cmccabe)

+      lock.lock();
+      } finally {
+        lock.unlock();
+    lock.lock();
+    } finally {
+      lock.unlock();
