Merge trunk into HDFS-3077 branch.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-3077@1377092 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.hadoop.hdfs.protocol.HdfsBlocksMetadata;
+  private final boolean getHdfsBlockLocationsEnabled;
+    this.getHdfsBlockLocationsEnabled = conf.getBoolean(
+        DFSConfigKeys.DFS_HDFS_BLOCKS_METADATA_ENABLED, 
+        DFSConfigKeys.DFS_HDFS_BLOCKS_METADATA_ENABLED_DEFAULT);
-      directoryScanner = new DirectoryScanner(this, data, conf);
+      directoryScanner = new DirectoryScanner(data, conf);
+
+  @Override
+  public HdfsBlocksMetadata getHdfsBlocksMetadata(List<ExtendedBlock> blocks,
+      List<Token<BlockTokenIdentifier>> tokens) throws IOException, 
+      UnsupportedOperationException {
+    if (!getHdfsBlockLocationsEnabled) {
+      throw new UnsupportedOperationException("Datanode#getHdfsBlocksMetadata "
+          + " is not enabled in datanode config");
+    }
+    if (blocks.size() != tokens.size()) {
+      throw new IOException("Differing number of blocks and tokens");
+    }
+    // Check access for each block
+    for (int i = 0; i < blocks.size(); i++) {
+      checkBlockToken(blocks.get(i), tokens.get(i), 
+          BlockTokenSecretManager.AccessMode.READ);
+    }
+    return data.getHdfsBlocksMetadata(blocks);
+  }
-  UpgradeManagerDatanode getUpgradeManagerDatanode(String bpid) {
-    BPOfferService bpos = blockPoolManager.get(bpid);
-    if(bpos==null) {
-      return null;
-    }
-    return bpos.getUpgradeManager();
-  }
-
-  private void transferBlock( ExtendedBlock block, 
-                              DatanodeInfo xferTargets[] 
-                              ) throws IOException {
+  private void transferBlock(ExtendedBlock block, DatanodeInfo xferTargets[])
+      throws IOException {
-    DatanodeInfo[] targets = rBlock.getLocations();
-    DatanodeID[] datanodeids = (DatanodeID[])targets;
+    DatanodeID[] datanodeids = rBlock.getLocations();

INS26 MOV31 INS40 INS23 INS83 INS83 INS39 INS59 INS78 INS83 UPD43 UPD42 INS44 INS43 INS43 INS8 INS42 INS21 INS42 UPD42 INS74 UPD42 INS74 INS42 INS42 INS42 INS25 MOV25 INS24 MOV41 INS7 INS43 INS43 INS43 INS74 INS38 INS8 UPD27 INS58 INS27 INS37 INS8 MOV5 INS22 INS32 UPD42 MOV42 INS42 INS42 INS43 INS43 INS42 INS53 INS32 INS32 INS53 INS39 INS59 INS42 INS32 INS42 INS21 UPD42 UPD42 INS42 UPD43 MOV43 MOV85 UPD42 INS52 INS42 INS42 INS42 INS40 INS40 INS42 UPD42 MOV42 INS14 INS42 INS42 INS42 INS42 INS14 INS42 INS34 INS42 INS42 INS32 MOV42 INS43 INS27 INS43 INS45 INS42 INS32 INS32 INS40 INS42 INS45 INS45 INS42 INS42 INS42 INS42 INS42 INS42 INS42 DEL52 DEL43 DEL42 DEL33 DEL33 DEL41 DEL43 DEL42 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL8 DEL42 DEL43 DEL85 DEL5 DEL42 DEL42 DEL11 DEL59 DEL60