HADOOP-10714. AmazonS3Client.deleteObjects() need to be limited to 1000 entries per call. Contributed by Juan Yu.

+import org.apache.commons.lang.StringUtils;
-  private String accessKey;
-  private String secretKey;
+  private final String accessKey;
+  private final String secretKey;
-    if (accessKey != null && secretKey != null) {
+    if (!StringUtils.isEmpty(accessKey) && !StringUtils.isEmpty(secretKey)) {
-

INS26 INS40 INS83 INS83 INS38 INS38 INS32 INS32 INS42 INS42 INS42 INS42 INS42 INS42 DEL42 DEL33 DEL27 DEL42 DEL33 DEL27