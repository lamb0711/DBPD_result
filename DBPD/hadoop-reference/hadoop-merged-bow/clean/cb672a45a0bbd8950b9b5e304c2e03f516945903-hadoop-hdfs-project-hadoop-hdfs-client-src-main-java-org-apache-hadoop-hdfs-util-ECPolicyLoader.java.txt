HDFS-11605. Allow user to customize new erasure code policies. Contributed by Huafeng Wang

+import java.net.MalformedURLException;
-    File policyFile = getPolicyFile(policyFilePath);
-    if (policyFile == null) {
-      LOG.warn("Not found any EC policy file");
-      return Collections.emptyList();
-    }
-
+      File policyFile = getPolicyFile(policyFilePath);
+      if (!policyFile.exists()) {
+        LOG.warn("Not found any EC policy file");
+        return Collections.emptyList();
+      }
-          + policyFile);
+          + policyFilePath);
-  private File getPolicyFile(String policyFilePath) {
+  private File getPolicyFile(String policyFilePath)
+      throws MalformedURLException {
-      URL url = Thread.currentThread().getContextClassLoader()
-          .getResource(policyFilePath);
-      if (url == null) {
-        LOG.warn(policyFilePath + " not found on the classpath.");
-        policyFile = null;
-      } else if (!url.getProtocol().equalsIgnoreCase("file")) {
+      URL url = new URL(policyFilePath);
+      if (!url.getProtocol().equalsIgnoreCase("file")) {

INS26 INS40 INS43 MOV8 INS42 MOV60 MOV41 MOV38 INS8 MOV60 MOV25 INS60 MOV25 INS38 MOV43 INS59 INS32 INS42 INS14 INS42 INS42 INS43 INS42 UPD42 INS42 DEL42 DEL33 DEL27 DEL42 DEL42 DEL42 DEL32 DEL42 DEL32 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL33 DEL27 DEL42 DEL42 DEL42 DEL45 DEL27 DEL32 DEL21 DEL42 DEL33 DEL7 DEL21 DEL8 DEL25 DEL8