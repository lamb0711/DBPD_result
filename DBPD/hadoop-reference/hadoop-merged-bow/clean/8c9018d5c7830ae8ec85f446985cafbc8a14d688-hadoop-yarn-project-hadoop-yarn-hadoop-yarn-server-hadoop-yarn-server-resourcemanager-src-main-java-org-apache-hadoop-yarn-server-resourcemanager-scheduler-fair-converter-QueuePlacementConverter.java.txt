YARN-9899. Migration tool that help to generate CS config based on FS config [Phase 2]. Contributed by Peter Bacsko

+import org.apache.hadoop.yarn.server.resourcemanager.placement.RejectPlacementRule;
-        if (mapping.length() > 0) {
-          mapping.append(";");
-        }
+            if (mapping.length() > 0) {
+              mapping.append(";");
+            }
+        if (mapping.length() > 0) {
+          mapping.append(";");
+        }
-      } else {
+      } else if (!(rule instanceof RejectPlacementRule)) {
+    if (mapping.length() > 0) {
+      mapping.append(";");
+    }

INS26 INS40 INS25 INS27 INS8 INS32 INS34 INS21 INS42 INS42 INS32 INS42 INS42 INS45 INS25 MOV25 INS25 INS38 MOV8 INS27 INS8 INS36 INS32 INS34 INS21 INS62 INS42 INS42 INS32 INS42 INS43 INS42 INS42 INS45 INS42