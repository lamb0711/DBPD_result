HDFS-6020. Fix the five findbugs warnings. Contributed by Kihwal Lee.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-5535@1572165 13f79535-47bb-0310-9956-ffa450edef68

-    if (outFile.exists()) {
-      outFile.delete();
+    if (outFile.exists() && !outFile.delete()) {
+      FsDatasetImpl.LOG.warn("Failed to delete old dfsUsed file in " +
+        outFile.getParent());
+    FileWriter out = null;
-        FileWriter out = new FileWriter(outFile);
+        out = new FileWriter(outFile);
+        out = null;
+    } finally {
+      IOUtils.cleanup(null, out);
-          restartMeta.delete();
+          if (restartMeta.delete()) {
+            FsDatasetImpl.LOG.warn("Failed to delete restart meta file: " +
+              restartMeta.getPath());
+          }

INS60 INS27 MOV43 INS59 INS8 MOV32 INS38 INS42 INS33 INS21 MOV32 INS32 INS32 INS40 INS42 INS27 INS21 INS21 INS42 INS42 INS33 INS42 INS45 INS32 INS7 INS7 INS42 INS42 INS42 MOV14 INS42 INS33 INS25 MOV32 INS8 MOV21 INS32 INS40 INS42 INS27 INS45 INS32 INS42 INS42 DEL42 DEL59 DEL60