YARN-2993. Several fixes (missing acl check, error log msg ...) and some refinement in AdminService. (Contributed by Yi Liu)

-import org.apache.hadoop.yarn.api.protocolrecords.GetClusterNodeLabelsRequest;
-import org.apache.hadoop.yarn.api.protocolrecords.GetClusterNodeLabelsResponse;
-import org.apache.hadoop.yarn.api.protocolrecords.GetNodesToLabelsRequest;
-import org.apache.hadoop.yarn.api.protocolrecords.GetNodesToLabelsResponse;
-import org.apache.hadoop.yarn.api.protocolrecords.impl.pb.GetClusterNodeLabelsResponsePBImpl;
-import org.apache.hadoop.yarn.api.protocolrecords.impl.pb.GetNodesToLabelsResponsePBImpl;
+    final String msg = "refresh queues.";
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not refresh queues.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, msg);
-      LOG.info("Exception refreshing queues ", ioe);
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "Exception refreshing queues");
-      throw RPCUtil.getRemoteException(ioe);
+      throw logAndWrapException(ioe, user.getShortUserName(), argName, msg);
+    final String msg = "refresh nodes.";
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not refresh nodes.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, msg);
-      LOG.info("Exception refreshing nodes ", ioe);
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService", "Exception refreshing nodes");
-      throw RPCUtil.getRemoteException(ioe);
+      throw logAndWrapException(ioe, user.getShortUserName(), argName, msg);
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not refresh super-user-groups.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, "refresh super-user-groups.");
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not refresh user-groups.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, "refresh user-groups.");
-    if (checkRMHAState && !isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not refresh user-groups.");
-      throwStandbyException();
+    if (checkRMHAState) {
+      checkRMStatus(user.getShortUserName(), argName, "refresh Admin ACLs.");
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(UserGroupInformation.getCurrentUser()
-          .getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not refresh Service ACLs.");
-      throwStandbyException();
-    }
+    UserGroupInformation user = checkAcls(argName);
+
+    checkRMStatus(user.getShortUserName(), argName, "refresh Service ACLs.");
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not update node resource.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, "update node resource.");
+    final String msg = "add labels.";
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not add labels.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, msg);
-      LOG.info("Exception add labels", ioe);
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService", "Exception add label");
-      throw RPCUtil.getRemoteException(ioe);
+      throw logAndWrapException(ioe, user.getShortUserName(), argName, msg);
+    final String msg = "remove labels.";
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not remove labels.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, msg);
-      LOG.info("Exception remove labels", ioe);
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService", "Exception remove label");
-      throw RPCUtil.getRemoteException(ioe);
+      throw logAndWrapException(ioe, user.getShortUserName(), argName, msg);
+    final String msg = "set node to labels.";
-    if (!isRMActive()) {
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "ResourceManager is not active. Can not set node to labels.");
-      throwStandbyException();
-    }
+    checkRMStatus(user.getShortUserName(), argName, msg);
-      LOG.info("Exception set node to labels. ", ioe);
-      RMAuditLogger.logFailure(user.getShortUserName(), argName,
-          adminAcl.toString(), "AdminService",
-          "Exception set node to labels.");
-      throw RPCUtil.getRemoteException(ioe);
+      throw logAndWrapException(ioe, user.getShortUserName(), argName, msg);
+
+  private void checkRMStatus(String user, String argName, String msg)
+      throws StandbyException {
+    if (!isRMActive()) {
+      RMAuditLogger.logFailure(user, argName, adminAcl.toString(), 
+          "AdminService", "ResourceManager is not active. Can not " + msg);
+      throwStandbyException();
+    }
+  }
+
+  private YarnException logAndWrapException(IOException ioe, String user,
+      String argName, String msg) throws YarnException {
+    LOG.info("Exception " + msg, ioe);
+    RMAuditLogger.logFailure(user, argName, adminAcl.toString(), 
+        "AdminService", "Exception " + msg);
+    return RPCUtil.getRemoteException(ioe);
+  }

INS31 INS31 INS83 INS39 INS42 INS44 INS44 INS44 INS43 INS8 INS83 INS43 INS42 INS44 INS44 INS44 INS44 INS43 INS8 INS60 MOV21 INS60 MOV21 MOV21 MOV21 INS60 INS21 MOV21 INS60 MOV21 INS60 MOV21 INS60 MOV21 INS43 INS42 INS43 INS42 INS43 INS42 INS42 INS25 INS42 INS43 INS42 INS43 INS42 INS43 INS42 INS43 INS42 INS42 INS21 INS21 INS41 INS83 INS43 INS59 INS83 INS43 INS59 INS42 INS43 INS59 INS32 INS83 INS43 INS59 INS83 INS43 INS59 INS83 INS43 INS59 INS42 INS42 INS42 MOV38 INS8 INS42 INS42 INS42 INS42 INS32 INS32 MOV32 INS42 INS42 INS45 UPD42 UPD42 MOV42 INS42 INS42 INS45 UPD42 INS32 UPD42 MOV42 UPD42 MOV42 UPD42 MOV32 UPD45 UPD42 MOV32 UPD45 INS42 INS42 INS32 UPD42 MOV42 MOV32 UPD42 MOV42 UPD45 MOV45 UPD42 UPD45 INS42 INS42 INS45 UPD42 MOV32 UPD42 MOV42 UPD42 MOV42 MOV44 INS8 INS42 INS42 INS45 UPD42 MOV32 UPD42 MOV42 UPD42 MOV42 MOV44 INS42 INS42 INS45 UPD42 MOV32 INS42 INS42 MOV44 INS21 MOV21 INS42 INS42 INS27 INS42 INS42 INS42 INS42 INS42 MOV32 INS45 INS27 INS53 UPD42 MOV42 UPD42 MOV42 INS53 UPD42 MOV42 MOV42 UPD42 MOV42 INS53 INS53 INS53 INS32 INS45 INS42 INS45 INS42 INS32 INS32 UPD42 MOV32 UPD45 INS32 INS32 INS32 INS42 INS42 INS42 INS42 MOV32 INS45 INS27 UPD42 MOV42 UPD42 MOV42 MOV32 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 MOV32 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 MOV32 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 MOV32 MOV42 INS42 UPD42 MOV42 UPD42 MOV42 MOV32 MOV42 UPD42 MOV42 INS45 INS42 DEL40 DEL26 DEL40 DEL26 DEL40 DEL26 DEL40 DEL26 DEL40 DEL26 DEL40 DEL26 DEL42 DEL42 DEL32 DEL45 DEL45 DEL42 DEL32 DEL38 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL42 DEL32 DEL45 DEL45 DEL32 DEL21 DEL42 DEL42 DEL42 DEL32 DEL53 DEL32 DEL45 DEL45 DEL42 DEL32 DEL38 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL42 DEL32 DEL45 DEL45 DEL32 DEL21 DEL42 DEL42 DEL42 DEL32 DEL53 DEL42 DEL42 DEL42 DEL32 DEL45 DEL42 DEL32 DEL38 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL42 DEL32 DEL45 DEL42 DEL32 DEL38 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL32 DEL38 DEL27 DEL42 DEL42 DEL42 DEL32 DEL45 DEL42 DEL32 DEL21 DEL42 DEL32 DEL38 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL32 DEL45 DEL32 DEL21 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL42 DEL32 DEL45 DEL42 DEL32 DEL38 DEL42 DEL32 DEL21 DEL8 DEL25 DEL32 DEL45 DEL45 DEL42 DEL32 DEL38 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL42 DEL32 DEL45 DEL45 DEL32 DEL21 DEL42 DEL42 DEL42 DEL32 DEL53 DEL8 DEL32 DEL45 DEL45 DEL8 DEL25 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL45 DEL45 DEL32 DEL21 DEL53 DEL42 DEL32 DEL38 DEL42 DEL42 DEL42 DEL45 DEL45 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL45 DEL45 DEL32 DEL21 DEL42 DEL42 DEL42 DEL32 DEL53