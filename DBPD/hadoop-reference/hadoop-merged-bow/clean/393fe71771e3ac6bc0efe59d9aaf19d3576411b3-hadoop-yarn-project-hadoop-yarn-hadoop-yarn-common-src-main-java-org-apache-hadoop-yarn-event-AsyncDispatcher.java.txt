YARN-3878. AsyncDispatcher can hang while stopping if it is configured for draining events on stop. Contributed by Varun Saxena

+        // Need to reset drained flag to true if event queue is empty,
+        // otherwise dispatcher will hang on stop.
+        drained = eventQueue.isEmpty();
+  protected boolean isEventThreadWaiting() {
+    return eventHandlingThread.getState() == Thread.State.WAITING;
+  }
+
+  @VisibleForTesting

INS31 INS78 INS83 INS39 INS42 INS8 INS42 INS41 INS27 INS32 INS40 INS42 INS42 INS21 INS7 INS42 INS32 INS42 INS42