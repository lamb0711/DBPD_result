HDFS-10458. getFileEncryptionInfo should return quickly for non-encrypted cluster.

-    if (!inode.isFile()) {
+    if (!inode.isFile() || !fsd.ezManager.hasCreatedEncryptionZone()) {

INS27 MOV38 INS38 INS32 INS40 INS42