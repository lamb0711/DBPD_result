HADOOP-11920. Refactor some codes for erasure coders. Contributed by Kai Zheng.

-    if (fact != null) {
-      return isEncoder ? fact.createEncoder() : fact.createDecoder();
-    }
-
-    return null;
+    return isEncoder ? fact.createEncoder() : fact.createDecoder();

MOV41 DEL42 DEL33 DEL27 DEL8 DEL25 DEL33 DEL41