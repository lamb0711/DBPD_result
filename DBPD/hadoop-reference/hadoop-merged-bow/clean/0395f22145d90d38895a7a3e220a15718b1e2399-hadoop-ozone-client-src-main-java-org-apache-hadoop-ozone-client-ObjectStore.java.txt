HDDS-1068. Improve the error propagation for ozone sh.
Contributed by Elek, Marton.

+import org.apache.hadoop.ozone.om.exceptions.OMException;
+import org.apache.hadoop.ozone.om.exceptions.OMException.ResultCodes;
-      } catch (IOException e) {
-        if (e.getMessage().contains("VOLUME_NOT_FOUND")) {
-          return new ArrayList<OzoneBucket>();
+      } catch (OMException e) {
+        if (e.getResult() == ResultCodes.VOLUME_NOT_FOUND) {
+          return new ArrayList<>();
+      } catch (IOException e) {
+        throw new RuntimeException(e);

INS26 INS26 INS40 INS40 INS12 INS44 MOV44 INS8 INS43 INS42 INS53 INS42 INS27 INS14 INS32 INS40 INS43 INS42 MOV42 UPD42 MOV42 INS42 UPD74 DEL32 DEL42 DEL45 DEL32 DEL42 DEL43