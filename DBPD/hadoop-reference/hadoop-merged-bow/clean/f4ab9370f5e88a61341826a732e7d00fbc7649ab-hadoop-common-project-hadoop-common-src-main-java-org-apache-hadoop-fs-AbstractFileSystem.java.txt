HADOOP-16026:Replace incorrect use of system property user.name.
Contributed by Dinesh Chitlangia.

+import org.apache.hadoop.security.UserGroupInformation;
-    return new Path("/user/"+System.getProperty("user.name")).makeQualified(
-                                                                getUri(), null);
+    String username;
+    try {
+      username = UserGroupInformation.getCurrentUser().getShortUserName();
+    } catch(IOException ex) {
+      LOG.warn("Unable to get user name. Fall back to system property " +
+          "user.name", ex);
+      username = System.getProperty("user.name");
+    }
+    return new Path("/user/" + username)
+        .makeQualified(getUri(), null);

INS26 INS40 INS60 INS54 INS43 INS59 INS8 INS12 INS42 INS42 INS21 INS44 INS8 INS7 INS43 INS42 INS21 INS21 INS42 INS32 INS42 INS32 INS7 INS42 INS32 INS42 INS42 INS42 INS27 INS42 INS42 MOV32 INS42 INS42 INS45 INS45