HDFS-14045. Use different metrics in DataNode to better measure latency of heartbeat/blockReports/incrementalBlockReports of Active/Standby NN. Contributed by Jiandan Yang.

+  private String serviceId = null;
+  private String nnId = null;
-  BPServiceActor(InetSocketAddress nnAddr, InetSocketAddress lifelineNnAddr,
-      BPOfferService bpos) {
+  BPServiceActor(String serviceId, String nnId, InetSocketAddress nnAddr,
+      InetSocketAddress lifelineNnAddr, BPOfferService bpos) {
+    if (serviceId != null) {
+      this.serviceId = serviceId;
+    }
+    if (nnId != null) {
+      this.nnId = nnId;
+    }
-        bpos.getBlockPoolId());
+        bpos.getBlockPoolId(), getRpcMetricSuffix());
-      dn.getMetrics().addBlockReport(brSendCost);
+      dn.getMetrics().addBlockReport(brSendCost, getRpcMetricSuffix());
+  private String getRpcMetricSuffix() {
+    if (serviceId == null && nnId == null) {
+      return null;
+    } else if (serviceId == null && nnId != null) {
+      return nnId;
+    } else if (serviceId != null && nnId == null) {
+      return serviceId;
+    } else {
+      return serviceId + "-" + nnId;
+    }
+  }
+
-            dn.getMetrics().addHeartbeat(scheduler.monotonicNow() - startTime);
+            dn.getMetrics().addHeartbeat(scheduler.monotonicNow() - startTime,
+                getRpcMetricSuffix());
-              bpos.getBlockPoolId());
+              bpos.getBlockPoolId(), getRpcMetricSuffix());
-              scheduler.monotonicNow() - startTime);
+              scheduler.monotonicNow() - startTime, getRpcMetricSuffix());
-      dn.getMetrics().addLifeline(scheduler.monotonicNow() - startTime);
+      dn.getMetrics().addLifeline(scheduler.monotonicNow() - startTime,
+          getRpcMetricSuffix());

INS23 INS23 INS31 INS83 INS43 INS59 INS83 INS43 INS59 INS44 INS44 INS83 INS43 INS42 INS8 INS42 INS42 INS33 INS42 INS42 INS33 INS43 INS42 INS43 INS42 INS25 INS25 INS42 INS25 INS42 INS42 INS27 INS8 INS27 INS8 INS27 INS8 INS25 INS42 INS33 INS21 INS42 INS33 INS21 INS32 INS27 INS27 INS41 INS27 INS8 INS25 INS7 INS7 INS42 INS42 INS33 INS42 INS33 INS33 INS27 INS27 INS41 INS27 INS8 INS8 INS32 INS22 INS42 INS22 INS42 INS32 INS42 INS33 INS42 INS33 INS42 INS27 INS27 INS41 INS41 INS42 INS52 INS42 INS52 INS42 INS42 INS42 INS33 INS42 INS33 INS42 INS27 INS42 INS45 INS42 INS32 INS32 INS42 INS42 INS32 INS42