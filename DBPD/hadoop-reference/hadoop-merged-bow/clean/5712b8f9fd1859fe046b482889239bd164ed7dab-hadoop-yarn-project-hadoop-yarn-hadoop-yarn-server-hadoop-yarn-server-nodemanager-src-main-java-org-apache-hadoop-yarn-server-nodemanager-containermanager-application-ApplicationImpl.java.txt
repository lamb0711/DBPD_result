YARN-3334. NM uses timeline client to publish container metrics to new timeline service. Contributed by Junping Du.

+import org.apache.hadoop.conf.Configuration;
+import org.apache.hadoop.yarn.client.api.TimelineClient;
+import org.apache.hadoop.yarn.conf.YarnConfiguration;
+  private TimelineClient timelineClient;
+    Configuration conf = context.getConf();
+    if (YarnConfiguration.systemMetricsPublisherEnabled(conf)) {
+      createAndStartTimelienClient(conf);
+    }
+  }
+  
+  private void createAndStartTimelienClient(Configuration conf) {
+    // create and start timeline client
+    this.timelineClient = TimelineClient.createTimelineClient(appId);
+    timelineClient.init(conf);
+    timelineClient.start();
+  
+  @Override
+  public TimelineClient getTimelineClient() {
+    return timelineClient;
+  }
-      app.context.getKnownCollectors().remove(app.getAppId());
+      // stop timelineClient when application get finished.
+      TimelineClient timelineClient = app.getTimelineClient();
+      if (timelineClient != null) {
+        timelineClient.stop();
+      }

INS26 INS26 INS26 INS40 INS40 INS40 INS23 INS31 INS31 INS83 INS43 INS59 INS83 INS39 INS42 INS44 INS8 INS78 INS83 INS43 INS42 INS8 INS42 INS42 INS60 INS25 INS43 INS42 INS21 INS21 INS21 INS42 INS42 INS41 INS43 INS59 INS32 INS8 INS42 INS7 INS32 INS32 INS42 INS60 INS25 INS42 INS42 INS32 INS42 INS42 INS42 INS21 INS22 INS32 INS42 INS42 INS42 INS42 INS42 INS43 INS59 INS27 INS8 INS42 INS42 INS32 INS52 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS42 INS33 INS21 INS42 INS42 MOV42 UPD42 MOV42 INS32 INS42 INS42 DEL40 DEL42 DEL32 DEL42 DEL32 DEL32 DEL21