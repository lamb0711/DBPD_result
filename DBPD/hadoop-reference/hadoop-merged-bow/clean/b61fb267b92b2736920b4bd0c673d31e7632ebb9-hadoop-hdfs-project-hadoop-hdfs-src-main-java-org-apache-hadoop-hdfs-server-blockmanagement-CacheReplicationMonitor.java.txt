HDFS-9390. Block management for maintenance states.

-      if (datanode.isDecommissioned() || datanode.isDecommissionInProgress()) {
+      if (!datanode.isInService()) {

INS38 INS32 MOV42 UPD42 MOV42 DEL32 DEL42 DEL42 DEL32 DEL27