HDDS-190. Improve shell error message for unrecognized option.
Contributed by Elek, Marton.

-import org.apache.commons.cli.CommandLine;
+import picocli.CommandLine.Command;
+import picocli.CommandLine.Option;
+import picocli.CommandLine.Parameters;
+
-import org.apache.hadoop.ozone.client.rest.OzoneException;
-import java.io.IOException;
-import java.net.URISyntaxException;
+@Command(name = "-createVolume",
+    description = "Creates a volume for the specified user")
-  private String rootName;
+  @Parameters(arity = "1..1", description = Shell.OZONE_VOLUME_URI_DESCRIPTION)
+  private String uri;
+
+  @Option(names = {"--user", "-user"},
+      description = "Owner of of the volume", required =
+      true)
-  private String volumeName;
+
+  @Option(names = {"--quota", "-quota"},
+      description =
+          "Quota of the newly created volume (eg. 1G)")
+  @Option(names = {"--root", "-root"},
+      description = "Development flag to execute the "
+          + "command as the admin (hdfs) user.")
+  private boolean root;
+
-   *
-   * @param cmd - CommandLine
-   * @throws IOException
-   * @throws OzoneException
-   * @throws URISyntaxException
-  protected void execute(CommandLine cmd)
-      throws IOException, OzoneException, URISyntaxException {
-    if (!cmd.hasOption(Shell.CREATE_VOLUME)) {
-      throw new OzoneClientException(
-          "Incorrect call : createVolume is missing");
-    }
+  public Void call() throws Exception {
-    String ozoneURIString = cmd.getOptionValue(Shell.CREATE_VOLUME);
-    URI ozoneURI = verifyURI(ozoneURIString);
+    URI ozoneURI = verifyURI(uri);
-    volumeName = ozoneURI.getPath().replaceAll("^/+", "");
+    String volumeName = ozoneURI.getPath().replaceAll("^/+", "");
-    if (cmd.hasOption(Shell.VERBOSE)) {
+    if (isVerbose()) {
-    if (cmd.hasOption(Shell.RUNAS)) {
+
+    String rootName;
+    if (root) {
-    if (!cmd.hasOption(Shell.USER)) {
-      throw new OzoneClientException(
-          "User name is needed in createVolume call.");
-    }
-
-    if (cmd.hasOption(Shell.QUOTA)) {
-      quota = cmd.getOptionValue(Shell.QUOTA);
-    }
-
-    userName = cmd.getOptionValue(Shell.USER);
-
-    if (cmd.hasOption(Shell.VERBOSE)) {
+    if (isVerbose()) {
+    return null;
+

MOV26 MOV26 UPD40 UPD40 UPD40 INS77 INS42 INS80 INS80 INS77 MOV43 INS59 INS77 INS77 MOV43 MOV59 INS77 INS39 INS59 UPD83 INS43 INS42 UPD43 INS42 INS45 INS42 INS45 INS42 INS80 INS80 INS42 INS42 INS80 INS80 INS80 INS42 INS80 INS80 INS42 INS80 INS80 INS42 UPD42 MOV42 UPD42 INS60 INS60 INS41 INS42 INS45 INS42 INS40 INS42 INS4 INS42 INS45 INS42 INS9 INS42 INS4 INS42 INS45 INS42 INS4 INS42 INS27 MOV43 INS59 MOV43 MOV59 INS42 INS33 INS45 INS45 INS45 INS45 INS45 INS45 INS45 INS45 INS42 MOV32 UPD42 UPD42 UPD42 DEL40 DEL26 DEL42 DEL59 DEL42 DEL66 DEL65 DEL42 DEL65 DEL42 DEL65 DEL42 DEL65 DEL39 DEL42 DEL43 DEL42 DEL44 DEL42 DEL43 DEL42 DEL43 DEL42 DEL42 DEL40 DEL32 DEL38 DEL42 DEL43 DEL45 DEL14 DEL53 DEL8 DEL25 DEL42 DEL42 DEL42 DEL40 DEL32 DEL59 DEL60 DEL42 DEL7 DEL21 DEL42 DEL40 DEL42 DEL42 DEL40 DEL32 DEL42 DEL42 DEL40 DEL32 DEL38 DEL42 DEL43 DEL45 DEL14 DEL53 DEL8 DEL25 DEL42 DEL42 DEL40 DEL32 DEL42 DEL42 DEL42 DEL40 DEL32 DEL7 DEL21 DEL8 DEL25 DEL42 DEL42 DEL42 DEL40 DEL32 DEL7 DEL21 DEL42 DEL40