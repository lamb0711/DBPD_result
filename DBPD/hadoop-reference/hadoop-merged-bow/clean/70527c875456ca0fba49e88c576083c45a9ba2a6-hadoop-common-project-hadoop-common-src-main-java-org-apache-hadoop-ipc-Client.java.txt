merge trunk into HDFS-4949 branch

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-4949@1513658 13f79535-47bb-0310-9956-ffa450edef68

-import static org.apache.hadoop.ipc.RpcConstants.CONNECTION_CONTEXT_CALL_ID;
+import static org.apache.hadoop.ipc.RpcConstants.*;
+import java.io.ByteArrayOutputStream;
+import javax.security.sasl.Sasl;
+import com.google.common.annotations.VisibleForTesting;
+    private ByteArrayOutputStream pingRequest; // ping message
+      if (doPing) {
+        // construct a RPC header with the callId as the ping callId
+        pingRequest = new ByteArrayOutputStream();
+        RpcRequestHeaderProto pingHeader = ProtoUtil
+            .makeRpcRequestHeader(RpcKind.RPC_PROTOCOL_BUFFER,
+                OperationProto.RPC_FINAL_PACKET, PING_CALL_ID,
+                RpcConstants.INVALID_RETRY_COUNT, clientId);
+        pingHeader.writeDelimitedTo(pingRequest);
+      }
+              authMethod = saslRpcClient.getAuthMethod();
+              // for testing
+              remoteId.saslQop =
+                  (String)saslRpcClient.getNegotiatedProperty(Sasl.QOP);
-            this.in = new DataInputStream(new BufferedInputStream(
-                new PingInputStream(inStream)));
-          } else {
-            this.in = new DataInputStream(new BufferedInputStream(inStream));
+            inStream = new PingInputStream(inStream);
-          this.out = new DataOutputStream(new BufferedOutputStream(outStream));
+          this.in = new DataInputStream(new BufferedInputStream(inStream));
+
+          // SASL may have already buffered the stream
+          if (!(outStream instanceof BufferedOutputStream)) {
+            outStream = new BufferedOutputStream(outStream);
+          }
+          this.out = new DataOutputStream(outStream);
+          
-          out.writeInt(RpcConstants.PING_CALL_ID);
+          out.writeInt(pingRequest.size());
+          pingRequest.writeTo(out);
+    private String saslQop; // here for testing
+    @VisibleForTesting
+    String getSaslQop() {
+      return saslQop;
+    }
+    

INS26 INS26 INS26 UPD40 INS40 INS40 INS40 INS23 INS23 INS31 INS83 INS43 INS59 INS83 INS43 INS59 INS78 INS43 INS42 INS8 INS42 INS42 INS25 INS42 INS42 INS42 INS42 INS41 INS42 INS8 INS42 INS21 INS60 INS21 INS7 INS43 INS59 INS32 INS42 INS14 INS42 INS42 INS32 INS42 INS42 INS42 INS25 MOV21 INS25 INS21 INS43 INS42 INS42 INS40 INS40 INS42 INS40 INS42 MOV42 INS8 INS38 MOV8 INS32 INS42 INS21 INS36 INS21 INS32 INS42 INS42 INS42 INS7 INS62 INS7 INS42 INS42 INS42 INS21 INS42 MOV14 INS42 INS43 INS42 MOV14 INS21 INS7 UPD42 MOV42 INS7 INS40 INS11 INS42 INS32 INS43 INS32 INS42 INS42 INS42 INS42 INS42 INS40 DEL52 DEL42 DEL22 DEL42 DEL43 DEL43 DEL14 DEL14 DEL7 DEL21 DEL8 DEL25 DEL40