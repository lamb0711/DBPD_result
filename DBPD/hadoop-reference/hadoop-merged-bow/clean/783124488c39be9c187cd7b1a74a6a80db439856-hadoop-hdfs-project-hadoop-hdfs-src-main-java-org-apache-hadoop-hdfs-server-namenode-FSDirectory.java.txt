Merging changes from trunk to pick HDFS-4124 changes.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1403306 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.hadoop.hdfs.server.namenode.INodeDirectory.INodesInPath;
-    INode[] dstInodes = new INode[dstComponents.length];
-    rootDir.getExistingPathINodes(dstComponents, dstInodes, false);
+    INodesInPath dstInodesInPath = rootDir.getExistingPathINodes(dstComponents,
+        dstComponents.length, false);
+    INode[] dstInodes = dstInodesInPath.getINodes();
-    verifyQuotaForRename(srcInodes,dstInodes);
+    verifyQuotaForRename(srcInodes, dstInodes);
-    final INode[] dstInodes = new INode[dstComponents.length];
-    rootDir.getExistingPathINodes(dstComponents, dstInodes, false);
+    INodesInPath dstInodesInPath = rootDir.getExistingPathINodes(dstComponents,
+        dstComponents.length, false);
+    final INode[] dstInodes = dstInodesInPath.getINodes();
-    INode[] inodes = new INode[components.length];
-    final int lastInodeIndex = inodes.length - 1;
+    final int lastInodeIndex = components.length - 1;
-      rootDir.getExistingPathINodes(components, inodes, false);
+      INodesInPath inodesInPath = rootDir.getExistingPathINodes(components,
+          components.length, false);
+      INode[] inodes = inodesInPath.getINodes();
-  /**
-   */
-    INode[] inodes = new INode[components.length];
-
-    rootDir.getExistingPathINodes(components, inodes, false);
+    INodesInPath inodesInPath = rootDir.getExistingPathINodes(components,
+        components.length, false);
+    INode[] inodes = inodesInPath.getINodes();
-    INode[] inodes = new INode[components.length];
-      rootDir.getExistingPathINodes(components, inodes, false);
+      INodesInPath inodesInPath = rootDir.getExistingPathINodes(components,
+          components.length, false);
+      INode[] inodes = inodesInPath.getINodes();

INS26 INS40 INS60 INS60 INS60 INS60 INS60 INS60 INS43 INS59 MOV5 INS59 INS43 INS59 INS83 MOV5 INS59 INS43 INS59 MOV5 INS59 UPD42 MOV42 UPD42 MOV42 INS32 INS42 INS32 UPD42 MOV42 INS42 INS32 INS42 INS32 INS60 INS60 UPD42 MOV42 UPD42 MOV42 INS32 INS42 INS32 INS60 INS60 MOV42 MOV42 MOV42 INS40 MOV9 INS42 INS42 MOV42 MOV42 MOV42 INS40 MOV9 INS42 INS42 UPD40 INS43 INS59 MOV5 INS59 MOV42 MOV42 MOV42 INS40 MOV9 INS42 INS42 INS43 INS59 MOV5 INS59 INS42 INS42 INS32 INS42 INS32 INS42 INS42 INS32 INS42 INS32 MOV42 MOV42 MOV42 INS40 MOV9 INS42 INS42 MOV42 MOV42 MOV42 INS40 MOV9 INS42 INS42 DEL43 DEL85 DEL5 DEL40 DEL3 DEL59 DEL60 DEL42 DEL32 DEL21 DEL83 DEL42 DEL43 DEL85 DEL5 DEL40 DEL3 DEL59 DEL60 DEL42 DEL32 DEL21 DEL42 DEL43 DEL85 DEL5 DEL42 DEL40 DEL3 DEL59 DEL60 DEL42 DEL32 DEL21 DEL29 DEL43 DEL85 DEL5 DEL40 DEL3 DEL59 DEL60 DEL42 DEL32 DEL21 DEL42 DEL43 DEL85 DEL5 DEL42 DEL40 DEL3 DEL59 DEL60 DEL42 DEL32 DEL21