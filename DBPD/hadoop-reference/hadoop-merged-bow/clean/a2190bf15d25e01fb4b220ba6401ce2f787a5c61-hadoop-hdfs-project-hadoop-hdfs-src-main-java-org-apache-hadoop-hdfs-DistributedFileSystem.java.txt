HDFS-8345. Storage policy APIs must be exposed via the FileSystem interface. (Arpit Agarwal)

+import java.util.Arrays;
+import java.util.Collection;
+import org.apache.hadoop.fs.BlockStoragePolicySpi;
+  @Override
-        if (fs instanceof DistributedFileSystem) {
-          ((DistributedFileSystem) fs).setStoragePolicy(p, policyName);
-          return null;
-        } else {
-          throw new UnsupportedOperationException(
-              "Cannot perform setStoragePolicy on a non-DistributedFileSystem: "
-                  + src + " -> " + p);
-        }
+        fs.setStoragePolicy(p, policyName);
+        return null;
-  /** Get all the existing storage policies */
+  @Override
+  public Collection<BlockStoragePolicy> getAllStoragePolicies()
+      throws IOException {
+    return Arrays.asList(dfs.getStoragePolicies());
+  }
+
+  /**
+   * Deprecated. Prefer {@link FileSystem#getAllStoragePolicies()}
+   * @return
+   * @throws IOException
+   */
+  @Deprecated

INS26 INS26 INS26 INS40 INS40 INS40 INS31 INS78 INS78 INS83 INS74 INS42 INS43 INS8 INS78 INS42 INS42 INS43 INS43 INS42 INS41 INS65 INS65 INS42 INS42 INS42 INS32 UPD66 INS65 INS42 INS42 INS42 INS32 INS68 INS42 INS42 INS42 INS42 INS8 MOV21 MOV41 INS42 DEL42 DEL43 DEL42 DEL11 DEL36 DEL42 DEL42 DEL43 DEL62 DEL8 DEL42 DEL43 DEL45 DEL42 DEL45 DEL42 DEL27 DEL14 DEL53 DEL8 DEL25 DEL8