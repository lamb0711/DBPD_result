HDFS-5788. listLocatedStatus response can be very large. Contributed by Nathan Roberts.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1560750 13f79535-47bb-0310-9956-ffa450edef68

-
+   * We will stop when any of the following conditions is met:
+   * 1) this.lsLimit files have been added
+   * 2) needLocation is true AND enough files have been added such
+   * that at least this.lsLimit block locations are in the response
+   *
+      int locationBudget = this.lsLimit;
+      int listingCnt = 0;
-      for (int i=0; i<numOfListing; i++) {
+      for (int i=0; i<numOfListing && locationBudget>0; i++) {
+        listingCnt++;
+        if (needLocation) {
+            // Once we  hit lsLimit locations, stop.
+            // This helps to prevent excessively large response payloads.
+            // Approximate #locations with locatedBlockCount() * repl_factor
+            LocatedBlocks blks = 
+                ((HdfsLocatedFileStatus)listing[i]).getBlockLocations();
+            locationBudget -= (blks == null) ? 0 :
+               blks.locatedBlockCount() * listing[i].getReplication();
+        }
+      }
+      // truncate return array if necessary
+      if (listingCnt < numOfListing) {
+          listing = Arrays.copyOf(listing, listingCnt);
-          listing, totalNumChildren-startChild-numOfListing);
+          listing, totalNumChildren-startChild-listingCnt);

INS66 INS66 INS66 INS66 INS60 INS60 INS25 INS39 INS59 INS39 INS59 INS27 INS27 INS8 INS42 INS22 INS42 INS34 MOV27 INS27 INS21 INS25 INS42 INS42 INS21 INS52 INS42 INS42 INS34 INS37 INS42 INS8 INS7 UPD42 INS42 INS60 INS21 INS42 INS32 INS43 INS59 INS7 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS42 INS16 INS36 INS42 INS36 INS34 INS27 INS11 INS27 INS32 INS32 INS43 INS2 INS42 INS33 INS42 INS42 INS2 INS42 INS42 INS42 INS42 INS42 INS42