Merge r1467713 through r1469041 from trunk.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1469042 13f79535-47bb-0310-9956-ffa450edef68

-  private CompressionCodecFactory compressionCodecs = null;
-  private LongWritable key = null;
-  private Text value = null;
-  private CompressionCodec codec;
+  private LongWritable key;
+  private Text value;
+  private boolean isCompressedInput;
-    compressionCodecs = new CompressionCodecFactory(job);
-    codec = compressionCodecs.getCodec(file);
-    if (isCompressedInput()) {
+    
+    CompressionCodec codec = new CompressionCodecFactory(job).getCodec(file);
+    if (null!=codec) {
+      isCompressedInput = true;	
-  private boolean isCompressedInput() {
-    return (codec != null);
-  }
-    return isCompressedInput()
+    return isCompressedInput
-    if (isCompressedInput() && null != filePosition) {
+    if (isCompressedInput && null != filePosition) {
-      if (newSize == 0) {
-        break;
-      }

INS23 MOV83 INS39 INS59 INS42 INS60 MOV43 INS59 INS27 INS42 INS32 INS33 INS42 INS21 INS42 INS42 MOV14 MOV42 MOV42 INS7 INS42 INS9 DEL83 DEL42 DEL43 DEL42 DEL33 DEL59 DEL23 DEL33 DEL33 DEL42 DEL59 DEL23 DEL42 DEL7 DEL21 DEL42 DEL42 DEL32 DEL7 DEL21 DEL42 DEL32 DEL83 DEL39 DEL42 DEL42 DEL33 DEL27 DEL36 DEL41 DEL8 DEL31 DEL42 DEL32 DEL42 DEL32 DEL42 DEL34 DEL27 DEL10 DEL8 DEL25