HDFS-14395. Remove WARN Logging From Interrupts. Contributed by David Mollitor.

-              LOG.warn("Caught exception", e);
+              LOG.debug("Thread interrupted", e);
-              LOG.warn("Caught exception", e);
+              LOG.debug("Thread interrupted", e);
-        if (LOG.isDebugEnabled()) {
-          LOG.debug("stage=" + stage + ", " + this);
-        }
+        LOG.debug("stage={}, {}", stage, this);
+
-                LOG.warn("Caught exception", e);
+                LOG.debug("Thread interrupted", e);
+        LOG.debug("Closed channel exception", cce);
-      } catch (ClosedChannelException ignored) {
+      } catch (ClosedChannelException cce) {
+        LOG.debug("Closed channel exception", cce);
-        LOG.warn("Caught exception", e);
+        LOG.debug("Thread interrupted", e);
+        Thread.currentThread().interrupt();
-          if (LOG.isDebugEnabled()) {
-            LOG.debug("DFSClient {}", ack);
-          }
+          LOG.debug("DFSClient {}", ack);
-      } catch (InterruptedException ignored) {
+      } catch (InterruptedException e) {
+        LOG.debug("Thread interrupted", e);

MOV21 INS21 UPD42 INS21 INS21 MOV21 UPD42 INS21 INS45 INS42 INS52 INS32 INS32 INS32 INS32 INS42 INS42 INS45 INS42 INS42 INS42 INS45 INS42 UPD42 UPD45 INS32 INS42 INS42 INS42 INS45 INS42 INS42 INS42 UPD42 UPD45 UPD42 UPD45 UPD42 UPD45 DEL45 DEL42 DEL45 DEL52 DEL27 DEL42 DEL42 DEL32 DEL8 DEL25 DEL42 DEL42 DEL32 DEL8 DEL25