Merge trunk into HA branch.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-1623@1213867 13f79535-47bb-0310-9956-ffa450edef68

-  protected DataChecksum checksum;
+  private DataChecksum checksum;
-  protected long startOffset;
-  protected final String filename;
+  private long startOffset;
+  private final String filename;
-  protected static DirectBufferPool bufferPool =
-    new DirectBufferPool();
+  private static DirectBufferPool bufferPool = new DirectBufferPool();
-  protected int bytesPerChecksum;
-  protected int checksumSize;
+  private int bytesPerChecksum;
+  private int checksumSize;
-  protected long bytesNeededToFinish;
+  private long bytesNeededToFinish;
-  protected final boolean verifyChecksum;
+  private final boolean verifyChecksum;
-    int n = 0;
-    for (;;) {
-      int nread = read(buf, offset + n, len - n);
-      if (nread <= 0) 
-        return (n == 0) ? nread : n;
-      n += nread;
-      if (n >= len)
-        return n;
-    }
+    return BlockReaderUtil.readAll(this, buf, offset, len);
-  public void readFully(byte[] buf, int off, int len)
-      throws IOException {
-    int toRead = len;
-    while (toRead > 0) {
-      int ret = read(buf, off, toRead);
-      if (ret < 0) {
-        throw new IOException("Premature EOF from inputStream");
-      }
-      toRead -= ret;
-      off += ret;
-    }    
+  public void readFully(byte[] buf, int off, int len) throws IOException {
+    BlockReaderUtil.readFully(this, buf, off, len);

UPD83 UPD83 UPD83 UPD83 UPD83 UPD83 UPD83 UPD83 INS8 INS41 INS21 INS32 INS32 INS42 UPD42 MOV42 INS52 MOV42 INS42 INS42 INS42 UPD42 MOV42 INS52 MOV42 MOV42 INS42 DEL39 DEL42 DEL34 DEL59 DEL60 DEL39 DEL42 DEL42 DEL42 DEL27 DEL42 DEL42 DEL27 DEL32 DEL59 DEL60 DEL42 DEL34 DEL27 DEL42 DEL34 DEL27 DEL36 DEL42 DEL42 DEL16 DEL41 DEL25 DEL42 DEL42 DEL7 DEL21 DEL42 DEL42 DEL27 DEL42 DEL41 DEL25 DEL8 DEL24 DEL8 DEL39 DEL42 DEL42 DEL59 DEL60 DEL42 DEL34 DEL27 DEL39 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL34 DEL27 DEL42 DEL43 DEL45 DEL14 DEL53 DEL8 DEL25 DEL42 DEL42 DEL7 DEL21 DEL42 DEL42 DEL7 DEL21 DEL8 DEL61