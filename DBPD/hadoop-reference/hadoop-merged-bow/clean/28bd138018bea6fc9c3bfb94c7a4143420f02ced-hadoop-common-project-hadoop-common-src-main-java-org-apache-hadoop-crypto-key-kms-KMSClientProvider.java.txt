HADOOP-12604. Exception may be swallowed in KMSClientProvider. (Yongjun Zhang)

+import org.apache.hadoop.io.IOUtils;
-      conn.getInputStream().close();
+      IOUtils.closeStream(conn.getInputStream());
-      } catch (IOException ex) {
-        if (is != null) {
-          is.close();
-        }
-        throw ex;
-        if (is != null) {
-          is.close();
-        }
+        IOUtils.closeStream(is);

INS26 INS40 INS8 MOV32 MOV21 INS42 UPD42 UPD42 UPD42 INS42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL33 DEL27 DEL8 DEL25 DEL42 DEL53 DEL8 DEL12 DEL42 DEL33 DEL27 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25 DEL8