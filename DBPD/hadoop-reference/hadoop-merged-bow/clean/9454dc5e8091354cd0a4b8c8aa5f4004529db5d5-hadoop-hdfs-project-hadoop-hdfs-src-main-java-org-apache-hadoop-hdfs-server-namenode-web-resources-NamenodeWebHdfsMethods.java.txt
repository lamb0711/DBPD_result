HDFS-11000. webhdfs PUT does not work if requests are routed to call queue. Contributed by Kihwal Lee.

-          namenode, ugi, userPrincipal.getName());
+          namenode, ugi, null);

INS33 DEL42 DEL42 DEL32