HADOOP-11659. o.a.h.FileSystem.Cache#remove should use a single hash map lookup. Contributed by Brahma Reddy Battula.

-      if (map.containsKey(key) && fs == map.get(key)) {
-        map.remove(key);
+      FileSystem cachedFs = map.remove(key);
+      if (fs == cachedFs) {
-        }
+      } else if (cachedFs != null) {
+        map.put(key, cachedFs);
+      }
-        remove(key, fs);
+        map.remove(key);
+        toAutoClose.remove(key);

INS60 INS25 INS43 INS59 INS27 INS8 INS25 INS42 INS42 INS32 MOV42 INS42 INS21 INS27 INS8 MOV21 MOV21 MOV42 UPD42 MOV42 MOV42 INS32 INS42 INS33 INS21 INS42 INS42 INS42 INS32 MOV42 UPD42 MOV42 MOV42 INS42 DEL32 DEL32 DEL27 DEL27 DEL8 DEL25 DEL42 DEL42 DEL42 DEL32 DEL21