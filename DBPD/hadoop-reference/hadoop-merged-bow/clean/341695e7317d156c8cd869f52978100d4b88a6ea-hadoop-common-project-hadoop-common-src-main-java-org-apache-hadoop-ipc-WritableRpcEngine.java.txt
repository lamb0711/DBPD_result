Merge trunk into branch


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/MR-2841@1612742 13f79535-47bb-0310-9956-ffa450edef68

+       long startTime = Time.now();
+       int qTime = (int) (startTime-receivedTime);
+       Exception exception = null;
-          long startTime = Time.now();
-          Method method = 
+          Method method =
-          int processingTime = (int) (Time.now() - startTime);
-          int qTime = (int) (startTime-receivedTime);
-          if (LOG.isDebugEnabled()) {
-            LOG.debug("Served: " + call.getMethodName() +
-                      " queueTime= " + qTime +
-                      " procesingTime= " + processingTime);
-          }
-          server.rpcMetrics.addRpcQueueTime(qTime);
-          server.rpcMetrics.addRpcProcessingTime(processingTime);
-          server.rpcDetailedMetrics.addProcessingTime(call.getMethodName(),
-                                               processingTime);
-
+            exception = (IOException)target;
+            exception = ioe;
+          exception = ioe;
-        }
+        } finally {
+         int processingTime = (int) (Time.now() - startTime);
+         if (LOG.isDebugEnabled()) {
+           String msg = "Served: " + call.getMethodName() +
+               " queueTime= " + qTime +
+               " procesingTime= " + processingTime;
+           if (exception != null) {
+             msg += " exception= " + exception.getClass().getSimpleName();
+           }
+           LOG.debug(msg);
+         }
+         String detailedMetricsName = (exception == null) ?
+             call.getMethodName() :
+             exception.getClass().getSimpleName();
+         server.rpcMetrics.addRpcQueueTime(qTime);
+         server.rpcMetrics.addRpcProcessingTime(processingTime);
+         server.rpcDetailedMetrics.addProcessingTime(detailedMetricsName,
+             processingTime);
+       }

MOV60 MOV60 INS60 INS43 INS59 INS8 INS42 INS42 INS33 MOV60 INS25 INS60 MOV21 MOV21 INS21 INS21 MOV32 INS8 INS43 INS59 INS32 INS7 INS60 INS25 INS21 INS42 INS42 INS16 INS40 INS42 INS42 INS42 INS21 INS21 INS42 INS42 INS43 INS59 INS27 INS8 INS32 INS36 MOV32 INS32 INS7 INS7 INS42 INS42 MOV27 INS42 INS33 INS21 INS42 INS42 INS42 INS27 INS32 INS42 INS42 INS11 INS42 INS42 INS7 INS42 INS33 INS42 INS42 INS43 INS42 INS42 INS27 INS42 INS45 INS32 INS32 INS42 INS42 INS42 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25 DEL40 DEL42 DEL42 DEL32 DEL21