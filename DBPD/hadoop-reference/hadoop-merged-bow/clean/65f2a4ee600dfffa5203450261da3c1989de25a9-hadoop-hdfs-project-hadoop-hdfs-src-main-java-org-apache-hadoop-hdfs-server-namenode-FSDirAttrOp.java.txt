HDFS-7543. Avoid path resolution when getting FileStatus for audit logs. Contributed by Haohui Mai.

+    INodesInPath iip;
-      final INodesInPath iip = fsd.getINodesInPath4Write(src);
+      iip = fsd.getINodesInPath4Write(src);
-    return fsd.getAuditFileInfo(src, false);
+    return fsd.getAuditFileInfo(iip);
+    INodesInPath iip;
-      final INodesInPath iip = fsd.getINodesInPath4Write(src);
+      iip = fsd.getINodesInPath4Write(src);
-    return fsd.getAuditFileInfo(src, false);
+    return fsd.getAuditFileInfo(iip);
+    INodesInPath iip;
-      final INodesInPath iip = fsd.getINodesInPath4Write(src);
+      iip = fsd.getINodesInPath4Write(src);
-    return fsd.getAuditFileInfo(src, false);
+    return fsd.getAuditFileInfo(iip);
+    INodesInPath iip;
-      final INodesInPath iip = fsd.getINodesInPath4Write(src);
+      iip = fsd.getINodesInPath4Write(src);
-    return fsd.getAuditFileInfo(src, false);
+    return fsd.getAuditFileInfo(iip);

INS60 INS60 INS60 INS60 MOV43 INS59 MOV43 INS59 MOV43 INS59 MOV43 INS59 INS42 INS21 UPD42 INS42 INS21 UPD42 INS42 INS21 UPD42 INS42 INS21 UPD42 INS7 INS7 INS7 INS7 INS42 MOV32 INS42 MOV32 INS42 MOV32 INS42 MOV32 DEL83 DEL42 DEL59 DEL60 DEL9 DEL83 DEL42 DEL59 DEL60 DEL9 DEL83 DEL42 DEL59 DEL60 DEL9 DEL83 DEL42 DEL59 DEL60 DEL9