HDFS-9350. Avoid creating temprorary strings in Block.toString() and getBlockName() (Staffan Friberg via cmccabe)

-    return BLOCK_FILE_PREFIX + String.valueOf(blockId);
+    return new StringBuilder().append(BLOCK_FILE_PREFIX)
+        .append(blockId).toString();
-    return b.getBlockName() + "_" + b.getGenerationStamp();
+    StringBuilder sb = new StringBuilder();
+    b.appendStringTo(sb);
+    return sb.toString();

INS60 INS21 INS41 INS32 INS43 INS59 INS32 INS32 INS32 INS42 INS42 INS42 INS14 MOV42 UPD42 MOV42 INS42 UPD42 MOV42 UPD42 MOV42 INS32 INS42 MOV42 INS43 INS14 UPD42 MOV42 INS42 INS42 INS43 INS42 DEL42 DEL42 DEL32 DEL27 DEL32 DEL45 DEL32 DEL27 DEL41