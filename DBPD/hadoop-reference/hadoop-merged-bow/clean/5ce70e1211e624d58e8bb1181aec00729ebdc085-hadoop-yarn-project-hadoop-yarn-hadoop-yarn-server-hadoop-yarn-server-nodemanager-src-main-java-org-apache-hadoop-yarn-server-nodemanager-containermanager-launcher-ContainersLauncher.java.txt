YARN-7644. NM gets backed up deleting docker containers. Contributed by Chandni Singh

-import org.apache.hadoop.yarn.api.records.ContainerExitStatus;
-        ContainerLaunch launcher = running.remove(containerId);
-        if (launcher == null) {
+        ContainerLaunch existingLaunch = running.remove(containerId);
+        if (existingLaunch == null) {
-        try {
-          launcher.cleanupContainer();
-        } catch (IOException e) {
-          LOG.warn("Got exception while cleaning container " + containerId
-              + ". Ignoring.");
-        }
+        ContainerCleanup cleanup = new ContainerCleanup(context, getConfig(),
+            dispatcher, exec, event.getContainer(), existingLaunch);
+        containerLauncher.submit(cleanup);

MOV26 INS60 INS21 INS43 INS59 MOV32 UPD42 UPD42 INS42 INS42 INS14 UPD42 UPD42 INS42 INS43 INS42 INS32 INS42 INS42 INS32 INS42 UPD42 MOV42 INS42 INS42 INS42 DEL40 DEL26 DEL45 DEL42 DEL45 DEL27 DEL42 DEL42 DEL32 DEL21 DEL8 DEL43 DEL42 DEL44 DEL21 DEL8 DEL12 DEL54