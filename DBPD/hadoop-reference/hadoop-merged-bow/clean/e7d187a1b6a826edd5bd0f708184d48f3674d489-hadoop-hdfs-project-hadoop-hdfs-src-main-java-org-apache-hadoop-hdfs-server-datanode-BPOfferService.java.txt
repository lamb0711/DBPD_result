HDFS-12140. Remove BPOfferService lock contention to get block pool id. Contributed by Daryn Sharp.

+  private volatile String bpId;
+    // avoid lock contention unless the registration hasn't completed.
+    String id = bpId;
+    if (id != null) {
+      return id;
+    }
-    return getNamespaceInfo() != null;
+    return getBlockPoolId(true) != null;
+  @VisibleForTesting
+  NamespaceInfo setNamespaceInfo(NamespaceInfo nsInfo) throws IOException {
+    writeLock();
+    try {
+      NamespaceInfo old = bpNSInfo;
+      if (bpNSInfo != null && nsInfo != null) {
+        checkNSEquality(bpNSInfo.getBlockPoolID(), nsInfo.getBlockPoolID(),
+            "Blockpool ID");
+        checkNSEquality(bpNSInfo.getNamespaceID(), nsInfo.getNamespaceID(),
+            "Namespace ID");
+        checkNSEquality(bpNSInfo.getClusterID(), nsInfo.getClusterID(),
+            "Cluster ID");
+      }
+      bpNSInfo = nsInfo;
+      // cache the block pool id for lock-free access.
+      bpId = (nsInfo != null) ? nsInfo.getBlockPoolID() : null;
+      return old;
+    } finally {
+      writeUnlock();
+    }
+  }
+
-    Preconditions.checkArgument(block.getBlockPoolId().equals(getBlockPoolId()),
+    final String bpId = getBlockPoolId();
+    Preconditions.checkArgument(block.getBlockPoolId().equals(bpId),
-        block.getBlockPoolId(), getBlockPoolId());
+        block.getBlockPoolId(), bpId);
-      if (this.bpNSInfo == null) {
-        this.bpNSInfo = nsInfo;
+      if (setNamespaceInfo(nsInfo) == null) {
-            this.bpNSInfo = null;
+            setNamespaceInfo(null);
-      } else {
-        checkNSEquality(bpNSInfo.getBlockPoolID(), nsInfo.getBlockPoolID(),
-            "Blockpool ID");
-        checkNSEquality(bpNSInfo.getNamespaceID(), nsInfo.getNamespaceID(),
-            "Namespace ID");
-        checkNSEquality(bpNSInfo.getClusterID(), nsInfo.getClusterID(),
-            "Cluster ID");

INS23 INS31 INS83 INS83 INS43 INS59 INS78 INS43 INS42 INS44 INS43 INS8 INS42 INS42 INS60 INS25 INS42 INS42 INS43 INS42 INS42 INS21 MOV54 INS60 INS54 INS43 INS59 INS27 INS8 INS42 INS32 INS83 INS43 INS59 INS8 INS8 INS42 INS42 INS42 INS42 INS33 INS41 INS42 INS60 INS21 INS21 INS41 INS42 INS42 MOV32 UPD42 MOV42 INS25 INS21 INS42 UPD42 INS9 INS43 INS59 INS27 INS7 INS7 INS42 INS42 INS27 MOV8 INS32 INS42 INS42 INS42 INS27 INS27 INS42 INS42 INS42 INS16 INS32 INS33 INS42 INS42 MOV33 INS42 INS33 INS36 INS32 INS33 INS42 INS42 INS27 INS42 INS42 INS42 INS33 INS32 INS42 INS33 DEL52 DEL42 DEL22 DEL27 DEL32 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL52 DEL42 DEL22 DEL33 DEL7