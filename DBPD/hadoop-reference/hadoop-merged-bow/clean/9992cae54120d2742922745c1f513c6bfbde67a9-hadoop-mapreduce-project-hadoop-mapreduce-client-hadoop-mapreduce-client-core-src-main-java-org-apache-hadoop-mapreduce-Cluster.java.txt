Reverting the previous trunk merge since it added other unintended changes in addition


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-1623@1177127 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.hadoop.security.token.SecretManager.InvalidToken;
+import org.apache.hadoop.security.token.SecretManager.InvalidToken;
-    this(null, conf);
-  }
-
-  public Cluster(InetSocketAddress jobTrackAddr, Configuration conf) 
-      throws IOException {
-    initialize(jobTrackAddr, conf);
-  }
-  
-  private void initialize(InetSocketAddress jobTrackAddr, Configuration conf)
-      throws IOException {
-
-    for (ClientProtocolProvider provider : ServiceLoader
-        .load(ClientProtocolProvider.class)) {
-      ClientProtocol clientProtocol = null;
-      if (jobTrackAddr == null) {
-        clientProtocol = provider.create(conf);
-      } else {
-        clientProtocol = provider.create(jobTrackAddr, conf);
-      }
-
+    for (ClientProtocolProvider provider : ServiceLoader.load(ClientProtocolProvider.class)) {
+      ClientProtocol clientProtocol = provider.create(conf);
+  }
-    if (null == clientProtocolProvider || null == client) {
-      throw new IOException(
-          "Cannot initialize Cluster. Please check your configuration for "
-              + MRConfig.FRAMEWORK_NAME
-              + " and the correspond server addresses.");
+  public Cluster(InetSocketAddress jobTrackAddr, Configuration conf) 
+      throws IOException {
+    this.conf = conf;
+    this.ugi = UserGroupInformation.getCurrentUser();
+    for (ClientProtocolProvider provider : ServiceLoader.load(ClientProtocolProvider.class)) {
+      ClientProtocol clientProtocol = provider.create(jobTrackAddr, conf);
+      if (clientProtocol != null) {
+        clientProtocolProvider = provider;
+        client = clientProtocol;
+        break;
+      }

MOV26 MOV44 MOV43 INS8 UPD83 UPD42 MOV21 MOV21 INS70 INS21 INS21 INS44 INS32 INS8 INS7 INS7 UPD43 MOV43 UPD42 MOV42 INS42 INS42 INS57 INS60 INS25 INS22 INS42 INS22 INS32 UPD42 INS43 INS43 INS59 INS27 INS8 INS52 INS42 INS52 INS42 INS42 INS42 INS42 UPD42 MOV42 INS42 MOV32 INS42 INS33 INS21 INS21 INS10 MOV32 INS7 INS7 INS42 INS42 INS42 INS42 DEL83 DEL42 DEL33 DEL42 DEL17 DEL8 DEL31 DEL44 DEL43 DEL42 DEL44 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL21 DEL8 DEL39 DEL33 DEL42 DEL33 DEL27 DEL42 DEL7 DEL21 DEL8 DEL42 DEL7 DEL21 DEL8 DEL25 DEL33 DEL42 DEL27 DEL33 DEL42 DEL27 DEL27 DEL42 DEL43 DEL45 DEL40 DEL45 DEL27 DEL14 DEL53 DEL8 DEL25