HDFS-5452. Fix TestReplicationPolicy and TestBlocksScheduledCounter.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2832@1538407 13f79535-47bb-0310-9956-ffa450edef68

-import com.google.common.annotations.VisibleForTesting;
-
+import org.apache.hadoop.hdfs.server.namenode.CachedBlock;
-import org.apache.hadoop.hdfs.server.namenode.CachedBlock;
+import com.google.common.annotations.VisibleForTesting;
+
+  /* Variables for maintaining number of blocks scheduled to be written to
+   * this storage. This count is approximate and might be slightly bigger
+   * in case of errors (e.g. datanode does not report if an error occurs
+   * while writing the block).
+   */
+  private int currApproxBlocksScheduled = 0;
+  private int prevApproxBlocksScheduled = 0;
+  private long lastBlocksScheduledRollTime = 0;
+  private static final int BLOCKS_SCHEDULED_ROLL_INTERVAL = 600*1000; //10min
-        storage.receivedHeartbeat(report, getLastUpdate());
+        storage.receivedHeartbeat(report);
+    rollBlocksScheduled(getLastUpdate());
-    int n = 0;
-    for(DatanodeStorageInfo storage : getStorageInfos()) {
-      n += storage.getBlocksScheduled();
+    return currApproxBlocksScheduled + prevApproxBlocksScheduled;
+  }
+
+  /** Increment the number of blocks scheduled. */
+  void incrementBlocksScheduled() {
+    currApproxBlocksScheduled++;
+  }
+  
+  /** Decrement the number of blocks scheduled. */
+  void decrementBlocksScheduled() {
+    if (prevApproxBlocksScheduled > 0) {
+      prevApproxBlocksScheduled--;
+    } else if (currApproxBlocksScheduled > 0) {
+      currApproxBlocksScheduled--;
+    } 
+    // its ok if both counters are zero.
+  }
+  
+  /** Adjusts curr and prev number of blocks scheduled every few minutes. */
+  private void rollBlocksScheduled(long now) {
+    if (now - lastBlocksScheduledRollTime > BLOCKS_SCHEDULED_ROLL_INTERVAL) {
+      prevApproxBlocksScheduled = currApproxBlocksScheduled;
+      currApproxBlocksScheduled = 0;
+      lastBlocksScheduledRollTime = now;
-    return n;

MOV26 MOV26 INS23 INS23 INS23 INS23 INS31 INS31 INS31 INS31 INS83 INS39 INS59 INS83 INS39 INS59 INS83 INS39 INS59 INS83 INS83 INS83 INS39 INS59 MOV29 INS83 INS39 INS42 INS8 INS29 INS39 INS42 INS8 INS29 INS39 INS42 INS8 INS29 INS83 INS39 INS42 INS44 INS8 INS42 INS34 UPD42 MOV42 MOV34 INS42 INS34 INS42 INS27 MOV21 INS41 INS65 INS21 INS65 INS25 INS65 INS39 INS42 INS25 INS34 INS34 INS27 INS66 INS37 INS66 INS27 INS8 INS25 INS66 INS27 INS8 UPD42 INS42 INS42 INS42 INS42 INS34 INS21 INS27 INS8 INS27 INS42 MOV21 INS21 INS21 INS37 INS42 INS34 INS21 INS42 INS42 UPD7 INS7 INS7 INS21 INS42 INS37 UPD42 INS42 INS42 INS34 INS42 INS42 INS32 INS42 INS42 INS42 INS42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL83 DEL39 DEL42 DEL39 DEL59 DEL60 DEL42 DEL43 DEL42 DEL44 DEL42 DEL32 DEL8 DEL70 DEL42 DEL41 DEL8 DEL31