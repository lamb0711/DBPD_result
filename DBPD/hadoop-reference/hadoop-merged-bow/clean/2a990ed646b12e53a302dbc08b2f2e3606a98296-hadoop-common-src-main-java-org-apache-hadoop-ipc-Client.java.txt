HADOOP-7472. RPC client should deal with IP address change. Contributed by Kihwal Lee.



git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1156350 13f79535-47bb-0310-9956-ffa450edef68

+
+    /**
+     * Update the server address if the address corresponding to the host
+     * name has changed.
+     *
+     * @return true if an addr change was detected.
+     * @throws IOException when the hostname cannot be resolved.
+     */
+    private synchronized boolean updateAddress() throws IOException {
+      // Do a fresh lookup with the old host name.
+      InetSocketAddress currentAddr =  new InetSocketAddress(
+                               server.getHostName(), server.getPort());
+
+      if (!server.equals(currentAddr)) {
+        LOG.warn("Address change detected. Old: " + server.toString() +
+                                 " New: " + currentAddr.toString());
+        server = currentAddr;
+        return true;
+      }
+      return false;
+    }
-          NetUtils.connect(this.socket, remoteId.getAddress(), 20000);
+          NetUtils.connect(this.socket, server, 20000);
+          /* Check for an address change and update the local reference.
+           * Reset the failure counter if the address was changed
+           */
+          if (updateAddress()) {
+            timeoutFailures = ioFailures = 0;
+          }
+          if (updateAddress()) {
+            timeoutFailures = ioFailures = 0;
+          }

INS31 INS29 INS83 INS83 INS39 INS42 INS43 INS8 INS65 INS65 INS65 INS42 INS60 INS25 INS41 INS66 INS66 INS66 INS42 INS66 INS43 INS59 INS38 INS8 INS9 INS42 INS42 INS14 INS32 INS21 INS21 INS41 INS43 INS32 INS32 INS42 INS42 INS42 INS32 INS7 INS9 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS27 INS42 INS42 INS45 INS32 INS45 INS32 INS25 INS25 INS42 INS42 INS42 INS42 UPD42 MOV42 INS32 INS8 INS32 INS8 INS42 INS21 INS42 INS21 INS7 INS7 INS42 INS7 INS42 INS7 INS42 INS34 INS42 INS34 DEL42 DEL32