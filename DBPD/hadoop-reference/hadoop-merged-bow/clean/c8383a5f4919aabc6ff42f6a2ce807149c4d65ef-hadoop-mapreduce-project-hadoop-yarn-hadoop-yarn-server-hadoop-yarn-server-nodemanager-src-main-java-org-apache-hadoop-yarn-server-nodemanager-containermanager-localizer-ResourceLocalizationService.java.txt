MAPREDUCE-2985. Fixed findbugs warnings in ResourceLocalizationService. Contributed by Thomas Graves.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1169955 13f79535-47bb-0310-9956-ffa450edef68

-      for (LocalResourceVisibility vis : rsrcs.keySet()) {
-        tracker = getLocalResourcesTracker(vis, c.getUser(), 
+      for (Map.Entry<LocalResourceVisibility, Collection<LocalResourceRequest>> e :
+           rsrcs.entrySet()) {
+        tracker = getLocalResourcesTracker(e.getKey(), c.getUser(), 
-        for (LocalResourceRequest req : rsrcs.get(vis)) {
-          tracker.handle(new ResourceRequestEvent(req, vis, ctxt));
+        for (LocalResourceRequest req : e.getValue()) {
+          tracker.handle(new ResourceRequestEvent(req, e.getKey(), ctxt));
-      for (LocalResourceVisibility vis : rsrcs.keySet()) {
-        tracker = getLocalResourcesTracker(vis, c.getUser(), 
+      for (Map.Entry<LocalResourceVisibility, Collection<LocalResourceRequest>> e :
+           rsrcs.entrySet()) {
+        tracker = getLocalResourcesTracker(e.getKey(), c.getUser(), 
-        for (LocalResourceRequest req : rsrcs.get(vis)) {
+        for (LocalResourceRequest req : e.getValue()) {

INS74 INS42 UPD42 INS74 INS42 UPD42 INS43 MOV43 INS74 INS43 MOV43 INS74 INS40 INS43 INS43 UPD42 UPD42 INS40 INS43 INS43 UPD42 UPD42 INS42 INS42 INS32 INS42 INS42 INS32 UPD42 MOV42 INS42 UPD42 MOV42 INS42 INS32 INS42 INS42 DEL42 DEL42 DEL42 DEL42 DEL42