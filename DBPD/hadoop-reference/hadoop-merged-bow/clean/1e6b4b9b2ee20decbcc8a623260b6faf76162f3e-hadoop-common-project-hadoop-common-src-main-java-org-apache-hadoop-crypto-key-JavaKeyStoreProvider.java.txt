Merge branch 'trunk' into HDFS-6581

+import org.apache.hadoop.classification.InterfaceAudience.Private;
+import com.google.common.annotations.VisibleForTesting;
+
+  @VisibleForTesting
+  JavaKeyStoreProvider(JavaKeyStoreProvider other) {
+    super(new Configuration());
+    uri = other.uri;
+    path = other.path;
+    fs = other.fs;
+    permissions = other.permissions;
+    keyStore = other.keyStore;
+    password = other.password;
+    changed = other.changed;
+    readLock = other.readLock;
+    writeLock = other.writeLock;
+  }
+
+    Path resetPath = path;
+      if (fileExisted) {
+        resetPath = oldPath;
+      }
+        resetPath = path;
+    } catch (IOException ioe) {
+      resetKeyStoreState(resetPath);
+      throw ioe;
+  private void resetKeyStoreState(Path path) {
+    LOG.debug("Could not flush Keystore.."
+        + "attempting to reset to previous state !!");
+    // 1) flush cache
+    cache.clear();
+    // 2) load keyStore from previous path
+    try {
+      loadFromPath(path, password);
+      LOG.debug("KeyStore resetting to previously flushed state !!");
+    } catch (Exception e) {
+      LOG.debug("Could not reset Keystore to previous state", e);
+    }
+  }
+
-  private void writeToNew(Path newPath) throws IOException {
+  protected void writeToNew(Path newPath) throws IOException {
-  private void revertFromOld(Path oldPath, boolean fileExisted)
-      throws IOException {
-    if (fileExisted) {
-      renameOrFail(oldPath, path);
-    }
-  }
-
-  private boolean backupToOld(Path oldPath)
+  protected boolean backupToOld(Path oldPath)
+  private void revertFromOld(Path oldPath, boolean fileExisted)
+      throws IOException {
+    if (fileExisted) {
+      renameOrFail(oldPath, path);
+    }
+  }
+
+

INS26 INS26 MOV31 INS40 INS40 INS31 INS31 INS78 INS42 INS44 INS8 INS83 INS39 INS42 INS44 INS8 UPD83 UPD83 INS42 INS43 INS42 INS46 INS21 INS21 INS21 INS21 INS21 INS21 INS21 INS21 INS21 INS60 INS43 INS42 INS21 INS21 INS54 INS42 INS14 INS7 INS7 INS7 INS7 INS7 INS7 INS7 INS7 INS7 INS43 INS59 INS12 INS42 INS32 INS32 INS8 INS12 INS43 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS40 INS42 INS42 INS42 INS25 INS44 INS8 INS42 INS42 INS27 INS42 INS42 INS21 INS21 INS44 INS8 INS42 INS42 INS8 INS43 INS42 INS21 INS53 INS45 INS45 INS32 INS32 INS43 INS42 INS21 INS21 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS45 INS42 INS32 INS7 INS21 INS42 INS42 INS42 INS42 INS45 INS42 INS42 INS42 INS7 INS42 INS42