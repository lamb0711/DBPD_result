YARN-3240. Implement client API to put generic entities. Contributed by Zhijie Shen

(cherry picked from commit 4487da249f448d5c67b712cd0aa723e764eed77d)

-import javax.ws.rs.Consumes;
-import javax.ws.rs.GET;
-import javax.ws.rs.POST;
-import javax.ws.rs.Path;
-import javax.ws.rs.Produces;
-import javax.ws.rs.WebApplicationException;
+import javax.ws.rs.*;
-import org.apache.hadoop.yarn.api.records.timeline.TimelineEntities;
-import org.apache.hadoop.yarn.api.records.timeline.TimelinePutResponse;
+import org.apache.hadoop.yarn.api.records.ApplicationId;
+import org.apache.hadoop.yarn.api.records.timelineservice.TimelineEntities;
+import org.apache.hadoop.yarn.util.ConverterUtils;
+import java.net.URI;
+
-  @POST
+  @PUT
+  @Path("/entities")
-  public TimelinePutResponse postEntities(
+  public Response putEntities(
+      @QueryParam("async") String async,
+      @QueryParam("appid") String appId,
+    boolean isAsync = async != null && async.trim().equalsIgnoreCase("true");
+
-      AppLevelAggregatorService service = getAggregatorService(req);
+      appId = parseApplicationId(appId);
+      if (appId == null) {
+        return Response.status(Response.Status.BAD_REQUEST).build();
+      }
+      AppLevelAggregatorService service = serviceManager.getService(appId);
-      return service.postEntities(entities, callerUgi);
+      service.postEntities(entities, callerUgi);
+      return Response.ok().build();
-  private AppLevelAggregatorService
-      getAggregatorService(HttpServletRequest req) {
-    String appIdString = getApplicationId(req);
-    return serviceManager.getService(appIdString);
-  }
-
-  private String getApplicationId(HttpServletRequest req) {
-    // TODO the application id from the request
-    // (most likely from the URI)
-    return null;
+  private String parseApplicationId(String appId) {
+    // Make sure the appId is not null and is valid
+    ApplicationId appID;
+    try {
+      if (appId != null) {
+        return ConverterUtils.toApplicationId(appId.trim()).toString();
+      } else {
+        return null;
+      }
+    } catch (Exception e) {
+      return null;
+    }

MOV26 MOV26 MOV26 UPD40 UPD40 UPD40 UPD40 UPD40 INS31 INS79 UPD43 UPD42 INS44 INS44 MOV83 MOV43 INS42 INS44 INS8 UPD42 INS42 INS45 UPD42 INS79 INS43 INS42 INS79 INS43 INS42 INS60 MOV43 INS42 INS60 INS54 INS42 INS45 INS42 INS42 INS45 INS42 INS39 INS59 INS43 INS59 INS8 INS12 INS42 INS27 INS21 INS25 INS21 UPD42 MOV42 INS42 INS25 INS44 INS8 INS27 INS32 INS7 INS27 INS8 MOV32 INS32 INS27 INS8 MOV8 UPD43 MOV43 UPD42 MOV42 INS41 INS42 INS33 INS32 INS42 INS45 INS42 INS32 INS42 INS33 INS41 INS32 INS42 INS42 INS33 INS41 UPD42 INS33 INS42 INS42 INS42 INS42 INS32 INS42 UPD42 UPD42 INS42 INS42 INS32 INS32 INS42 INS32 UPD42 MOV42 INS42 INS42 INS40 UPD42 MOV42 UPD42 MOV42 INS32 UPD42 MOV42 UPD42 MOV42 DEL40 DEL26 DEL40 DEL26 DEL40 DEL26 DEL42 DEL43 DEL42 DEL43 DEL42 DEL44 DEL42 DEL32 DEL59 DEL60 DEL32 DEL41 DEL8 DEL31 DEL83 DEL42 DEL44 DEL31