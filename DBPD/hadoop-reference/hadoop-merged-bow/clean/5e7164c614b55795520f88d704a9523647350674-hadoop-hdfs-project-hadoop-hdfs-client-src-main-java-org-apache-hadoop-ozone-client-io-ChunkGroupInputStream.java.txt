HDFS-13137. Ozone: Ozonefs read fails because ChunkGroupInputStream#read does not iterate through all the blocks in the key.
Contributed by Mukul Kumar Singh.

-    checkNotClosed();
-    if (streamEntries.size() <= currentStreamIndex) {
+    byte[] buf = new byte[1];
+    if (read(buf, 0, 1) == EOF) {
-    ChunkInputStreamEntry entry = streamEntries.get(currentStreamIndex);
-    int data = entry.read();
-    return data;
+    return Byte.toUnsignedInt(buf[0]);
-        return totalReadLen > 0? totalReadLen : EOF;
+        return totalReadLen > 0 ? totalReadLen : EOF;

INS60 INS41 INS5 INS59 UPD27 INS32 INS39 INS85 INS42 INS3 UPD42 UPD42 MOV42 UPD42 MOV42 INS2 INS5 INS34 UPD42 UPD42 INS34 INS34 INS42 INS34 INS39 INS85 DEL42 DEL32 DEL21 DEL42 DEL43 DEL42 DEL42 DEL32 DEL59 DEL60 DEL39 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL41