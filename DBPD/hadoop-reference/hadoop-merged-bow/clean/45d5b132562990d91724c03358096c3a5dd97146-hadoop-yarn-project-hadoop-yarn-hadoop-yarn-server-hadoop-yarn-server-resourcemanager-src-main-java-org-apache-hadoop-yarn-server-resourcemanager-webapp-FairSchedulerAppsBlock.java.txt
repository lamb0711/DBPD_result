Merge changes from trunk

+      if (fairShare == FairSchedulerInfo.INVALID_FAIR_SHARE) {
+        // FairScheduler#applications don't have the entry. Skip it.
+        continue;
+      }

INS25 INS27 INS8 INS42 INS40 INS18