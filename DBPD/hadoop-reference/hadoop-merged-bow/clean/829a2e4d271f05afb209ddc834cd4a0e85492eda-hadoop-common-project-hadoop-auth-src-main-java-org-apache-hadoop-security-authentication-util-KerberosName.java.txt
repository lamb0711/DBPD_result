HADOOP-12751. While using kerberos Hadoop incorrectly assumes names with '@' to be non-simple. (Bolke de Bruin via stevel).

-        throw new NoMatchingRule("Non-simple name " + result +
-                                 " after auth_to_local rule " + this);
+        LOG.info("Non-simple name {} after auth_to_local rule {}",
+            result, this);
-   * @return the short name
+   * @return the user name
-    throw new NoMatchingRule("No rules applied to " + toString());
+    LOG.info("No auth_to_local rules applied to {}", this);
+    return toString();

INS21 INS41 UPD66 INS32 MOV32 INS42 INS42 INS45 INS52 INS21 INS32 INS42 INS42 INS45 INS42 INS52 DEL42 DEL43 DEL45 DEL42 DEL45 DEL52 DEL27 DEL14 DEL53 DEL42 DEL43 DEL45 DEL27 DEL14 DEL53