Reverting the previous trunk merge since it added other unintended changes in addition


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-1623@1177127 13f79535-47bb-0310-9956-ffa450edef68

-                if(LOG.isDebugEnabled()) {
-                  LOG.debug("Token (" + id + ") for " + token.getService());
-                }
+                System.out.println("Token (" + id + ") for " + token.getService());
-                  if(LOG.isDebugEnabled()) {
-                	  LOG.debug("Renewed token via " + webUrl + " for "
-                          + token.getService() + " until: " + new Date(result));
-                  }
+                  System.out.println("Renewed token via " + webUrl + " for "
+                      + token.getService() + " until: " + new Date(result));
-                  if(LOG.isDebugEnabled()) {
-                    LOG.debug("Cancelled token via " + webUrl + " for "
-                	    + token.getService());
-                  }
+                  System.out.println("Cancelled token via " + webUrl + " for "
+                      + token.getService());
-                  if(LOG.isDebugEnabled()) {	
-                    LOG.debug("Fetched token via " + webUrl + " for "
-                        + token.getService() + " into " + tokenFile);
-                  }
+                  System.out.println("Fetched token via " + webUrl + " for "
+                      + token.getService() + " into " + tokenFile);
-                  if(LOG.isDebugEnabled()) {
-                    LOG.debug("Cancelled token for "
-                        + token.getService());
-                  }
+                  System.out.println("Cancelled token for "
+                      + token.getService());
-                  if(LOG.isDebugEnabled()) {
-                    LOG.debug("Renewed token for " + token.getService()
-                        + " until: " + new Date(result));
-                  }
+                  System.out.println("Renewed token for " + token.getService()
+                      + " until: " + new Date(result));
-                if(LOG.isDebugEnabled()) {
-                  LOG.debug("Fetched token for " + token.getService()
-                      + " into " + tokenFile);
-                }
+                System.out.println("Fetched token for " + token.getService()
+                    + " into " + tokenFile);
-      
-      if(LOG.isDebugEnabled()) {
-        LOG.debug("Retrieving token from: " + url);
-      }
-      

MOV31 MOV29 INS83 INS39 UPD42 MOV44 MOV43 MOV60 MOV60 MOV21 MOV21 MOV21 MOV21 MOV21 MOV60 MOV60 MOV60 MOV60 MOV60 MOV60 MOV60 MOV60 MOV25 MOV25 MOV60 MOV60 MOV21 MOV32 UPD42 INS14 MOV74 INS1 INS31 MOV79 MOV78 INS83 MOV43 INS42 MOV43 MOV8 MOV25 MOV41 MOV8 MOV27 INS8 MOV60 INS70 MOV41 INS25 MOV60 INS25 MOV44 MOV32 MOV8 INS42 MOV8 INS25 MOV42 MOV8 INS25 MOV60 MOV21 MOV60 INS70 INS42 INS8 INS8 INS42 INS8 INS8 MOV44 MOV32 MOV8 MOV70 MOV60 INS21 INS70 MOV32 MOV60 INS70 MOV60 MOV60 MOV21 MOV21 MOV21 INS40 UPD42 MOV21 INS44 INS32 INS8 INS32 MOV44 MOV32 INS8 MOV74 MOV42 MOV21 MOV44 MOV32 INS8 INS74 INS42 INS42 INS42 INS42 INS21 INS21 INS42 INS42 INS42 MOV42 MOV42 INS42 INS42 INS21 MOV76 MOV32 MOV21 MOV21 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS40 UPD42 MOV27 INS40 UPD42 INS43 INS76 INS32 INS32 INS42 INS43 INS76 INS32 MOV42 MOV11 INS40 UPD42 MOV27 MOV32 MOV42 INS42 INS42 MOV42 INS11 INS40 INS42 INS27 INS42 INS40 INS42 INS27 UPD42 MOV42 INS40 UPD42 MOV27 MOV42 INS74 INS42 INS45 MOV42 MOV45 INS32 INS45 MOV42 MOV45 INS32 INS45 INS42 MOV43 MOV42 MOV42 INS43 INS43 INS42 INS42 INS42 INS42 MOV42 MOV42 INS42 INS42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL25 DEL70 DEL8 DEL42 DEL42 DEL42 DEL42 DEL32 DEL25 DEL42 DEL42 DEL42 DEL32 DEL8 DEL25 DEL42 DEL32 DEL8 DEL25 DEL8 DEL70 DEL8 DEL25 DEL42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25 DEL8 DEL70 DEL8 DEL42 DEL42 DEL42 DEL32 DEL8 DEL25 DEL8 DEL70 DEL8 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25 DEL8 DEL25 DEL25 DEL8 DEL25 DEL83 DEL83 DEL39 DEL42 DEL42 DEL1 DEL14 DEL32 DEL21 DEL8 DEL31 DEL42 DEL42 DEL32 DEL42 DEL42 DEL45 DEL42 DEL27 DEL32 DEL21 DEL8 DEL25