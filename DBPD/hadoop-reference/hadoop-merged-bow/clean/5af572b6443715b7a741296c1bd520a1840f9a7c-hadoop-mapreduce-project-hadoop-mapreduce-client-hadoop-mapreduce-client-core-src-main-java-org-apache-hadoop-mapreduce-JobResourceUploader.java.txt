HADOOP-13427. Eliminate needless uses of FileSystem#{exists(), isFile(), isDirectory()}. Contributed by Steve Loughran and Mingliang Liu

-    if (!jtFs.exists(submitJobDir)) {
+    try {
+      jtFs.getFileStatus(submitJobDir);
+    } catch (FileNotFoundException e) {
-          + "It should just be created, so something wrong here.");
+          + "It should just be created, so something wrong here.", e);
-      if (!localFs.exists(path)) {
-        throw new FileNotFoundException("File " + file + " does not exist.");
-      }
+      localFs.getFileStatus(path);
-      if (!fs.exists(path)) {
-        throw new FileNotFoundException("File " + file + " does not exist.");
-      }
+      fs.getFileStatus(path);

INS54 INS8 INS12 INS21 INS44 MOV8 INS21 INS21 INS32 MOV43 INS42 INS32 INS32 MOV42 UPD42 MOV42 MOV42 MOV42 UPD42 MOV42 MOV42 MOV42 UPD42 MOV42 MOV42 INS42 DEL32 DEL38 DEL25 DEL32 DEL38 DEL45 DEL42 DEL45 DEL27 DEL14 DEL53 DEL8 DEL25 DEL32 DEL38 DEL42 DEL43 DEL45 DEL42 DEL45 DEL27 DEL14 DEL53 DEL8 DEL25