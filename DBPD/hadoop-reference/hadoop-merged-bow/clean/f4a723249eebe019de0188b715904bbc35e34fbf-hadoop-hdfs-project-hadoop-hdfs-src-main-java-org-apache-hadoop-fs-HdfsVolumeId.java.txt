Merge r1415804 through r1419190 from trunk.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1419193 13f79535-47bb-0310-9956-ffa450edef68

+ * 
+ * Note that invalid IDs are represented by {@link VolumeId#INVALID_VOLUME_ID}.
-
+  
-  private final boolean isValid;
-  public HdfsVolumeId(byte[] id, boolean isValid) {
+  public HdfsVolumeId(byte[] id) {
+    if (id == null) {
+      throw new NullPointerException("A valid Id can only be constructed " +
+      		"with a non-null byte array.");
+    }
-    this.isValid = isValid;
-  public boolean isValid() {
-    return isValid;
+  public final boolean isValid() {
+    return true;
+    if (arg0 == null) {
+      return 1;
+    }
+    if (!arg0.isValid()) {
+      // any valid ID is greater 
+      // than any invalid ID: 
+      return 1;
+    }
-
+    // NB: if (!obj.isValid()) { return false; } check is not necessary
+    // because we have class identity checking above, and for this class
+    // isValid() is always true.

INS8 INS83 INS66 INS65 INS66 INS25 MOV21 INS25 INS25 INS67 INS27 INS8 INS9 INS27 INS8 INS38 INS8 INS42 INS42 INS42 INS33 INS53 INS42 INS33 INS41 INS32 INS41 INS14 INS34 INS42 INS42 INS34 INS43 INS27 INS42 INS45 INS45 DEL83 DEL83 DEL39 DEL42 DEL59 DEL23 DEL39 DEL42 DEL44 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL8 DEL42