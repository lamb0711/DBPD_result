YARN-6332. Make RegistrySecurity use short user names for ZK ACLs. Contributed by Billie Rinaldi

+  private boolean usesRealm = true;
+
+      usesRealm = system.contains("@");
-    String userName = ugi.getUserName();
+    String userName = null;
+    if (usesRealm) {
+      userName = ugi.getUserName();
+    } else {
+      userName = ugi.getShortUserName();
+    }
-    if (!username.contains("@")) {
+    if (usesRealm && !username.contains("@")) {

INS23 INS83 INS39 INS59 INS42 INS9 INS25 INS42 INS8 INS8 INS27 INS21 INS33 INS21 INS21 INS42 MOV38 INS7 INS7 INS7 INS42 INS32 INS42 MOV32 INS42 INS32 INS42 INS42 INS45 INS42 INS42