Merge branch 'trunk' into HDFS-6581

+import org.apache.hadoop.crypto.CipherSuite;
-  private final String path;
-  private final String keyName;
+  private final String path;
+  private final CipherSuite suite;
+  private final String keyName;
-  public EncryptionZone(String path, String keyName, long id) {
-    this.path = path;
-    this.keyName = keyName;
+  public EncryptionZone(long id, String path,
+      CipherSuite suite, String keyName) {
-  }
-
-  public String getPath() {
-    return path;
-  }
-
-  public String getKeyName() {
-    return keyName;
+    this.path = path;
+    this.suite = suite;
+    this.keyName = keyName;
+  public String getPath() {
+    return path;
+  }
+
+  public CipherSuite getSuite() {
+    return suite;
+  }
+
+  public String getKeyName() {
+    return keyName;
+  }
+
-      append(path).append(keyName).append(id).
+      append(id).append(path).
+      append(suite).append(keyName).
-      append(path, rhs.path).
-      append(keyName, rhs.keyName).
+      append(path, rhs.path).
+      append(suite, rhs.suite).
+      append(keyName, rhs.keyName).
-    return "EncryptionZone [path=" + path +
-        ", keyName=" + keyName +
-        ", id=" + id + "]";
+    return "EncryptionZone [id=" + id +
+        ", path=" + path +
+        ", suite=" + suite +
+        ", keyName=" + keyName + "]";

INS26 MOV23 MOV31 INS40 INS23 MOV44 INS31 INS83 INS83 INS43 INS59 INS44 MOV21 INS83 INS43 INS42 INS8 INS42 INS42 INS43 INS42 INS21 INS42 INS41 INS42 INS7 INS42 INS32 INS32 INS22 INS42 MOV32 INS42 MOV32 INS42 UPD45 INS42 INS45 UPD45 UPD42 UPD45 UPD42 INS52 INS42 INS42 UPD42 UPD42 INS42 INS40 UPD42 UPD42 UPD40 UPD42 UPD42 UPD40 UPD42 UPD42 UPD40