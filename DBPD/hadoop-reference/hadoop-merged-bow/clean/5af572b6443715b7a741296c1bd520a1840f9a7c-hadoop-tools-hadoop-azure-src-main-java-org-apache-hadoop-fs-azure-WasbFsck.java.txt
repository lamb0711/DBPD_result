HADOOP-13427. Eliminate needless uses of FileSystem#{exists(), isFile(), isDirectory()}. Contributed by Steve Loughran and Mingliang Liu

+import java.io.FileNotFoundException;
-    if (!fs.exists(p)) {
+    FileStatus status;
+    try {
+      status = fs.getFileStatus(p);
+    } catch (FileNotFoundException e) {
-    if (fs.isFile(p)) {
+    if (status.isFile()) {

INS26 INS40 INS60 INS54 INS43 INS59 INS8 INS12 INS42 INS42 INS21 INS44 MOV8 UPD42 INS7 INS43 INS42 INS42 INS32 INS42 MOV42 UPD42 MOV42 MOV42 DEL32 DEL38 DEL25 DEL42