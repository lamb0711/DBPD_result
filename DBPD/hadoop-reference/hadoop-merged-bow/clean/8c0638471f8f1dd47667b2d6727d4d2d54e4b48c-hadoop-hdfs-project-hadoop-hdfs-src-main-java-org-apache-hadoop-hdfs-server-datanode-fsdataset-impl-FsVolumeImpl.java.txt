HADOOP-10682. Replace FsDatasetImpl object lock with a separate lock object. (Chen Liang)

+import org.apache.hadoop.util.AutoCloseableLock;
-    synchronized(dataset) {
+    try(AutoCloseableLock lock = dataset.acquireDatasetLock()) {
-    synchronized (dataset) {
+    try(AutoCloseableLock lock = dataset.acquireDatasetLock()) {
-    synchronized(dataset) {
+    try(AutoCloseableLock lock = dataset.acquireDatasetLock()) {
-    synchronized(dataset) {
+    try(AutoCloseableLock lock = dataset.acquireDatasetLock()) {

INS26 INS40 INS54 INS54 INS54 INS54 INS58 MOV8 INS58 MOV8 INS58 MOV8 INS58 MOV8 INS43 INS59 INS43 INS59 INS43 INS59 INS43 INS59 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 DEL42 DEL51 DEL42 DEL51 DEL42 DEL51 DEL42 DEL51