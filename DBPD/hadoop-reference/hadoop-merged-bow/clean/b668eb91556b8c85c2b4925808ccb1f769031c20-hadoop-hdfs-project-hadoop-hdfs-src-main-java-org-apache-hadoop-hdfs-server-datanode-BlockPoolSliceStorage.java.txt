HDFS-10675. Datanode support to read from external stores.

+import org.apache.hadoop.fs.StorageType;
+    if (sd.getStorageLocation().getStorageType() == StorageType.PROVIDED) {
+      return false; // regular startup for PROVIDED storage directories
+    }
+    //no upgrades for storage directories that are PROVIDED
+    if (bpSd.getRoot() == null) {
+      return;
+    }
-    if (!prevDir.exists())
+    if (prevDir == null || !prevDir.exists()) {
+    }
+    LOG.info("doFinalize: " + dnCurDir);
+    if (dnCurDir == null) {
+      return; //we do nothing if the directory is null
+    }
+      if (sd.getCurrentDir() == null) {
+        return;
+      }
+      if (sd.getCurrentDir() == null) {
+        continue;
+      }

INS26 INS40 INS25 INS25 INS21 INS25 INS27 INS8 INS27 INS8 INS27 INS8 INS32 INS27 INS8 INS32 INS40 INS41 INS32 INS33 INS41 INS27 MOV38 INS41 INS42 INS42 INS27 INS42 INS33 INS41 INS25 INS25 INS32 INS42 INS9 INS42 INS42 INS42 INS33 INS45 INS42 INS27 INS8 INS27 INS8 INS42 INS42 INS32 INS33 INS41 INS32 INS33 INS18 INS42 INS42 INS42 INS42 DEL41