HDFS-5574. Remove buffer copy in BlockReader.skip. Contributed by Binglin Chang.

-  
-  byte[] skipBuf = null;
-  ByteBuffer checksumBytes = null;
-  /** Amount of unread data in the current received packet */
-  int dataLeft = 0;
-  
+
-  public int read(ByteBuffer buf) throws IOException {
+  public synchronized int read(ByteBuffer buf) throws IOException {
-     * in majority of the cases?. This one throws. */  
-    if ( skipBuf == null ) {
-      skipBuf = new byte[bytesPerChecksum]; 
-    }
-
-    long nSkipped = 0;
-    while ( nSkipped < n ) {
-      int toSkip = (int)Math.min(n-nSkipped, skipBuf.length);
-      int ret = read(skipBuf, 0, toSkip);
-      if ( ret <= 0 ) {
-        return nSkipped;
+     * in majority of the cases?. This one throws. */
+    long skipped = 0;
+    while (skipped < n) {
+      long needToSkip = n - skipped;
+      if (curDataSlice == null || curDataSlice.remaining() == 0 && bytesNeededToFinish > 0) {
+        readNextPacket();
-      nSkipped += ret;
+      if (curDataSlice.remaining() == 0) {
+        // we're at EOF now
+        break;
+      }
+
+      int skip = (int)Math.min(curDataSlice.remaining(), needToSkip);
+      curDataSlice.position(curDataSlice.position() + skip);
+      skipped += skip;
-    return nSkipped;
+    return skipped;

INS83 UPD42 UPD42 UPD42 INS25 INS60 INS21 UPD39 INS27 INS8 UPD27 INS39 INS59 INS32 UPD42 INS27 INS27 INS27 INS21 INS32 INS10 INS42 INS11 INS42 INS42 INS27 UPD42 UPD42 MOV42 UPD42 MOV42 INS42 INS33 INS27 INS27 INS32 INS42 INS42 INS39 INS32 INS32 INS42 INS32 INS34 INS42 INS34 UPD42 MOV42 INS42 INS42 INS32 INS42 INS42 INS42 INS42 UPD42 MOV42 INS42 INS42 DEL39 DEL85 DEL5 DEL42 DEL33 DEL59 DEL23 DEL42 DEL43 DEL42 DEL33 DEL59 DEL23 DEL66 DEL65 DEL29 DEL39 DEL42 DEL34 DEL59 DEL23 DEL42 DEL33 DEL27 DEL42 DEL39 DEL85 DEL5 DEL42 DEL3 DEL7 DEL21 DEL8 DEL25 DEL39 DEL42 DEL42 DEL27 DEL40 DEL32 DEL11 DEL39 DEL42 DEL42 DEL34 DEL32 DEL59 DEL60 DEL42 DEL42 DEL41