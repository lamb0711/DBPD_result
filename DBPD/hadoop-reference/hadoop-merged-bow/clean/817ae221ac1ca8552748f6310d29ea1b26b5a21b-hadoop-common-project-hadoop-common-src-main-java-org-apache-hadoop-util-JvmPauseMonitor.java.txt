HADOOP-12313. NPE in JvmPauseMonitor when calling stop() before start(). Contributed by Gabor Liptak.

-    monitorThread.start();
+    if (shouldRun) {
+      monitorThread.start();
+    } else {
+      LOG.warn("stop() was called before start() completed");
+    }
-    if (monitorThread != null) {
+    if (isStarted()) {

INS25 INS42 INS8 INS8 INS32 MOV21 INS21 INS42 INS32 INS42 INS42 INS45 DEL42 DEL33 DEL27