HDDS-1475 : Fix OzoneContainer start method. (#788)



-      this.scrubber = new ContainerScrubber(containerSet, config);
+      if (this.scrubber == null) {
+        this.scrubber = new ContainerScrubber(containerSet, config);
+      }

INS8 INS25 MOV21 INS27 MOV8 INS22 INS33 INS52 INS42