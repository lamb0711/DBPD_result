HDFS-10711. Optimize FSPermissionChecker group membership check. Contributed by Daryn Sharp.

-import java.util.Arrays;
-import java.util.Collections;
-import java.util.HashSet;
-import java.util.Set;
+import java.util.Collection;
-  private final Set<String> groups;
+  private final Collection<String> groups;
-    HashSet<String> s =
-        new HashSet<String>(Arrays.asList(callerUgi.getGroupNames()));
-    groups = Collections.unmodifiableSet(s);
+    this.groups = callerUgi.getGroups();
-  public boolean containsGroup(String group) {
+  public boolean isMemberOfGroup(String group) {
-  public Set<String> getGroups() {
-    return groups;
-  }
-
-    } else if (getGroups().contains(inode.getGroupName())) { //group class
+    } else if (isMemberOfGroup(inode.getGroupName())) { //group class
-          if (getGroups().contains(group)) {
+          if (isMemberOfGroup(group)) {
-    if (getGroups().contains(pool.getGroupName())
+    if (isMemberOfGroup(pool.getGroupName())

UPD40 UPD74 UPD42 UPD43 UPD42 INS22 INS52 INS42 UPD42 UPD42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 DEL40 DEL26 DEL40 DEL26 DEL40 DEL26 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL14 DEL59 DEL60 DEL42 DEL42 DEL83 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL41 DEL8 DEL31 DEL32 DEL42 DEL32 DEL42 DEL32 DEL42