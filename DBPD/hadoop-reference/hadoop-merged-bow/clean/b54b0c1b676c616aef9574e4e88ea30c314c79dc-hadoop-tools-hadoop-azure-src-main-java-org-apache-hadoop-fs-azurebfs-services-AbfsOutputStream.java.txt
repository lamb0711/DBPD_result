HADOOP-15659. Code changes for bug fix and new tests.
Contributed by Da Zhou.

+  private boolean supportFlush;
-      final int bufferSize) {
+      final int bufferSize,
+      final boolean supportFlush) {
+    this.supportFlush = supportFlush;
-    flushInternalAsync();
+    if (supportFlush) {
+      flushInternalAsync();
+    }
-    flushInternal();
+    if (supportFlush) {
+      flushInternal();
+    }
-    flushInternal();
+    if (supportFlush) {
+      flushInternal();
+    }
-          ex = (AzureBlobFileSystemException)ex.getCause();
+          ex = (AzureBlobFileSystemException) ex.getCause();
-
-    this.lastTotalAppendOffset = 0;
-        lastError = (AzureBlobFileSystemException)e.getCause();
+        lastError = (AzureBlobFileSystemException) e.getCause();
-        lastError = (IOException)new InterruptedIOException(e.toString()).initCause(e);
+        lastError = (IOException) new InterruptedIOException(e.toString()).initCause(e);

INS23 INS83 INS39 INS59 INS44 INS8 INS8 INS42 INS83 INS39 INS42 INS21 INS25 INS25 INS25 INS7 INS42 MOV8 INS42 INS8 INS42 MOV8 INS22 INS42 MOV21 INS52 INS42 DEL52 DEL42 DEL22 DEL34 DEL7 DEL21