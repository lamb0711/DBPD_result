Merge r1460409 through r1462697 from trunk.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1462698 13f79535-47bb-0310-9956-ffa450edef68

-  private final int maxRetries;
+  private final int maxAppAttempts;
-    this.maxRetries = conf.getInt(YarnConfiguration.RM_AM_MAX_RETRIES,
-        YarnConfiguration.DEFAULT_RM_AM_MAX_RETRIES);
+    int globalMaxAppAttempts = conf.getInt(YarnConfiguration.RM_AM_MAX_ATTEMPTS,
+        YarnConfiguration.DEFAULT_RM_AM_MAX_ATTEMPTS);
+    int individualMaxAppAttempts = submissionContext.getMaxAppAttempts();
+    if (individualMaxAppAttempts <= 0 ||
+        individualMaxAppAttempts > globalMaxAppAttempts) {
+      this.maxAppAttempts = globalMaxAppAttempts;
+      LOG.warn("The specific max attempts: " + individualMaxAppAttempts
+          + " for application: " + applicationId.getId()
+          + " is invalid, because it is out of the range [1, "
+          + globalMaxAppAttempts + "]. Use the global max attempts instead.");
+    } else {
+      this.maxAppAttempts = individualMaxAppAttempts;
+    }
+  public int getMaxAppAttempts() {
+    return this.maxAppAttempts;
+  }
+
+  @Override
-      } else if (app.attempts.size() >= app.maxRetries) {
+      } else if (app.attempts.size() >= app.maxAppAttempts) {
-            + app.maxRetries + " times due to " + failedEvent.getDiagnostics()
+            + app.maxAppAttempts + " times due to " + failedEvent.getDiagnostics()

INS31 INS78 INS83 INS39 INS42 INS8 UPD42 INS60 INS60 INS25 INS42 INS41 INS39 INS59 INS39 INS59 INS27 INS8 INS8 INS22 INS42 INS32 INS42 INS32 INS27 INS27 INS21 INS21 INS21 INS52 INS42 MOV42 MOV42 UPD40 MOV40 UPD40 MOV40 INS42 INS42 INS42 INS34 INS42 INS42 INS7 INS32 INS7 INS22 INS42 INS42 INS42 INS27 INS22 INS42 UPD40 INS52 INS42 INS45 INS42 INS45 INS32 INS45 INS42 INS45 INS52 INS42 INS42 INS42 UPD40 DEL52 DEL42 DEL22 DEL32 DEL7 DEL21