YARN-392. Make it possible to specify hard locality constraints in resource requests. (sandyr via tucu)

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1488326 13f79535-47bb-0310-9956-ffa450edef68

+        if (localRequest != null && !localRequest.getRelaxLocality()) {
+          LOG.warn("Relax locality off is not supported on local request: "
+              + localRequest);
+        }
+        
+        
+        if (rackLocalRequest != null && !rackLocalRequest.getRelaxLocality()) {
+          continue;
+        }
+        if (offSwitchRequest != null && !offSwitchRequest.getRelaxLocality()) {
+          continue;
+        }
+        
-    // TODO: add checks stuff about node specific scheduling here
-    ResourceRequest request = app.getResourceRequest(prio, ResourceRequest.ANY);
-    return request.getNumContainers() > 0 && 
+    ResourceRequest anyRequest = app.getResourceRequest(prio, ResourceRequest.ANY);
+    ResourceRequest rackRequest = app.getResourceRequest(prio, node.getRackName());
+    ResourceRequest nodeRequest = app.getResourceRequest(prio, node.getHostName());
+
+    return
+        // There must be outstanding requests at the given priority:
+        anyRequest != null && anyRequest.getNumContainers() > 0 &&
+        // If locality relaxation is turned off at *-level, there must be a
+        // non-zero request for the node's rack:
+        (anyRequest.getRelaxLocality() ||
+            (rackRequest != null && rackRequest.getNumContainers() > 0)) &&
+        // If locality relaxation is turned off at rack-level, there must be a
+        // non-zero request at the node:
+        (rackRequest == null || rackRequest.getRelaxLocality() ||
+            (nodeRequest != null && nodeRequest.getNumContainers() > 0)) &&
+        // The requested container must be able to fit on the node:
-            request.getCapability(), node.getRMNode().getTotalCapability());
+            anyRequest.getCapability(), node.getRMNode().getTotalCapability());

INS60 INS60 INS43 INS59 INS43 INS59 UPD42 INS42 INS42 INS32 INS42 INS42 INS32 INS27 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS32 INS27 INS36 INS25 INS25 INS25 INS42 INS42 INS42 INS42 INS27 INS36 INS27 UPD42 INS27 INS8 INS27 INS8 INS27 INS8 INS27 INS27 INS27 INS27 INS36 INS27 INS38 INS21 INS27 INS38 INS18 INS27 INS38 INS18 INS42 INS33 INS32 INS34 INS32 INS36 INS27 INS32 INS27 INS42 INS33 INS32 INS32 INS42 INS33 INS32 INS42 INS33 INS32 INS42 INS42 INS42 INS42 INS27 INS42 INS33 INS42 INS42 INS27 INS27 INS42 INS42 INS42 INS42 INS27 INS42 INS42 INS42 INS42 INS27 MOV27 INS42 INS33 INS32 INS34 INS45 INS42 INS42 INS33 INS42 INS42 UPD42