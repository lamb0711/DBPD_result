HDDS-187. Command status publisher for datanode.
Contributed by Ajay Kumar.

-import org.apache.hadoop.hdds.scm.container.ContainerID;
-
+    super();
+    this.containerID = containerID;
+    this.sourceDatanodes = sourceDatanodes;
+  }
+
+  // Should be called only for protobuf conversion
+  public ReplicateContainerCommand(long containerID,
+      List<DatanodeDetails> sourceDatanodes, long cmdId) {
+    super(cmdId);
+        .setCmdId(getCmdId())
-        .stream()
-        .map(DatanodeDetails::getFromProtoBuf)
-        .collect(Collectors.toList());
+            .stream()
+            .map(DatanodeDetails::getFromProtoBuf)
+            .collect(Collectors.toList());
-        datanodeDetails);
+        datanodeDetails, protoMessage.getCmdId());

INS31 INS83 INS42 INS44 INS44 INS44 INS8 INS46 INS39 INS42 INS74 INS42 INS39 INS42 INS46 INS21 INS21 INS43 INS43 INS42 INS7 INS7 INS42 INS42 INS22 INS42 INS22 INS42 INS32 INS32 INS52 INS42 INS52 INS42 MOV32 INS42 INS42 INS42 INS42 UPD42 INS32 UPD42 MOV42 DEL40 DEL26