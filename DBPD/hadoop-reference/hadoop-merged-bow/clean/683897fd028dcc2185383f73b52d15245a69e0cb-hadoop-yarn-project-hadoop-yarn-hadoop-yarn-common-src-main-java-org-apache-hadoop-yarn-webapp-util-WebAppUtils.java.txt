YARN-2713. "RM Home" link in NM should point to one of the RMs in an HA setup. (kasha)

+    String rmId = null;
+    if (HAUtil.isHAEnabled(conf)) {
+      // If HA enabled, pick one of the RM-IDs and rely on redirect to go to
+      // the Active RM
+      rmId = (String) HAUtil.getRMHAIds(conf).toArray()[0];
+    }
+
-          conf.getSocketAddr(YarnConfiguration.RM_WEBAPP_HTTPS_ADDRESS,
+          conf.getSocketAddr(
+              rmId == null
+                  ? YarnConfiguration.RM_WEBAPP_HTTPS_ADDRESS
+                  : HAUtil.addSuffix(
+                  YarnConfiguration.RM_WEBAPP_HTTPS_ADDRESS, rmId),
-          conf.getSocketAddr(YarnConfiguration.RM_WEBAPP_ADDRESS,
+          conf.getSocketAddr(
+              rmId == null
+                  ? YarnConfiguration.RM_WEBAPP_ADDRESS
+                  : HAUtil.addSuffix(
+                  YarnConfiguration.RM_WEBAPP_ADDRESS, rmId),

INS60 INS25 INS43 INS59 INS32 INS8 INS42 INS42 INS33 INS42 INS42 INS42 INS21 INS7 INS42 INS11 INS43 INS2 INS16 INS16 INS42 INS32 INS34 INS27 INS40 INS32 INS27 INS40 INS32 INS32 INS42 INS42 INS33 INS42 INS42 INS40 INS42 INS42 INS33 INS42 INS42 INS40 INS42 INS42 INS42 INS42 DEL40 DEL40