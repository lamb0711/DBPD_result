MAPREDUCE-6251. Added a new config for JobClient to retry JobStatus calls so that they don't fail on history-server backed by DFSes with not so strong guarantees. Contributed by Craig Welch.

+import org.apache.hadoop.mapreduce.MRJobConfig;
+  private int maxRetry = MRJobConfig.DEFAULT_MR_CLIENT_JOB_MAX_RETRIES;
+  private long retryInterval =
+      MRJobConfig.DEFAULT_MR_CLIENT_JOB_RETRY_INTERVAL;
+
+
+    maxRetry = conf.getInt(MRJobConfig.MR_CLIENT_JOB_MAX_RETRIES,
+      MRJobConfig.DEFAULT_MR_CLIENT_JOB_MAX_RETRIES);
+
+    retryInterval =
+      conf.getLong(MRJobConfig.MR_CLIENT_JOB_RETRY_INTERVAL,
+        MRJobConfig.DEFAULT_MR_CLIENT_JOB_RETRY_INTERVAL);
+
-  /**
-   * Get an {@link RunningJob} object to track an ongoing job.  Returns
-   * null if the id does not correspond to any known job.
-   * 
-   * @param jobid the jobid of the job.
-   * @return the {@link RunningJob} handle to track the job, null if the 
-   *         <code>jobid</code> doesn't correspond to any known job.
-   * @throws IOException
-   */
-  public RunningJob getJob(final JobID jobid) throws IOException {
+
+  protected RunningJob getJobInner(final JobID jobid) throws IOException {
-  /**@deprecated Applications should rather use {@link #getJob(JobID)}. 
+  /**
+   * Get an {@link RunningJob} object to track an ongoing job.  Returns
+   * null if the id does not correspond to any known job.
+   *
+   * @param jobid the jobid of the job.
+   * @return the {@link RunningJob} handle to track the job, null if the
+   *         <code>jobid</code> doesn't correspond to any known job.
+   * @throws IOException
+   */
+  public RunningJob getJob(final JobID jobid) throws IOException {
+     for (int i = 0;i <= maxRetry;i++) {
+       if (i > 0) {
+         try {
+           Thread.sleep(retryInterval);
+         } catch (Exception e) { }
+       }
+       RunningJob job = getJobInner(jobid);
+       if (job != null) {
+         return job;
+       }
+     }
+     return null;
+  }
+
+  /**@deprecated Applications should rather use {@link #getJob(JobID)}.

INS26 INS40 INS23 INS23 INS31 INS83 INS39 INS59 INS83 INS39 INS59 UPD83 UPD42 MOV29 INS83 INS43 INS42 INS44 INS43 INS8 INS42 INS40 INS42 INS40 INS21 INS21 INS42 INS83 INS43 INS42 INS42 INS24 INS41 INS7 INS7 UPD66 INS42 INS58 INS27 INS37 INS8 INS33 UPD66 INS42 INS32 INS42 INS32 INS39 INS59 INS42 INS42 INS42 INS25 INS60 INS25 INS42 INS42 INS40 INS40 INS42 INS42 INS40 INS40 INS42 INS34 INS27 INS8 INS43 INS59 INS27 INS8 INS42 INS34 INS54 INS42 INS42 INS32 INS42 INS33 INS41 INS8 INS12 INS42 INS42 INS42 INS21 INS44 INS8 INS32 INS43 INS42 INS42 INS42 INS42 INS42