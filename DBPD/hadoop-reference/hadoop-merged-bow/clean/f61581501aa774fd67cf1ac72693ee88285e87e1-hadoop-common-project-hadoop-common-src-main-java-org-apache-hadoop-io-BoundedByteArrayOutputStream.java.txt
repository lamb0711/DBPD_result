Merge trunk into HDFS-347 branch.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-347@1446832 13f79535-47bb-0310-9956-ffa450edef68

-  private final byte[] buffer;
+  private byte[] buffer;
+  private int startOffset;
-  private int count;
+  private int currentPointer;
+    this(new byte[capacity], 0, limit);
+  }
+
+  protected BoundedByteArrayOutputStream(byte[] buf, int offset, int limit) {
+    resetBuffer(buf, offset, limit);
+  }
+  
+  protected void resetBuffer(byte[] buf, int offset, int limit) {
+    int capacity = buf.length - offset;
-    this.buffer = new byte[capacity];
-    this.limit = limit;
-    this.count = 0;
+    this.buffer = buf;
+    this.startOffset = offset;
+    this.currentPointer = offset;
+    this.limit = offset + limit;
-
+  
-    if (count >= limit) {
+    if (currentPointer >= limit) {
-    buffer[count++] = (byte) b;
+    buffer[currentPointer++] = (byte) b;
-    if (count + len > limit) {
+    if (currentPointer + len > limit) {
-    System.arraycopy(b, off, buffer, count, len);
-    count += len;
+    System.arraycopy(b, off, buffer, currentPointer, len);
+    currentPointer += len;
-    if (newlim > buffer.length) {
+    if (newlim > (buffer.length - startOffset)) {
-    this.count = 0;
+    this.currentPointer = startOffset;
-    this.limit = buffer.length;
-    this.count = 0;
+    this.limit = buffer.length - startOffset;
+    this.currentPointer = startOffset;
-    return count;
+    return currentPointer - startOffset;
+  }
+  
+  public int available() {
+    return limit - currentPointer;

MOV23 INS23 INS31 INS31 INS31 INS83 INS39 INS59 MOV29 INS83 INS42 MOV44 MOV44 INS8 INS83 INS42 INS44 INS44 INS44 INS8 UPD83 INS39 UPD42 INS44 INS44 INS44 MOV21 INS83 INS39 INS42 INS8 UPD42 INS42 INS17 INS5 INS42 INS39 INS42 INS39 INS42 INS21 INS5 INS42 INS39 INS42 INS39 INS42 INS60 INS21 INS41 MOV3 INS34 INS42 INS39 INS85 INS32 INS39 INS85 INS39 INS59 INS7 INS27 INS27 INS42 INS42 INS42 INS42 INS42 INS27 INS42 INS42 INS22 INS42 INS27 UPD42 UPD42 UPD42 INS36 INS42 INS27 INS42 INS42 INS42 INS42 INS42 INS40 INS42 UPD42 INS52 INS42 INS42 INS42 UPD42 INS27 UPD42 INS40 INS42 UPD42 UPD42 MOV40 INS42 DEL83 DEL34 DEL42 DEL34 DEL40 DEL34 DEL42