Merge changes from trunk

-    if (provider == null) {
-      LOG.info("No KeyProvider found.");
-    } else {
-      LOG.info("Found KeyProvider: " + provider.toString());
+    if (LOG.isDebugEnabled()) {
+      if (provider == null) {
+        LOG.debug("No KeyProvider found.");
+      } else {
+        LOG.debug("Found KeyProvider: " + provider.toString());
+      }
+  public DFSInotifyEventInputStream getInotifyEventStream() throws IOException {
+    return new DFSInotifyEventInputStream(namenode);
+  }
+
+  public DFSInotifyEventInputStream getInotifyEventStream(long lastReadTxid)
+      throws IOException {
+    return new DFSInotifyEventInputStream(namenode, lastReadTxid);
+  }
+

INS31 INS31 INS83 INS43 INS42 INS43 INS8 INS83 INS43 INS42 INS44 INS43 INS8 INS25 INS42 INS42 INS41 INS42 INS39 INS42 INS42 INS41 INS32 INS8 INS14 INS14 INS42 INS42 MOV25 INS43 INS42 INS43 INS42 INS42 INS42 INS42 UPD42 UPD42