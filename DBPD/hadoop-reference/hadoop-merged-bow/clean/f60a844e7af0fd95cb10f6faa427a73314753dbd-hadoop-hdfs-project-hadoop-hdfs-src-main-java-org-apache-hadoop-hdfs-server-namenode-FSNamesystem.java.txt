HDFS-4119. Complete the allowSnapshot code and add a test for it.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1403087 13f79535-47bb-0310-9956-ffa450edef68

+  /** @return the FSDirectory. */
+  public FSDirectory getFSDirectory() {
+    return dir;
+  }
-  // Allow snapshot on a directroy.
-  @VisibleForTesting
-  public void allowSnapshot(String snapshotRoot)
-      throws SafeModeException, IOException {
-    // TODO: implement
+  /** Allow snapshot on a directroy. */
+  public void allowSnapshot(String path) throws SafeModeException, IOException {
+    writeLock();
+    try {
+      checkOperation(OperationCategory.WRITE);
+      if (isInSafeMode()) {
+        throw new SafeModeException("Cannot allow snapshot for " + path, safeMode);
+      }
+      checkOwner(path);
+
+      //TODO: do not hardcode snapshot quota value
+      snapshotManager.setSnapshottable(path, 256);
+      getEditLog().logAllowSnapshot(path);
+    } finally {
+      writeUnlock();
+    }
+    getEditLog().logSync();
+    
+    //TODO: audit log
+        getEditLog().logCreateSnapshot(snapshotName, path);

INS31 INS31 INS29 INS83 INS43 INS42 INS8 INS29 MOV83 MOV39 MOV42 MOV44 MOV43 MOV43 INS8 INS65 INS42 INS41 INS65 UPD42 INS21 INS54 INS21 INS66 INS42 INS66 INS32 INS8 INS8 INS32 INS42 INS21 INS25 INS21 INS21 INS21 INS21 INS32 INS42 INS32 INS32 INS8 INS32 INS32 INS32 INS32 INS42 INS42 INS40 INS42 INS53 INS42 INS42 INS42 INS42 INS42 INS34 INS32 INS42 INS42 INS42 INS21 INS14 INS42 INS32 INS43 INS27 INS42 INS32 INS42 INS42 INS42 INS42 INS45 INS42 INS42 DEL42 DEL78 DEL8 DEL31