YARN-4986. Add a check in the coprocessor for table to operated on (Vrushali C via sjlee)

+  private boolean isFlowRunRegion = false;
+      isFlowRunRegion = TimelineStorageUtils.isFlowRunTable(
+          region.getRegionInfo(), env.getConfiguration());
+  public boolean isFlowRunRegion() {
+    return isFlowRunRegion;
+  }
+
+    if (!isFlowRunRegion) {
+      return;
+    }
+    if (!isFlowRunRegion) {
+      return;
+    }
+
-      RegionScanner s) throws IOException {
-    // set max versions for scan to see all
-    // versions to aggregate for metrics
-    scan.setMaxVersions();
-    return s;
+      RegionScanner scanner) throws IOException {
+
+    if (isFlowRunRegion) {
+      // set max versions for scan to see all
+      // versions to aggregate for metrics
+      scan.setMaxVersions();
+    }
+    return scanner;
+    if (!isFlowRunRegion) {
+      return scanner;
+    }
+    if (!isFlowRunRegion) {
+      return scanner;
+    }
+    if (!isFlowRunRegion) {
+      return;
+    }
+    if (!isFlowRunRegion) {
+      return scanner;
+    }

INS23 INS31 INS83 INS39 INS59 INS83 INS39 INS42 INS8 INS8 INS42 INS9 INS41 INS25 INS25 UPD42 INS25 INS41 INS25 INS25 INS25 INS25 INS42 INS38 INS8 INS38 INS8 INS42 MOV8 INS42 INS38 INS8 INS38 INS8 INS38 INS8 INS38 INS8 INS21 INS42 INS41 INS42 INS41 INS42 INS41 INS42 INS41 INS42 INS41 INS42 INS41 INS7 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS32 INS32 INS42 INS42 INS42 INS42 DEL42 DEL41