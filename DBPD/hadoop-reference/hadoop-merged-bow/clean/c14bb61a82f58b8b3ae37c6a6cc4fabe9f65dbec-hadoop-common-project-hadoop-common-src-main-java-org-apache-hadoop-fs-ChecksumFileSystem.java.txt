Merge trunk into HA branch


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-1623@1242635 13f79535-47bb-0310-9956-ffa450edef68

+  private boolean writeChecksum = true;
+  @Override
+  public void setWriteChecksum(boolean writeChecksum) {
+    this.writeChecksum = writeChecksum;
+  }
+  
-    private long fileLen = -1L;
+  }
+  
+  private static class FSDataBoundedInputStream extends FSDataInputStream {
+    private FileSystem fs;
+    private Path file;
+    private long fileLen = -1L;
+
+    FSDataBoundedInputStream(FileSystem fs, Path file, InputStream in)
+        throws IOException {
+      super(in);
+      this.fs = fs;
+      this.file = file;
+    }
+    
+    @Override
+    public boolean markSupported() {
+      return false;
+    }
-    return verifyChecksum
-      ? new FSDataInputStream(new ChecksumFSInputChecker(this, f, bufferSize))
-      : getRawFileSystem().open(f, bufferSize);
+    FileSystem fs;
+    InputStream in;
+    if (verifyChecksum) {
+      fs = this;
+      in = new ChecksumFSInputChecker(this, f, bufferSize);
+    } else {
+      fs = getRawFileSystem();
+      in = fs.open(f, bufferSize);
+    }
+    return new FSDataBoundedInputStream(fs, f, in);
-    final FSDataOutputStream out = new FSDataOutputStream(
-        new ChecksumFSOutputSummer(this, f, overwrite, bufferSize, replication,
-            blockSize, progress), null);
+    final FSDataOutputStream out;
+    if (writeChecksum) {
+      out = new FSDataOutputStream(
+          new ChecksumFSOutputSummer(this, f, overwrite, bufferSize, replication,
+              blockSize, progress), null);
+    } else {
+      out = fs.create(f, permission, overwrite, bufferSize, replication,
+          blockSize, progress);
+      // remove the checksum file since we aren't writing one
+      Path checkFile = getChecksumFile(f);
+      if (fs.exists(checkFile)) {
+        fs.delete(checkFile, true);
+      }
+    }
+      if (fs.isDirectory(dst)) {
+        dst = new Path(dst, src.getName());
+      }
-      Path checkFile = getChecksumFile(src);
-      if (fs.exists(checkFile)) { //try to rename checksum
-        if (fs.isDirectory(dst)) {
-          value = fs.rename(checkFile, dst);
-        } else {
-          value = fs.rename(checkFile, getChecksumFile(dst));
-        }
+      Path srcCheckFile = getChecksumFile(src);
+      Path dstCheckFile = getChecksumFile(dst);
+      if (fs.exists(srcCheckFile)) { //try to rename checksum
+        value = fs.rename(srcCheckFile, dstCheckFile);
+      } else if (fs.exists(dstCheckFile)) {
+        // no src checksum, so remove dst checksum
+        value = fs.delete(dstCheckFile, true); 

INS23 INS31 INS55 INS83 INS39 INS59 INS78 INS83 INS39 INS42 INS44 INS8 INS83 INS83 INS42 MOV43 INS23 INS23 MOV23 INS31 INS31 MOV31 MOV31 MOV31 INS42 INS9 INS42 INS39 INS42 INS21 INS83 INS43 INS59 INS83 INS43 INS59 INS42 INS44 INS44 INS44 INS43 INS8 INS78 INS83 INS39 INS42 INS8 INS60 INS60 INS25 INS41 INS25 INS7 INS42 INS42 INS42 INS42 INS43 INS42 INS43 INS42 INS43 INS42 INS42 INS46 INS21 INS21 INS42 INS41 INS43 INS59 INS43 INS59 INS42 INS8 INS8 INS14 INS42 INS8 INS8 INS22 INS42 INS42 INS42 INS42 INS42 INS7 INS7 INS9 INS42 INS42 INS42 INS42 INS21 INS21 INS21 INS21 INS43 INS42 INS42 INS42 INS21 INS21 INS60 INS25 INS25 INS60 INS52 INS42 INS22 INS42 INS22 INS42 INS7 INS7 INS7 INS7 INS42 INS7 INS7 INS43 INS59 INS32 INS8 MOV32 INS8 INS43 INS59 MOV8 INS25 INS52 INS42 INS52 INS42 INS42 INS52 INS42 MOV14 INS42 MOV32 INS42 INS32 INS42 MOV14 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 INS21 INS21 UPD42 INS42 INS42 MOV32 UPD42 INS32 MOV8 INS42 MOV42 MOV42 MOV42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS7 INS42 INS42 INS42 INS42 INS42 INS42 INS9 INS42 INS14 INS43 INS42 INS32 UPD42 UPD42 INS42 INS42 INS42 UPD42 UPD42 INS9 DEL42 DEL14 DEL32 DEL16 DEL41 DEL25 DEL8