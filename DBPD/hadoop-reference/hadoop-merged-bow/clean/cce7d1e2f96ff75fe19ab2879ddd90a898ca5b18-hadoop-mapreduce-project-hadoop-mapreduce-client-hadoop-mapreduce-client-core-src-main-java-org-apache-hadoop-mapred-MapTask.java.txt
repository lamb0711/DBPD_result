Merge remote-tracking branch 'origin/trunk' into MR-2841

Conflicts:
	hadoop-mapreduce-project/hadoop-mapreduce-client/hadoop-mapreduce-client-core/src/main/java/org/apache/hadoop/mapred/MapTask.java

+import org.apache.hadoop.mapreduce.CryptoUtils;
-    MapOutputCollector<KEY, VALUE> collector
-      = (MapOutputCollector<KEY, VALUE>)
-       ReflectionUtils.newInstance(
-                        job.getClass(JobContext.MAP_OUTPUT_COLLECTOR_CLASS_ATTR,
-                        MapOutputBuffer.class, MapOutputCollector.class), job);
-                           new MapOutputCollector.Context(this, job, reporter);
+      new MapOutputCollector.Context(this, job, reporter);
-    try {
-      collector.init(context);
-    } catch (Exception e) {
-      LOG.warn("Nativetask falling back to Java MapOutputCollector", e);
-      collector = new MapOutputBuffer();
-      collector.init(context);
-    } finally {
-      LOG.info("Map output collector class = " + collector.getClass().getName());
-      return collector;
+    Class<?>[] collectorClasses = job.getClasses(
+      JobContext.MAP_OUTPUT_COLLECTOR_CLASS_ATTR, MapOutputBuffer.class);
+    int remainingCollectors = collectorClasses.length;
+    for (Class clazz : collectorClasses) {
+      try {
+        if (!MapOutputCollector.class.isAssignableFrom(clazz)) {
+          throw new IOException("Invalid output collector class: " + clazz.getName() +
+            " (does not implement MapOutputCollector)");
+        }
+        Class<? extends MapOutputCollector> subclazz =
+          clazz.asSubclass(MapOutputCollector.class);
+        LOG.debug("Trying map output collector class: " + subclazz.getName());
+        MapOutputCollector<KEY, VALUE> collector =
+          ReflectionUtils.newInstance(subclazz, job);
+        collector.init(context);
+        LOG.info("Map output collector class = " + collector.getClass().getName());
+        return collector;
+      } catch (Exception e) {
+        String msg = "Unable to initialize MapOutputCollector " + clazz.getName();
+        if (--remainingCollectors > 0) {
+          msg += " (" + remainingCollectors + " more collector(s) to try)";
+        }
+        LOG.warn(msg, e);
+      }
+    throw new IOException("Unable to initialize any output collector");
-            writer = new Writer<K, V>(job, out, keyClass, valClass, codec,
+            FSDataOutputStream partitionOut = CryptoUtils.wrapIfNecessary(job, out);
+            writer = new Writer<K, V>(job, partitionOut, keyClass, valClass, codec,
-            rec.rawLength = writer.getRawLength();
-            rec.partLength = writer.getCompressedLength();
+            rec.rawLength = writer.getRawLength() + CryptoUtils.cryptoPadding(job);
+            rec.partLength = writer.getCompressedLength() + CryptoUtils.cryptoPadding(job);
-            writer = new IFile.Writer<K,V>(job, out, keyClass, valClass, codec,
+            FSDataOutputStream partitionOut = CryptoUtils.wrapIfNecessary(job, out);
+            writer = new IFile.Writer<K,V>(job, partitionOut, keyClass, valClass, codec,
-            rec.rawLength = writer.getRawLength();
-            rec.partLength = writer.getCompressedLength();
+            rec.rawLength = writer.getRawLength() + CryptoUtils.cryptoPadding(job);
+            rec.partLength = writer.getCompressedLength() + CryptoUtils.cryptoPadding(job);
+            FSDataOutputStream finalPartitionOut = CryptoUtils.wrapIfNecessary(job, finalOut);
-              new Writer<K, V>(job, finalOut, keyClass, valClass, codec, null);
+              new Writer<K, V>(job, finalPartitionOut, keyClass, valClass, codec, null);
-            rec.rawLength = writer.getRawLength();
-            rec.partLength = writer.getCompressedLength();
+            rec.rawLength = writer.getRawLength() + CryptoUtils.cryptoPadding(job);
+            rec.partLength = writer.getCompressedLength() + CryptoUtils.cryptoPadding(job);
+          FSDataOutputStream finalPartitionOut = CryptoUtils.wrapIfNecessary(job, finalOut);
-              new Writer<K, V>(job, finalOut, keyClass, valClass, codec,
+              new Writer<K, V>(job, finalPartitionOut, keyClass, valClass, codec,
-          rec.rawLength = writer.getRawLength();
-          rec.partLength = writer.getCompressedLength();
+          rec.rawLength = writer.getRawLength() + CryptoUtils.cryptoPadding(job);
+          rec.partLength = writer.getCompressedLength() + CryptoUtils.cryptoPadding(job);

INS26 INS40 INS60 INS60 INS70 INS53 INS5 INS59 INS39 INS59 INS44 INS42 INS8 INS14 INS74 INS85 INS42 INS32 INS42 INS40 INS43 INS42 INS54 UPD43 MOV43 INS45 INS43 INS76 INS42 INS42 INS40 MOV57 INS42 INS8 INS12 UPD42 INS42 INS25 INS60 INS21 INS60 MOV21 MOV21 MOV41 MOV44 INS8 INS60 INS38 INS8 INS74 INS59 INS32 MOV74 MOV59 INS60 INS25 MOV21 INS43 INS59 MOV74 INS32 INS53 UPD43 MOV43 INS76 INS42 INS32 INS42 INS42 INS27 INS32 INS43 INS59 INS27 INS8 INS42 INS42 INS32 INS27 INS27 INS57 INS42 INS42 INS14 UPD42 INS43 INS42 INS42 MOV57 INS45 INS32 MOV42 MOV42 UPD42 MOV42 MOV42 INS42 INS42 INS27 INS38 INS34 MOV21 INS42 INS60 INS60 INS60 INS42 INS42 INS42 INS42 MOV74 UPD42 MOV32 INS32 MOV32 INS32 INS43 INS43 INS27 INS42 INS42 INS42 INS45 INS32 INS42 INS7 INS43 INS59 INS43 INS59 INS43 INS59 MOV74 INS42 INS42 INS42 INS42 INS42 INS42 MOV42 UPD42 MOV42 INS45 INS32 INS45 INS42 INS42 INS42 INS27 INS42 INS42 INS32 INS27 INS27 INS42 INS42 INS32 INS27 INS27 INS42 INS42 INS32 INS27 INS27 INS42 INS42 INS45 INS42 INS45 INS42 INS42 INS42 INS42 UPD42 MOV32 INS32 MOV32 INS32 INS42 INS42 INS42 INS42 UPD42 MOV32 INS32 MOV32 INS32 INS42 INS42 INS42 INS42 MOV74 UPD42 MOV32 INS32 MOV32 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 DEL43 DEL43 DEL74 DEL60 DEL42 DEL40 DEL32 DEL32 DEL11 DEL42 DEL42 DEL42 DEL32 DEL45 DEL8 DEL42 DEL14 DEL7 DEL21 DEL8 DEL12 DEL8 DEL54