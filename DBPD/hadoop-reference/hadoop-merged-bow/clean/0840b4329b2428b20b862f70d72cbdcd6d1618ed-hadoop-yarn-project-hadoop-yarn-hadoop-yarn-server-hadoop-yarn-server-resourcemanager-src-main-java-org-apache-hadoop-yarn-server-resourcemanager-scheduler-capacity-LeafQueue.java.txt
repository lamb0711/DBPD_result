YARN-5756. Add state-machine implementation for scheduler queues. (Xuan Gong via wangda)

-              + " [= currentNumContainers ]" + "\n" + "state = " + state
+              + " [= currentNumContainers ]" + "\n" + "state = " + getState()
+
+    appFinished();
+
+
+  @Override
+  public void stopQueue() {
+    try {
+      writeLock.lock();
+      if (getNumApplications() > 0) {
+        updateQueueState(QueueState.DRAINING);
+      } else {
+        updateQueueState(QueueState.STOPPED);
+      }
+    } finally {
+      writeLock.unlock();
+    }
+  }

INS31 INS78 INS83 INS39 INS42 INS8 INS21 INS42 INS54 INS32 INS8 INS8 INS42 INS21 INS25 INS21 INS32 INS27 INS8 INS8 INS32 INS42 INS42 INS32 INS34 INS21 INS21 INS42 INS42 INS32 INS42 INS32 INS32 INS42 INS42 INS40 INS42 INS40 DEL42