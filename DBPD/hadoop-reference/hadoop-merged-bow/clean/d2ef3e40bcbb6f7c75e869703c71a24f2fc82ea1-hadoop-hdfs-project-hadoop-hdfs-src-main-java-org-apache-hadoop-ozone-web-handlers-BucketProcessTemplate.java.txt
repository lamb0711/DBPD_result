HDFS-12126. Ozone: Ozone shell: Add more testing for bucket shell commands. Contributed by Yiqun Lin.

+    OzoneException exp = null;
-      throw ErrorTable
+      exp = ErrorTable
-      throw ErrorTable
+      exp = ErrorTable
-      throw ErrorTable
+      exp = ErrorTable
-    // default we don't handle this exception yet.
-
-    throw ErrorTable.newError(ErrorTable.SERVER_ERROR, reqID, bucket, hostName);
+    // Default we don't handle this exception yet,
+    // report a Server Internal Error.
+    if (exp == null) {
+      exp =
+          ErrorTable.newError(ErrorTable.SERVER_ERROR, reqID, bucket, hostName);
+      if (fsExp != null) {
+        exp.setMessage(fsExp.getMessage());
+      }
+    }
+    throw exp;

INS60 INS25 INS43 INS59 INS27 INS8 INS42 INS42 INS42 INS33 INS21 INS21 INS21 INS42 INS33 INS21 INS25 INS7 INS7 INS7 INS7 INS27 INS8 INS42 MOV32 INS42 MOV32 INS42 MOV32 INS42 MOV32 INS42 INS33 INS21 INS32 INS42 INS42 INS32 INS42 INS42 DEL53 DEL53 DEL53