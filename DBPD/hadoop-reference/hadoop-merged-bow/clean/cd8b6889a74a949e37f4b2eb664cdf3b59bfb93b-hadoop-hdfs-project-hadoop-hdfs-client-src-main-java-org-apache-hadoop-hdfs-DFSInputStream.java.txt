HDFS-9579. Provide bytes-read-by-network-distance metrics at FileSystem.Statistics level (Ming Ma via sjlee)

-      } else if (blockReader.isLocal()) {
+      } else if (blockReader.getNetworkDistance() == 0) {
+      dfsClient.updateFileSystemReadStats(blockReader.getNetworkDistance(),
+          nRead);
+        dfsClient.updateFileSystemReadStats(blockReader.getNetworkDistance(),
+            ret);
-          if (dfsClient.stats != null) {
-            dfsClient.stats.incrementBytesRead(result);
-          }
+        dfsClient.updateFileSystemReadStats(
+            reader.getNetworkDistance(), nread);
-    if (dfsClient.stats != null) {
-      dfsClient.stats.incrementBytesRead(realLen);
-    }

INS21 INS32 INS42 INS42 INS32 INS42 INS21 INS27 INS42 INS42 INS32 INS21 INS32 INS34 INS42 INS42 INS32 INS42 INS32 MOV42 UPD42 MOV42 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 DEL32 DEL40 DEL33 DEL27 DEL40 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25 DEL40 DEL33 DEL27 DEL40 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25