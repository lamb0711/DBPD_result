HDDS-419. ChunkInputStream bulk read api does not read from all the chunks. Contributed by Lokesh Jain and Mukul Kumar.

-    int available = prepareRead(len);
-    if (available == EOF) {
-      return EOF;
+    int total = 0;
+    while (len > 0) {
+      int available = prepareRead(len);
+      if (available == EOF) {
+        return total != 0 ? total : EOF;
+      }
+      buffers.get(bufferIndex).get(b, off + total, available);
+      len -= available;
+      total += available;
-    buffers.get(bufferIndex).get(b, off, available);
-    return available;
+    return total;
+    final ChunkInfo chunkInfo = chunks.get(chunkIndex);
-      readChunkResponse = ContainerProtocolCalls.readChunk(xceiverClient,
-          chunks.get(chunkIndex), blockID, traceID);
+      readChunkResponse = ContainerProtocolCalls
+          .readChunk(xceiverClient, chunkInfo, blockID, traceID);
+    if (byteString.size() != chunkInfo.getLen()) {
+      // Bytes read from chunk should be equal to chunk size.
+      throw new IOException(String
+          .format("Inconsistent read for chunk=%s len=%d bytesRead=%d",
+              chunkInfo.getChunkName(), chunkInfo.getLen(), byteString.size()));
+    }
+
+  public BlockID getBlockID() {
+    return blockID;
+  }

INS31 INS83 INS43 INS42 INS8 INS60 INS61 INS41 INS60 INS25 INS42 INS41 INS39 INS59 INS27 INS8 INS42 INS83 INS43 INS59 INS27 INS8 INS42 INS42 INS34 INS42 INS34 MOV60 MOV25 MOV21 INS21 INS21 INS42 INS42 MOV32 INS32 INS32 INS53 INS7 INS7 INS42 INS42 INS42 INS42 INS14 INS27 INS42 INS42 INS42 INS42 INS43 INS32 INS16 INS42 INS42 INS42 INS42 INS42 INS42 INS45 INS32 INS32 INS32 INS27 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS34 DEL42 DEL42 DEL42 DEL41