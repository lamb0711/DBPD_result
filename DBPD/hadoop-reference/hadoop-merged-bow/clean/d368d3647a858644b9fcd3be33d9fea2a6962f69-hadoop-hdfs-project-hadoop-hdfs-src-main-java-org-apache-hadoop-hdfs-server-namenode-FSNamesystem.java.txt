HDFS-7957. Truncate should verify quota before making changes. Contributed by Jing Zhao.

-    boolean onBlockBoundary = dir.truncate(iip, newLength,
-        toRemoveBlocks, mtime);
+    final QuotaCounts delta = new QuotaCounts.Builder().build();
+    boolean onBlockBoundary = dir.truncate(iip, newLength, toRemoveBlocks,
+        mtime, delta);
-    if(! onBlockBoundary) {
+    if(!onBlockBoundary) {
+
+    // update the quota: use the preferred block size for UC block
+    dir.writeLock();
+    try {
+      dir.updateCountNoQuotaCheck(iip, iip.length() - 1, delta);
+    } finally {
+      dir.writeUnlock();
+    }
+
-    if(shouldRecoverNow)
+    if (shouldRecoverNow) {
+    }
-    // update the quota: use the preferred block size for UC block
-    final long diff =
-        file.getPreferredBlockSize() - truncatedBlockUC.getNumBytes();
-    dir.updateSpaceConsumed(iip, 0, diff, file.getBlockReplication());

INS60 INS21 INS54 INS83 INS43 INS59 INS32 INS8 INS8 INS8 INS42 INS42 INS32 INS42 INS42 INS21 INS21 MOV21 INS14 INS42 INS42 INS32 INS32 INS43 INS42 INS42 INS42 INS27 INS42 INS42 INS42 INS40 INS32 INS34 INS42 INS42 DEL83 DEL39 DEL42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL27 DEL59 DEL60 DEL42 DEL42 DEL42 DEL34 DEL42 DEL42 DEL42 DEL32 DEL32 DEL21