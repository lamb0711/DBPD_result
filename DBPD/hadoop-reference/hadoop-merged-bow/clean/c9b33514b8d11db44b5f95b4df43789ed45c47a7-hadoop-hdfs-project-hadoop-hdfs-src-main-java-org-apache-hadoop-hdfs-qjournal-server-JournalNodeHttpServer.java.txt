HDFS-13462. Add BIND_HOST configuration for JournalNode's HTTP and RPC Servers. Contributed by Lukas Majercak.

+import org.apache.hadoop.http.HttpConfig;
+  private InetSocketAddress httpAddress;
+  private InetSocketAddress httpsAddress;
+  private final InetSocketAddress bindAddress;
+
-  JournalNodeHttpServer(Configuration conf, JournalNode jn) {
+  JournalNodeHttpServer(Configuration conf, JournalNode jn,
+      InetSocketAddress bindAddress) {
+    this.bindAddress = bindAddress;
-    final InetSocketAddress httpAddr = getAddress(conf);
+    final InetSocketAddress httpAddr = bindAddress;
+    if (httpsAddr != null) {
+      // If DFS_JOURNALNODE_HTTPS_BIND_HOST_KEY exists then it overrides the
+      // host name portion of DFS_NAMENODE_HTTPS_ADDRESS_KEY.
+      final String bindHost =
+          conf.getTrimmed(DFSConfigKeys.DFS_JOURNALNODE_HTTPS_BIND_HOST_KEY);
+      if (bindHost != null && !bindHost.isEmpty()) {
+        httpsAddr = new InetSocketAddress(bindHost, httpsAddr.getPort());
+      }
+    }
+
+
+    HttpConfig.Policy policy = DFSUtil.getHttpPolicy(conf);
+    int connIdx = 0;
+    if (policy.isHttpEnabled()) {
+      httpAddress = httpServer.getConnectorAddress(connIdx++);
+      conf.set(DFSConfigKeys.DFS_JOURNALNODE_HTTP_ADDRESS_KEY,
+          NetUtils.getHostPortString(httpAddress));
+    }
+
+    if (policy.isHttpsEnabled()) {
+      httpsAddress = httpServer.getConnectorAddress(connIdx);
+      conf.set(DFSConfigKeys.DFS_JOURNALNODE_HTTPS_ADDRESS_KEY,
+          NetUtils.getHostPortString(httpsAddress));
+    }
+
+  /**
+   * Return the actual HTTP/HTTPS address bound to by the running server.
+   */
+  public InetSocketAddress getAddress() {
+    assert httpAddress != null || httpsAddress != null;
+    return httpAddress != null ? httpAddress : httpsAddress;
+  }
-  @Deprecated
-  public InetSocketAddress getAddress() {
-    InetSocketAddress addr = httpServer.getConnectorAddress(0);
-    assert addr.getPort() != 0;
-    return addr;
+  public InetSocketAddress getHttpAddress() {
+    return httpAddress;
+  }
+
+  /**
+   * Return the actual address bound to by the running server.
+   */
+  public InetSocketAddress getHttpsAddress() {
+    return httpsAddress;
-  private static InetSocketAddress getAddress(Configuration conf) {
-    String addr = conf.get(DFSConfigKeys.DFS_JOURNALNODE_HTTP_ADDRESS_KEY,
-        DFSConfigKeys.DFS_JOURNALNODE_HTTP_ADDRESS_DEFAULT);
-    return NetUtils.createSocketAddr(addr,
-        DFSConfigKeys.DFS_JOURNALNODE_HTTP_PORT_DEFAULT,
-        DFSConfigKeys.DFS_JOURNALNODE_HTTP_ADDRESS_KEY);
-  }
-

INS26 INS40 INS23 INS23 INS23 INS31 INS31 INS31 INS83 INS43 INS59 INS83 MOV43 INS59 INS83 INS83 MOV43 INS59 INS44 INS29 INS83 MOV43 INS42 INS8 MOV29 UPD83 MOV83 INS43 INS42 INS8 INS29 INS83 INS43 INS42 INS8 MOV42 UPD42 MOV42 INS42 INS42 INS43 INS42 INS21 INS60 INS25 INS60 INS60 INS25 INS25 INS65 MOV6 MOV41 INS42 INS41 INS65 UPD42 MOV42 MOV41 INS42 INS7 MOV83 INS43 INS59 INS27 INS8 INS43 INS59 INS39 INS59 INS32 INS8 INS32 INS8 INS66 INS27 INS16 INS42 INS66 INS42 INS22 INS42 INS42 MOV42 INS42 INS42 INS33 INS60 INS25 INS40 INS42 MOV32 INS42 INS34 INS42 INS42 INS21 INS21 INS42 INS42 INS21 INS21 INS27 INS27 INS27 INS42 INS42 INS52 INS42 INS83 MOV43 INS59 INS27 INS8 INS42 UPD42 INS7 INS32 INS7 INS32 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS42 INS32 INS27 INS38 INS21 INS42 INS32 INS42 INS42 INS40 INS32 INS42 INS32 INS42 INS42 INS40 INS32 INS42 INS42 INS40 INS42 INS33 INS32 INS7 INS42 INS42 INS37 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS14 INS42 MOV43 INS42 INS32 INS42 INS42 INS42 DEL59 DEL60 DEL42 DEL42 DEL32 DEL34 DEL27 DEL42 DEL42 DEL42 DEL42 DEL40 DEL40 DEL32 DEL42 DEL78 DEL83 DEL42 DEL42 DEL42 DEL34 DEL32 DEL59 DEL60 DEL8 DEL31 DEL83 DEL42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL42 DEL40 DEL40 DEL32 DEL59 DEL60 DEL8 DEL31