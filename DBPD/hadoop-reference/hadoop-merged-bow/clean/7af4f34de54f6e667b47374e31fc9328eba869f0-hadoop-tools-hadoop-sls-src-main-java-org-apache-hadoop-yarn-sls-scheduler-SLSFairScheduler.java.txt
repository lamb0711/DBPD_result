YARN-7929. Support to set container execution type in SLS. (Jiandan Yang via Weiwei Yang)

-      schedulerMetrics.tearDown();
+      if (metricsON) {
+        schedulerMetrics.tearDown();
+      }

INS8 INS25 INS42 MOV8