HDFS-8999. Allow a file to be closed with COMMITTED but not yet COMPLETE blocks.

+import org.apache.hadoop.hdfs.protocol.ErasureCodingPolicy;
-import org.apache.hadoop.hdfs.protocol.ErasureCodingPolicy;
+import org.apache.hadoop.ipc.RetriableException;
+import org.apache.htrace.core.Tracer;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
-import org.apache.htrace.core.Tracer;
-
-import org.slf4j.Logger;
-import org.slf4j.LoggerFactory;
+  /**
+   * Invoke namenode append RPC.
+   * It retries in case of {@link BlockNotYetCompleteException}.
+   */
+  private LastBlockWithStatus callAppend(String src,
+      EnumSetWritable<CreateFlag> flag) throws IOException {
+    final long startTime = Time.monotonicNow();
+    for(;;) {
+      try {
+        return namenode.append(src, clientName, flag);
+      } catch(RemoteException re) {
+        if (Time.monotonicNow() - startTime > 5000
+            || !RetriableException.class.getName().equals(
+                re.getClassName())) {
+          throw re;
+        }
+
+        try { // sleep and retry
+          Thread.sleep(500);
+        } catch (InterruptedException e) {
+          throw DFSUtilClient.toInterruptedIOException("callAppend", e);
+        }
+      }
+    }
+  }
+
-      LastBlockWithStatus blkWithStatus = namenode.append(src, clientName,
+      final LastBlockWithStatus blkWithStatus = callAppend(src,
-        DFSClient.LOG.debug("NameNode is on an older version, request file " +
-            "info with additional RPC call for file: " + src);
+        LOG.debug("NameNode is on an older version, request file " +
+            "info with additional RPC call for file: {}", src);

MOV26 MOV26 MOV26 MOV26 INS26 INS40 INS31 INS29 INS83 INS43 INS42 INS44 INS44 INS43 INS8 INS65 INS42 INS43 INS42 INS74 INS42 INS42 INS60 INS24 INS66 INS66 INS65 INS66 INS42 INS43 INS43 INS83 INS39 INS59 INS8 INS42 INS42 INS42 INS42 INS32 INS54 INS42 INS42 INS8 INS12 INS83 INS41 INS44 INS8 INS32 INS43 INS42 INS25 INS54 UPD42 INS42 INS42 INS42 INS42 INS42 INS42 INS27 INS8 INS8 INS12 INS42 INS27 INS42 INS27 INS38 INS53 INS21 INS44 INS8 MOV45 UPD45 MOV45 INS27 INS34 INS32 INS42 INS32 INS43 INS42 INS53 INS32 INS42 INS32 INS42 INS32 INS42 INS42 INS34 INS42 INS32 INS42 INS42 INS57 INS42 INS42 INS42 INS42 INS42 INS45 INS42 INS43 INS42 DEL42 DEL42 DEL40 DEL42 DEL27