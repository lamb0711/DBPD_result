HADOOP-14914. Change to a safely casting long to int. Contributed by Ajay Kumar.

-      int remainingLen = (int) requestedLen;
+      int remainingLen = Math.toIntExact(requestedLen);

INS32 INS42 INS42 INS42 DEL39 DEL42 DEL11