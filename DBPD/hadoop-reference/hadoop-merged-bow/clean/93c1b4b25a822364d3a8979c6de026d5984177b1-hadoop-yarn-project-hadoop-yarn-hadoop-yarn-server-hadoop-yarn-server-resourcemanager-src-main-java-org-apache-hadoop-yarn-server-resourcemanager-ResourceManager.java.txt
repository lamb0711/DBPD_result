Merge r1414455:r1426018 from trunk

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1432246 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.hadoop.util.ExitUtil;
+import org.apache.hadoop.yarn.server.resourcemanager.recovery.NullRMStateStore;
-import org.apache.hadoop.yarn.server.resourcemanager.recovery.StoreFactory;
+import org.apache.hadoop.yarn.server.resourcemanager.recovery.RMStateStoreFactory;
+import com.google.common.annotations.VisibleForTesting;
+
-  private final RMStateStore store;
+  private boolean recoveryEnabled;
-
-  public ResourceManager(RMStateStore store) {
+  
+  public ResourceManager() {
-    this.store = store;
+    boolean isRecoveryEnabled = conf.getBoolean(
+        YarnConfiguration.RECOVERY_ENABLED,
+        YarnConfiguration.DEFAULT_RM_RECOVERY_ENABLED);
+    
+    RMStateStore rmStore = null;
+    if(isRecoveryEnabled) {
+      recoveryEnabled = true;
+      rmStore =  RMStateStoreFactory.getStore(conf);
+    } else {
+      recoveryEnabled = false;
+      rmStore = new NullRMStateStore();
+    }
+    try {
+      rmStore.init(conf);
+      rmStore.setDispatcher(rmDispatcher);
+    } catch (Exception e) {
+      // the Exception from stateStore.init() needs to be handled for 
+      // HA and we need to give up master status if we got fenced
+      LOG.error("Failed to init state store", e);
+      ExitUtil.terminate(1, e);
+    }
+    
-        new RMContextImpl(this.rmDispatcher,
+        new RMContextImpl(this.rmDispatcher, rmStore,
-
+    
-
+    
+  
+  @VisibleForTesting
+  protected void setRMStateStore(RMStateStore rmStore) {
+    rmStore.setDispatcher(rmDispatcher);
+    ((RMContextImpl) rmContext).setStateStore(rmStore);
+  }
+    if(recoveryEnabled) {
+      try {
+        RMStateStore rmStore = rmContext.getStateStore();
+        RMState state = rmStore.loadState();
+        recover(state);
+      } catch (Exception e) {
+        // the Exception from loadState() needs to be handled for 
+        // HA and we need to give up master status if we got fenced
+        LOG.error("Failed to load/recover state", e);
+        ExitUtil.terminate(1, e);
+      }
+    }
+
+    RMStateStore store = rmContext.getStateStore();
+    try {
+      store.close();
+    } catch (Exception e) {
+      LOG.error("Error closing store.", e);
+    }
+      
+    // recover applications
+    rmAppManager.recover(state);
-      RMStateStore store =  StoreFactory.getStore(conf);
-      ResourceManager resourceManager = new ResourceManager(store);
+      ResourceManager resourceManager = new ResourceManager();
-      //resourceManager.recover(store.restore());

INS26 INS26 INS26 MOV23 INS40 INS40 UPD40 INS40 INS31 INS31 INS39 MOV83 MOV42 INS8 INS78 INS83 INS39 INS42 MOV44 MOV8 UPD42 MOV46 INS60 INS60 INS25 INS54 INS42 MOV43 UPD42 INS21 INS25 INS60 INS54 INS21 INS39 INS59 MOV43 INS59 INS42 INS8 INS8 INS8 INS12 INS32 INS32 INS42 INS8 MOV43 INS59 INS8 INS12 INS32 INS42 INS32 INS42 INS33 INS21 INS21 INS21 INS21 INS21 INS21 INS44 INS8 INS42 INS42 INS42 INS36 INS42 INS42 INS54 INS42 INS32 INS21 INS44 INS8 INS42 INS42 INS42 INS42 INS42 INS40 INS40 INS7 INS7 INS7 INS7 INS32 INS32 INS43 INS42 INS21 INS21 INS42 INS11 INS8 INS12 INS42 INS42 INS32 INS43 INS42 INS21 MOV43 INS42 INS9 INS42 INS32 INS42 INS9 INS42 INS14 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS32 INS43 INS42 INS60 INS60 INS21 INS44 INS8 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS43 INS42 INS42 INS45 INS42 INS42 INS42 INS34 INS42 INS42 INS43 INS59 INS43 INS59 INS32 INS43 INS42 INS21 INS21 INS42 INS42 INS45 INS42 MOV43 INS42 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 INS32 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS45 INS42 INS42 INS42 INS34 INS42 DEL83 DEL31 DEL52 DEL42 DEL22 DEL42 DEL7 DEL42 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42