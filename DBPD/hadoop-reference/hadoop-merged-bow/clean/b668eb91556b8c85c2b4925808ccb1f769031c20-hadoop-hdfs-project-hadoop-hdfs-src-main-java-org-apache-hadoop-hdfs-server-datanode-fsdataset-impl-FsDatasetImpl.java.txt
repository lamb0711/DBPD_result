HDFS-10675. Datanode support to read from external stores.

+import org.apache.hadoop.hdfs.server.datanode.fsdataset.FsVolumeSpi.ScanInfo;
+        //skip blocks in PROVIDED storage
+        if (b.getVolume().getStorageType() == StorageType.PROVIDED) {
+          continue;
+        }
-
-  public void checkAndUpdate(String bpid, long blockId, File diskFile,
-      File diskMetaFile, FsVolumeSpi vol) throws IOException {
+  public void checkAndUpdate(String bpid, ScanInfo scanInfo)
+      throws IOException {
+
+    long blockId = scanInfo.getBlockId();
+    File diskFile = scanInfo.getBlockFile();
+    File diskMetaFile = scanInfo.getMetaFile();
+    FsVolumeSpi vol = scanInfo.getVolume();
+
-      if (memBlockInfo != null && memBlockInfo.getState() != ReplicaState.FINALIZED) {
+      if (memBlockInfo != null &&
+          memBlockInfo.getState() != ReplicaState.FINALIZED) {
+      if (vol.getStorageType() == StorageType.PROVIDED) {
+        if (memBlockInfo == null) {
+          //replica exists on provided store but not in memory
+          ReplicaInfo diskBlockInfo =
+              new ReplicaBuilder(ReplicaState.FINALIZED)
+              .setFileRegion(scanInfo.getFileRegion())
+              .setFsVolume(vol)
+              .setConf(conf)
+              .build();
+
+          volumeMap.add(bpid, diskBlockInfo);
+          LOG.warn("Added missing block to memory " + diskBlockInfo);
+        } else {
+          //replica exists in memory but not in the provided store
+          volumeMap.remove(bpid, blockId);
+          LOG.warn("Deleting missing provided block " + memBlockInfo);
+        }
+        return;
+      }
+
-

INS26 INS40 INS43 INS42 INS60 INS60 INS60 INS60 INS42 INS39 INS59 MOV43 INS59 MOV43 INS59 MOV43 INS59 INS42 INS32 INS42 INS32 INS42 INS32 INS42 INS32 INS25 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS27 INS8 INS25 INS32 INS40 INS25 INS41 INS27 INS8 INS42 INS42 INS27 INS8 INS8 INS32 INS40 INS18 INS42 INS33 INS60 INS21 INS21 INS21 INS21 INS32 INS42 INS43 INS59 INS32 INS32 INS32 INS32 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS27 INS42 INS42 INS42 INS42 INS42 INS42 INS27 INS32 INS42 INS45 INS42 INS45 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS14 INS42 INS32 INS43 INS40 INS42 INS42 INS42 DEL39 DEL42 DEL42 DEL44 DEL42 DEL44 DEL42 DEL44