HDFS-2864. Remove some redundant methods and the constant METADATA_VERSION from FSDataset.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1238969 13f79535-47bb-0310-9956-ffa450edef68

-    nullCrcFileData[0] = (byte) ((FSDataset.METADATA_VERSION >>> 8) & 0xff);
-    nullCrcFileData[1] = (byte) (FSDataset.METADATA_VERSION & 0xff);
+    nullCrcFileData[0] = (byte) ((BlockMetadataHeader.VERSION >>> 8) & 0xff);
+    nullCrcFileData[1] = (byte) (BlockMetadataHeader.VERSION & 0xff);
-      int numInjectedBlocks = 0;
-        numInjectedBlocks++;
+  private BInfo getBInfo(final ExtendedBlock b) {
+    final Map<Block, BInfo> map = blockMap.get(b.getBlockPoolId());
+    return map == null? null: map.get(b.getLocalBlock());
+  }
+
+  @Override // {@link FSDatasetInterface}
+  public boolean contains(ExtendedBlock block) {
+    return getBInfo(block) != null;
+  }
+
-    final Map<Block, BInfo> map = blockMap.get(b.getBlockPoolId());
-    if (map == null) {
-      return false;
-    }
-    BInfo binfo = map.get(b.getLocalBlock());
-    if (binfo == null) {
-      return false;
-    }
-    return binfo.isFinalized();
+    final BInfo binfo = getBInfo(b);
+    return binfo != null && binfo.isFinalized();
-    final Map<Block, BInfo> map = blockMap.get(b.getBlockPoolId());
-    if (map == null) {
-      return false;
-    }
-    BInfo binfo = map.get(b.getLocalBlock());
-    if (binfo == null) {
-      return false;
-    }
-    return !binfo.isFinalized();  
+    final BInfo binfo = getBInfo(b);
+    return binfo != null && !binfo.isFinalized();  

INS31 INS31 INS31 UPD83 MOV43 INS42 INS44 INS78 MOV83 INS39 INS42 INS44 INS8 MOV78 INS83 MOV83 MOV39 UPD42 MOV42 MOV44 INS8 MOV78 INS83 INS83 INS39 INS42 MOV44 INS8 INS83 INS43 INS42 MOV41 INS42 INS43 INS42 INS41 MOV60 MOV41 MOV60 MOV41 INS42 INS16 INS42 INS27 INS43 INS27 INS83 INS27 MOV27 INS33 MOV32 INS32 INS33 MOV42 UPD42 INS27 MOV32 INS27 MOV38 INS42 INS42 UPD42 MOV42 INS42 INS33 UPD42 MOV42 INS42 INS33 UPD40 UPD40 DEL39 DEL42 DEL34 DEL59 DEL60 DEL42 DEL37 DEL21 DEL83 DEL39 DEL42 DEL9 DEL8 DEL25 DEL42 DEL59 DEL60 DEL42 DEL33 DEL27 DEL9 DEL41 DEL8 DEL25 DEL42 DEL43 DEL42 DEL43 DEL43 DEL74 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL33 DEL27 DEL9 DEL41 DEL8 DEL25 DEL42 DEL33 DEL27 DEL9 DEL41 DEL8 DEL25 DEL8 DEL31