YARN-6736. Consider writing to both ats v1 & v2 from RM for smoother upgrades. Contributed by Aaron Gresch.

-  private TimelineV2Client timelineV2Client;
+  @VisibleForTesting
+  TimelineV2Client timelineV2Client;
-    if (YarnConfiguration.timelineServiceEnabled(conf)) {
-      timelineServiceV2Enabled =
-          ((int) YarnConfiguration.getTimelineServiceVersion(conf) == 2);
-      timelineServiceV1Enabled = !timelineServiceV2Enabled;
-    } else {
+    if (!YarnConfiguration.timelineServiceEnabled(conf)) {
-    }
-
-    if (timelineServiceV2Enabled) {
-    } else if (timelineServiceV1Enabled) {
+    }
+
+    if (timelineServiceV1Enabled) {
+            timelineServiceV1Enabled =
+                YarnConfiguration.timelineServiceV1Enabled(conf);
+            timelineServiceV2Enabled =
+                YarnConfiguration.timelineServiceV2Enabled(conf);
+            if (timelineServiceV1Enabled) {
+              timelineClient = TimelineClient.createTimelineClient();
+              timelineClient.init(conf);
+              timelineClient.start();
+              LOG.info("Timeline service V1 client is enabled");
+            }
-            } else {
-              timelineClient = TimelineClient.createTimelineClient();
-              timelineClient.init(conf);
-              timelineClient.start();
-              LOG.info("Timeline service V1 client is enabled");
+    if (timelineServiceV1Enabled) {
+      publishApplicationAttemptEvent(timelineClient, appAttemptID.toString(),
+          DSEvent.DS_APP_ATTEMPT_END, domainId, appSubmitterUgi);
+    }
+
-    } else if (timelineServiceV1Enabled) {
-      publishApplicationAttemptEvent(timelineClient, appAttemptID.toString(),
-          DSEvent.DS_APP_ATTEMPT_END, domainId, appSubmitterUgi);
-    } else if (timelineServiceV2Enabled) {
+    }
+    if (timelineServiceV2Enabled) {
-        } else if (timelineServiceV1Enabled) {
+        }
+        if (timelineServiceV1Enabled) {
-      } else if (applicationMaster.timelineServiceV1Enabled) {
+      }
+      if (applicationMaster.timelineServiceV1Enabled) {

MOV31 INS78 MOV78 INS39 INS42 MOV44 MOV43 MOV43 MOV43 INS8 INS42 MOV25 INS54 MOV25 MOV25 INS38 MOV8 MOV12 MOV25 MOV32 MOV21 MOV21 MOV25 UPD42 INS42 INS14 MOV74 INS1 INS31 MOV78 INS83 MOV43 INS42 MOV43 MOV8 MOV41 MOV32 INS8 MOV8 INS21 INS21 INS25 INS25 INS21 INS7 INS7 INS42 MOV8 MOV42 MOV8 INS32 INS42 INS32 INS42 INS32 INS42 MOV42 MOV45 INS42 INS42 INS42 INS42 INS42 INS42 DEL83 DEL42 DEL39 DEL42 DEL42 DEL42 DEL32 DEL11 DEL34 DEL27 DEL36 DEL7 DEL21 DEL42 DEL42 DEL38 DEL7 DEL21 DEL8 DEL42 DEL8 DEL25 DEL25 DEL83 DEL42 DEL39 DEL42 DEL42 DEL42 DEL1 DEL14 DEL32 DEL21 DEL8 DEL54 DEL8 DEL31