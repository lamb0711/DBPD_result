Revert "YARN-9606. Set sslfactory for AuthenticatedURL() while creating LogsCLI#webServiceClient."

This reverts commit 7836bc4c3533e93e7adc0c7da0659bc04bdf2494.

+import com.sun.jersey.client.urlconnection.HttpURLConnectionFactory;
+import com.sun.jersey.client.urlconnection.URLConnectionClientHandler;
+import java.net.HttpURLConnection;
+import java.net.URL;
+import org.apache.hadoop.security.authentication.client.AuthenticatedURL;
+import org.apache.hadoop.security.authentication.client.AuthenticationException;
-import org.apache.hadoop.yarn.server.webapp.WebServiceClient;
-  private Client client = null;
+  private Client webServiceClient = null;
-      client = WebServiceClient.getWebServiceClient().createClient();
+      webServiceClient = new Client(new URLConnectionClientHandler(
+          new HttpURLConnectionFactory() {
+          @Override
+          public HttpURLConnection getHttpURLConnection(URL url)
+              throws IOException {
+            AuthenticatedURL.Token token = new AuthenticatedURL.Token();
+            HttpURLConnection conn = null;
+            try {
+              conn = new AuthenticatedURL().openConnection(url, token);
+            } catch (AuthenticationException e) {
+              throw new IOException(e);
+            }
+            return conn;
+          }
+        }));
-      if (client != null) {
-        client.destroy();
+      if (webServiceClient != null) {
+        webServiceClient.destroy();
-    client.addFilter(retryFilter);
+    webServiceClient.addFilter(retryFilter);
-    WebServiceClient.initialize(conf);
-    WebServiceClient.destroy();
-      Builder builder = client.resource(webAppAddress)
+      Builder builder = webServiceClient.resource(webAppAddress)
-    WebResource webResource = client.resource(webAppAddress);
+    WebResource webResource = webServiceClient.resource(webAppAddress);
-      WebResource webResource = client
+      WebResource webResource = webServiceClient
-              client, request, logFile);
+              webServiceClient, request, logFile);
-    WebResource webResource = client.resource(webAppAddress);
+    WebResource webResource = webServiceClient.resource(webAppAddress);

MOV26 INS26 INS26 INS26 INS26 INS26 INS40 INS40 INS40 INS40 UPD40 INS40 UPD42 UPD42 UPD42 UPD42 INS42 INS14 UPD42 INS43 INS14 UPD42 INS42 INS43 INS14 UPD42 INS42 INS43 INS1 INS42 INS31 INS78 INS83 INS43 INS42 INS44 INS43 INS8 UPD42 INS42 INS42 INS43 INS42 INS42 INS60 INS60 INS54 INS41 INS42 INS43 INS59 INS43 INS59 INS8 INS12 INS42 INS40 INS42 INS14 INS42 INS42 INS33 INS21 INS44 INS8 UPD42 INS43 INS7 INS43 INS42 INS53 INS40 INS42 INS32 INS42 INS14 INS14 UPD42 MOV42 INS42 INS42 INS43 INS42 INS43 INS42 INS42 DEL42 DEL42 DEL32 DEL42 DEL32 DEL42 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL21