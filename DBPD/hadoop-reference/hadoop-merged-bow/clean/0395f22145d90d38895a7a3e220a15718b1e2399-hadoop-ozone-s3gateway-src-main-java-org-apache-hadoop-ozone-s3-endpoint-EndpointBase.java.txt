HDDS-1068. Improve the error propagation for ozone sh.
Contributed by Elek, Marton.

+import org.apache.hadoop.ozone.om.exceptions.OMException;
+import org.apache.hadoop.ozone.om.exceptions.OMException.ResultCodes;
-    } catch (IOException ex) {
-      LOG.error("Error occurred is {}", ex);
-      if (ex.getMessage().contains("NOT_FOUND")) {
-        OS3Exception oex =
-            S3ErrorTable.newError(S3ErrorTable.NO_SUCH_BUCKET, bucketName);
-        throw oex;
+    } catch (OMException ex) {
+      if (ex.getResult() == ResultCodes.KEY_NOT_FOUND) {
+        throw S3ErrorTable.newError(S3ErrorTable.NO_SUCH_BUCKET, bucketName);
-    } catch (IOException ex) {
-      LOG.error("Error occurred is {}", ex);
-      if (ex.getMessage().contains("NOT_FOUND")) {
-        OS3Exception oex =
-            S3ErrorTable.newError(S3ErrorTable.NO_SUCH_BUCKET, bucketName);
-        throw oex;
+    } catch (OMException ex) {
+      if (ex.getResult() == ResultCodes.BUCKET_NOT_FOUND
+          || ex.getResult() == ResultCodes.S3_BUCKET_NOT_FOUND) {
+        throw S3ErrorTable.newError(S3ErrorTable.NO_SUCH_BUCKET, bucketName);
-    } catch (Exception ex) {
-      if (ex.getMessage().contains("NOT_FOUND")) {
+    } catch (OMException ex) {
+      if (ex.getResult() == ResultCodes.VOLUME_NOT_FOUND) {
-    } catch (IOException ex) {
-      LOG.error("createS3Bucket error:", ex);
-      if (!ex.getMessage().contains("ALREADY_EXISTS")) {
+    } catch (OMException ex) {
+      if (ex.getResult() != ResultCodes.VOLUME_ALREADY_EXISTS) {

INS26 INS26 INS40 INS40 UPD43 UPD43 UPD43 UPD43 UPD42 INS27 UPD42 INS27 UPD42 INS27 UPD42 INS27 INS32 INS40 INS27 INS27 INS32 INS40 INS32 INS40 MOV42 UPD42 MOV42 MOV32 INS32 INS40 INS32 INS40 MOV32 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL32 DEL42 DEL45 DEL32 DEL42 DEL43 DEL42 DEL59 DEL60 DEL42 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL32 DEL45 DEL32 DEL42 DEL43 DEL42 DEL59 DEL60 DEL42 DEL32 DEL42 DEL45 DEL32 DEL42 DEL42 DEL45 DEL42 DEL32 DEL21 DEL32 DEL42 DEL45 DEL32 DEL38