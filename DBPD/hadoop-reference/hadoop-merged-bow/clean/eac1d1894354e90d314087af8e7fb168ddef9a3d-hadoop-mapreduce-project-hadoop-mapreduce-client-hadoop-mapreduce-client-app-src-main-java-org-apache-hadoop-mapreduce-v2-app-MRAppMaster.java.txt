MAPREDUCE-6418. MRApp should not shutdown LogManager during shutdown. Contributed by Chang Li

-  
+
+  protected void shutdownTaskLog() {
+    TaskLog.syncLogsShutdown(logSyncer);
+  }
+
-    TaskLog.syncLogsShutdown(logSyncer);
+    shutdownTaskLog();
+  protected void shutdownLogManager() {
+    LogManager.shutdown();
+  }
+
-    LogManager.shutdown();
+    shutdownLogManager();

INS31 INS31 UPD83 UPD42 MOV78 INS83 INS39 INS42 INS8 INS83 INS39 INS42 MOV8 INS8 MOV21 INS21 MOV21 INS21 INS32 INS32 INS42 INS42