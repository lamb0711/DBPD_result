Merge r1360400 through r1399945 from trunk.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-2802@1399950 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.hadoop.mapreduce.MRJobConfig;
+import org.apache.hadoop.mapreduce.TaskAttemptID;
-  private WeightSelector selector;
-  private Random rnd;
+  private int taskId;
-      Long rndSeed = config.getRandomSeed();
-      if (rndSeed != null) {
-        rnd = new Random(rndSeed);
-      } else {
-        rnd = new Random();
-      }
-      selector = new WeightSelector(config, rnd);
-
+    if(conf.get(MRJobConfig.TASK_ATTEMPT_ID) != null ) {
+      this.taskId = TaskAttemptID.forName(conf.get(MRJobConfig.TASK_ATTEMPT_ID))
+        .getTaskID().getId();
+    } else {
+      // So that branch-1/0.20 can run this same code as well
+      this.taskId = TaskAttemptID.forName(conf.get("mapred.task.id"))
+          .getTaskID().getId();
+    }
-   * Gets the operation selector to use for this object
-   * 
-   * @return WeightSelector
-   */
-  private WeightSelector getSelector() {
-    return selector;
-  }
-
-  /**
+    //Add taskID to randomSeed to deterministically seed rnd.
+    Random rnd = config.getRandomSeed() != null ?
+      new Random(this.taskId + config.getRandomSeed()) : new Random();
+    WeightSelector selector = new WeightSelector(config, rnd);
-    WeightSelector selector = getSelector();

INS26 INS26 INS40 INS40 INS39 MOV60 UPD42 INS25 INS60 INS8 INS27 INS8 INS8 MOV43 MOV43 INS59 MOV21 MOV21 INS32 INS33 INS21 INS21 UPD42 INS16 INS42 MOV14 INS42 INS42 INS40 INS7 INS7 INS27 INS14 MOV14 INS22 INS32 INS22 INS32 INS32 INS33 MOV43 INS27 INS52 INS42 INS32 INS42 INS52 INS42 INS32 INS42 INS42 UPD42 MOV42 INS22 MOV32 INS32 INS42 INS32 INS42 INS52 INS42 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS40 INS42 INS42 INS45 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL42 DEL43 DEL42 DEL59 DEL60 DEL42 DEL33 DEL27 DEL42 DEL42 DEL14 DEL7 DEL21 DEL8 DEL42 DEL7 DEL21 DEL8 DEL25 DEL42 DEL7 DEL21 DEL8 DEL66 DEL65 DEL66 DEL65 DEL29 DEL83 DEL42 DEL43 DEL42 DEL42 DEL41 DEL8 DEL31 DEL32