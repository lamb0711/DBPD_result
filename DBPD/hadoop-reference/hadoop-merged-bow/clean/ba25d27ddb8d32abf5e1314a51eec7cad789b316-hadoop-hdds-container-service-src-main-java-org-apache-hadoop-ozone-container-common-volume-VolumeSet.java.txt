HDDS-257. Hook up VolumeSet#shutdown from HddsDispatcher#shutdown. Contributed by Hanisha Koneru

-import org.apache.hadoop.hdds.protocol.DatanodeDetails;
+
+import static org.apache.hadoop.util.RunJar.SHUTDOWN_HOOK_PRIORITY;
+
+import org.apache.hadoop.util.ShutdownHookManager;
+  private Runnable shutdownHook;
+
+
+    // Ensure volume threads are stopped and scm df is saved during shutdown.
+    shutdownHook = () -> {
+      shutdown();
+    };
+    ShutdownHookManager.get().addShutdownHook(shutdownHook,
+        SHUTDOWN_HOOK_PRIORITY);
+
+    if (shutdownHook != null) {
+      ShutdownHookManager.get().removeShutdownHook(shutdownHook);
+    }

MOV26 INS26 INS40 UPD40 INS23 INS83 INS43 INS59 INS42 INS42 INS21 INS21 INS25 INS7 INS32 INS27 INS8 INS42 INS86 INS32 INS42 INS42 INS42 INS42 INS33 INS21 INS8 INS42 INS42 INS32 INS21 INS32 INS42 INS42 INS32 INS42 INS42 INS42