HDDS-49. Standalone protocol should use grpc in place of netty.
Contributed by Mukul Kumar Singh.

-import static org.apache.hadoop.hdds.protocol.proto.HddsProtos
-    .ReplicationType.RATIS;
+  private final boolean useGrpc;
+    this.useGrpc = conf.getBoolean(ScmConfigKeys.DFS_CONTAINER_GRPC_ENABLED_KEY,
+        ScmConfigKeys.DFS_CONTAINER_GRPC_ENABLED_DEFAULT);
-            XceiverClientSpi client = pipeline.getType() == RATIS ?
-                    XceiverClientRatis.newXceiverClientRatis(pipeline, conf)
-                    : new XceiverClient(pipeline, conf);
+            XceiverClientSpi client = null;
+            switch (pipeline.getType()) {
+            case RATIS:
+              client = XceiverClientRatis.newXceiverClientRatis(pipeline, conf);
+              break;
+            case STAND_ALONE:
+              client = useGrpc ? new XceiverClientGrpc(pipeline, conf) :
+                  new XceiverClient(pipeline, conf);
+              break;
+            case CHAINED:
+            default:
+              throw new IOException ("not implemented" + pipeline.getType());
+            }

INS23 INS83 INS83 INS39 INS59 INS42 INS21 INS7 INS22 INS32 INS52 INS42 INS42 INS42 INS40 INS40 INS60 INS50 MOV43 INS59 MOV32 INS49 INS21 INS10 INS49 INS21 INS10 INS49 INS49 INS53 MOV42 INS33 INS42 INS7 INS42 INS7 INS42 INS14 INS42 MOV32 INS42 INS16 INS43 INS27 INS42 INS14 MOV14 INS42 INS45 INS32 INS43 INS42 INS42 INS42 INS42 INS42 DEL40 DEL26 DEL42 DEL27 DEL16 DEL59 DEL60