Merge commit '456e901a4c5c639267ee87b8e5f1319f256d20c2' (HDFS-6407. Add sorting and pagination in the datanode tab of the NN Web UI. Contributed by Haohui Mai.) into HDFS-7285-merge

+import org.apache.hadoop.mapreduce.MRJobConfig;
+import org.apache.hadoop.mapreduce.TaskAttemptID;
-      "\t-baseDir <base DFS path. default is /becnhmarks/NNBench. " +
+      "\t-baseDir <base DFS path. default is /benchmarks/NNBench. " +
+      int taskId =
+          TaskAttemptID.forName(conf.get(MRJobConfig.TASK_ATTEMPT_ID))
+              .getTaskID().getId();
+        String filePrefix = "file_" + taskId + "_";
-          doCreateWriteOp("file_" + hostName + "_", reporter);
+          doCreateWriteOp(filePrefix, reporter);
-          doOpenReadOp("file_" + hostName + "_", reporter);
+          doOpenReadOp(filePrefix, reporter);
-          doRenameOp("file_" + hostName + "_", reporter);
+          doRenameOp(filePrefix, reporter);
-          doDeleteOp("file_" + hostName + "_", reporter);
+          doDeleteOp(filePrefix, reporter);

INS26 INS26 INS40 INS40 INS60 MOV25 INS39 INS59 MOV32 INS8 MOV8 UPD45 INS42 INS32 INS60 MOV25 MOV21 MOV21 INS32 INS42 INS43 INS59 MOV32 INS32 UPD42 MOV42 INS42 INS42 INS27 MOV32 INS42 INS42 UPD42 MOV42 UPD42 MOV42 INS32 INS45 INS42 INS45 MOV32 INS25 INS42 INS42 INS42 UPD42 MOV42 INS40 UPD42 INS42 MOV32 MOV8 UPD42 INS42 UPD42 INS42 UPD42 INS42 DEL45 DEL42 DEL45 DEL27 DEL45 DEL42 DEL45 DEL27 DEL45 DEL42 DEL45 DEL27 DEL45 DEL42 DEL45 DEL27 DEL8 DEL25