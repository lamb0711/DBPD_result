HDFS-10342. BlockManager#createLocatedBlocks should not check corrupt replicas if none are corrupt. Contributed by Kuhu Shukla.

+      final boolean noCorrupt = (numCorruptReplicas == 0);
-        final DatanodeDescriptor d = storage.getDatanodeDescriptor();
-        final boolean replicaCorrupt = corruptReplicas.isReplicaCorrupt(blk, d);
-        if ((isCorrupt || (!replicaCorrupt)) &&
-            storage.getState() != State.FAILED) {
-          machines[j++] = storage;
-          // TODO this can be more efficient
-          if (blockIndices != null) {
-            byte index = ((BlockInfoStriped) blk).getStorageBlockIndex(storage);
-            assert index >= 0;
-            blockIndices[i++] = index;
+        if (storage.getState() != State.FAILED) {
+          if (noCorrupt) {
+            machines[j++] = storage;
+            i = setBlockIndices(blk, blockIndices, i, storage);
+          } else {
+            final DatanodeDescriptor d = storage.getDatanodeDescriptor();
+            final boolean replicaCorrupt = isReplicaCorrupt(blk, d);
+            if (isCorrupt || !replicaCorrupt) {
+              machines[j++] = storage;
+              i = setBlockIndices(blk, blockIndices, i, storage);
+            }
+
+  boolean isReplicaCorrupt(BlockInfo blk, DatanodeDescriptor d) {
+    return corruptReplicas.isReplicaCorrupt(blk, d);
+  }
+
+  private int setBlockIndices(BlockInfo blk, byte[] blockIndices, int i,
+                              DatanodeStorageInfo storage) {
+    // TODO this can be more efficient
+    if (blockIndices != null) {
+      byte index = ((BlockInfoStriped)blk).getStorageBlockIndex(storage);
+      assert index >= 0;
+      blockIndices[i++] = index;
+    }
+    return i;
+  }

INS31 INS31 INS39 INS42 INS44 INS44 INS8 INS83 INS39 INS42 INS44 INS44 INS44 MOV44 MOV8 INS43 INS42 INS43 INS42 INS41 INS43 INS42 INS5 INS42 INS39 INS42 INS41 INS42 INS42 MOV32 INS42 INS39 INS85 INS42 INS60 INS83 INS39 INS59 INS44 INS8 INS42 INS36 INS43 INS42 INS25 INS27 INS42 MOV27 INS8 INS42 INS34 INS25 INS42 INS8 INS8 MOV21 INS21 MOV60 MOV60 MOV25 INS7 MOV27 INS8 INS42 INS32 INS32 MOV38 INS21 INS21 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS7 INS7 INS2 INS42 INS42 INS32 INS42 INS37 INS42 INS42 INS42 INS42 INS42 INS42 DEL36 DEL36 DEL27 DEL8