HDFS-5356. MiniDFSCluster should close all open FileSystems when shutdown() (Contributed by Rakesh R)

+import java.util.Set;
+import com.google.common.collect.Sets;
-  
+  private Set<FileSystem> fileSystems = Sets.newHashSet();
+
+    shutdown(deleteDfsDir, true);
+  }
+
+  /**
+   * Shutdown all the nodes in the cluster.
+   */
+  public void shutdown(boolean deleteDfsDir, boolean closeFileSystem) {
+    if (closeFileSystem) {
+      for (FileSystem fs : fileSystems) {
+        try {
+          fs.close();
+        } catch (IOException ioe) {
+          LOG.warn("Exception while closing file system", ioe);
+        }
+      }
+      fileSystems.clear();
+    }
-    return (DistributedFileSystem)FileSystem.get(getURI(nnIndex),
-        nameNodes[nnIndex].conf);
+    DistributedFileSystem dfs = (DistributedFileSystem) FileSystem.get(
+        getURI(nnIndex), nameNodes[nnIndex].conf);
+    fileSystems.add(dfs);
+    return dfs;
-    return FileSystem.newInstance(getURI(nnIndex), nameNodes[nnIndex].conf);
+    FileSystem dfs = FileSystem.newInstance(getURI(nnIndex), nameNodes[nnIndex].conf);
+    fileSystems.add(dfs);
+    return dfs;

INS26 INS26 INS40 INS40 INS23 INS31 INS83 INS74 INS59 INS29 INS83 INS39 INS42 INS44 INS8 INS44 INS43 INS43 INS43 INS43 INS42 INS32 INS65 INS39 INS42 INS21 INS39 INS42 INS25 INS42 INS60 INS21 INS42 INS60 INS21 INS42 INS42 INS42 INS42 INS66 INS32 INS42 INS8 MOV43 INS59 INS32 INS42 MOV43 INS59 INS32 INS42 INS42 INS42 INS9 INS70 INS21 INS42 MOV11 INS42 INS42 INS42 INS42 MOV32 INS42 INS42 INS42 INS44 INS42 INS8 INS32 INS43 INS42 INS54 INS42 INS42 INS42 INS8 INS12 INS21 INS44 INS8 INS32 INS43 INS42 INS21 INS42 INS42 INS42 INS32 INS42 INS42 INS45 INS42