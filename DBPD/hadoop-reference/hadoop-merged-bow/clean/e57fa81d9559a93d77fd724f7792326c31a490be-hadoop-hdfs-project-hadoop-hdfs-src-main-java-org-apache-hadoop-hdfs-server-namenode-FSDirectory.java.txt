HDFS-10980. Optimize check for existence of parent directory. Contributed by Daryn Sharp.

-  void verifyParentDir(INodesInPath iip, String src)
+  void verifyParentDir(INodesInPath iip)
-    Path parent = new Path(src).getParent();
-    if (parent != null) {
+    if (iip.length() > 2) {
-            + parent);
-      } else if (!parentNode.isDirectory() && !parentNode.isSymlink()) {
+            + iip.getParentPath());
+      } else if (!parentNode.isDirectory()) {
-            + parent);
+            + iip.getParentPath());

UPD27 INS32 INS34 INS42 INS42 MOV38 INS32 INS42 INS42 INS32 INS42 INS42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL14 DEL42 DEL32 DEL59 DEL60 DEL42 DEL33 DEL42 DEL42 DEL42 DEL32 DEL38 DEL27 DEL42