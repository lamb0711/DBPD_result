Merge branch 'HDFS-9806' into trunk

+    if (props == null) {
+      throw new IOException(
+          "Properties not found  for storage directory " + sd);
+    }
-        cid = props.getProperty("clusterID");
+        if (props == null) {
+          cid = null;
+        } else {
+          cid = props.getProperty("clusterID");
+        }

INS25 INS27 INS8 INS42 INS33 INS53 INS14 INS43 INS27 INS25 INS42 INS45 INS42 INS27 INS8 INS8 INS42 INS33 INS21 MOV21 INS7 INS42 INS33