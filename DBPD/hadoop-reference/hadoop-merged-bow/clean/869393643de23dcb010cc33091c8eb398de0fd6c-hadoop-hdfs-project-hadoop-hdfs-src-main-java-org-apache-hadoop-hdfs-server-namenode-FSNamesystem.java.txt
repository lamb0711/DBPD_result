HDFS-10745. Directly resolve paths into INodesInPath. Contributed by Daryn Sharp.

-        src = dir.resolvePath(pc, srcArg);
-        final INodesInPath iip = dir.getINodesInPath(src, true);
+        final INodesInPath iip = dir.resolvePath(pc, srcArg);
+        src = iip.getPath();
+
-      src = dir.resolvePath(pc, src);
-      final INodesInPath iip = dir.getINodesInPath4Write(src);
+      final INodesInPath iip = dir.resolvePathForWrite(pc, src);
+      src = iip.getPath();
-      src = dir.resolvePath(pc, src);
+      final INodesInPath iip = dir.resolvePath(pc, src, fileId);
+      src = iip.getPath();
-      final INode inode;
-      if (fileId == HdfsConstants.GRANDFATHER_INODE_ID) {
-        // Older clients may not have given us an inode ID to work with.
-        // In this case, we have to try to resolve the path and hope it
-        // hasn't changed or been deleted since the file was opened for write.
-        inode = dir.getINode(src);
-      } else {
-        inode = dir.getInode(fileId);
-        if (inode != null) src = inode.getFullPathName();
-      }
-      final INodeFile file = checkLease(src, clientName, inode, fileId);
+      final INodeFile file = checkLease(iip, clientName, fileId);
-  INodeFile checkLease(String src, String holder, INode inode, long fileId)
+  INodeFile checkLease(INodesInPath iip, String holder, long fileId)
+    String src = iip.getPath();
+    INode inode = iip.getLastINode();
-      src = dir.resolvePath(pc, src);
-      final INode inode;
-      if (fileId == HdfsConstants.GRANDFATHER_INODE_ID) {
-        // Older clients may not have given us an inode ID to work with.
-        // In this case, we have to try to resolve the path and hope it
-        // hasn't changed or been deleted since the file was opened for write.
-        inode = dir.getINode(src);
-      } else {
-        inode = dir.getInode(fileId);
-        if (inode != null) src = inode.getFullPathName();
-      }
-      final INodeFile pendingFile = checkLease(src, clientName, inode, fileId);
+      INodesInPath iip = dir.resolvePath(pc, src, fileId);
+      src = iip.getPath();
+      final INodeFile pendingFile = checkLease(iip, clientName, fileId);
+    FSPermissionChecker pc = getPermissionChecker();
-      src = FSDirectory.resolvePath(src, dir);
-      final INodesInPath iip = dir.getINodesInPath(src, true);
+      final INodesInPath iip = dir.resolvePath(pc, src);
+      src = iip.getPath();
-        FSPermissionChecker pc = getPermissionChecker();

MOV44 UPD43 UPD42 INS60 INS60 MOV60 UPD42 INS43 INS59 INS43 INS59 INS60 INS21 INS60 INS21 INS42 INS42 INS32 INS42 INS42 INS32 INS60 INS21 INS60 INS83 MOV43 INS59 INS7 MOV83 UPD43 MOV43 INS59 MOV7 INS42 INS42 INS42 INS42 UPD43 MOV43 INS59 MOV7 INS83 MOV43 INS59 INS60 INS21 INS42 INS32 INS42 INS32 UPD42 UPD42 MOV42 INS32 UPD42 UPD42 MOV42 INS32 INS42 MOV32 MOV83 MOV43 INS59 INS7 MOV42 UPD42 MOV42 MOV42 MOV42 UPD42 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 MOV42 UPD42 UPD42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 MOV42 UPD42 UPD42 UPD42 UPD42 UPD42 MOV42 MOV32 INS42 INS32 UPD42 MOV42 UPD42 MOV42 DEL42 DEL7 DEL21 DEL42 DEL9 DEL32 DEL59 DEL60 DEL42 DEL32 DEL7 DEL21 DEL83 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL59 DEL60 DEL42 DEL40 DEL27 DEL42 DEL32 DEL7 DEL21 DEL8 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL42 DEL33 DEL27 DEL21 DEL25 DEL8 DEL25 DEL42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL7 DEL21 DEL83 DEL59 DEL60 DEL42 DEL40 DEL27 DEL42 DEL32 DEL7 DEL21 DEL8 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL42 DEL33 DEL27 DEL21 DEL25 DEL8 DEL25 DEL42 DEL42 DEL42 DEL83 DEL42 DEL42 DEL42 DEL42 DEL9 DEL32 DEL59 DEL60