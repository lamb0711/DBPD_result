YARN-5355: YARN Timeline Service v.2: alpha 2 (varunsaxena)

+import org.apache.hadoop.yarn.util.SystemClock;
+  private long startTime;
-   * Create an event to record task failure
+   * Create an event to record task failure.
+   * @param startTs task start time.
-      TaskAttemptID failedDueToAttempt, Counters counters) {
+      TaskAttemptID failedDueToAttempt, Counters counters, long startTs) {
+    this.startTime = startTs;
+  }
+
+  public TaskFailedEvent(TaskID id, long finishTime, TaskType taskType,
+      String error, String status, TaskAttemptID failedDueToAttempt,
+      Counters counters) {
+    this(id, finishTime, taskType, error, status, failedDueToAttempt, counters,
+        SystemClock.getInstance().getTime());
-	      TaskType taskType, String error, String status,
-	      TaskAttemptID failedDueToAttempt) {
-    this(id, finishTime, taskType, error, status,
-        failedDueToAttempt, EMPTY_COUNTERS);
+      TaskType taskType, String error, String status,
+      TaskAttemptID failedDueToAttempt) {
+    this(id, finishTime, taskType, error, status, failedDueToAttempt,
+        EMPTY_COUNTERS);
-  
+
-  /** Get the task id */
+  /** Gets the task id. */
-  /** Get the error string */
+  /** Gets the error string. */
-  /** Get the finish time of the attempt */
+  /** Gets the finish time of the attempt. */
-  /** Get the task type */
+  /**
+   * Gets the task start time to be reported to ATSv2.
+   * @return task start time.
+   */
+  public long getStartTime() {
+    return startTime;
+  }
+  /** Gets the task type. */
-  /** Get the attempt id due to which the task failed */
+  /** Gets the attempt id due to which the task failed. */
-  /** Get the task status */
+  /**
+   * Gets the task status.
+   * @return task status
+   */
-  /** Get task counters */
+  /** Gets task counters. */
-  /** Get the event type */
+  /** Gets the event type. */

INS26 INS40 INS23 INS31 INS31 INS83 INS39 INS59 INS44 INS83 INS42 INS44 INS44 INS44 INS44 INS44 INS44 INS44 INS8 INS29 INS83 INS39 INS42 INS8 INS42 INS65 INS39 INS42 INS21 INS43 INS42 INS39 INS42 INS43 INS42 INS43 INS42 INS43 INS42 INS43 INS42 INS43 INS42 INS17 INS65 INS65 INS41 INS65 UPD66 INS42 INS66 INS7 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS32 UPD66 UPD66 UPD66 INS66 INS66 INS42 UPD66 UPD66 UPD66 INS66 UPD66 UPD66 INS22 INS42 INS32 INS42 INS52 INS42 INS42 INS42