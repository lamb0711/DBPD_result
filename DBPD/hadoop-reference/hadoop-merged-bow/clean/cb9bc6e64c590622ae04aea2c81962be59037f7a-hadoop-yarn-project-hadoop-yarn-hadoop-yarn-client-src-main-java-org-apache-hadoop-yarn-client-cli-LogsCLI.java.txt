YARN-9581.  Fixed yarn logs cli to access RM2.
            Contributed by Prabhu Joseph

+import com.sun.jersey.api.client.WebResource.Builder;
+      if (webServiceClient != null) {
+        webServiceClient.destroy();
+      }
-      Configuration conf, String appId) throws ClientHandlerException,
+      Configuration conf, String appId) throws Exception {
+    return WebAppUtils.execOnActiveRM(conf, this::getAMContainerInfoFromRM,
+        appId);
+  }
+
+  private List<JSONObject> getAMContainerInfoFromRM(
+      String webAppAddress, String appId) throws ClientHandlerException,
-    String webAppAddress = WebAppUtils.getRMWebAppURLWithScheme(conf);
-
-    WebResource webResource = webServiceClient.resource(webAppAddress);
-
-    ClientResponse response =
-        webResource.path("ws").path("v1").path("cluster").path("apps")
-          .path(appId).path("appattempts").accept(MediaType.APPLICATION_JSON)
-          .get(ClientResponse.class);
-    JSONObject json =
-        response.getEntity(JSONObject.class).getJSONObject("appAttempts");
-    JSONArray requests = json.getJSONArray("appAttempt");
-    for (int i = 0; i < requests.length(); i++) {
-      amContainersList.add(requests.getJSONObject(i));
+    ClientResponse response = null;
+    try {
+      Builder builder = webServiceClient.resource(webAppAddress)
+          .path("ws").path("v1").path("cluster")
+          .path("apps").path(appId).path("appattempts")
+          .accept(MediaType.APPLICATION_JSON);
+      response = builder.get(ClientResponse.class);
+      JSONObject json = response.getEntity(JSONObject.class)
+          .getJSONObject("appAttempts");
+      JSONArray requests = json.getJSONArray("appAttempt");
+      for (int j = 0; j < requests.length(); j++) {
+        amContainersList.add(requests.getJSONObject(j));
+      }
+      return amContainersList;
+    } finally {
+      if (response != null) {
+        response.close();
+      }
-    return amContainersList;

INS26 INS40 INS31 INS83 INS74 INS42 MOV44 INS44 INS43 INS8 UPD83 UPD42 INS44 MOV60 INS43 INS43 INS43 INS42 INS42 INS41 MOV43 INS42 INS54 INS42 INS42 INS42 INS32 MOV43 INS8 INS8 INS25 INS42 INS42 INS42 INS90 INS42 UPD42 INS33 INS60 INS21 MOV60 MOV60 MOV24 MOV41 INS25 INS27 INS8 INS52 INS42 UPD43 MOV43 INS59 INS7 INS27 INS8 INS42 INS33 INS21 UPD42 UPD42 MOV42 INS32 INS42 INS32 UPD42 UPD42 INS42 INS33 INS21 INS32 MOV32 MOV42 MOV40 INS42 MOV42 MOV57 UPD42 INS32 INS42 INS42 INS42 INS42 UPD42 MOV32 DEL59 DEL60 DEL42 DEL32 DEL32 DEL59 DEL60 DEL42 DEL42 DEL42 DEL32 DEL42