YARN-5967. Fix slider core module findbugs warnings. Contributed by Jian He

-      commander.parse(getArgs());
+      commander.parse(args);
-                                             (getArgs() != null
-                                              ? (SliderUtils.join(getArgs(),
+                                             (args != null
+                                              ? (SliderUtils.join(args,
-  public String[] getArgs() {
-    return args;
-  }

UPD42 MOV42 INS42 UPD42 MOV42 DEL32 DEL42 DEL32 DEL32 DEL83 DEL42 DEL43 DEL85 DEL5 DEL42 DEL42 DEL41 DEL8 DEL31