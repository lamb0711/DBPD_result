HDFS-7773. Additional metrics in HDFS to be accessed via jmx. Contributed by Anu Engineer.

+import org.apache.hadoop.util.Time;
-
+    long read = 0;
-      long read = blockSender.sendBlock(out, baseStream, null); // send data
-
+      long beginRead = Time.monotonicNow();
+      read = blockSender.sendBlock(out, baseStream, null); // send data
+      long duration = Time.monotonicNow() - beginRead;
+      datanode.metrics.incrTotalReadTime(duration);
-    datanode.metrics.incrReadsFromClient(peer.isLocal());
+    datanode.metrics.incrReadsFromClient(peer.isLocal(), read);
-
+    long size = 0;
-      
+      if(isClient) {
+        size = block.getNumBytes();
+      }
-    datanode.metrics.incrWritesFromClient(peer.isLocal());
+    datanode.metrics.incrWritesFromClient(peer.isLocal(), size);
-      // send block content to the target
-      long read = blockSender.sendBlock(reply, baseStream, 
-                                        dataXceiverServer.balanceThrottler);
+      long beginRead = Time.monotonicNow();
+      // send block content to the target
+      long read = blockSender.sendBlock(reply, baseStream,
+                                        dataXceiverServer.balanceThrottler);
+      long duration = Time.monotonicNow() - beginRead;
+      datanode.metrics.incrTotalReadTime(duration);

INS26 INS40 INS60 INS60 INS39 INS59 INS39 INS59 INS42 INS34 INS21 INS60 INS21 INS42 INS42 INS34 INS25 INS42 INS60 INS60 INS21 INS7 INS39 INS59 INS32 INS42 INS8 INS39 INS59 INS39 INS59 INS32 UPD42 INS32 INS42 MOV32 INS42 INS27 INS40 INS42 INS42 INS21 INS42 INS32 INS42 INS27 INS40 INS42 INS42 INS42 INS42 INS32 INS42 INS7 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42