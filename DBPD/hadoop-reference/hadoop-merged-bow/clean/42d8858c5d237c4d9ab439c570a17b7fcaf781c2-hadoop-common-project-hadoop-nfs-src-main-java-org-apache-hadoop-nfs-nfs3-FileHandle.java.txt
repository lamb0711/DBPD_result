HDFS-7516. Fix findbugs warnings in hdfs-nfs project. Contributed by Brandon Li

+import org.apache.commons.io.Charsets;
-    byte[] in = s.getBytes();
-    for (int i = 0; i < in.length; i++) {
-      digest.update(in[i]);
-    }
+    byte[] in = s.getBytes(Charsets.UTF_8);
+    digest.update(in);

INS26 INS40 MOV21 INS42 INS40 DEL42 DEL42 DEL2 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL8 DEL24