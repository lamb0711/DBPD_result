HDFS-7305. NPE seen in wbhdfs FS while running SLive. Contributed by Jing Zhao.

-      if (re.getMessage().startsWith(
+      if (re.getMessage() != null && re.getMessage().startsWith(

INS27 INS27 MOV32 INS32 INS33 INS42 INS42