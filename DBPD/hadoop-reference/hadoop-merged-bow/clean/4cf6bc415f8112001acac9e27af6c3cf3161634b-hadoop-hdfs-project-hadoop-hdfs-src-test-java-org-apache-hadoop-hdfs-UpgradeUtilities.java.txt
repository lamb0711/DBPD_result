HDFS-2414. Fix TestDFSRollback to avoid spurious failures. Contributed by Todd Lipcon.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1180541 13f79535-47bb-0310-9956-ffa450edef68

-import java.io.RandomAccessFile;
-import java.util.Random;
+import com.google.common.base.Preconditions;
+import com.google.common.io.Files;
+import com.google.common.primitives.Bytes;
+
-  public static void corruptFile(File file) throws IOException {
+  public static void corruptFile(File file,
+      byte[] stringToCorrupt,
+      byte[] replacement) throws IOException {
+    Preconditions.checkArgument(replacement.length == stringToCorrupt.length);
-                                         "Given argument is not a file:" + file);
+          "Given argument is not a file:" + file);
-    RandomAccessFile raf = new RandomAccessFile(file,"rws");
-    Random random = new Random();
-    for (long i = 0; i < raf.length(); i++) {
-      raf.seek(i);
-      if (random.nextBoolean()) {
-        raf.writeByte(random.nextInt());
-      }
+    byte[] data = Files.toByteArray(file);
+    int index = Bytes.indexOf(data, stringToCorrupt);
+    if (index == -1) {
+      throw new IOException(
+          "File " + file + " does not contain string " +
+          new String(stringToCorrupt));
-    raf.close();
+
+    for (int i = 0; i < stringToCorrupt.length; i++) {
+      data[index + i] = replacement[i];
+    }
+    Files.write(data, file);

MOV26 MOV26 INS26 INS40 UPD40 UPD40 INS44 INS44 INS5 INS42 INS5 INS42 INS21 INS60 INS60 INS25 INS24 MOV21 INS39 INS85 INS39 INS85 INS32 INS5 MOV59 INS39 INS59 INS27 INS8 MOV58 MOV27 MOV37 INS8 INS42 INS42 INS27 INS39 INS85 UPD42 INS32 INS42 INS32 INS42 INS38 INS53 UPD39 INS40 INS21 UPD42 UPD42 UPD42 MOV42 UPD42 MOV42 INS40 INS40 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS34 INS14 INS7 INS43 INS27 INS2 INS2 UPD42 MOV42 INS45 INS42 INS45 INS14 INS42 INS27 INS42 INS42 UPD43 MOV43 INS42 INS42 INS42 UPD42 DEL42 DEL43 DEL42 DEL45 DEL14 DEL42 DEL42 DEL32 DEL32 DEL42 DEL43 DEL60 DEL43 DEL42 DEL14 DEL59 DEL60 DEL42 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL8 DEL25 DEL8 DEL24 DEL42 DEL42 DEL32 DEL21