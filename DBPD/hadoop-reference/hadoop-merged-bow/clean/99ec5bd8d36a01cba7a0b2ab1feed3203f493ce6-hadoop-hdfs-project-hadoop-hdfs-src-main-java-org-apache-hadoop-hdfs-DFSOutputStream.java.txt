Merge trunk into QJM branch

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-3077@1380990 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.hadoop.hdfs.protocol.datatransfer.DataTransferProtocol;
+import org.apache.hadoop.hdfs.protocol.datatransfer.DataTransferProtocol;
-  private final DFSClient dfsClient;
+  private final DFSClient dfsClient;
-  private class Packet {
-    long    seqno;               // sequencenumber of buffer in block
-    long    offsetInBlock;       // offset in block
-    private boolean lastPacketInBlock;   // is this the last packet in block?
-    boolean syncBlock;          // this packet forces the current block to disk
-    int     numChunks;           // number of chunks currently in packet
-    int     maxChunks;           // max chunks in packet
-
+  private static class Packet {
+    private static final long HEART_BEAT_SEQNO = -1L;
+    long seqno; // sequencenumber of buffer in block
+    final long offsetInBlock; // offset in block
+    boolean syncBlock; // this packet forces the current block to disk
+    int numChunks; // number of chunks currently in packet
+    final int maxChunks; // max chunks in packet
+    private boolean lastPacketInBlock; // is this the last packet in block?
-    int dataStart;
+    final int dataStart;
-    private static final long HEART_BEAT_SEQNO = -1L;
-
-    Packet() {
-      this.lastPacketInBlock = false;
-      this.numChunks = 0;
-      this.offsetInBlock = 0;
-      this.seqno = HEART_BEAT_SEQNO;
-      
-      buf = new byte[PacketHeader.PKT_MAX_HEADER_LEN];
-      
-      checksumStart = checksumPos = dataPos = dataStart = PacketHeader.PKT_MAX_HEADER_LEN;
-      maxChunks = 0;
+    Packet(int checksumSize) {
+      this(0, 0, 0, HEART_BEAT_SEQNO, checksumSize);
-     * @param pktSize maximum size of the packet, including checksum data and actual data.
+     * @param pktSize maximum size of the packet, 
+     *                including checksum data and actual data.
-    Packet(int pktSize, int chunksPerPkt, long offsetInBlock) {
+    Packet(int pktSize, int chunksPerPkt, long offsetInBlock, 
+                              long seqno, int checksumSize) {
-      this.seqno = currentSeqno;
-      currentSeqno++;
+      this.seqno = seqno;
-      dataStart = checksumStart + (chunksPerPkt * checksum.getChecksumSize());
+      dataStart = checksumStart + (chunksPerPkt * checksumSize);
+            DFSClient.LOG.warn("Caught exception ", e);
+                DFSClient.LOG.warn("Caught exception ", e);
-              one = new Packet();  // heartbeat packet
+              one = new Packet(checksum.getChecksumSize());  // heartbeat packet
+                  DFSClient.LOG.warn("Caught exception ", e);
-            // write to primary DN 
+            // write to primary DN
+          DFSClient.LOG.warn("Caught exception ", e);
+                  DFSClient.LOG.warn("Caught exception ", ie);
-          bytesCurBlock);
+          bytesCurBlock, currentSeqno++, this.checksum.getChecksumSize());
-        currentPacket = new Packet(0, 0, bytesCurBlock);
+        currentPacket = new Packet(0, 0, bytesCurBlock, 
+            currentSeqno++, this.checksum.getChecksumSize());
-                bytesCurBlock);
+                bytesCurBlock, currentSeqno++, this.checksum.getChecksumSize());
-                bytesCurBlock);
+                bytesCurBlock, currentSeqno++, this.checksum.getChecksumSize());
-        currentPacket = new Packet(0, 0, bytesCurBlock);
+        currentPacket = new Packet(0, 0, bytesCurBlock, 
+            currentSeqno++, this.checksum.getChecksumSize());
+          DFSClient.LOG.warn("Caught exception ", ie);

MOV26 MOV23 MOV23 MOV23 INS83 INS83 INS83 INS83 INS44 INS8 INS44 INS44 INS39 INS42 INS17 INS39 INS42 INS39 INS42 INS34 INS34 INS34 INS42 INS42 INS66 UPD66 UPD42 MOV37 INS32 INS42 INS21 INS22 INS42 INS32 INS52 INS42 INS37 INS32 INS37 INS32 INS21 INS21 INS40 INS42 INS45 INS42 INS42 INS22 INS42 INS42 INS22 INS42 INS32 INS32 INS52 INS42 INS52 INS42 INS40 INS42 INS45 INS42 INS40 INS42 INS45 INS42 MOV43 INS37 INS32 MOV43 INS37 INS32 INS21 MOV32 INS42 INS22 INS42 INS42 INS22 INS42 INS32 INS52 INS42 INS52 INS42 INS40 INS42 INS45 INS42 INS21 INS32 INS21 INS40 INS42 INS45 INS42 INS32 INS40 INS42 INS45 INS42 DEL52 DEL42 DEL22 DEL9 DEL7 DEL21 DEL52 DEL42 DEL22 DEL34 DEL7 DEL21 DEL52 DEL42 DEL22 DEL34 DEL7 DEL21 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL42 DEL39 DEL85 DEL5 DEL40 DEL3 DEL7 DEL21 DEL42 DEL42 DEL42 DEL42 DEL40 DEL7 DEL7 DEL7 DEL7 DEL21 DEL42 DEL34 DEL7 DEL21 DEL8 DEL21