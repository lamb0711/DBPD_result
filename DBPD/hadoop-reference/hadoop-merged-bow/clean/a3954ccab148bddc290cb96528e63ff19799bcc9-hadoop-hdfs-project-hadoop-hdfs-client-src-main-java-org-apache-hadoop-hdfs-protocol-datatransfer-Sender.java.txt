HDFS-9807. Add an optional StorageID to writes. Contributed by Ewan Higgs

+import java.util.Arrays;
-      final boolean[] targetPinnings) throws IOException {
+      final boolean[] targetPinnings,
+      final String storageId,
+      final String[] targetStorageIds) throws IOException {
-        .addAllTargetPinnings(PBHelperClient.convert(targetPinnings, 1));
-
+        .addAllTargetPinnings(PBHelperClient.convert(targetPinnings, 1))
+        .addAllTargetStorageIds(PBHelperClient.convert(targetStorageIds, 1));
+    if (storageId != null) {
+      proto.setStorageId(storageId);
+    }
-      final StorageType[] targetStorageTypes) throws IOException {
+      final StorageType[] targetStorageTypes,
+      final String[] targetStorageIds) throws IOException {
+        .addAllTargetStorageIds(Arrays.asList(targetStorageIds))
-      final DatanodeInfo source) throws IOException {
-    OpReplaceBlockProto proto = OpReplaceBlockProto.newBuilder()
+      final DatanodeInfo source,
+      final String storageId) throws IOException {
+    OpReplaceBlockProto.Builder proto = OpReplaceBlockProto.newBuilder()
-        .setSource(PBHelperClient.convertDatanodeInfo(source))
-        .build();
+        .setSource(PBHelperClient.convertDatanodeInfo(source));
+    if (storageId != null) {
+      proto.setStorageId(storageId);
+    }
-    send(out, Op.REPLACE_BLOCK, proto);
+    send(out, Op.REPLACE_BLOCK, proto.build());

INS26 INS40 INS44 INS44 INS44 INS44 INS83 INS43 INS42 INS83 INS5 INS42 INS25 INS83 INS5 INS42 INS83 INS43 INS42 INS25 INS42 INS43 INS85 INS27 INS8 INS43 INS85 INS42 UPD43 INS27 INS8 INS42 INS32 INS42 INS33 INS21 INS42 INS32 INS40 MOV32 INS42 INS33 INS21 INS32 MOV32 INS42 INS32 INS32 MOV32 INS42 INS32 MOV42 INS42 INS42 INS42 INS42 INS34 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 UPD42 MOV42 INS42 INS42 DEL42 DEL42 DEL32