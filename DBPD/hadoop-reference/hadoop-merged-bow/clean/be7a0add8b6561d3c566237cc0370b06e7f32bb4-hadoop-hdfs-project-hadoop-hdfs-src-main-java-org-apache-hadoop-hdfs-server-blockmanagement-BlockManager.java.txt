HDFS-9223. Code cleanup for DatanodeDescriptor and HeartbeatManager. Contributed by Jing Zhao.

-    if (isBlockTokenEnabled() && nodeinfo.needKeyUpdate) {
+    if (isBlockTokenEnabled() && nodeinfo.needKeyUpdate()) {
-      nodeinfo.needKeyUpdate = false;
+      nodeinfo.setNeedKeyUpdate(false);
-      if (node == null || !node.isAlive) {
+      if (node == null || !node.isAlive()) {
-    if (node == null || !node.isAlive) {
+    if (node == null || !node.isAlive()) {
-    if (node.isAlive) {
+    if (node.isAlive()) {

INS32 INS32 INS42 INS42 INS42 INS42 INS32 INS32 INS42 INS42 INS9 INS42 INS42 INS32 INS42 INS42 DEL40 DEL40 DEL9 DEL7 DEL40 DEL40 DEL40