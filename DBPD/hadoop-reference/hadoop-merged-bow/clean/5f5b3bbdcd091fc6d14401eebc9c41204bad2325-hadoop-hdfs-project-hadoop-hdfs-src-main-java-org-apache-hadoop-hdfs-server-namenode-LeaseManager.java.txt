HDFS-2577. NN fails to start since it tries to start secret manager in safemode. Contributed by Todd Lipcon.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-1623@1205689 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.hadoop.util.Daemon;
+
+import com.google.common.annotations.VisibleForTesting;
+import com.google.common.base.Preconditions;
+  private Daemon lmthread;
+  private volatile boolean shouldRunMonitor;
+
-      for(; fsnamesystem.isRunning(); ) {
-        fsnamesystem.writeLock();
+      for(; shouldRunMonitor && fsnamesystem.isRunning(); ) {
-          if (!fsnamesystem.isInSafeMode()) {
-            checkLeases();
+          fsnamesystem.writeLockInterruptibly();
+          try {
+            if (!fsnamesystem.isInSafeMode()) {
+              checkLeases();
+            }
+          } finally {
+            fsnamesystem.writeUnlock();
-        } finally {
-          fsnamesystem.writeUnlock();
-        }
-
-
-        try {
+  
+  
+
+  void startMonitor() {
+    Preconditions.checkState(lmthread == null,
+        "Lease Monitor already running");
+    shouldRunMonitor = true;
+    lmthread = new Daemon(new Monitor());
+    lmthread.start();
+  }
+  
+  void stopMonitor() {
+    if (lmthread != null) {
+      shouldRunMonitor = false;
+      try {
+        lmthread.interrupt();
+        lmthread.join(3000);
+      } catch (InterruptedException ie) {
+        LOG.warn("Encountered exception ", ie);
+      }
+      lmthread = null;
+    }
+  }
+
+  /**
+   * Trigger the currently-running Lease monitor to re-check
+   * its leases immediately. This is for use by unit tests.
+   */
+  @VisibleForTesting
+  void triggerMonitorCheckNow() {
+    Preconditions.checkState(lmthread != null,
+        "Lease monitor is not running");
+    lmthread.interrupt();
+  }

INS26 INS26 INS26 INS40 INS40 INS40 INS23 INS23 INS31 INS31 INS31 INS83 INS43 INS59 INS83 INS83 INS39 INS59 INS39 INS42 INS8 INS39 INS42 INS8 INS29 INS78 INS39 INS42 INS8 INS42 INS42 INS42 INS21 INS21 INS21 INS21 INS25 INS65 INS42 INS21 INS21 INS32 INS7 INS7 INS32 INS27 INS8 INS66 INS66 INS32 INS32 INS27 INS42 INS42 INS27 INS45 INS42 INS9 INS42 INS14 INS42 INS42 INS42 INS33 INS21 INS54 INS21 INS42 INS42 INS27 INS45 INS42 INS42 INS42 MOV32 INS42 INS33 INS43 INS14 INS7 INS8 INS12 INS7 INS42 INS33 INS42 INS43 INS42 INS9 INS21 INS21 INS44 INS8 INS42 INS33 INS21 MOV54 INS42 INS32 INS32 INS43 INS42 INS21 INS32 INS42 INS42 INS42 INS42 INS34 INS42 INS32 INS42 INS42 INS42 INS42 INS45 INS42 DEL42 DEL42 DEL32 DEL21