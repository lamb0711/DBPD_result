HDFS-13795. Fix potential NPE in InMemoryLevelDBAliasMapServer.

-      aliasMap.close();
+      if (aliasMap != null) {
+        aliasMap.close();
+      }
-    aliasMapServer.stop();
+    if (aliasMapServer != null) {
+      aliasMapServer.stop();
+    }

INS25 INS8 INS27 INS8 INS25 INS42 INS33 MOV21 INS27 MOV8 INS42 INS33