YARN-8508.  Release GPU resource for killed container.
            Contributed by Chandni Singh

-
-      try {
-        if (resourceHandlerChain != null) {
-          resourceHandlerChain.postComplete(containerId);
-        }
-      } catch (ResourceHandlerException e) {
-        LOG.warn("ResourceHandlerChain.postComplete failed for " +
-            "containerId: " + containerId + ". Exception: " + e);
-      }
+      postComplete(containerId);
-      if (resourceHandlerChain != null) {
-        try {
-          resourceHandlerChain.postComplete(containerId);
-        } catch (ResourceHandlerException e) {
-          LOG.warn("ResourceHandlerChain.postComplete failed for " +
-              "containerId: " + containerId + " Exception: " + e);
-        }
-      }
+      postComplete(containerId);
+    } finally {
+      postComplete(container.getContainerId());
+
+  @VisibleForTesting
+  void postComplete(final ContainerId containerId) {
+    try {
+      if (resourceHandlerChain != null) {
+        LOG.debug("{} post complete", containerId);
+        resourceHandlerChain.postComplete(containerId);
+      }
+    } catch (ResourceHandlerException e) {
+      LOG.warn("ResourceHandlerChain.postComplete failed for " +
+          "containerId: {}. Exception: ", containerId, e);
+    }
+  }

INS31 INS78 INS39 INS42 INS44 INS8 INS42 INS83 INS43 INS42 INS54 INS8 INS8 INS8 INS42 INS8 INS12 MOV21 MOV21 MOV21 MOV21 INS21 INS25 MOV44 INS8 INS32 MOV27 MOV8 INS21 UPD42 INS42 INS42 INS32 INS21 INS32 INS42 INS42 INS32 INS42 INS42 INS27 INS42 INS42 INS42 INS42 INS45 INS42 INS45 INS45 DEL42 DEL42 DEL33 DEL27 DEL8 DEL25 DEL8 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL45 DEL45 DEL27 DEL42 DEL45 DEL42 DEL27 DEL32 DEL21 DEL8 DEL12 DEL54 DEL8 DEL42 DEL45 DEL45 DEL27 DEL42 DEL45 DEL42 DEL27 DEL8 DEL12 DEL54 DEL8 DEL25 DEL8