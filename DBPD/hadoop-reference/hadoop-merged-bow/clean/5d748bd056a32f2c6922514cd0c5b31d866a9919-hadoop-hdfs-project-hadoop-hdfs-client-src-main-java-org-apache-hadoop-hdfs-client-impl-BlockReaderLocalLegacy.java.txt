HDFS-13702. Remove HTrace hooks from DFSClient to reduce CPU usage. Contributed by Todd Lipcon.

-import org.apache.htrace.core.TraceScope;
-import org.apache.htrace.core.Tracer;
-  private final Tracer tracer;
-      long startOffset, long length, StorageType storageType,
-      Tracer tracer) throws IOException {
+      long startOffset, long length, StorageType storageType)
+      throws IOException {
-            startOffset, checksum, true, dataIn, firstChunkOffset, checksumIn,
-            tracer);
+            startOffset, checksum, true, dataIn, firstChunkOffset, checksumIn);
-            startOffset, dataIn, tracer);
+            startOffset, dataIn);
-      ExtendedBlock block, long startOffset, FileInputStream dataIn,
-      Tracer tracer) throws IOException {
+      ExtendedBlock block, long startOffset, FileInputStream dataIn)
+      throws IOException {
-        dataIn, startOffset, null, tracer);
+        dataIn, startOffset, null);
-      FileInputStream checksumIn, Tracer tracer) throws IOException {
+      FileInputStream checksumIn) throws IOException {
-    this.tracer = tracer;
-    try (TraceScope ignored = tracer.
-        newScope("BlockReaderLocalLegacy#fillBuffer(" + blockId + ")")) {
-      int bytesRead = stream.getChannel().read(buf);
-      if (bytesRead < 0) {
+    int bytesRead = stream.getChannel().read(buf);
+    if (bytesRead < 0) {
+      //EOF
+      return bytesRead;
+    }
+    while (buf.remaining() > 0) {
+      int n = stream.getChannel().read(buf);
+      if (n < 0) {
-      while (buf.remaining() > 0) {
-        int n = stream.getChannel().read(buf);
-        if (n < 0) {
-          //EOF
-          return bytesRead;
-        }
-        bytesRead += n;
-      }
-      return bytesRead;
+      bytesRead += n;
+    return bytesRead;

MOV8 MOV43 MOV43 DEL40 DEL26 DEL40 DEL26 DEL83 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL43 DEL42 DEL44 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL42 DEL43 DEL42 DEL42 DEL42 DEL45 DEL42 DEL45 DEL27 DEL32 DEL59 DEL58 DEL54 DEL8