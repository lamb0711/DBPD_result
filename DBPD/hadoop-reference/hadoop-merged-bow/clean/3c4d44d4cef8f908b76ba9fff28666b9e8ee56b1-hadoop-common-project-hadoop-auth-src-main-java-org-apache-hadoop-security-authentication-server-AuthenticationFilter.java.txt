HADOOP-10301. AuthenticationFilter should return Forbidden for failed authentication. Contributed by Daryn Sharp.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1582883 13f79535-47bb-0310-9956-ffa450edef68

-    String unauthorizedMsg = "";
+    int errCode = HttpServletResponse.SC_UNAUTHORIZED;
+    AuthenticationException authenticationEx = null;
+        // will be sent back in a 401 unless filter authenticates
+        authenticationEx = ex;
-      unauthorizedMsg = ex.toString();
+      // exception from the filter itself is fatal
+      errCode = HttpServletResponse.SC_FORBIDDEN;
+      authenticationEx = ex;
-        httpResponse.sendError(HttpServletResponse.SC_UNAUTHORIZED,
-                unauthorizedMsg);
+        if (authenticationEx == null) {
+          httpResponse.sendError(errCode, "Authentication required");
+        } else {
+          httpResponse.sendError(errCode, authenticationEx.getMessage());
+        }

INS60 INS39 INS59 UPD43 INS42 INS40 UPD42 UPD42 INS33 INS21 INS8 INS7 MOV21 INS25 INS21 INS42 INS40 UPD42 INS42 INS27 INS8 MOV8 INS7 INS42 INS33 INS21 INS42 INS42 INS32 INS42 INS42 INS42 INS45 INS42 INS32 UPD42 MOV42 INS42 DEL45 DEL42 DEL42 DEL32 DEL40