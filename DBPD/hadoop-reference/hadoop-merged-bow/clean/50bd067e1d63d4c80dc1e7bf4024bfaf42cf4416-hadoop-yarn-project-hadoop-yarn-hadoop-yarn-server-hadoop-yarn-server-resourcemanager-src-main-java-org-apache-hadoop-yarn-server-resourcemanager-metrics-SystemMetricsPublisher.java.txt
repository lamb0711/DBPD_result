YARN-4452. NPE when submit Unmanaged application. Contributed by Naganarasimha G R.

+      ContainerId container = (appAttempt.getMasterContainer() == null) ? null
+          : appAttempt.getMasterContainer().getId();
-              appAttempt.getMasterContainer().getId(),
+              container,
+      ContainerId container = (appAttempt.getMasterContainer() == null) ? null
+          : appAttempt.getMasterContainer().getId();
-              appAttempt.getMasterContainer().getId()));
+              container));
-    eventInfo.put(
-        AppAttemptMetricsConstants.MASTER_CONTAINER_EVENT_INFO,
-        event.getMasterContainerId().toString());
+    if (event.getMasterContainerId() != null) {
+      eventInfo.put(AppAttemptMetricsConstants.MASTER_CONTAINER_EVENT_INFO,
+          event.getMasterContainerId().toString());
+    }
-    eventInfo.put(
-        AppAttemptMetricsConstants.MASTER_CONTAINER_EVENT_INFO,
-        event.getMasterContainerId().toString());
+    if (event.getMasterContainerId() != null) {
+      eventInfo.put(AppAttemptMetricsConstants.MASTER_CONTAINER_EVENT_INFO,
+          event.getMasterContainerId().toString());
+    }

INS25 INS25 INS27 INS8 INS27 INS8 INS60 INS60 INS32 INS33 MOV21 INS32 INS33 MOV21 INS43 INS59 INS43 INS59 INS42 INS42 INS42 INS42 INS42 INS42 INS16 INS42 INS42 INS16 INS36 INS33 MOV32 INS42 INS36 INS33 MOV32 INS42 INS27 INS27 INS32 INS33 INS32 INS33 INS42 INS42 INS42 INS42