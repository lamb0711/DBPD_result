HADOOP-16127. In ipc.Client, put a new connection could happen after stop.

+    final int count;
-      client.decCount();
-      if (client.isZeroReference()) {
+      count = client.decAndGetCount();
+      if (count == 0) {
-    if (client.isZeroReference()) {
+    if (count == 0) {

INS60 INS83 INS39 INS59 INS27 INS42 INS42 INS34 INS7 INS27 INS42 INS32 INS42 INS34 MOV42 UPD42 MOV42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32