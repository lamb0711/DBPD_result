HDFS-7356. Use DirectoryListing.hasMore() directly in nfs. Contributed by Li Lu.

-    boolean eof = (n < fstatus.length) ? false : (dlisting
-        .getRemainingEntries() == 0);
+    boolean eof = (n >= fstatus.length) && !dlisting.hasMore();
-    boolean eof = (n < fstatus.length) ? false : (dlisting
-        .getRemainingEntries() == 0);
+    boolean eof = (n >= fstatus.length) && !dlisting.hasMore();

INS27 INS27 INS36 INS38 INS36 INS38 UPD27 MOV27 INS32 UPD27 MOV27 INS32 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 DEL36 DEL9 DEL32 DEL34 DEL27 DEL36 DEL16 DEL36 DEL9 DEL32 DEL34 DEL27 DEL36 DEL16