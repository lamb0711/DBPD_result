YARN-3223. Resource update during NM graceful decommission. Contributed by Brook Zhou.

+import org.apache.hadoop.yarn.api.records.NodeState;
+import org.apache.hadoop.yarn.server.resourcemanager.rmnode.RMNodeResourceUpdateEvent;
+    // If the node is decommissioning, send an update to have the total
+    // resource equal to the used resource, so no available resource to
+    // schedule.
+    if (nm.getState() == NodeState.DECOMMISSIONING) {
+      this.rmContext
+          .getDispatcher()
+          .getEventHandler()
+          .handle(
+              new RMNodeResourceUpdateEvent(nm.getNodeID(), ResourceOption
+                  .newInstance(getSchedulerNode(nm.getNodeID())
+                      .getUsedResource(), 0)));
+    }
+

INS26 INS26 INS40 INS40 INS25 INS27 INS8 INS32 INS40 INS21 INS42 INS42 INS32 INS32 INS42 INS14 INS32 INS42 INS43 INS32 INS32 INS22 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS34 INS52 INS42 INS32 INS42 INS42 INS32 INS42 INS42