HDFS-15075. Remove process command timing from BPServiceActor. Contributed by Xiaoqiao He.

-
-            long startProcessCommands = monotonicNow();
-            long endProcessCommands = monotonicNow();
-            if (endProcessCommands - startProcessCommands > 2000) {
-              LOG.info("Took " + (endProcessCommands - startProcessCommands)
-                  + "ms to process " + resp.getCommands().length
-                  + " commands from NN");
-            }
+        long startProcessCommands = monotonicNow();
+        long processCommandsMs = monotonicNow() - startProcessCommands;
+        if (cmds.length > 0) {
+          dn.getMetrics().addNumProcessedCommands(processCommandsMs);
+        }
+        if (processCommandsMs > dnConf.getProcessCommandsThresholdMs()) {
+          LOG.info("Took {} ms to process {} commands from NN",
+              processCommandsMs, cmds.length);
+        }

MOV60 MOV60 INS25 INS25 INS27 INS8 INS27 INS8 INS42 INS27 INS40 INS34 INS21 INS42 INS32 INS21 MOV32 INS42 INS32 INS42 INS42 INS32 INS32 INS42 INS42 INS42 INS42 INS45 INS42 INS40 INS42 INS42 DEL42 DEL42 DEL27 DEL34 DEL27 DEL42 DEL42 DEL45 DEL42 DEL42 DEL27 DEL36 DEL45 DEL42 DEL42 DEL32 DEL42 DEL22 DEL45 DEL27 DEL32 DEL21 DEL8 DEL25 DEL42