HDFS-9939. Increase DecompressorStream skip buffer size. Contributed by John Zhuge.

+import com.google.common.annotations.VisibleForTesting;
-import org.apache.hadoop.io.compress.Decompressor;
+  /**
+   * The maximum input buffer size.
+   */
+  private static final int MAX_INPUT_BUFFER_SIZE = 512;
+  /**
+   * MAX_SKIP_BUFFER_SIZE is used to determine the maximum buffer size to
+   * use when skipping. See {@link java.io.InputStream}.
+   */
+  private static final int MAX_SKIP_BUFFER_SIZE = 2048;
+
+  private byte[] skipBytes;
+  private byte[] oneByte = new byte[1];
+
-  public DecompressorStream(InputStream in, Decompressor decompressor,
-                            int bufferSize)
-  throws IOException {
+  @VisibleForTesting
+  DecompressorStream(InputStream in, Decompressor decompressor,
+                            int bufferSize, int skipBufferSize)
+      throws IOException {
+    skipBytes = new byte[skipBufferSize];
+  }
+
+  public DecompressorStream(InputStream in, Decompressor decompressor,
+                            int bufferSize)
+      throws IOException {
+    this(in, decompressor, bufferSize, MAX_SKIP_BUFFER_SIZE);
-  throws IOException {
-    this(in, decompressor, 512);
+      throws IOException {
+    this(in, decompressor, MAX_INPUT_BUFFER_SIZE);
-  
-  private byte[] oneByte = new byte[1];
+
-    int n = 0;
+    int n;
-  private byte[] skipBytes = new byte[512];
-    
+

MOV26 MOV23 UPD40 INS23 INS23 INS23 INS31 INS29 INS83 INS83 INS83 INS39 INS59 INS29 INS83 INS83 INS83 INS39 INS59 INS83 MOV5 INS59 INS78 INS44 INS44 INS43 INS83 INS42 INS44 INS44 MOV43 INS8 INS65 INS42 INS34 INS65 INS42 INS34 INS42 INS42 INS39 INS42 INS21 INS39 INS42 INS42 INS43 INS42 INS43 INS42 INS17 INS66 INS66 INS66 INS65 INS66 INS7 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS40 INS42 INS3 MOV5 INS42 DEL83 DEL34 DEL34 DEL83 DEL42 DEL34 DEL3 DEL59 DEL23