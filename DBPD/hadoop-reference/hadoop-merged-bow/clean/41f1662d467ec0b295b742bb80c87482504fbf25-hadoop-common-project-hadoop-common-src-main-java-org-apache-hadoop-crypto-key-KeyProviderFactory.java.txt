HADOOP-11054. Add a KeyProvider instantiation based on a URI. (tucu)

-        boolean found = false;
-        for(KeyProviderFactory factory: serviceLoader) {
-          KeyProvider kp = factory.createProvider(uri, conf);
-          if (kp != null) {
-            result.add(kp);
-            found = true;
-            break;
-          }
-        }
-        if (!found) {
+        KeyProvider kp = get(uri, conf);
+        if (kp != null) {
+          result.add(kp);
+        } else {
+
+  /**
+   * Create a KeyProvider based on a provided URI.
+   *
+   * @param uri key provider URI
+   * @param conf configuration to initialize the key provider
+   * @return the key provider for the specified URI, or <code>NULL</code> if
+   *         a provider for the specified URI scheme could not be found.
+   * @throws IOException thrown if the provider failed to initialize.
+   */
+  public static KeyProvider get(URI uri, Configuration conf)
+      throws IOException {
+    KeyProvider kp = null;
+    for (KeyProviderFactory factory : serviceLoader) {
+      kp = factory.createProvider(uri, conf);
+      if (kp != null) {
+        break;
+      }
+    }
+    return kp;
+  }
+

INS31 INS29 INS83 INS83 MOV43 INS42 INS44 INS44 INS43 INS8 INS65 INS65 INS65 INS65 INS65 INS43 INS42 INS43 INS42 INS42 INS60 INS70 INS41 INS66 INS42 INS66 INS42 INS66 INS66 INS66 INS42 INS66 INS42 INS42 INS43 INS59 MOV44 INS42 INS8 INS42 INS42 INS42 INS33 INS21 INS25 INS7 INS27 INS8 INS60 MOV25 INS42 MOV32 INS42 INS33 INS10 INS43 INS59 MOV8 INS42 MOV42 INS32 INS42 INS42 INS42 DEL42 DEL9 DEL7 DEL21 DEL10 DEL39 DEL42 DEL9 DEL59 DEL60 DEL42 DEL59 DEL60 DEL8 DEL70 DEL42 DEL38 DEL25