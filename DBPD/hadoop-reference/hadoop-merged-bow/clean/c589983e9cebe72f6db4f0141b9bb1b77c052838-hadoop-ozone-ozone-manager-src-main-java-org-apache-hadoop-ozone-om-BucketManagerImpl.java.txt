HDDS-1893. Fix bug in removeAcl in Bucket. (#1216)


+      boolean removed = false;
-          bits = (BitSet) acl.getAclBitSet().clone();
-          bits.and(a.getAclBitSet());
+
+          removed = true;
-        } else {
-          return false;
-      metadataManager.getBucketTable().put(dbBucketKey, bucketInfo);
+      if (removed) {
+        metadataManager.getBucketTable().put(dbBucketKey, bucketInfo);
+      }
+      return removed;
-
-    return true;

INS60 INS25 INS41 INS39 INS59 INS42 INS8 INS42 INS42 INS9 MOV21 MOV21 UPD42 INS9 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL21 DEL42 DEL43 DEL42 DEL42 DEL32 DEL42 DEL32 DEL11 DEL9 DEL41 DEL8 DEL9 DEL41