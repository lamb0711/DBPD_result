HDFS-11762. [SPS]: Empty files should be ignored in StoragePolicySatisfier. Surendra Singh Lilhore.

-    List<XAttr> xAttrs = Lists.newArrayListWithCapacity(1);
-      xAttrs.add(satisfyXAttr);
-      fsd.getEditLog().logSetXAttrs(src, xAttrs, logRetryCache);
+      if (satisfyXAttr != null) {
+        List<XAttr> xAttrs = Lists.newArrayListWithCapacity(1);
+        xAttrs.add(satisfyXAttr);
+        fsd.getEditLog().logSetXAttrs(src, xAttrs, logRetryCache);
+      }
-    if (inode.isFile()) {
+    if (inode.isFile() && inode.asFile().numBlocks() != 0) {
-        if (node.isFile()) {
+        if (node.isFile() && node.asFile().numBlocks() != 0) {
+    if (candidateNodes.isEmpty()) {
+      return null;
+    }

INS25 INS27 INS32 INS8 INS25 MOV32 INS27 INS42 INS42 INS41 INS27 INS8 INS32 INS34 INS33 INS42 INS33 MOV60 MOV21 MOV21 INS32 INS42 INS42 INS42 INS27 MOV32 INS27 INS32 INS34 INS32 INS42 INS42 INS42