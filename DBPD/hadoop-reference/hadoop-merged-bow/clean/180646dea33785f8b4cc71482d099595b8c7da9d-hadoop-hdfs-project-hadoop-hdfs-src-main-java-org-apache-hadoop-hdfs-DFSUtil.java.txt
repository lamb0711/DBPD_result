HDFS-2418. Change ConfiguredFailoverProxyProvider to take advantage of HDFS-2231. (atm)


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-1623@1190078 13f79535-47bb-0310-9956-ffa450edef68

-    for (String nameserviceId : nameserviceIds) {
-      for (String nnId : namenodeIds) {
-        String keySuffix = concatSuffixes(nameserviceId, nnId);
-        String address = getConfValue(null, keySuffix, conf, keys);
-        if (address == null) {
-          return null;
+    if (federationEnabled && haEnabled) {
+      for (String nameserviceId : nameserviceIds) {
+        for (String nnId : namenodeIds) {
+          String keySuffix = concatSuffixes(nameserviceId, nnId);
+          String address = getConfValue(null, keySuffix, conf, keys);
+          if (address != null) {
+            isas.add(NetUtils.createSocketAddr(address));
+          }
-        isas.add(NetUtils.createSocketAddr(address));
+      }
+    } else if (!federationEnabled && haEnabled) {
+      for (String nnId : namenodeIds) {
+        String address = getConfValue(null, nnId, conf, keys);
+        if (address != null) {
+          isas.add(NetUtils.createSocketAddr(address));
+        }
+      }
+    } else if (federationEnabled && !haEnabled) {
+      for (String nameserviceId : nameserviceIds) {
+          String address = getConfValue(null, nameserviceId, conf, keys);
+          if (address != null) {
+            isas.add(NetUtils.createSocketAddr(address));
+          }
+
+  /**
+   * Returns list of InetSocketAddress corresponding to HA NN RPC addresses from
+   * the configuration.
+   * 
+   * @param conf configuration
+   * @return list of InetSocketAddresses
+   * @throws IOException if no addresses are configured
+   */
+  public static List<InetSocketAddress> getHaNnRpcAddresses(
+      Configuration conf) throws IOException {
+    List<InetSocketAddress> addressList = getAddresses(conf, null,
+        DFSConfigKeys.DFS_NAMENODE_RPC_ADDRESS_KEY);
+    if (addressList == null) {
+      throw new IOException("Incorrect configuration: HA name node addresses "
+          + DFS_NAMENODE_RPC_ADDRESS_KEY + " is not configured.");
+    }
+    return addressList;
+  }

INS31 INS29 INS83 INS83 INS74 INS42 INS44 INS43 INS8 INS25 INS65 INS65 INS65 INS65 INS43 INS43 INS43 INS42 INS42 INS60 INS25 INS41 INS27 INS8 INS25 INS66 INS66 INS42 INS66 INS66 INS42 INS66 INS42 INS42 INS42 INS74 INS59 INS27 INS8 INS42 INS42 INS42 MOV70 INS27 INS8 INS25 INS43 INS43 INS42 INS32 INS42 INS33 INS53 INS38 INS42 INS70 INS27 INS8 INS42 INS42 INS42 INS42 INS33 INS40 INS14 INS42 INS44 INS42 INS8 INS42 INS38 INS70 INS43 INS27 INS43 INS42 INS60 INS25 INS42 INS44 INS42 INS8 INS42 INS45 INS42 INS45 INS42 INS43 INS59 INS27 INS8 INS43 INS42 INS60 INS25 UPD27 INS42 INS42 INS32 INS42 INS33 INS21 INS42 INS43 INS59 INS27 INS8 MOV21 INS42 INS33 INS42 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS33 INS21 INS42 INS42 INS32 INS42 INS33 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 DEL33 DEL41