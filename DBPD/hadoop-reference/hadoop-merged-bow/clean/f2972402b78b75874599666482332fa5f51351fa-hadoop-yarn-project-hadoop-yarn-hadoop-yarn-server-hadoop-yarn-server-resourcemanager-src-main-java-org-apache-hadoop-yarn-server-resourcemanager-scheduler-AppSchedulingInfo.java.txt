Merge r1555021 through r1563041 from trunk.


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-5535@1563042 13f79535-47bb-0310-9956-ffa450edef68

-  private final ActiveUsersManager activeUsersManager;
+  private ActiveUsersManager activeUsersManager;
-        metrics.incrPendingResources(user, request.getNumContainers()
-            - lastRequestContainers, Resources.subtractFrom( // save a clone
-            Resources.multiply(request.getCapability(), request
-                .getNumContainers()), Resources.multiply(lastRequestCapability,
-                lastRequestContainers)));
+        metrics.incrPendingResources(user, request.getNumContainers(),
+            request.getCapability());
+        metrics.decrPendingResources(user, lastRequestContainers,
+            lastRequestCapability);
-    LOG.debug("allocate: user: " + user + ", memory: "
-        + request.getCapability());
-    metrics.allocateResources(user, 1, request.getCapability());
+
+    if (LOG.isDebugEnabled()) {
+      LOG.debug("allocate: applicationId=" + applicationId
+          + " container=" + container.getId()
+          + " host=" + container.getNodeId().toString()
+          + " user=" + user
+          + " resource=" + request.getCapability());
+    }
+    metrics.allocateResources(user, 1, request.getCapability(), true);
-    // Update consumption and track allocations
-    allocate(container);
-
-
-    // Update consumption and track allocations
-    allocate(container);
-
-
-    // Update consumption and track allocations
-    allocate(container);
-
-  synchronized private void allocate(Container container) {
-    // Update consumption and track allocations
-    //TODO: fixme sharad
-    /* try {
-        store.storeContainer(container);
-      } catch (IOException ie) {
-        // TODO fix this. we shouldnt ignore
-      }*/
-    
-    LOG.debug("allocate: applicationId=" + applicationId + " container="
-        + container.getId() + " host="
-        + container.getNodeId().toString());
+  synchronized public void move(Queue newQueue) {
+    QueueMetrics oldMetrics = queue.getMetrics();
+    QueueMetrics newMetrics = newQueue.getMetrics();
+    for (Map<String, ResourceRequest> asks : requests.values()) {
+      ResourceRequest request = asks.get(ResourceRequest.ANY);
+      if (request != null) {
+        oldMetrics.decrPendingResources(user, request.getNumContainers(),
+            request.getCapability());
+        newMetrics.incrPendingResources(user, request.getNumContainers(),
+            request.getCapability());
+      }
+    }
+    oldMetrics.moveAppFrom(this);
+    newMetrics.moveAppTo(this);
+    activeUsersManager.deactivateApplication(user, applicationId);
+    activeUsersManager = newQueue.getActiveUsersManager();
+    activeUsersManager.activateApplication(user, applicationId);
+    this.queue = newQueue;
-            Resources.multiply(request.getCapability(), request
-                .getNumContainers()));
+            request.getCapability());

MOV31 UPD42 INS44 UPD83 UPD42 MOV44 INS8 INS25 INS43 INS42 INS60 INS60 INS21 INS21 INS21 INS21 INS21 INS21 MOV60 INS70 MOV21 MOV21 INS32 INS8 INS42 INS43 INS59 INS43 INS59 INS32 INS32 INS32 INS7 INS32 INS7 INS44 INS32 INS8 INS42 INS42 MOV21 INS9 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS52 INS42 INS42 INS52 INS42 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS22 INS42 INS74 INS42 UPD42 MOV42 UPD42 MOV42 INS60 INS25 INS42 INS42 INS42 INS42 INS42 INS42 INS52 INS42 INS43 INS43 INS43 INS43 INS59 INS27 INS8 INS21 INS21 INS21 UPD42 MOV42 INS42 INS42 INS42 INS42 INS32 INS42 INS33 INS21 INS32 INS32 UPD45 INS42 INS45 MOV32 INS45 MOV32 INS45 UPD45 INS32 UPD42 MOV42 UPD42 MOV42 INS40 INS32 MOV42 MOV42 MOV42 MOV32 MOV32 INS42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 INS42 INS42 INS42 INS42 INS42 MOV32 MOV32 UPD42 UPD42 MOV32 INS42 INS42 UPD42 MOV42 MOV32 INS32 UPD42 UPD42 INS42 INS42 UPD42 MOV42 UPD42 MOV42 DEL83 DEL42 DEL27 DEL42 DEL42 DEL32 DEL32 DEL32 DEL21 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL43 DEL42 DEL44 DEL45 DEL42 DEL45 DEL45 DEL27 DEL32 DEL21 DEL8