MAPREDUCE-7174. Mapreduce example wordmedian should handle generic options. Contributed by Fei Hui.

+import org.apache.hadoop.util.GenericOptionsParser;
-    if (args.length != 2) {
+    Configuration conf = new Configuration();
+    String[] otherArgs =
+        new GenericOptionsParser(conf, args).getRemainingArgs();
+    if (otherArgs.length != 2) {
-    setConf(new Configuration());
-    Configuration conf = getConf();
+    setConf(conf);
-    FileInputFormat.addInputPath(job, new Path(args[0]));
-    FileOutputFormat.setOutputPath(job, new Path(args[1]));
+    FileInputFormat.addInputPath(job, new Path(otherArgs[0]));
+    FileOutputFormat.setOutputPath(job, new Path(otherArgs[1]));

INS26 INS40 MOV25 INS60 INS21 MOV43 INS59 INS5 INS32 INS42 MOV14 INS43 INS85 UPD42 UPD40 INS42 INS42 INS42 INS14 INS42 MOV43 MOV43 INS43 INS42 INS42 UPD42 UPD42 INS42 DEL42 DEL32 DEL21 DEL42