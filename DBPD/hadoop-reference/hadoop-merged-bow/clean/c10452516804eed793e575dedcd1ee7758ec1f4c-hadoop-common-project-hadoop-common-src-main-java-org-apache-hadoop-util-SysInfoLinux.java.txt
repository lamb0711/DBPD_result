Merge remote-tracking branch 'apache-commit/trunk' into HDDS-48

+   *
+   * Wrapper for Long.parseLong() that returns zero if the value is
+   * invalid. Under some circumstances, swapFree in /proc/meminfo can
+   * go negative, reported as a very large decimal value.
+   */
+  private long safeParseLong(String strVal) {
+    long parsedVal;
+    try {
+      parsedVal = Long.parseLong(strVal);
+    } catch (NumberFormatException nfe) {
+      parsedVal = 0;
+    }
+    return parsedVal;
+  }
+  /**
-            ramSizeFree = Long.parseLong(mat.group(2));
+            ramSizeFree = safeParseLong(mat.group(2));
-            swapSizeFree = Long.parseLong(mat.group(2));
+            swapSizeFree = safeParseLong(mat.group(2));

INS31 INS29 INS83 INS39 INS42 INS44 INS8 MOV8 INS65 INS43 INS42 INS60 INS54 INS41 MOV25 MOV60 MOV60 MOV54 MOV60 INS54 MOV21 INS66 INS66 INS66 INS42 INS39 INS59 INS8 INS12 INS42 MOV8 MOV12 MOV8 INS42 INS21 INS44 INS8 MOV60 INS61 INS7 INS43 INS42 INS21 MOV27 MOV8 INS42 INS32 INS42 INS7 MOV21 MOV21 INS42 INS42 INS42 INS42 INS34 MOV32 INS8 MOV25 MOV32 MOV8 MOV32 MOV8 MOV32 INS25 MOV32 MOV8 MOV25 UPD42 UPD42 UPD42 UPD42 DEL42 DEL42 DEL25 DEL61 DEL54 DEL8