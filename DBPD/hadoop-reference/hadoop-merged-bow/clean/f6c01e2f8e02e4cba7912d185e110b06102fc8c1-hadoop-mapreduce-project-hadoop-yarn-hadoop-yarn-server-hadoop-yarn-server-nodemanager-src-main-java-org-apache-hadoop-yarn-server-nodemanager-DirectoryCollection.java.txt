Merge trunk into auto-HA branch


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/branches/HDFS-3042@1333291 13f79535-47bb-0310-9956-ffa450edef68

-import java.util.ArrayList;
-import java.util.Arrays;
+import java.util.concurrent.CopyOnWriteArrayList;
+import java.util.Collections;
-import java.util.ListIterator;
-    localDirs = new ArrayList<String>();
-    localDirs.addAll(Arrays.asList(dirs));
-    failedDirs = new ArrayList<String>();
+    localDirs = new CopyOnWriteArrayList<String>(dirs);
+    failedDirs = new CopyOnWriteArrayList<String>();
-    return localDirs;
+    return Collections.unmodifiableList(localDirs);
-    return failedDirs;
+    return Collections.unmodifiableList(failedDirs);
-    ListIterator<String> it = localDirs.listIterator();
-    while (it.hasNext()) {
-      final String dir = it.next();
+    for (final String dir : localDirs) {
-        it.remove();
+        localDirs.remove(dir);
-    if (numFailures > oldNumFailures) {
-      return true;
-    }
-    return false;
+    return numFailures > oldNumFailures;

UPD40 UPD40 INS70 MOV41 INS32 INS32 INS44 INS42 MOV8 MOV27 INS42 INS42 INS42 INS42 INS42 INS42 INS83 MOV43 INS42 UPD74 INS42 UPD74 UPD43 UPD43 UPD42 UPD42 UPD42 INS42 DEL40 DEL26 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL21 DEL42 DEL42 DEL83 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL9 DEL42 DEL43 DEL74 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL61 DEL8 DEL25 DEL9 DEL41