HDFS-7077. Separate CipherSuite from crypto protocol version. (wang)

+import org.apache.hadoop.crypto.CryptoProtocolVersion;
+  private final CryptoProtocolVersion version;
-  public EncryptionZone(long id, String path,
-      CipherSuite suite, String keyName) {
+  public EncryptionZone(long id, String path, CipherSuite suite,
+      CryptoProtocolVersion version, String keyName) {
+    this.version = version;
+  public CryptoProtocolVersion getVersion() { return version; }
+
-    return new HashCodeBuilder(13, 31).
-      append(id).append(path).
-      append(suite).append(keyName).
+    return new HashCodeBuilder(13, 31)
+        .append(id)
+        .append(path)
+        .append(suite)
+        .append(version)
+        .append(keyName).
+      append(version, rhs.version).
+        ", version=" + version +

INS26 INS40 INS23 INS31 INS83 INS83 INS43 INS59 INS44 INS83 INS43 INS42 INS8 INS42 INS42 INS43 INS42 INS21 INS42 INS41 INS42 INS7 INS42 INS32 INS32 INS22 INS42 MOV32 INS42 MOV32 INS42 INS45 INS42 INS52 INS42 INS42 UPD42 UPD42 INS42 INS40 UPD42 UPD42 UPD40