HDFS-7235. DataNode#transferBlock should report blocks that don't exist using reportBadBlock (yzhang via cmccabe)

+  /**
+   * Check if a block is valid.
+   *
+   * @param b           The block to check.
+   * @param minLength   The minimum length that the block must have.  May be 0.
+   * @param state       If this is null, it is ignored.  If it is non-null, we
+   *                        will check that the replica has this state.
+   *
+   * @throws ReplicaNotFoundException          If the replica is not found
+   *
+   * @throws UnexpectedReplicaStateException   If the replica is not in the 
+   *                                             expected state.
+   */
+  @Override // {@link FsDatasetSpi}
+  public void checkBlock(ExtendedBlock b, long minLength, ReplicaState state)
+      throws ReplicaNotFoundException, UnexpectedReplicaStateException {
+    final BInfo binfo = getBInfo(b);
+    
+    if (binfo == null) {
+      throw new ReplicaNotFoundException(b);
+    }
+    if ((state == ReplicaState.FINALIZED && !binfo.isFinalized()) ||
+        (state != ReplicaState.FINALIZED && binfo.isFinalized())) {
+      throw new UnexpectedReplicaStateException(b,state);
+    }
+  }
+
-    final BInfo binfo = getBInfo(b);
-    return binfo != null && binfo.isFinalized();
+    try {
+      checkBlock(b, 0, ReplicaState.FINALIZED);
+    } catch (IOException e) {
+      return false;
+    }
+    return true;
-    final BInfo binfo = getBInfo(b);
-    return binfo != null && !binfo.isFinalized();  
+    try {
+      checkBlock(b, 0, ReplicaState.RBW);
+    } catch (IOException e) {
+      return false;
+    }
+    return true;

INS31 INS29 MOV78 INS83 INS39 INS42 MOV44 INS44 INS44 INS43 INS43 MOV8 INS78 INS44 INS8 INS65 INS65 INS65 INS65 INS65 INS65 INS39 INS42 INS43 INS42 INS42 INS42 INS25 INS25 INS54 INS41 INS42 INS43 INS42 INS54 INS41 INS66 INS42 INS66 INS42 INS66 INS42 INS66 INS66 INS42 INS66 INS42 INS66 INS66 INS42 INS27 INS8 INS27 INS8 INS8 INS12 INS9 INS42 INS8 INS12 INS9 INS42 INS33 INS53 INS36 INS36 INS53 INS21 INS44 INS8 INS21 INS44 INS8 INS14 MOV27 MOV27 INS14 INS32 INS43 INS42 MOV41 INS32 INS43 INS42 INS41 INS43 INS42 UPD27 INS43 INS42 INS42 INS42 INS42 INS34 INS40 INS42 INS9 INS42 INS42 INS34 INS40 INS42 INS9 INS42 UPD42 INS40 UPD42 INS40 INS42 DEL33 DEL33 DEL41 DEL83 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60