MAPREDUCE-6363. [NNBench] Lease mismatch error when running with multiple mappers. Contributed by Brahma Reddy Battula.

+import org.apache.hadoop.mapreduce.MRJobConfig;
+import org.apache.hadoop.mapreduce.TaskAttemptID;
+import org.apache.hadoop.mapreduce.v2.api.records.TaskId;
+      int taskId =
+          TaskAttemptID.forName(conf.get(MRJobConfig.TASK_ATTEMPT_ID))
+              .getTaskID().getId();
+        String filePrefix = "file_" + taskId + "_";
-          doCreateWriteOp("file_" + hostName + "_", reporter);
+          doCreateWriteOp(filePrefix, reporter);
-          doOpenReadOp("file_" + hostName + "_", reporter);
+          doOpenReadOp(filePrefix, reporter);
-          doRenameOp("file_" + hostName + "_", reporter);
+          doRenameOp(filePrefix, reporter);
-          doDeleteOp("file_" + hostName + "_", reporter);
+          doDeleteOp(filePrefix, reporter);

INS26 INS26 INS26 INS40 INS40 INS40 INS60 MOV25 INS39 INS59 MOV32 INS8 MOV8 INS42 INS32 INS60 MOV25 MOV21 MOV21 INS32 INS42 INS43 INS59 MOV32 INS25 INS32 UPD42 MOV42 INS42 INS42 INS27 MOV32 MOV8 MOV25 INS42 INS42 UPD42 MOV42 UPD42 MOV42 INS32 INS45 INS42 INS45 MOV32 INS25 INS42 INS42 INS42 UPD42 MOV42 INS40 UPD42 INS42 MOV32 MOV8 INS42 UPD42 INS42 UPD42 INS42 DEL45 DEL42 DEL45 DEL27 DEL45 DEL42 DEL45 DEL27 DEL45 DEL42 DEL45 DEL27 DEL45 DEL42 DEL45 DEL27 DEL25 DEL8 DEL25