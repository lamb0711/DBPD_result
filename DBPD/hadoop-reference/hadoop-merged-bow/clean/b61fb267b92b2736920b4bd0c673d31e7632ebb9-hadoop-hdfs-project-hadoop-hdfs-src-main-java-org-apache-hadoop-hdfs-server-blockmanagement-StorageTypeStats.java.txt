HDFS-9390. Block management for maintenance states.

-    if (!(node.isDecommissionInProgress() || node.isDecommissioned())) {
+    if (node.isInService()) {
-    if (!(node.isDecommissionInProgress() || node.isDecommissioned())) {
+    if (node.isInService()) {
-    if (!(node.isDecommissionInProgress() || node.isDecommissioned())) {
+    if (node.isInService()) {
-    if (!(node.isDecommissionInProgress() || node.isDecommissioned())) {
+    if (node.isInService()) {

INS32 INS32 INS32 INS32 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 DEL42 DEL42 DEL32 DEL32 DEL27 DEL36 DEL38 DEL42 DEL42 DEL32 DEL32 DEL27 DEL36 DEL38 DEL42 DEL42 DEL32 DEL32 DEL27 DEL36 DEL38 DEL42 DEL42 DEL32 DEL32 DEL27 DEL36 DEL38