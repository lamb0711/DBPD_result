HADOOP-16129. Misc. bug fixes for KMS Benchmark. Contributed by George Huang.

Signed-off-by: Wei-Chiu Chuang <weichiu@apache.org>

-import org.apache.hadoop.fs.CommonConfigurationKeysPublic;
-import org.slf4j.Logger;
-import org.slf4j.LoggerFactory;
+import org.apache.hadoop.fs.CommonConfigurationKeysPublic;
-import org.apache.log4j.Level;
+import org.slf4j.Logger;
+import org.slf4j.LoggerFactory;
-  private static final String GENERAL_OPTIONS_USAGE = "[-logLevel L] |";
+  private static final String GENERAL_OPTIONS_USAGE = "[-threads int] |" +
+          " [-numops int] | [{-warmup (true|false)}]";
+    try {
+      eek = kp.generateEncryptedKey(encryptionKeyName);
+    } catch (GeneralSecurityException e) {
+      LOG.warn("failed to generate key", e);
+    }
-    // logging level, ERROR by default
-    private Level logLevel;
-
-      logLevel = Level.ERROR;
-      int llIndex = args.indexOf("-logLevel");
-      if (llIndex >= 0) {
-        if (args.size() <= llIndex + 1) {
-          printUsage();
-        }
-        logLevel = Level.toLevel(args.get(llIndex+1), Level.ERROR);
-        args.remove(llIndex+1);
-        args.remove(llIndex);
-      }
-
-        eek = kp.generateEncryptedKey(encryptionKeyName);

MOV26 MOV26 MOV26 INS27 INS54 INS45 INS45 INS8 INS12 MOV21 INS44 INS8 INS43 INS42 INS21 INS42 INS32 INS42 INS42 INS45 INS42 DEL40 DEL26 DEL45 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL42 DEL40 DEL7 DEL21 DEL39 DEL42 DEL42 DEL42 DEL45 DEL32 DEL59 DEL60 DEL42 DEL34 DEL27 DEL42 DEL42 DEL32 DEL42 DEL34 DEL27 DEL27 DEL42 DEL32 DEL21 DEL8 DEL25 DEL42 DEL42 DEL42 DEL42 DEL42 DEL42 DEL34 DEL27 DEL32 DEL40 DEL32 DEL7 DEL21 DEL42 DEL42 DEL42 DEL34 DEL27 DEL32 DEL21 DEL42 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25