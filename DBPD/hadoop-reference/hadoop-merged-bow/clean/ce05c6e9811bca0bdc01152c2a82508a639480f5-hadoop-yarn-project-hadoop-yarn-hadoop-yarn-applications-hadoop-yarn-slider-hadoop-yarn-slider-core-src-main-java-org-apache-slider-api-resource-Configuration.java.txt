YARN-6545. Followup fix for YARN-6405. Contributed by Jian He

-    if (name == null) {
-      return defaultValue;
-    }
-    String value = properties.get(name.trim());
+    String value = getProperty(name);
-    if (name == null) {
-      return defaultValue;
-    }
-    String value = properties.get(name.trim());
+    String value = getProperty(name);
-    if (name == null) {
-      return defaultValue;
-    }
-    String value = properties.get(name.trim());
+    String value = getProperty(name);
-    if (name == null) {
+    String value = getProperty(name);
+    if (StringUtils.isEmpty(value)) {
-    return properties.get(name.trim());
+    return value;
-    if (name == null) {
-      return null;
-    }
-    if (name == null) {
-      return null;
-    }

INS31 INS31 MOV83 MOV43 MOV42 MOV44 MOV44 INS8 INS83 MOV43 INS42 MOV44 MOV8 INS60 INS60 MOV25 INS41 INS43 MOV59 MOV43 MOV43 INS59 INS32 INS42 INS42 INS42 INS32 INS42 INS42 INS42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 INS42 INS42 DEL42 DEL33 DEL27 DEL42 DEL41 DEL8 DEL25 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL33 DEL27 DEL42 DEL41 DEL8 DEL25 DEL60 DEL42 DEL33 DEL27 DEL42 DEL41 DEL8 DEL25 DEL42 DEL42 DEL32 DEL42 DEL33 DEL27 DEL33 DEL31 DEL42 DEL33 DEL27 DEL42 DEL41 DEL8 DEL25 DEL83 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL41 DEL8 DEL31 DEL42 DEL33 DEL27 DEL33 DEL41 DEL8 DEL25