HDDS-190. Improve shell error message for unrecognized option.
Contributed by Elek, Marton.

-import java.io.IOException;
-import java.net.URISyntaxException;
-import org.apache.commons.cli.CommandLine;
-import org.apache.hadoop.ozone.client.*;
-import org.apache.hadoop.ozone.client.rest.OzoneException;
+import org.apache.hadoop.ozone.client.OzoneBucket;
+import org.apache.hadoop.ozone.client.OzoneClientException;
+import org.apache.hadoop.ozone.client.OzoneClientUtils;
+import org.apache.hadoop.ozone.client.OzoneKeyDetails;
+import org.apache.hadoop.ozone.client.OzoneVolume;
+import picocli.CommandLine.Command;
+import picocli.CommandLine.Parameters;
+
+@Command(name = "-infoKey",
+    description = "returns information about an existing key")
-  private String volumeName;
-  private String bucketName;
-  private String keyName;
+  @Parameters(arity = "1..1", description = Shell.OZONE_KEY_URI_DESCRIPTION)
+  private String uri;
-   *
-   * @param cmd - CommandLine
-   * @throws IOException
-   * @throws OzoneException
-   * @throws URISyntaxException
-  protected void execute(CommandLine cmd)
-      throws IOException, OzoneException, URISyntaxException {
-    if (!cmd.hasOption(Shell.INFO_KEY)) {
-      throw new OzoneClientException("Incorrect call : infoKey is missing");
-    }
-
-    String ozoneURIString = cmd.getOptionValue(Shell.INFO_KEY);
-    URI ozoneURI = verifyURI(ozoneURIString);
+  public Void call() throws Exception {
+    URI ozoneURI = verifyURI(uri);
-    volumeName = path.getName(0).toString();
-    bucketName = path.getName(1).toString();
+    String volumeName = path.getName(0).toString();
+    String bucketName = path.getName(1).toString();
-    keyName = ozoneURIString.substring(ozoneURIString.indexOf(searchString) +
+    String keyName =
+        uri.substring(uri.indexOf(searchString) +
-    if (cmd.hasOption(Shell.VERBOSE)) {
+    if (isVerbose()) {
+    return null;

MOV26 MOV26 MOV26 INS26 INS26 UPD40 UPD40 UPD40 UPD40 UPD40 INS40 INS40 INS77 INS23 INS42 INS80 INS80 INS77 MOV83 MOV43 MOV59 UPD83 INS43 INS42 UPD43 INS42 INS45 INS42 INS45 INS42 INS80 INS80 UPD42 UPD42 MOV42 UPD42 INS60 INS60 INS60 INS41 INS42 INS45 INS42 INS40 MOV43 INS59 MOV43 INS59 MOV43 INS59 INS33 INS42 MOV32 INS42 MOV32 INS42 INS32 UPD42 UPD42 UPD42 MOV42 MOV42 MOV27 UPD42 DEL83 DEL42 DEL59 DEL23 DEL83 DEL42 DEL59 DEL23 DEL23 DEL42 DEL66 DEL65 DEL42 DEL65 DEL42 DEL65 DEL42 DEL65 DEL39 DEL42 DEL43 DEL42 DEL44 DEL42 DEL43 DEL42 DEL43 DEL42 DEL42 DEL40 DEL32 DEL38 DEL42 DEL43 DEL45 DEL14 DEL53 DEL8 DEL25 DEL42 DEL42 DEL42 DEL40 DEL32 DEL59 DEL60 DEL42 DEL7 DEL21 DEL42 DEL7 DEL21 DEL42 DEL32 DEL7 DEL21 DEL42 DEL40