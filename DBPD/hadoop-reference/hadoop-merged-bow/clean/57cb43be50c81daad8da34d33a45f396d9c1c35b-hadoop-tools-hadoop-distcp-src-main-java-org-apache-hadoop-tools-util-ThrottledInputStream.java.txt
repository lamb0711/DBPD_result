HADOOP-11354. ThrottledInputStream doesn't perform effective throttling. Contributed by Ted Yu.

-    if (getBytesPerSec() > maxBytesPerSec) {
+    while (getBytesPerSec() > maxBytesPerSec) {

INS61 MOV27 MOV8 DEL25