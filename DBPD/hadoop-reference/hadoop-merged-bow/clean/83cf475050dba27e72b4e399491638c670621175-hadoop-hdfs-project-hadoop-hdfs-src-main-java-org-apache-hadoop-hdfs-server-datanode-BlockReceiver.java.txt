HDFS-744. Support hsync in HDFS. Contributed by Lars Hofhans


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1344419 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.hadoop.hdfs.server.common.Util;
+  private boolean syncOnClose;
+
-
+    if (syncOnClose && (out != null || checksumOut != null)) {
+      datanode.metrics.incrFsyncCount();      
+    }
-        if (datanode.getDnConf().syncOnClose && (cout instanceof FileOutputStream)) {
+        if (syncOnClose && (cout instanceof FileOutputStream)) {
+          long start = Util.now();
+          datanode.metrics.addFsync(Util.now() - start);
-        if (datanode.getDnConf().syncOnClose && (out instanceof FileOutputStream)) {
+        if (syncOnClose && (out instanceof FileOutputStream)) {
+          long start = Util.now();
+          datanode.metrics.addFsync(Util.now() - start);
-  void flush() throws IOException {
+  void flushOrSync(boolean isSync) throws IOException {
+    if (isSync && (out != null || checksumOut != null)) {
+      datanode.metrics.incrFsyncCount();      
+    }
+      if (isSync && (cout instanceof FileOutputStream)) {
+        long start = Util.now();
+        ((FileOutputStream)cout).getChannel().force(true);
+        datanode.metrics.addFsync(Util.now() - start);
+      }
+      if (isSync && (out instanceof FileOutputStream)) {
+        long start = Util.now();
+        ((FileOutputStream)out).getChannel().force(true);
+        datanode.metrics.addFsync(Util.now() - start);
+      }
-      header.getDataLen(), endOfHeader);
+      header.getDataLen(),
+      header.getSyncBlock(),
+      endOfHeader);
-      boolean lastPacketInBlock, int len, int endOfHeader) throws IOException {
+      boolean lastPacketInBlock, int len, boolean syncBlock,
+      int endOfHeader) throws IOException {
+                " syncBlock " + syncBlock +
-    
+    // make sure the block gets sync'ed upon close
+    this.syncOnClose |= syncBlock && lastPacketInBlock;
+
+      // flush unless close() would flush anyway
+      if (syncBlock && !lastPacketInBlock) {
+        flushOrSync(true);
+      }
-          /// flush entire packet
-          flush();
+          /// flush entire packet, sync unless close() will sync
+          flushOrSync(syncBlock && !lastPacketInBlock);
+      syncOnClose = datanode.getDnConf().syncOnClose;
-          // Finalize the block. Does this fsync()?
+          // Finalize the block.

INS26 INS40 INS23 INS83 INS39 INS59 UPD42 INS44 INS44 INS42 INS25 INS39 INS42 INS25 INS25 INS39 INS42 INS21 INS21 INS27 INS8 INS27 MOV27 INS8 INS27 INS8 INS7 INS7 INS42 INS36 INS21 INS42 INS36 INS21 MOV21 MOV25 INS42 INS33 MOV21 INS25 INS32 INS22 INS27 INS25 INS42 MOV22 INS27 INS32 INS27 INS32 INS27 INS27 INS8 INS42 INS42 INS52 INS42 INS42 INS42 INS27 INS8 INS27 INS27 INS40 INS42 MOV27 INS27 INS40 INS42 INS42 INS36 INS60 INS21 INS21 INS42 INS36 INS60 INS21 INS21 INS42 INS38 INS21 INS42 INS33 INS42 INS33 INS42 INS33 INS62 INS39 INS59 INS32 INS32 INS62 INS39 INS59 INS32 INS32 INS45 INS42 INS42 INS32 INS42 INS60 INS21 INS42 INS60 INS21 INS42 INS43 INS42 INS32 INS32 INS42 INS9 INS40 INS42 INS27 INS42 INS43 INS42 INS32 INS32 INS42 INS9 INS40 INS42 INS27 INS42 INS9 INS39 INS59 INS32 INS39 INS59 INS32 INS42 INS42 INS42 INS36 INS42 INS32 INS42 INS42 INS42 INS42 INS36 INS42 INS32 INS42 INS42 INS32 INS40 INS42 INS27 INS42 INS32 INS40 INS42 INS27 INS11 INS42 INS42 INS11 INS42 INS42 UPD42 INS27 INS42 INS42 INS32 INS42 INS42 INS42 INS32 INS42 INS43 INS42 INS43 INS42 INS42 INS38 INS42 INS42 INS42 INS42 INS42 INS42 INS42 DEL42 DEL42 DEL32 DEL42 DEL22