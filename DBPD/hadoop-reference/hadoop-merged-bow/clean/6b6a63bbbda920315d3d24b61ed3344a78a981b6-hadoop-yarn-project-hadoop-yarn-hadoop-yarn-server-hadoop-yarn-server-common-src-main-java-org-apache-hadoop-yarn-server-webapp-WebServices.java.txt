Merge commit '456e901a4c5c639267ee87b8e5f1319f256d20c2' (HDFS-6407. Add sorting and pagination in the datanode tab of the NN Web UI. Contributed by Haohui Mai.) into HDFS-7285-merge

+import org.apache.commons.lang.math.LongRange;
+import org.apache.hadoop.yarn.exceptions.ApplicationAttemptNotFoundException;
+import org.apache.hadoop.yarn.exceptions.ApplicationNotFoundException;
+import org.apache.hadoop.yarn.exceptions.ContainerNotFoundException;
-    long num = 0;
-    boolean checkCount = false;
-    boolean checkStart = false;
-    long countNum = 0;
+    long countNum = Long.MAX_VALUE;
-      checkCount = true;
-      checkStart = true;
-      checkStart = true;
+    final GetApplicationsRequest request =
+        GetApplicationsRequest.newInstance();
+    request.setLimit(countNum);
+    request.setStartRange(new LongRange(sBegin, sEnd));
-        GetApplicationsRequest request = GetApplicationsRequest.newInstance();
-            return appBaseProt.getApplications(
-                GetApplicationsRequest.newInstance()).getApplicationList();
+            return appBaseProt.getApplications(request).getApplicationList();
-      if (checkCount && num == countNum) {
-        break;
-      }
-
-      if (checkStart
-          && (appReport.getStartTime() < sBegin || appReport.getStartTime() > sEnd)) {
-        continue;
-      }
-      num++;
-      if (e.getCause() instanceof AuthorizationException) {
-        throw new ForbiddenException(e.getCause());
-      } else {
-        throw new WebApplicationException(e.getCause());
-      }
+      rewrapAndThrowThrowable(e.getCause());
-      if (e instanceof AuthorizationException) {
-        throw new ForbiddenException(e);
-      } else {
-        throw new WebApplicationException(e);
-      }
+      rewrapAndThrowThrowable(e);
+    }
+  }
+
+  private static void rewrapAndThrowThrowable(Throwable t) {
+    if (t instanceof AuthorizationException) {
+      throw new ForbiddenException(t);
+    } else if (t instanceof ApplicationNotFoundException ||
+        t instanceof ApplicationAttemptNotFoundException ||
+        t instanceof ContainerNotFoundException) {
+      throw new NotFoundException(t);
+    } else {
+      throw new WebApplicationException(t);

INS26 INS26 INS26 INS26 INS40 INS40 INS40 INS40 INS31 INS31 MOV60 MOV83 MOV83 MOV39 MOV42 MOV44 INS8 INS83 INS83 INS39 INS42 INS44 INS8 MOV60 INS21 INS21 MOV25 INS43 INS42 INS25 INS83 INS32 INS32 MOV62 INS8 INS8 INS42 MOV62 MOV8 INS25 UPD42 INS40 INS42 INS42 INS42 INS42 INS42 INS14 INS21 INS21 INS42 INS27 MOV8 MOV8 INS43 INS42 INS42 INS32 INS32 INS62 INS62 INS62 INS42 INS42 MOV32 INS42 INS42 INS42 UPD42 MOV42 UPD43 MOV43 INS42 INS43 INS42 INS43 UPD42 UPD42 MOV42 INS42 UPD43 UPD42 INS42 UPD42 UPD42 MOV42 DEL39 DEL42 DEL9 DEL59 DEL60 DEL39 DEL42 DEL9 DEL59 DEL60 DEL34 DEL39 DEL42 DEL34 DEL59 DEL60 DEL42 DEL9 DEL7 DEL21 DEL42 DEL9 DEL7 DEL21 DEL42 DEL9 DEL7 DEL21 DEL42 DEL32 DEL42 DEL42 DEL42 DEL27 DEL27 DEL10 DEL8 DEL25 DEL42 DEL42 DEL42 DEL32 DEL42 DEL27 DEL42 DEL42 DEL32 DEL42 DEL27 DEL27 DEL36 DEL27 DEL18 DEL8 DEL25 DEL42 DEL37 DEL21 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL8 DEL62 DEL43 DEL42 DEL14 DEL53 DEL8 DEL25 DEL8 DEL25 DEL8 DEL31