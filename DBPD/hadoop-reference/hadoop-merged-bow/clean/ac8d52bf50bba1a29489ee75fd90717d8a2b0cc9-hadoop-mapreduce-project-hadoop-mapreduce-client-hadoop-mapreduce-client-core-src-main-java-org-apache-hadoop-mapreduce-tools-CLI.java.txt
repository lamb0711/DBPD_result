MAPREDUCE-4431. mapred command should print the reason on killing already completed jobs. Contributed by Devaraj K.

-          job.killJob();
-          System.out.println("Killed job " + jobid);
-          exitCode = 0;
+          JobStatus jobStatus = job.getStatus();
+          if (jobStatus.getState() == JobStatus.State.FAILED) {
+            System.out.println("Could not mark the job " + jobid
+                + " as killed, as it has already failed.");
+            exitCode = -1;
+          } else if (jobStatus.getState() == JobStatus.State.KILLED) {
+            System.out
+                .println("The job " + jobid + " has already been killed.");
+            exitCode = -1;
+          } else if (jobStatus.getState() == JobStatus.State.SUCCEEDED) {
+            System.out.println("Could not kill the job " + jobid
+                + ", as it has already succeeded.");
+            exitCode = -1;
+          } else {
+            job.killJob();
+            System.out.println("Killed job " + jobid);
+            exitCode = 0;
+          }

INS25 MOV27 MOV8 INS8 INS60 INS25 INS43 INS59 INS27 INS8 INS25 INS42 INS42 INS32 INS32 INS40 INS21 INS21 INS27 INS8 MOV25 INS42 INS42 INS42 INS42 INS32 INS7 INS32 INS40 INS21 INS21 INS27 INS8 INS40 INS42 INS27 INS42 INS38 INS42 INS42 INS32 INS7 INS32 INS40 INS21 INS21 INS45 INS42 INS45 INS34 INS40 INS42 INS27 INS42 INS38 INS42 INS42 INS32 INS7 INS45 INS42 INS45 INS34 INS40 INS42 INS27 INS42 INS38 INS45 INS42 INS45 INS34