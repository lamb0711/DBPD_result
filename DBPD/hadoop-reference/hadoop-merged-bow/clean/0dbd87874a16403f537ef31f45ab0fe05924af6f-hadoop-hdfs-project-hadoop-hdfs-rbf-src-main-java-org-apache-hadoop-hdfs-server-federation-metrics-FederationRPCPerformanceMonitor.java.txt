HDFS-14388. RBF: Prevent loading metric system when disabled. Contributed by Inigo Goiri.

-    metrics.incrProxyOpFailureCommunicate();
+    if (metrics != null) {
+      metrics.incrProxyOpFailureCommunicate();
+    }
-    metrics.incrProxyOpFailureClientOverloaded();
+    if (metrics != null) {
+      metrics.incrProxyOpFailureClientOverloaded();
+    }
-    metrics.incrProxyOpNotImplemented();
+    if (metrics != null) {
+      metrics.incrProxyOpNotImplemented();
+    }
-    metrics.incrProxyOpRetries();
+    if (metrics != null) {
+      metrics.incrProxyOpRetries();
+    }
-    metrics.incrProxyOpNoNamenodes();
+    if (metrics != null) {
+      metrics.incrProxyOpNoNamenodes();
+    }
-    metrics.incrRouterFailureStateStore();
+    if (metrics != null) {
+      metrics.incrRouterFailureStateStore();
+    }
-    metrics.incrRouterFailureSafemode();
+    if (metrics != null) {
+      metrics.incrRouterFailureSafemode();
+    }
-    metrics.incrRouterFailureReadOnly();
+    if (metrics != null) {
+      metrics.incrRouterFailureReadOnly();
+    }
-    metrics.incrRouterFailureLocked();
+    if (metrics != null) {
+      metrics.incrRouterFailureLocked();
+    }

INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS25 INS25 INS25 INS25 INS25 INS25 INS25 INS25 INS25 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS27 MOV8 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS33 INS42 INS33