HDFS-13843. RBF: Add optional parameter -d for detailed listing of mount points. Contributed by Ayush Saxena.

-      return "\t[-ls <path>]";
+      return "\t[-ls [-d] <path>]";
-      if (arg.length > 2) {
+      if (arg.length > 3) {
-            "Too many arguments, Max=1 argument allowed");
+            "Too many arguments, Max=2 argument allowed");
-        if (argv.length > 1) {
-          listMounts(argv[i]);
-        } else {
-          listMounts("/");
-        }
+        listMounts(argv, i);
-  public void listMounts(String path) throws IOException {
+  public void listMounts(String[] argv, int i) throws IOException {
+    String path;
+    boolean detail = false;
+    if (argv.length == 1) {
+      path = "/";
+    } else if (argv[i].equals("-d")) { // Check if -d parameter is specified.
+      detail = true;
+      if (argv.length == 2) {
+        path = "/"; // If no path is provide with -ls -d.
+      } else {
+        path = argv[++i];
+      }
+    } else {
+      path = argv[i];
+    }
+
-    printMounts(entries);
+    printMounts(entries, detail);
-  private static void printMounts(List<MountTable> entries) {
+  private static void printMounts(List<MountTable> entries, boolean detail) {
-    System.out.println(String.format(
-        "%-25s %-25s %-25s %-25s %-25s %-25s",
-        "Source", "Destinations", "Owner", "Group", "Mode", "Quota/Usage"));
+    if (detail) {
+      System.out.println(
+          String.format("%-25s %-25s %-25s %-25s %-10s %-30s %-10s %-10s %-15s",
+              "Source", "Destinations", "Owner", "Group", "Mode", "Quota/Usage",
+              "Order", "ReadOnly", "FaultTolerant"));
+    } else {
+      System.out.println(String.format("%-25s %-25s %-25s %-25s %-10s %-30s",
+          "Source", "Destinations", "Owner", "Group", "Mode", "Quota/Usage"));
+    }
-      System.out.print(String.format(" %-25s %-25s %-25s",
+      System.out.print(String.format(" %-25s %-25s %-10s",
-      System.out.println(String.format(" %-25s", entry.getQuota()));
+      System.out.print(String.format(" %-30s", entry.getQuota()));
+
+      if (detail) {
+        System.out.print(String.format(" %-10s", entry.getDestOrder()));
+
+        System.out.print(
+            String.format(" %-10s", entry.isReadOnly() ? "Read-Only" : ""));
+
+        System.out.println(String.format(" %-15s",
+            entry.isFaultTolerant() ? "Fault-Tolerant" : ""));
+      }

INS44 INS44 INS5 INS42 INS39 INS42 INS60 INS60 INS25 INS39 INS42 INS25 INS43 INS85 MOV43 INS59 INS39 INS59 INS27 INS8 INS25 INS42 INS8 INS8 INS42 INS42 INS42 INS9 INS40 INS34 INS21 INS32 INS8 MOV8 INS42 INS21 MOV21 INS25 INS7 INS2 INS42 INS45 INS21 INS25 INS32 INS42 INS8 UPD34 INS42 INS45 INS42 INS42 INS7 INS27 INS8 INS8 INS7 INS40 INS42 INS32 UPD42 INS21 INS21 INS21 INS42 INS9 INS40 INS34 INS21 INS21 INS42 MOV2 INS42 INS42 INS45 INS45 INS45 INS45 INS45 INS45 INS45 INS45 INS45 INS45 UPD45 UPD45 UPD45 INS32 INS32 INS32 UPD45 UPD45 MOV8 INS7 INS7 INS40 INS42 INS32 INS40 INS42 INS32 INS40 INS42 INS32 INS42 INS45 INS42 INS2 INS42 INS42 INS45 INS32 INS42 INS42 INS45 INS16 INS42 INS42 INS45 INS16 INS42 INS38 INS42 INS42 INS32 INS45 INS45 INS32 INS45 INS45 INS42 INS42 INS42 INS42 INS42 INS42 INS42 DEL45 DEL40 DEL34 DEL27 DEL25 DEL8 DEL42 DEL42 DEL32