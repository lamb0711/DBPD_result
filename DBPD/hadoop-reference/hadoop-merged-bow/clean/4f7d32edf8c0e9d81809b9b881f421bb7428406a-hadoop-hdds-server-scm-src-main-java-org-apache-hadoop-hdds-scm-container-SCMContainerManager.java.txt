HDDS-1081. CLOSING state containers should not be added to pipeline on SCM start.
Contributed by Lokesh Jain.

-      if (container.isOpen()) {
+      if (container.getState() == LifeCycleState.OPEN) {

INS27 INS32 INS40 MOV42 UPD42 MOV42 DEL32