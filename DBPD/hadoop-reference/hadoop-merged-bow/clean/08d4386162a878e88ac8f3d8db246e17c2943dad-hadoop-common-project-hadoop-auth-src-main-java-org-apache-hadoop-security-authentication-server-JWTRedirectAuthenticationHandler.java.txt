HADOOP-11864. JWTRedirectAuthenticationHandler breaks java8 javadocs. (Larry McCay via stevel)

- * <p/>
+ * <p>
+ * </p>
-   * @param pk
+   * @param pk publicKey for the token signtature verification
-   * <p/>
+   * <p>
-   *
+   * </p>
-      String loginURL = constructLoginURL(request, response);
+      String loginURL = constructLoginURL(request);
-        String loginURL = constructLoginURL(request, response);
+        String loginURL = constructLoginURL(request);
-   * @param serializedJWT
-   * @param req
+   * @param req servlet request to get the JWT token from
-   * @param request
-   * @param response
+   * @param request for getting the original request URL
-  protected String constructLoginURL(HttpServletRequest request,
-      HttpServletResponse response) {
+  protected String constructLoginURL(HttpServletRequest request) {
-   * @param jwtToken
+   * @param jwtToken the token to validate
-   * @throws AuthenticationException
-   * @param jwtToken
-   * @throws AuthenticationException
+   * @param jwtToken the token that contains the signature to be validated
+   * @return valid true if signature verifies successfully; false otherwise
-   * @param jwtToken
-   * @throws AuthenticationException
+   * @param jwtToken the token that contains the expiration date to validate
+   * @return valid true if the token has not expired; false otherwise

UPD66 INS66 UPD65 UPD65 INS66 UPD66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 DEL42 DEL42 DEL42 DEL65 DEL42 DEL65 DEL42 DEL43 DEL42 DEL44 DEL42 DEL65 DEL42 DEL42