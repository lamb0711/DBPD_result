HDDS-476. Add Pipeline reports to make pipeline active on SCM restart.
Contributed by Mukul Kumar Singh.

-    int count = getReplicationCount(factor);
-        if (newNodesList.size() == count) {
+        if (newNodesList.size() == factor.getNumber()) {
-          PipelineID pipelineID = PipelineID.randomId();
+          // Standalone pipeline use node id as pipeline
+          PipelineID pipelineID =
+                  PipelineID.valueOf(newNodesList.get(0).getUuid());
-              count, pipelineID);
+              factor.getNumber(), pipelineID);
+  public void processPipelineReport(Pipeline pipeline, DatanodeDetails dn) {
+    super.processPipelineReport(pipeline, dn);
+    standAloneMembers.add(dn);
+  }
+
+  public synchronized boolean finalizePipeline(Pipeline pipeline) {
+    activePipelines.get(pipeline.getFactor().ordinal())
+            .removePipeline(pipeline.getId());
+    return false;
+  }
+
-
-  /**
-   * list members in the pipeline .
-   *
-   * @param pipelineID
-   * @return the datanode
-   */
-  @Override
-  public List<DatanodeDetails> getMembers(PipelineID pipelineID)
-      throws IOException {
-    return null;
-  }
-
-  /**
-   * Update the datanode list of the pipeline.
-   *
-   * @param pipelineID
-   * @param newDatanodes
-   */
-  @Override
-  public void updatePipeline(PipelineID pipelineID, List<DatanodeDetails>
-      newDatanodes) throws IOException {
-
-  }

INS31 INS31 MOV83 INS39 INS42 MOV44 INS44 INS8 MOV83 INS83 UPD39 MOV39 UPD42 MOV42 MOV44 INS8 UPD43 UPD42 MOV43 INS42 INS21 INS21 UPD43 UPD42 INS21 INS41 UPD42 INS48 INS32 UPD42 INS32 INS9 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS32 UPD42 MOV42 INS42 INS32 UPD42 MOV42 INS42 INS42 INS34 DEL39 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL66 DEL65 DEL42 DEL65 DEL66 DEL65 DEL29 DEL42 DEL78 DEL42 DEL43 DEL74 DEL42 DEL42 DEL43 DEL33 DEL41 DEL8 DEL31 DEL66 DEL65 DEL42 DEL65 DEL42 DEL65 DEL29 DEL42 DEL78 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL44 DEL42 DEL43 DEL8 DEL31