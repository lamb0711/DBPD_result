Fix potential FSImage corruption. Contributed by Ekanth Sethuramalingam & Arpit Agarwal.
-    GROUP(MODE.BITS, 25),
-    USER(GROUP.BITS, 23);
+    GROUP(MODE.BITS, 24),
+    USER(GROUP.BITS, 24);

UPD34 UPD34