Merge branch 'trunk' into HADOOP-12756

+    boolean needShutdown = false;
-        shutdownExecutor();
+        scheduler.shutdown();
+        needShutdown = true;
+    // no need to hold lock while shutting down executor.
+    if (needShutdown) {
+      shutdownExecutor();
+    }
-    scheduler.shutdown();
+    boolean needShutdown = false;
-        this.blockMover.setExitFlag();
-        shutdownExecutor();
+        this.blockMover.setExitFlag();
+        scheduler.shutdown();
+        needShutdown = true;
+    // no need to hold lock while shutting down executor.
+    if (needShutdown) {
+      shutdownExecutor();
+    }
-                planFile, planID);
-        try {
-          for (Map.Entry<VolumePair, DiskBalancerWorkItem> entry :
-              workMap.entrySet()) {
-            blockMover.copyBlocks(entry.getKey(), entry.getValue());
-          }
-        } finally {
-          blockMover.setExitFlag();
+            planFile, planID);
+        for (Map.Entry<VolumePair, DiskBalancerWorkItem> entry :
+            workMap.entrySet()) {
+          blockMover.setRunnable();
+          blockMover.copyBlocks(entry.getKey(), entry.getValue());
-        LOG.info("Maximum error count exceeded. Error count: {} Max error:{} "
-            , item.getErrorCount(), item.getMaxDiskErrors());
+        LOG.info("Maximum error count exceeded. Error count: {} Max error:{} ",
+            item.getErrorCount(), item.getMaxDiskErrors());
-              break;
+              this.setExitFlag();
+              continue;
-              break;
+              this.setExitFlag();
+              continue;
-              break;
+              this.setExitFlag();
+              continue;
-              break;
+              continue;
-              break;
+              this.setExitFlag();
+              continue;

MOV31 MOV29 UPD83 UPD42 INS60 INS25 INS60 INS25 MOV21 MOV21 MOV25 MOV21 INS39 INS59 INS42 INS8 INS39 INS59 INS42 INS8 INS7 INS42 INS9 MOV21 INS42 INS9 MOV21 MOV22 INS32 MOV21 UPD42 MOV42 UPD42 MOV42 INS14 MOV21 INS21 INS21 INS21 MOV43 INS1 INS7 INS32 INS7 INS31 INS42 INS9 INS42 INS42 INS42 INS9 MOV78 INS83 INS39 INS42 MOV8 MOV21 MOV21 INS21 INS21 INS18 INS21 INS18 INS21 INS18 INS18 INS21 INS18 INS32 INS32 INS32 INS32 INS32 INS42 INS42 INS52 INS42 INS52 INS42 INS52 INS42 INS52 INS42 DEL32 DEL8 DEL54 DEL83 DEL39 DEL42 DEL42 DEL42 DEL1 DEL14 DEL32 DEL7 DEL21 DEL8 DEL31 DEL10 DEL10 DEL10 DEL10 DEL10