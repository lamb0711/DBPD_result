HDFS-9110. Use Files.walkFileTree in NNUpgradeUtil#doPreUpgrade for better efficiency. Contributed by Charlie Helin.

-import java.io.FilenameFilter;
+import java.nio.file.FileVisitOption;
+import java.nio.file.FileVisitResult;
-import java.util.List;
+import java.nio.file.Path;
+import java.nio.file.SimpleFileVisitor;
+import java.nio.file.attribute.BasicFileAttributes;
+import java.util.Collections;
-import org.apache.hadoop.io.IOUtils;
-    final File curDir = sd.getCurrentDir();
-    final File tmpDir = sd.getPreviousTmp();
-    List<String> fileNameList = IOUtils.listDirectory(tmpDir, new FilenameFilter() {
-      @Override
-      public boolean accept(File dir, String name) {
-        return dir.equals(tmpDir)
-            && name.startsWith(NNStorage.NameNodeFile.EDITS.getName());
-      }
-    });
+    final Path curDir = sd.getCurrentDir().toPath();
+    final Path tmpDir = sd.getPreviousTmp().toPath();
-    for (String s : fileNameList) {
-      File prevFile = new File(tmpDir, s);
-      File newFile = new File(curDir, prevFile.getName());
-      Files.createLink(newFile.toPath(), prevFile.toPath());
-    }
+    Files.walkFileTree(tmpDir,
+      /* do not follow links */ Collections.<FileVisitOption>emptySet(),
+        1, new SimpleFileVisitor<Path>() {
+
+          @Override
+          public FileVisitResult visitFile(Path file, BasicFileAttributes attrs)
+              throws IOException {
+
+            String name = file.getFileName().toString();
+
+            if (Files.isRegularFile(file)
+                && name.startsWith(NNStorage.NameNodeFile.EDITS.getName())) {
+
+              Path newFile = curDir.resolve(name);
+              Files.createLink(newFile, file);
+            }
+
+            return super.visitFile(file, attrs);
+          }
+        }
+    );

MOV26 MOV26 INS26 INS26 INS26 UPD40 INS40 UPD40 INS40 INS40 UPD40 INS21 UPD43 UPD43 INS32 UPD42 INS32 UPD42 INS32 INS42 INS42 INS42 INS32 INS34 INS14 MOV32 INS42 MOV32 INS42 UPD42 MOV42 INS43 UPD42 MOV42 INS74 INS1 INS42 INS43 INS43 INS31 UPD42 MOV42 INS42 MOV78 MOV83 INS43 INS42 MOV44 INS44 INS43 INS8 UPD42 MOV42 UPD43 UPD42 INS43 UPD42 MOV42 INS42 MOV60 INS25 INS41 UPD42 INS42 MOV43 MOV27 INS8 INS48 UPD42 INS32 MOV60 INS21 INS42 INS42 INS42 INS32 INS42 UPD42 UPD42 UPD42 UPD43 INS32 INS42 INS42 UPD42 INS32 MOV42 MOV42 MOV42 UPD42 MOV42 INS42 UPD42 MOV42 UPD42 MOV42 DEL42 DEL43 DEL42 DEL43 DEL42 DEL42 DEL14 DEL42 DEL43 DEL42 DEL32 DEL14 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL43 DEL39 DEL42 DEL43 DEL42 DEL44 DEL41 DEL8 DEL31 DEL1 DEL14 DEL32 DEL59 DEL60 DEL44 DEL42 DEL32 DEL42 DEL42 DEL32 DEL32 DEL21 DEL8 DEL70