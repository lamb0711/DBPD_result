Merge branch 'trunk' into HADOOP-12756

+  /**Path component added when the proxy redirects the connection.*/
+  public static final String REDIRECT = "redirect/";
-   * @param id the application id to use.
-   * @return the base path to that application through the proxy.
+   *
+   * @param id the application id to use
+   * @return the base path to that application through the proxy
-    if(id == null) {
-      throw new IllegalArgumentException("Application id cannot be null ");
-    }
-    return ujoin(PROXY_BASE, uriEncode(id));
+    return getPath(id, false);
-   * @param id the application id to use.
-   * @param path the rest of the path to the application.
-   * @return the base path to that application through the proxy.
+   *
+   * @param id the application id to use
+   * @param redirected whether the path should contain the redirect component
+   * @return the base path to that application through the proxy
+   */
+  public static String getPath(ApplicationId id, boolean redirected) {
+    if (id == null) {
+      throw new IllegalArgumentException("Application id cannot be null ");
+    }
+
+    if (redirected) {
+      return ujoin(PROXY_BASE, REDIRECT, uriEncode(id));
+    } else {
+      return ujoin(PROXY_BASE, uriEncode(id));
+    }
+  }
+
+  /**
+   * Get the proxied path for an application.
+   *
+   * @param id the application id to use
+   * @param path the rest of the path to the application
+   * @return the base path to that application through the proxy
-    if(path == null) {
-      return getPath(id);
+    return getPath(id, path, false);
+  }
+
+  /**
+   * Get the proxied path for an application.
+   *
+   * @param id the application id to use
+   * @param path the rest of the path to the application
+   * @param redirected whether the path should contain the redirect component
+   * @return the base path to that application through the proxy
+   */
+  public static String getPath(ApplicationId id, String path,
+      boolean redirected) {
+    if (path == null) {
+      return getPath(id, redirected);
-      return ujoin(getPath(id), path);
+      return ujoin(getPath(id, redirected), path);

INS23 INS31 INS31 INS29 INS83 INS83 INS83 INS43 INS59 INS29 INS83 INS83 INS43 INS42 INS44 INS8 INS44 INS29 INS83 INS83 INS43 INS42 INS44 INS44 INS8 INS44 INS65 INS42 INS42 INS45 MOV65 INS65 INS65 INS42 INS43 INS42 INS41 INS65 INS65 INS39 INS42 INS25 INS65 INS65 INS65 INS65 INS42 INS43 INS42 INS43 INS42 INS41 INS65 INS39 INS42 INS66 INS42 INS66 INS66 INS42 INS32 INS66 UPD66 INS42 INS66 UPD66 INS42 INS8 INS8 INS66 INS42 INS66 INS42 INS66 INS66 INS42 INS42 INS32 UPD66 UPD66 INS42 INS66 UPD66 INS42 INS42 INS9 INS41 MOV41 INS42 INS42 INS42 INS9 INS32 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42