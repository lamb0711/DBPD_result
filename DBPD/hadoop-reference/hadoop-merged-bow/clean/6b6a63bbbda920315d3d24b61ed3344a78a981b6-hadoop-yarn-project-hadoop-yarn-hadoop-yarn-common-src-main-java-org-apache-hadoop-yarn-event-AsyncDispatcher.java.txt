Merge commit '456e901a4c5c639267ee87b8e5f1319f256d20c2' (HDFS-6407. Add sorting and pagination in the datanode tab of the NN Web UI. Contributed by Haohui Mai.) into HDFS-7285-merge

+import org.apache.hadoop.yarn.conf.YarnConfiguration;
+      long endTime = System.currentTimeMillis() + getConfig()
+          .getLong(YarnConfiguration.DISPATCHER_DRAIN_EVENTS_TIMEOUT,
+              YarnConfiguration.DEFAULT_DISPATCHER_DRAIN_EVENTS_TIMEOUT);
+
-        while (!drained && eventHandlingThread.isAlive()) {
+        while (!drained && eventHandlingThread != null
+            && eventHandlingThread.isAlive()
+            && System.currentTimeMillis() < endTime) {
+        // Need to reset drained flag to true if event queue is empty,
+        // otherwise dispatcher will hang on stop.
+        drained = eventQueue.isEmpty();
+  protected boolean isEventThreadWaiting() {
+    return eventHandlingThread.getState() == Thread.State.WAITING;
+  }
+
+  @VisibleForTesting

INS26 INS40 INS31 INS78 INS83 INS39 INS42 INS8 INS42 INS41 INS27 INS60 INS32 INS40 INS39 INS59 INS42 INS42 INS42 INS27 INS21 INS32 INS32 INS27 INS7 INS42 INS42 INS32 INS42 INS40 INS40 MOV27 INS27 INS42 INS32 INS42 INS27 INS32 INS42 INS42 INS42 MOV38 INS27 INS42 INS42 INS42 INS33