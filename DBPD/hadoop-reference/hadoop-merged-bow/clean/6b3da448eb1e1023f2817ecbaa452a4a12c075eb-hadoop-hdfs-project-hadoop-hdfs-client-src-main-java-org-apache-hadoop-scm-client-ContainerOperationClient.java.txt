 HDFS-11675. Ozone: SCM CLI: Implement delete container command. Contributed by Weiwei Yang.

-      LOG.info("Created container " + containerId +
-          " leader:" + pipeline.getLeader() +
-          " machines:" + pipeline.getMachines());
+      if (LOG.isDebugEnabled()) {
+        LOG.debug("Created container " + containerId
+            + " leader:" + pipeline.getLeader()
+            + " machines:" + pipeline.getMachines());
+      }
+   * @param force - True to forcibly delete the container.
-  public void deleteContainer(Pipeline pipeline) throws IOException {
-    // TODO
+  public void deleteContainer(Pipeline pipeline, boolean force)
+      throws IOException {
+    XceiverClientSpi client = null;
+    try {
+      client = xceiverClientManager.acquireClient(pipeline);
+      String traceID = UUID.randomUUID().toString();
+      ContainerProtocolCalls.deleteContainer(client, force, traceID);
+      LOG.info("Deleted container {}, leader: {}, machines: {} ",
+          pipeline.getContainerName(),
+          pipeline.getLeader(),
+          pipeline.getMachines());
+    } finally {
+      if (client != null) {
+        xceiverClientManager.releaseClient(client);
+      }
+    }
-    // TODO
-    return null;
+    return storageContainerLocationClient.getContainer(containerId);

INS44 INS8 INS65 INS39 INS42 INS60 INS54 INS42 INS66 INS43 INS59 INS8 INS8 INS32 INS25 INS42 INS42 INS33 INS21 INS60 INS21 INS21 INS25 INS42 INS42 INS42 INS32 INS8 INS7 INS43 INS59 INS32 INS32 INS27 INS8 INS42 INS42 MOV21 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS45 INS32 INS32 INS32 INS42 INS33 INS21 INS42 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS32 UPD42 INS42 INS42 INS42 INS42 INS42 DEL8 DEL33