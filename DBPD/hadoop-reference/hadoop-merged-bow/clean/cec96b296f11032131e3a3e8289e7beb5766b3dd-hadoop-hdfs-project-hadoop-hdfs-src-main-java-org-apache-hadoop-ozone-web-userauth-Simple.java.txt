HDFS-12549. Ozone: OzoneClient: Support for REST protocol. Contributed by Nanda Kumar.

+import org.apache.hadoop.security.UserGroupInformation;
+import java.io.IOException;
-    String user = getUser(userArgs);
-
+    String user;
+    String currentUser;
+    try {
+      user = getUser(userArgs);
+      currentUser = UserGroupInformation.getCurrentUser().getShortUserName();
+    } catch (IOException e) {
+      throw ErrorTable.newError(ErrorTable.BAD_AUTHORIZATION, userArgs);
+    }
-            (user.compareToIgnoreCase(OzoneConsts.OZONE_SIMPLE_HDFS_USER) == 0);
+            (user.compareToIgnoreCase(OzoneConsts.OZONE_SIMPLE_HDFS_USER) == 0)
+            || (user.compareToIgnoreCase(currentUser) == 0);

INS26 INS26 INS40 INS40 INS60 INS54 INS43 INS59 INS8 INS12 INS42 INS42 INS21 INS21 INS44 INS8 INS36 INS7 INS7 INS43 INS42 INS53 INS27 INS42 MOV32 INS42 INS32 INS42 INS32 INS32 INS34 INS32 INS42 INS42 INS42 INS40 INS42 INS42 INS42 INS42 INS42 INS42