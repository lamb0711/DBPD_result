HDFS-4186. logSync() is called with the write lock held while releasing lease (Kihwal Lee via daryn)


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1409988 13f79535-47bb-0310-9956-ffa450edef68

+    boolean skipSync = false;
+    } catch (StandbyException se) {
+      skipSync = true;
+      throw se;
-    }
-    getEditLog().logSync();
+      // There might be transactions logged while trying to recover the lease.
+      // They need to be sync'ed even when an exception was thrown.
+      if (!skipSync) {
+        getEditLog().logSync();
+      }
+    } 
+
+    boolean skipSync = false;
+    } catch (StandbyException se) {
+      skipSync = true;
+      throw se;
+      // There might be transactions logged while trying to recover the lease.
+      // They need to be sync'ed even when an exception was thrown.
+      if (!skipSync) {
+        getEditLog().logSync();
+      }
+    boolean skipSync = false;
+    } catch (StandbyException se) {
+      skipSync = true;
+      throw se;
+      // There might be transactions logged while trying to recover the lease.
+      // They need to be sync'ed even when an exception was thrown.
+      if (!skipSync) {
+        getEditLog().logSync();
+      }
-    getEditLog().logSync();
-   *         false if block recovery has been initiated
+   *         false if block recovery has been initiated. Since the lease owner
+   *         has been changed and logged, caller should call logSync().
+    // The following transaction is not synced. Make sure it's sync'ed later.
-    writeLock();
-    try {
-      getEditLog().logReassignLease(leaseHolder, src, newHolder);
-    } finally {
-      writeUnlock();
-    }
-    getEditLog().logSync();
+    assert hasWriteLock();
+    getEditLog().logReassignLease(leaseHolder, src, newHolder);

MOV8 INS60 INS60 INS60 INS6 INS39 INS59 INS12 INS8 INS39 INS59 INS12 INS8 INS39 INS59 INS12 INS8 UPD66 INS66 INS32 INS42 INS9 INS44 INS8 MOV21 INS25 INS42 INS9 INS44 INS8 MOV21 INS25 INS42 INS9 INS44 INS8 MOV21 INS25 INS42 INS43 INS42 INS21 INS53 INS38 INS8 INS43 INS42 INS21 INS53 INS38 INS8 INS43 INS42 INS21 INS53 INS38 INS8 INS42 INS7 INS42 INS42 MOV21 INS42 INS7 INS42 INS42 MOV21 INS42 INS7 INS42 INS42 MOV21 INS42 INS9 INS42 INS9 INS42 INS9 DEL8 DEL8 DEL8 DEL42 DEL32 DEL21 DEL42 DEL32 DEL21 DEL8 DEL54 DEL8