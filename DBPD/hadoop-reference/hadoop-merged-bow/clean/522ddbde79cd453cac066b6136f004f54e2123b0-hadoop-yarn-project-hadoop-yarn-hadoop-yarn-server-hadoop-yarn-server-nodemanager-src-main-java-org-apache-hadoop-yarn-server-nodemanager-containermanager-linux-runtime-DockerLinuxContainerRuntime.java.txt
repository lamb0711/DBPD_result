YARN-5394. Remove bind-mount /etc/passwd for Docker containers. Contributed by Zhankun Tang.

-        .setCapabilities(capabilities)
-        .addMountLocation("/etc/passwd", "/etc/password:ro");
+        .setCapabilities(capabilities);

MOV32 DEL42 DEL45 DEL45 DEL32