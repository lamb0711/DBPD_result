HDFS-9080. Update htrace version to 4.0.1 (cmccabe)

-import org.apache.htrace.Span;
-import org.apache.htrace.Trace;
+import org.apache.htrace.core.Span;
+import org.apache.htrace.core.Tracer;
-    if (Trace.isTracing()) {
-      Span s = Trace.currentSpan();
+    Span span = Tracer.getCurrentSpan();
+    if (span != null) {
-          .setParentId(s.getSpanId())
-          .setTraceId(s.getTraceId()).build());
+          .setTraceId(span.getSpanId().getHigh())
+          .setParentId(span.getSpanId().getLow())
+            .build());

UPD40 UPD40 MOV60 INS25 INS27 INS8 UPD42 INS42 INS33 MOV21 UPD42 UPD42 UPD42 INS32 UPD42 INS32 MOV32 INS42 MOV32 INS42 UPD42 UPD42 UPD42 DEL42 DEL42 DEL32 DEL8 DEL25