HADOOP-11602. Fix toUpperCase/toLowerCase to use Locale.ENGLISH. (ozawa)

+import org.apache.hadoop.util.StringUtils;
-        state.toString().toLowerCase());
+          StringUtils.toLowerCase(state.toString()));

INS26 INS40 MOV32 INS42