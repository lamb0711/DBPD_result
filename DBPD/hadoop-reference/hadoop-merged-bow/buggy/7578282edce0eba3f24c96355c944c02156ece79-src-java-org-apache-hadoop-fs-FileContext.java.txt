HADOOP-6585.  Add FileStatus#isDirectory and isFile.  Contributed by Eli Collins.

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@949658 13f79535-47bb-0310-9956-ffa450edef68

-    if (!status.isDir()) {
+    if (status.isFile()) {
-      if (!status.isDir()) {
-        // f is a file
+      if (status.isFile()) {
-      // f is a directory
-        ContentSummary c = s.isDir() ? getContentSummary(s.getPath()) :
+        ContentSummary c = s.isDirectory() ? getContentSummary(s.getPath()) :
-      if (fs.isDir()) {
+      if (fs.isDirectory()) {
-      if (dstFs.isDir()) {
+      if (dstFs.isDirectory()) {

INS32 MOV42 UPD42 MOV42 INS32 MOV42 UPD42 MOV42 UPD42 UPD42 UPD42 DEL32 DEL38 DEL32 DEL38