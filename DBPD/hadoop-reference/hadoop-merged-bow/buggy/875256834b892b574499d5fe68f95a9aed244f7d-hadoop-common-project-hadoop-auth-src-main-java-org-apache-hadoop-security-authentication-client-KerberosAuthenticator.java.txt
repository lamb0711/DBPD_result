HADOOP-11467. KerberosAuthenticator can connect to a non-secure cluster. (yzhangal via rkanter)

+import org.apache.hadoop.security.authentication.util.AuthToken;
+
+      boolean needFallback = false;
-        return;
-      } else if (isNegotiate()) {
+        if (isTokenKerberos(token)) {
+          return;
+        }
+        needFallback = true;
+      }
+      if (!needFallback && isNegotiate()) {
+   * Check if the passed token is of type "kerberos" or "kerberos-dt"
+   */
+  private boolean isTokenKerberos(AuthenticatedURL.Token token)
+      throws AuthenticationException {
+    if (token.isSet()) {
+      AuthToken aToken = AuthToken.parse(token.toString());          
+      if (aToken.getType().equals("kerberos") ||
+          aToken.getType().equals("kerberos-dt")) {              
+        return true;
+      }
+    }
+    return false;
+  }
+
+  /*

INS26 INS40 INS31 MOV8 INS83 INS39 INS42 INS44 INS43 INS8 INS43 INS42 INS42 INS25 INS41 MOV38 INS8 INS40 INS32 INS8 INS9 MOV21 MOV21 MOV21 MOV25 MOV21 MOV21 INS60 INS25 MOV25 INS42 INS42 INS60 INS25 INS39 INS59 MOV27 MOV8 INS27 INS43 INS59 INS27 INS8 INS42 INS9 INS25 INS21 INS38 MOV32 INS42 INS42 INS32 INS32 INS32 INS41 INS32 INS8 INS7 INS42 INS42 INS42 INS32 INS32 INS42 INS45 INS32 INS42 INS45 INS9 INS42 INS42 MOV41 INS42 INS9 INS42 INS42 INS42 INS42 INS42 INS42 DEL25 DEL8