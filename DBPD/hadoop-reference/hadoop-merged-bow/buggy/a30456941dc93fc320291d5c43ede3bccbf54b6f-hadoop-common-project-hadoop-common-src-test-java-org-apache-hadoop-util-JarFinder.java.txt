HADOOP-8509. JarFinder duplicate entry: META-INF/MANIFEST.MF exception (tucu)

git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1350008 13f79535-47bb-0310-9956-ffa450edef68

+import java.io.BufferedOutputStream;
+import java.io.OutputStream;
+import java.util.jar.JarFile;
-  private static void zipDir(File dir, String relativePath, ZipOutputStream zos)
+  private static void copyToZipStream(InputStream is, ZipEntry entry,
+                              ZipOutputStream zos) throws IOException {
+    zos.putNextEntry(entry);
+    byte[] arr = new byte[4096];
+    int read = is.read(arr);
+    while (read > -1) {
+      zos.write(arr, 0, read);
+      read = is.read(arr);
+    }
+    is.close();
+    zos.closeEntry();
+  }
+
+  public static void jarDir(File dir, String relativePath, ZipOutputStream zos)
+
+    // by JAR spec, if there is a manifest, it must be the first entry in the
+    // ZIP.
+    File manifestFile = new File(dir, JarFile.MANIFEST_NAME);
+    ZipEntry manifestEntry = new ZipEntry(JarFile.MANIFEST_NAME);
+    if (!manifestFile.exists()) {
+      zos.putNextEntry(manifestEntry);
+      new Manifest().write(new BufferedOutputStream(zos));
+      zos.closeEntry();
+    } else {
+      InputStream is = new FileInputStream(manifestFile);
+      copyToZipStream(is, manifestEntry, zos);
+    }
+    zos.closeEntry();
-          ZipEntry anEntry = new ZipEntry(relativePath + f.getName());
-          zos.putNextEntry(anEntry);
-          InputStream is = new FileInputStream(f);
-          byte[] arr = new byte[4096];
-          int read = is.read(arr);
-          while (read > -1) {
-            zos.write(arr, 0, read);
-            read = is.read(arr);
+          String path = relativePath + f.getName();
+          if (!path.equals(JarFile.MANIFEST_NAME)) {
+            ZipEntry anEntry = new ZipEntry(path);
+            InputStream is = new FileInputStream(f);
+            copyToZipStream(is, anEntry, zos);
-          is.close();
-          zos.closeEntry();
-    JarOutputStream zos = new JarOutputStream(new FileOutputStream(jarFile),
-                                              new Manifest());
-    zipDir(dir, "", zos);
+    JarOutputStream zos = new JarOutputStream(new FileOutputStream(jarFile));
+    jarDir(dir, "", zos);
-

INS26 INS26 INS26 INS40 INS40 INS40 INS31 INS83 INS83 INS39 INS42 INS44 INS44 INS44 INS43 MOV8 UPD83 UPD42 INS8 INS43 INS42 INS43 INS42 INS43 INS42 INS42 MOV21 MOV21 INS60 INS60 INS25 INS21 MOV21 MOV21 INS42 INS42 INS42 INS43 INS59 MOV43 INS59 INS38 INS8 INS8 INS32 UPD42 INS42 INS42 INS14 INS42 INS14 INS32 INS21 INS21 INS21 INS60 INS21 INS42 INS42 UPD42 INS43 INS42 INS40 INS43 INS40 INS42 INS42 INS32 INS32 INS32 INS43 INS59 INS32 INS42 INS42 INS42 INS42 INS42 MOV14 INS42 INS14 INS42 INS42 INS42 INS42 INS14 INS42 INS42 INS42 INS42 INS43 INS42 INS43 INS42 INS8 INS42 INS42 MOV60 INS25 UPD43 INS38 INS8 UPD42 UPD42 MOV27 INS32 INS60 MOV60 INS21 INS42 INS42 INS40 INS43 INS59 INS32 INS42 INS42 INS14 INS42 INS42 INS42 INS42 INS43 INS42 INS42 DEL8 DEL14