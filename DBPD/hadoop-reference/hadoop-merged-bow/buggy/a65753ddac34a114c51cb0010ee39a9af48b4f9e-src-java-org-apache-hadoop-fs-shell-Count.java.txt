HADOOP-7202. Improve shell Command base class.  Contributed by Daryn Sharp


git-svn-id: https://svn.apache.org/repos/asf/hadoop/common/trunk@1090039 13f79535-47bb-0310-9956-ffa450edef68

-import java.io.*;
-import java.util.List;
+import java.io.IOException;
+import java.util.Arrays;
+import java.util.LinkedList;
+import org.apache.hadoop.classification.InterfaceAudience;
+import org.apache.hadoop.classification.InterfaceStability;
-import org.apache.hadoop.fs.FileSystem;
-import org.apache.hadoop.fs.Path;
+import org.apache.hadoop.fs.ContentSummary;
+import org.apache.hadoop.fs.FsShell;
-public class Count extends Command {
+@InterfaceAudience.Private
+@InterfaceStability.Evolving
+
+public class Count extends FsCommand {
-  private boolean qOption;
+  private boolean showQuotas;
-  /** Constructor
-   * 
-   * @param cmd the count command
-   * @param pos the starting index of the arguments 
-   */
-  public Count(String[] cmd, int pos, Configuration conf) {
-    super(conf);
-    CommandFormat c = new CommandFormat(NAME, 1, Integer.MAX_VALUE, "q");
-    List<String> parameters = c.parse(cmd, pos);
-    this.args = parameters.toArray(new String[parameters.size()]);
-    if (this.args.length == 0) { // default path is the current working directory
-      this.args = new String[] {"."};
-    }
-    this.qOption = c.getOpt("q") ? true: false;
+  /** Constructor */
+  public Count() {
+    setCommandName(NAME);
-  /** Check if a command is the count command
-   * 
-   * @param cmd A string representation of a command starting with "-"
-   * @return true if this is a count command; false otherwise
+  /** Constructor
+   * @deprecated invoke via {@link FsShell}
+   * @param cmd the count command
+   * @param pos the starting index of the arguments 
+   * @param conf configuration
-  public static boolean matches(String cmd) {
-    return ("-" + NAME).equals(cmd); 
+  @Deprecated
+  public Count(String[] cmd, int pos, Configuration conf) {
+    super(conf);
+    setCommandName(NAME);
+    LinkedList<String> parameters = new LinkedList<String>(Arrays.asList(cmd));
+    parameters.subList(0, pos).clear();
+    processOptions(parameters);
+    this.args = parameters.toArray(new String[0]);
-  public String getCommandName() {
-    return NAME;
+  protected void processOptions(LinkedList<String> args) {
+    CommandFormat cf = new CommandFormat(NAME, 1, Integer.MAX_VALUE, "q");
+    cf.parse(args);
+    if (args.isEmpty()) { // default path is the current working directory
+      args.add(".");
+    }
+    showQuotas = cf.getOpt("q");
-  protected void run(Path path) throws IOException {
-    FileSystem fs = path.getFileSystem(getConf());
-    System.out.println(fs.getContentSummary(path).toString(qOption) + path);
+  protected void processPath(PathData src) throws IOException {
+    ContentSummary summary = src.fs.getContentSummary(src.path);
+    out.println(summary.toString(showQuotas) + src.path);

INS26 INS26 INS26 UPD40 INS40 UPD40 INS40 INS40 UPD40 UPD40 INS78 INS78 UPD43 INS31 INS31 INS40 INS40 UPD42 INS29 INS83 INS42 INS8 INS78 MOV78 UPD83 MOV83 UPD39 MOV39 UPD42 MOV42 MOV44 INS8 UPD42 UPD42 INS65 INS21 INS65 INS65 INS42 INS21 MOV21 INS74 INS42 MOV60 INS21 INS25 INS21 UPD43 UPD42 INS66 INS32 INS66 INS65 INS42 INS66 INS32 UPD74 INS32 INS32 INS43 MOV43 INS32 INS32 INS8 INS7 UPD42 UPD43 INS42 INS42 INS42 INS42 INS42 UPD43 INS14 INS32 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS32 INS42 UPD42 INS42 UPD42 MOV42 UPD42 MOV42 INS42 INS42 INS21 INS42 INS32 UPD42 UPD42 INS32 INS42 UPD42 INS74 INS32 MOV42 UPD42 MOV42 INS34 UPD42 MOV42 INS42 INS42 MOV3 INS32 INS42 INS42 INS45 INS40 UPD42 MOV42 INS40 INS40 INS43 MOV43 UPD42 MOV42 UPD42 MOV42 MOV42 INS34 INS42 INS42 INS45 UPD42 MOV42 UPD42 INS42 DEL42 DEL32 DEL52 DEL42 DEL22 DEL42 DEL43 DEL85 DEL5 DEL32 DEL3 DEL32 DEL7 DEL52 DEL42 DEL22 DEL42 DEL22 DEL34 DEL27 DEL8 DEL25 DEL52 DEL42 DEL22 DEL45 DEL32 DEL9 DEL9 DEL16 DEL7 DEL45 DEL4 DEL42 DEL66 DEL65 DEL42 DEL66 DEL65 DEL66 DEL65 DEL29 DEL83 DEL45 DEL42 DEL27 DEL36 DEL32 DEL41 DEL8 DEL31 DEL83 DEL42 DEL42 DEL41 DEL8 DEL31 DEL42 DEL42 DEL32 DEL32 DEL40 DEL42 DEL42 DEL32 DEL42