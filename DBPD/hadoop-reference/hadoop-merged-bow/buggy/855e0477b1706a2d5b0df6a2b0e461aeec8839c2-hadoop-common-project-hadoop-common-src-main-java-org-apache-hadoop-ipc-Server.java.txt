HADOOP-14035. Reduce fair call queue backoff's impact on clients. Contributed by Daryn Sharp.

+import org.apache.hadoop.ipc.CallQueueManager.CallQueueOverflowException;
-        queueCall(call);
+        internalQueueCall(call);
+      } catch (RpcServerException rse) {
+        throw rse;
-    if (!callQueue.isClientBackoffEnabled()) {
+    // external non-rpc calls don't need server exception wrapper.
+    try {
+      internalQueueCall(call);
+    } catch (RpcServerException rse) {
+      throw (IOException)rse.getCause();
+    }
+  }
+
+  private void internalQueueCall(Call call)
+      throws IOException, InterruptedException {
+    try {
-    } else if (callQueue.shouldBackOff(call) || !callQueue.offer(call)) {
+    } catch (CallQueueOverflowException cqe) {
-      throw new RetriableException("Server is too busy.");
+      // unwrap retriable exception.
+      throw cqe.getCause();

INS26 INS40 INS31 INS83 INS39 INS42 INS44 INS43 INS43 INS8 UPD83 UPD42 INS43 INS42 INS42 INS42 INS54 INS54 INS42 INS8 INS12 MOV8 INS12 INS12 INS21 INS44 INS8 INS44 MOV8 INS44 INS8 INS32 INS43 INS42 INS53 INS43 INS42 INS43 INS42 INS53 INS42 INS42 INS42 INS11 INS42 INS32 UPD42 INS42 INS42 INS43 INS32 INS42 INS42 INS42 INS42 INS42 DEL42 DEL43 DEL45 DEL14 DEL42 DEL42 DEL32 DEL38 DEL42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL42 DEL32 DEL38 DEL27 DEL25 DEL25