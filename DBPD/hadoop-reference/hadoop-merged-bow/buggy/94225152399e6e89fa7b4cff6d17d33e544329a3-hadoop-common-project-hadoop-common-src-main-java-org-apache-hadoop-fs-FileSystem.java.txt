HADOOP-13442. Optimize UGI group lookups. Contributed by Daryn Sharp.

-import java.util.Arrays;
-    List<String> groups = Arrays.asList(ugi.getGroupNames());
-    } else if (groups.contains(stat.getGroup())) {
+    } else if (ugi.getGroups().contains(stat.getGroup())) {

INS32 INS42 UPD42 MOV42 DEL40 DEL26 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL59 DEL60