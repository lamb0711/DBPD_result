HDFS-6984. Serialize FileStatus via protobuf.

-      return status.isDir()? DIRECTORY: status.isSymlink()? SYMLINK: FILE;
+      if (status.isDirectory()) {
+        return DIRECTORY;
+      }
+      if (status.isSymlink()) {
+        return SYMLINK;
+      }
+      return FILE;

INS25 INS25 INS41 INS32 INS8 MOV32 INS8 INS42 MOV42 UPD42 MOV42 INS41 INS41 INS42 INS42 DEL32 DEL42 DEL42 DEL42 DEL16 DEL16 DEL41