HADOOP-11754. RM fails to start in non-secure mode due to authentication filter failure. Contributed by Haohui Mai.

+    Map<String, String> filterConfig = getFilterConfigMap(conf, PREFIX);
+
+    container.addFilter("authentication",
+                        AuthenticationFilter.class.getName(),
+                        filterConfig);
+  }
+
+  public static Map<String, String> getFilterConfigMap(Configuration conf,
+      String prefix) {
-      if (name.startsWith(PREFIX)) {
+      if (name.startsWith(prefix)) {
-        name = name.substring(PREFIX.length());
+        name = name.substring(prefix.length());
-
-    container.addFilter("authentication",
-                        AuthenticationFilter.class.getName(),
-                        filterConfig);
+    return filterConfig;

INS31 MOV29 MOV78 INS83 INS39 INS42 MOV44 INS44 INS8 INS83 INS74 UPD42 INS44 INS43 INS42 INS60 MOV21 INS43 INS43 INS43 INS43 INS42 INS41 INS42 INS74 INS59 INS42 INS42 INS42 INS42 INS42 INS43 INS43 INS43 INS42 INS32 INS42 INS42 INS42 INS42 INS42 INS42 UPD42 UPD42 DEL39