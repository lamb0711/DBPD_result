HDFS-9080. Update htrace version to 4.0.1 (cmccabe)

-import org.apache.htrace.Trace;
-import org.apache.htrace.TraceScope;
+import org.apache.htrace.core.TraceScope;
+import org.apache.htrace.core.Tracer;
-      TraceScope traceScope = null;
-      if (Trace.isTracing()) {
-        traceScope = Trace.startSpan(RpcClientUtil.methodToTraceString(method));
+      Tracer tracer = Tracer.curThreadTracer();
+      TraceScope traceScope = null;
+      if (tracer != null) {
+        traceScope = tracer.newScope(RpcClientUtil.methodToTraceString(method));
-        if (Trace.isTracing()) {
-          traceScope.getSpan().addTimelineAnnotation(
-              "Call got exception: " + e.toString());
+        if (traceScope != null) {
+          traceScope.addTimelineAnnotation("Call got exception: " +
+              e.toString());

UPD40 UPD40 INS60 INS43 INS59 INS27 INS42 INS42 INS32 INS42 INS33 INS42 INS42 INS27 UPD42 UPD42 INS42 INS33 MOV42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL32