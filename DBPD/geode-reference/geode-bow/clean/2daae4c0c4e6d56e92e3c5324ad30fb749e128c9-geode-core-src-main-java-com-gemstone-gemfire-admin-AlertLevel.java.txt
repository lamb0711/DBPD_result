GEODE-1744: Fix probable bugs from == use

This closes #230

-		if (this.name != that.name &&
-	  		!(this.name != null &&
-	  		this.name.equals(that.name))) return false;
+		if (this.name != null &&
+	  		!this.name.equals(that.name)) return false;
