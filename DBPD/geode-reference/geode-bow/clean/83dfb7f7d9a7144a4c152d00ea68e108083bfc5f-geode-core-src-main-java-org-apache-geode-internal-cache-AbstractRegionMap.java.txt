GEODE-849 GMSJoinLeaveJUnitTest.testNetworkPartitionMessage

Reduced number of members to avoid the possibility of an interim view
being sent by the view creator thread before a network partition is
declared and a NetworkPartition message is sent.

-      logger.debug("Clearing entries for {} rvv={}", _getOwner(), " rvv=" + rvv);
+      logger.debug("Clearing entries for {} rvv={}", _getOwner(), rvv);
