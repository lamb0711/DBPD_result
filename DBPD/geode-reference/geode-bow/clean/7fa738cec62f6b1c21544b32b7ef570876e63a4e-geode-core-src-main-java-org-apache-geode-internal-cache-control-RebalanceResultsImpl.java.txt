GEODE-7980: update rebalance output on colocated region (#4955)

* GEODE-7980: update rebalance output on colocated region

* empty commit to re-launch CI

* update test
-    totalNumOfMembers += details.getNumberOfMembersExecutedOn();
+    if (totalNumOfMembers == 0)
+      totalNumOfMembers += details.getNumberOfMembersExecutedOn();
