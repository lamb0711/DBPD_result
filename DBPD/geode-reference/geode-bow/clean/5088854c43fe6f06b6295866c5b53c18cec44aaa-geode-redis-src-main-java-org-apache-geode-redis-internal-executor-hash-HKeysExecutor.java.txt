GEODE-8113: change getMap to never return null (#5101)

The redis Hash command executors now never see a "null" RedisHash instance
-
-      if (keyMap == null || keyMap.isEmpty()) {
-        command.setResponse(Coder.getEmptyArrayResponse(context.getByteBufAllocator()));
-        return;
-      }
-
