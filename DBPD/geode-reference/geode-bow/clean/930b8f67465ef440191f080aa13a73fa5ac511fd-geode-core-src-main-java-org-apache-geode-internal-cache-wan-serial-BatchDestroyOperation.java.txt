GEODE-5211: event processor could be null when Concurrent Serial Gateway Sender is being stopped (#1960)


-            ep = csgep.processors.get(index);
+            if (csgep != null && csgep.processors != null) {
+              ep = csgep.processors.get(index);
+            }
