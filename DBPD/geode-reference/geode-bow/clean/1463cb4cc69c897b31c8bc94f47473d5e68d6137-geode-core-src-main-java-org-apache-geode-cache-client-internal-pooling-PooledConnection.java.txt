GEODE-6794: optimize returning client pooled coonection

Now notifyAll is only called if some other thread is waiting to be notified.
-      notifyAll();
+      if (this.waitingToSwitch) {
+        notifyAll();
+      }
