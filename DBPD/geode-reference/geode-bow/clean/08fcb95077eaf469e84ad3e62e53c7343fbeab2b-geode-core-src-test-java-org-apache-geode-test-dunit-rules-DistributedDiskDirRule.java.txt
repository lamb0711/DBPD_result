GEODE-5431: Improve error message when using DistributedDiskDirRule with VM.bounce()

+    if (data == null) {
+      throw new Error("Failed to invoke " + getClass().getSimpleName() + ".before in VM-"
+          + VM.getCurrentVMNum() + ". Rule does not support VM.bounce().");
+    }
