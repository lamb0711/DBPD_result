GEODE-7036: Update of solution after review

-        setLocator(this);
+        boolean noprevlocator = false;
+        if (!hasLocator()) {
+          setLocator(this);
+          noprevlocator = true;
+        }
+          if (noprevlocator) {
+            removeLocator(this);
+          }
