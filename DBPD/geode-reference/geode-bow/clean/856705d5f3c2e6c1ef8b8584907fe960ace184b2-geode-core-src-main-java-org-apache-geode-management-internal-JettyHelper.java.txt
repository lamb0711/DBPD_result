GEODE-5445: Allow Jetty webapps to discover resources when symlinks are used (#2151)


+import org.eclipse.jetty.server.handler.AllowSymLinkAliasChecker;
+    webapp.addAliasCheck(new AllowSymLinkAliasChecker());
