Merge remote-tracking branch 'upstream/develop' into develop

-      if (result != null) {
-        context.getResultSender().lastResult(result);
-      }
+      context.getResultSender().lastResult(result);
