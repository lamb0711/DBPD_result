GEODE-6339: Breadcrumbs match P2P messaging header

Use correct case when matching thread name.

Authored-by: Bill Burcham <bburcham@pivotal.io>

-      if (Thread.currentThread().getName().startsWith("P2P Message Reader")) {
+      if (Thread.currentThread().getName().startsWith("P2P message reader")) {

UPD45