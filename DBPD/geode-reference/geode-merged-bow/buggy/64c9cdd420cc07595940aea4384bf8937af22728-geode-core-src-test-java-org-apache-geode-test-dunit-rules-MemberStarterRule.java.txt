Test Rule Fix: clean up client DS when using LocatorServerStartupRule

-    DistributedSystem ds = InternalDistributedSystem.getConnectedInstance();
-    if (ds != null) {
-      ds.disconnect();
-    }
+    disconnectDSIfAny();
+  public static void disconnectDSIfAny() {
+    DistributedSystem ds = InternalDistributedSystem.getConnectedInstance();
+    if (ds != null) {
+      ds.disconnect();
+    }
+  }
+

INS31 INS83 INS83 INS39 INS42 INS8 INS21 MOV60 MOV25 INS32 INS42