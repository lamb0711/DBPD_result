Initial import of geode-1.0.0.0-SNAPSHOT-2.
All the new sub-project directories (like jvsd) were not imported.
A diff was done to confirm that this commit is exactly the same as
the open directory the snapshot was made from.

+    m.setTransactionDistributed(r.getCache().getTxManager().isDistributed());
+    try {
-      event = new EntryEventImpl(r, getOperation(), this.key, null/*newValue*/,
+      event = EntryEventImpl.create(r, getOperation(), this.key, null/*newValue*/,
-      event = new EntryEventImpl(
+      event = EntryEventImpl.create(
+      try {
+      } finally {
+        // if e2 == ev then no need to free it here. The outer finally block will get it.
+        if (e2 != event) {
+          e2.release();
+        }
+      }
+    } finally {
+      if (event != null) {
+        event.release();
+      }
+    }
+  
+  @Override
+  protected boolean mayAddToMultipleSerialGateways(DistributionManager dm) {
+    return _mayAddToMultipleSerialGateways(dm);
+  }

INS31 INS78 INS83 INS39 INS42 INS44 INS8 INS21 INS54 INS42 INS43 INS42 INS41 INS32 INS8 INS8 INS42 INS32 INS42 INS42 INS32 MOV25 MOV25 MOV25 MOV21 MOV21 MOV21 MOV21 MOV60 MOV60 MOV25 MOV41 INS25 INS42 INS42 INS32 INS42 INS27 INS8 INS32 INS42 INS54 INS42 INS33 INS21 INS42 INS42 INS8 INS8 INS32 INS32 INS32 MOV21 INS25 INS42 INS42 INS42 INS42 INS42 MOV32 MOV22 INS33 MOV32 INS9 INS42 INS9 INS42 INS42 INS42 MOV32 MOV22 INS33 MOV32 INS9 INS42 INS9 INS9 INS27 INS8 INS42 INS42 INS21 INS32 INS42 INS42 DEL42 DEL43 DEL42 DEL33 DEL9 DEL42 DEL9 DEL14 DEL42 DEL43 DEL42 DEL33 DEL9 DEL42 DEL9 DEL9 DEL14