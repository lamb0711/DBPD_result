GEODE-5076 jmx client should not access or modify internal regions

Modified the management packages to use a filtered cache that doesn't
allow users access to internal regions.

This closes #2866

-import org.apache.geode.cache.CacheFactory;
+import org.apache.geode.management.internal.ManagementAgent;
-    InternalCache cache = getCache();
+    InternalCache cache =
+        ((InternalCache) context.getCache()).getCacheForProcessingClientRequests();
-    InternalCache cache = (InternalCache) CacheFactory.getAnyInstance();
+    InternalCache cache = ManagementAgent.getCache();
-  private InternalCache getCache() {
-    return (InternalCache) CacheFactory.getAnyInstance();
-  }
-
-      InternalCache cache = getCache();
+      InternalCache cache =
+          ((InternalCache) context.getCache()).getCacheForProcessingClientRequests();
-    private InternalCache getCache() {
-      return (InternalCache) CacheFactory.getAnyInstance();
-    }
-

MOV26 UPD40 MOV43 INS32 INS32 MOV43 INS36 INS42 UPD42 MOV42 UPD42 MOV42 INS32 INS11 INS36 INS42 MOV43 INS32 INS11 INS42 MOV42 MOV43 INS32 INS42 MOV42 DEL32 DEL32 DEL11 DEL83 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL42 DEL32 DEL11 DEL41 DEL8 DEL31 DEL32 DEL83 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL11 DEL41 DEL8 DEL31