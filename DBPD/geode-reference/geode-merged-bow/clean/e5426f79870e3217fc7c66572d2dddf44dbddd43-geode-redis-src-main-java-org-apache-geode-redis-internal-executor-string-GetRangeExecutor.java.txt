change more string commands to have CommandFunction support (#5184)

* CommandFunction now supports getrange, setrange, bitcount, bitop, bitpos, getbit, setbit, incrbyfloat, mget, and strlen.

Added integration test coverage for untested string commands.

-import java.util.Arrays;
-    ByteArrayWrapper valueWrapper = stringCommands.get(key);
-    if (valueWrapper == null) {
-      return RedisResponse.emptyString();
-    }
+    ByteArrayWrapper returnRange = stringCommands.getrange(key, start, end);
-    byte[] value = valueWrapper.toBytes();
-    int length = value.length;
-
-    start = getBoundedStartIndex(start, length);
-    end = getBoundedEndIndex(end, length);
-
-    /*
-     * Can't 'start' at end of value
-     */
-    if (start > end || start == length) {
-      return RedisResponse.emptyString();
-    }
-    /*
-     * 1 is added to end because the end in copyOfRange is exclusive but in Redis it is inclusive
-     */
-    if (end != length) {
-      end++;
-    }
-    byte[] returnRange = Arrays.copyOfRange(value, (int) start, (int) end);
-    if (returnRange == null || returnRange.length == 0) {
+    if (returnRange == null) {
+    } else if (returnRange.length() == 0) {
+      return RedisResponse.emptyString();
+    } else {
+      return respondBulkStrings(returnRange);
-
-    return respondBulkStrings(returnRange);

INS25 MOV43 MOV27 MOV8 INS25 INS27 MOV8 INS8 UPD42 UPD42 UPD42 INS42 INS42 INS32 MOV34 MOV41 INS42 INS42 DEL40 DEL26 DEL42 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL33 DEL27 DEL25 DEL39 DEL85 DEL5 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL39 DEL42 DEL40 DEL59 DEL60 DEL42 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL42 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL42 DEL42 DEL27 DEL42 DEL42 DEL27 DEL27 DEL42 DEL42 DEL32 DEL41 DEL8 DEL25 DEL42 DEL42 DEL27 DEL42 DEL37 DEL21 DEL8 DEL25 DEL39 DEL85 DEL5 DEL39 DEL42 DEL11 DEL39 DEL42 DEL11 DEL40 DEL27 DEL27 DEL25