Merge branch 'release/1.8.0'

-        getCache().createRegionFactory(regionType).create(regionName);
+        RegionFactory rf = getCache().createRegionFactory(regionType);
+        rf.create(regionName);
-    // stop CacheServer and then close cache -- cache.close() will stop any running CacheServers
-    if (server != null) {
-      try {
-        server.stop();
-      } catch (Exception e) {
-      } finally {
-        server = null;
-      }
-    }
-
+    server = null;

MOV21 INS60 INS43 INS59 INS42 INS42 MOV32 INS42 DEL42 DEL33 DEL27 DEL42 DEL42 DEL32 DEL21 DEL8 DEL42 DEL43 DEL42 DEL44 DEL8 DEL12 DEL8 DEL54 DEL8 DEL25