GEODE-8182: change String commands to use one region (#5165)

* changed all String commands to use the function and to use a single region
* renabled tests ignored for GEODE-8182
* removed the RedisLockService since it is not used in the new data model
* removed the unused del methods on RedisSetCommands and RedisHashCommands
* removed the KeyRegistar since it was no longer needed with a single data region

Co-authored-by: Ray Ingles <ringles@pivotal.io>
+import static org.apache.geode.redis.internal.executor.string.SetOptions.Exists.NX;
+
-import org.apache.geode.cache.Region;
-import org.apache.geode.redis.internal.RedisData;
-    Region<ByteArrayWrapper, RedisData> region =
-        context.getRegionProvider().getStringsRegion();
-
-    checkAndSetDataType(key, context);
-    byte[] value = commandElems.get(VALUE_INDEX);
+    ByteArrayWrapper value = new ByteArrayWrapper(commandElems.get(VALUE_INDEX));
-    Object oldValue =
-        region.putIfAbsent(key, (RedisData) new RedisString(new ByteArrayWrapper(value)));
+    RedisStringCommands stringCommands = getRedisStringCommands(context);
+    SetOptions setOptions = new SetOptions(NX, 0L, null, false);
-    if (oldValue != null) {
-      command.setResponse(Coder.getIntegerResponse(context.getByteBufAllocator(), NOT_SET));
-    } else {
+    boolean result = stringCommands.set(key, value, setOptions);
+
+    if (result) {
+    } else {
+      command.setResponse(Coder.getIntegerResponse(context.getByteBufAllocator(), NOT_SET));
-
-

MOV26 UPD40 MOV60 INS60 INS60 MOV8 MOV43 INS43 INS43 INS59 INS39 MOV59 INS42 INS14 UPD42 MOV42 UPD42 INS42 INS42 INS14 UPD42 MOV43 MOV32 UPD42 MOV42 UPD42 INS43 INS42 INS34 INS33 INS9 UPD42 UPD42 INS42 INS42 INS42 DEL40 DEL26 DEL42 DEL42 DEL42 DEL32 DEL21 DEL39 DEL85 DEL5 DEL42 DEL43 DEL43 DEL74 DEL42 DEL32 DEL42 DEL43 DEL42 DEL43 DEL42 DEL14 DEL14 DEL11 DEL42 DEL43 DEL60 DEL42 DEL33 DEL27