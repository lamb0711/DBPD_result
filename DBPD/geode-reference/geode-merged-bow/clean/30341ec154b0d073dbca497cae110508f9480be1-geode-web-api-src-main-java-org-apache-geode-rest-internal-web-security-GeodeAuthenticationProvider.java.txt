GEODE-2247: GFSH connect over HTTP without credentials should fail earlier

+import org.apache.geode.internal.security.SecurityService;
+import org.apache.geode.management.internal.security.ResourceConstants;
+import org.apache.geode.security.GemFireSecurityException;
-import org.apache.geode.internal.security.SecurityService;
-import org.apache.geode.security.GemFireSecurityException;
+import java.util.Properties;
+    Properties credentials = new Properties();
+    if (username != null)
+      credentials.put(ResourceConstants.USER_NAME, username);
+    if (password != null)
+      credentials.put(ResourceConstants.PASSWORD, password);
-      securityService.login(username, password);
+      securityService.login(credentials);

MOV26 MOV26 INS26 INS26 INS40 INS40 INS60 INS25 INS25 INS43 INS59 INS27 INS21 INS27 INS21 INS42 INS42 INS14 INS42 INS33 INS32 INS42 INS33 INS32 INS43 INS42 INS42 INS40 INS42 INS42 INS42 INS40 INS42 INS42 UPD42 DEL42