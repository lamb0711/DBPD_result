GEODE-4858: Added CliFunction abstract class (#1932)

Added a new CliFunction abstract class to define the 
return type for CLI functions to be CliFunctionResult.

-import java.util.List;
+import org.apache.geode.management.internal.cli.functions.CliFunctionResult;
-        List<?> result =
-            (List<?>) executeFunction(new DescribeMappingFunction(), regionName, targetMember)
-                .getResult();
-        if (!result.isEmpty()) {
-          mapping = (ConnectorService.RegionMapping) result.get(0);
+        CliFunctionResult result = executeFunctionAndGetFunctionResult(
+            new DescribeMappingFunction(), regionName, targetMember);
+        if (result != null) {
+          mapping = (ConnectorService.RegionMapping) result.getResultObject();

MOV26 UPD40 INS43 INS27 UPD42 MOV42 MOV32 INS42 INS33 UPD42 UPD42 DEL43 DEL76 DEL74 DEL42 DEL43 DEL76 DEL74 DEL42 DEL32 DEL11 DEL42 DEL42 DEL32 DEL38 DEL34