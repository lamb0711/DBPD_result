GEODE-5992 Fix bugs identified by static analysis

-        return Long.valueOf(o1.getAddress()).compareTo(o2.getAddress());
+        return Long.compare(o1.getAddress(), o2.getAddress());
-        return Long.valueOf(o1.getAddress()).compareTo(o2.getAddress());
+        return Long.compare(o1.getAddress(), o2.getAddress());

MOV32 MOV32 UPD42 MOV32 UPD42 MOV32 DEL42 DEL32 DEL42 DEL32