GEODE-7839: add gfsh oauth token support (#4736)

The gfsh connect command now has a --token option.

Co-authored-by: Joris Melchior <joris.melchior@gmail.com>
Co-authored-by: Darrel Schneider <dschneider@pivotal.io>

+    String token = request.getHeader(ResourceConstants.TOKEN);
-    if (username != null)
-      credentials.put(ResourceConstants.USER_NAME, username);
-    if (password != null)
-      credentials.put(ResourceConstants.PASSWORD, password);
+    if (token != null) {
+      credentials.put(ResourceConstants.TOKEN, token);
+    } else {
+      if (username != null) {
+        credentials.put(ResourceConstants.USER_NAME, username);
+      }
+      if (password != null) {
+        credentials.put(ResourceConstants.PASSWORD, password);
+      }
+    }

INS60 INS25 INS43 INS59 INS27 INS8 INS8 INS42 INS42 INS32 INS42 INS33 INS21 MOV25 MOV25 INS42 INS42 INS40 INS32 INS8 INS8 INS42 INS42 INS40 INS42 MOV21 MOV21