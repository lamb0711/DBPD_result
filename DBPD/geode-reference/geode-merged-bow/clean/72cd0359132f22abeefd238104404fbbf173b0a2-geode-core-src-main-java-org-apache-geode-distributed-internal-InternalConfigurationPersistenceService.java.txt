GEODE-7902: fix flaky test (#4845)

* wait for a second before restarting the server for the file to get synced to the file system.
* unify the file copy operation
-import java.nio.file.StandardCopyOption;
-    Files.copy(jarFile.toPath(), jarToWrite.toPath(), StandardCopyOption.REPLACE_EXISTING);
+    FileUtils.copyFile(jarFile, jarToWrite);

UPD42 UPD42 MOV42 MOV42 DEL40 DEL26 DEL42 DEL32 DEL42 DEL32 DEL40