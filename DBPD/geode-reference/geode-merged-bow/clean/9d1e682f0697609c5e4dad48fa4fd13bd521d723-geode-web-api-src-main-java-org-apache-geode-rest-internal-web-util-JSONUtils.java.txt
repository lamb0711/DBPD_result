GEODE-7869: Cleanup warnings in geode-web-api

+import com.fasterxml.jackson.core.json.JsonWriteFeature;
-    generator.enable(Feature.ESCAPE_NON_ASCII);
+    generator.enable(JsonWriteFeature.ESCAPE_NON_ASCII.mappedFeature());
-    HeapDataOutputStream outputStream =
-        new HeapDataOutputStream(Version.CURRENT);
-    try {
+    try (HeapDataOutputStream outputStream = new HeapDataOutputStream(Version.CURRENT)) {
-    } finally {
-      outputStream.close();
-    HeapDataOutputStream outputStream =
-        new HeapDataOutputStream(Version.CURRENT);
-    try {
+    try (HeapDataOutputStream outputStream = new HeapDataOutputStream(Version.CURRENT)) {
-    } finally {
-      outputStream.close();
-    HeapDataOutputStream outputStream =
-        new HeapDataOutputStream(Version.CURRENT);
-    try {
+    try (HeapDataOutputStream outputStream = new HeapDataOutputStream(Version.CURRENT)) {
-    } finally {
-      outputStream.close();
-    HeapDataOutputStream outputStream =
-        new HeapDataOutputStream(Version.CURRENT);
-    try {
+    try (HeapDataOutputStream outputStream = new HeapDataOutputStream(Version.CURRENT)) {
-    } finally {
-      outputStream.close();
-    HeapDataOutputStream outputStream =
-        new HeapDataOutputStream(Version.CURRENT);
-    try {
+    try (HeapDataOutputStream outputStream = new HeapDataOutputStream(Version.CURRENT)) {
-    } finally {
-      outputStream.close();
-    HeapDataOutputStream outputStream =
-        new HeapDataOutputStream(Version.CURRENT);
-    try {
+    try (HeapDataOutputStream outputStream = new HeapDataOutputStream(Version.CURRENT)) {
-    } finally {
-      outputStream.close();

INS26 INS40 INS58 INS58 INS58 INS58 INS58 INS58 INS32 MOV43 MOV59 MOV43 MOV59 MOV43 MOV59 MOV43 MOV59 MOV43 MOV59 MOV43 MOV59 UPD40 MOV40 INS42 DEL60 DEL42 DEL42 DEL32 DEL21 DEL8 DEL60 DEL42 DEL42 DEL32 DEL21 DEL8 DEL60 DEL42 DEL42 DEL32 DEL21 DEL8 DEL60 DEL42 DEL42 DEL32 DEL21 DEL8 DEL60 DEL42 DEL42 DEL32 DEL21 DEL8 DEL60 DEL42 DEL42 DEL32 DEL21 DEL8