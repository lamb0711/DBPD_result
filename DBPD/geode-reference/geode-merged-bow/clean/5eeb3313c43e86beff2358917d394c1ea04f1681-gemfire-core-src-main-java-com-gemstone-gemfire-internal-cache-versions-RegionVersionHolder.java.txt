avoid invoking addOlderVersion for a version that isn't old

-    this.addOlderVersion(version);
+    if (this.version > version) {
+      this.addOlderVersion(version);
+    }

INS25 INS27 INS8 INS22 INS42 MOV21 INS52 INS42