GEODE-5424: Replacing calls to waitForCriterion with Awaitility

Replacing all of the calls to waitForCriterion with awaitility instead,
to use a standard timeout.

+import static org.junit.Assert.fail;
+
+import org.awaitility.core.ThrowingRunnable;
+
-public interface WaitCriterion {
+public interface WaitCriterion extends ThrowingRunnable {
+  default void run() {
+    if (!done()) {
+      fail(description());
+    }
+
+  }
+

INS26 INS26 INS40 INS40 INS43 INS31 INS42 INS83 INS39 INS42 INS8 INS25 INS38 INS8 INS32 INS21 INS42 INS32 INS42 INS32 INS42