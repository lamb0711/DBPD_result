GEODE-2928: get rid of the isGfshVM static variable

* consolidate the availability indicators
* remove the isGfshVM and isGfshVM() method
* enhance the MultiStepCommand to include info on shellOnly commands to enhance command validation
* remove the SUPPORT_MULTIPLE_GFSH static flag and properly remove the gfsh instance at the end of each test

-  public static boolean SUPPORT_MUTLIPLESHELL = false;
-      signalHandler = (CliUtil.isGfshVM() ? new GfshSignalHandler()
-          : new AbstractSignalNotificationHandler() {});
+      signalHandler = new GfshSignalHandler();
-    if (!SUPPORT_MUTLIPLESHELL) {
-      return instance;
-    } else {
-      return gfshThreadLocal.get();
-    }
+    return instance;
+    instance = null;

MOV8 INS21 INS7 INS42 INS33 MOV14 DEL83 DEL83 DEL39 DEL42 DEL9 DEL59 DEL23 DEL42 DEL42 DEL32 DEL42 DEL43 DEL1 DEL14 DEL16 DEL36 DEL42 DEL38 DEL42 DEL42 DEL32 DEL41 DEL8 DEL25 DEL8