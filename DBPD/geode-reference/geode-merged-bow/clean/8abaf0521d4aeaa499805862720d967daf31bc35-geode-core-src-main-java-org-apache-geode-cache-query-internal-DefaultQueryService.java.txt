GEODE-5246: Changed DefaultQueryService to return an empty list when no indexes are defined


-      Collection indexes = getIndexes(region);
-      if (indexes != null)
-        allIndexes.addAll(indexes);
+      allIndexes.addAll(getIndexes(region));
+
-        indexes = getIndexes((Region) subRegions.next());
-        if (indexes != null)
-          allIndexes.addAll(indexes);
+        allIndexes.addAll(getIndexes((Region) subRegions.next()));
+
+
-    if (indexManager == null)
-      return null;
+    if (indexManager == null) {
+      return Collections.emptyList();
+    }
+
-    if (indexManager == null)
-      return null;
+    if (indexManager == null) {
+      return Collections.emptyList();
+    }
+

INS8 INS8 INS21 INS41 INS41 MOV32 INS32 INS32 MOV32 INS42 INS42 INS42 INS42 INS32 INS42 INS42 MOV32 DEL42 DEL42 DEL43 DEL42 DEL59 DEL60 DEL42 DEL33 DEL27 DEL21 DEL25 DEL42 DEL7 DEL42 DEL33 DEL27 DEL42 DEL42 DEL42 DEL32 DEL21 DEL25 DEL33 DEL41 DEL33 DEL41