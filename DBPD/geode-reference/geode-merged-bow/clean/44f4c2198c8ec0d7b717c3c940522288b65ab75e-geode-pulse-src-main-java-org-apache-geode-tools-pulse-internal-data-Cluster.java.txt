GEODE-7755: change Pulse to not keep locator alive (#4661)

* Cluster now makes itself a daemon if embedded.
If the JMXDataUpdater loses its connection it now tells the Cluster it is no longer connected.

* removed debug logging

-  private boolean connectedFlag;
+  private volatile boolean connectedFlag;
+    if (Boolean.getBoolean(PulseConstants.SYSTEM_PROPERTY_PULSE_EMBEDDED)) {
+      this.setDaemon(true);
+    }
-      while (!this.stopUpdates) {
+      while (!this.stopUpdates && isConnectedFlag()) {

INS83 INS25 INS32 INS8 INS42 INS42 INS40 INS21 INS32 INS27 INS52 INS42 INS9 MOV38 INS32 INS42