revert "refactor test rules"

-import org.apache.geode.test.dunit.standalone.DUnitLauncher;
+import org.junit.After;
+import org.junit.Before;
+import java.nio.file.Path;
-  public LocatorServerStartupRule() {
-    DUnitLauncher.launchIfNeeded();
-  }
-
-  @Override
-  protected void before() throws Throwable {
+  @Before
+  public void before() throws Throwable {
-  @Override
-  protected void after() {
+  @After
+  public void after() {
-    Locator locator = new Locator(locatorVM, locatorPort, workingDir, name);
+    Locator locator = new Locator(locatorVM, locatorPort, workingDir);
+
-    Locator locator = new Locator(locatorVM, locatorPort, workingDir, name);
+    Locator locator = new Locator(locatorVM, locatorPort, workingDir);
-    Server server = new Server(serverVM, port, workingDir, name);
+    Server server = new Server(serverVM, port, workingDir);
-      serverStarter = null;
-      locatorStarter = null;

MOV26 INS26 INS26 UPD40 INS40 INS40 UPD83 UPD83 UPD42 UPD42 MOV43 MOV43 MOV43 MOV43 MOV43 MOV43 DEL83 DEL42 DEL42 DEL42 DEL32 DEL21 DEL8 DEL31 DEL42 DEL42 DEL42 DEL42 DEL33 DEL7 DEL21 DEL42 DEL33 DEL7 DEL21