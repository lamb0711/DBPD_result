GEODE-3240: Refactor LuceneSerializer to have only a toDocuments method

Refactoring our internal LuceneSerializer API to have a method called
toDocuments, which matches the API we want to have when we make this
class public.

+import java.util.Collection;
-      Document doc = new Document();
-      SerializerUtil.addKey(key, doc);
-      serializer.toDocument(value, doc);
-      writer.addDocument(doc);
+      Collection<Document> docs = serializer.toDocuments(value);
+      docs.forEach(doc -> SerializerUtil.addKey(key, doc));
+      writer.addDocuments(docs);
-      Document doc = new Document();
-      SerializerUtil.addKey(key, doc);
-      serializer.toDocument(value, doc);
-      writer.updateDocument(SerializerUtil.getKeyTerm(doc), doc);
+      Collection<Document> docs = serializer.toDocuments(value);
+      docs.forEach(doc -> SerializerUtil.addKey(key, doc));
+      Term keyTerm = SerializerUtil.toKeyTerm(key);
+      writer.updateDocuments(keyTerm, docs);

INS26 INS40 INS60 INS60 INS60 INS21 INS74 INS59 INS32 INS74 INS59 INS32 INS43 INS59 INS32 INS43 MOV43 UPD42 MOV42 INS32 INS42 INS42 INS86 UPD42 UPD42 INS43 MOV43 UPD42 MOV42 INS32 INS42 INS42 INS86 INS42 INS42 INS32 INS42 INS42 INS42 UPD42 MOV42 UPD42 MOV42 MOV42 UPD42 MOV42 MOV42 INS59 MOV32 UPD42 MOV42 MOV42 UPD42 MOV42 MOV42 INS59 MOV32 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 INS42 DEL43 DEL14 DEL59 DEL60 DEL42 DEL32 DEL21 DEL43 DEL14 DEL59 DEL60 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL32 DEL21