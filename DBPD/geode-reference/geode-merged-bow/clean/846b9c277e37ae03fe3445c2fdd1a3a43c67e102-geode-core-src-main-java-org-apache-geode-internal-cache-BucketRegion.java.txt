GEODE-6762: Prevent unnecessary PartitionedRegion callback events



-  private EntryEventImpl createEventForPR(EntryEventImpl sourceEvent) {
+  EntryEventImpl createEventForPR(EntryEventImpl sourceEvent) {
-
+  private boolean skipPrEvent(final EntryEventImpl event, final boolean callDispatchListenerEvent) {
+    if (!event.isGenerateCallbacks()) {
+      return true;
+    }
+    boolean needsPrEvent = (partitionedRegion.isInitialized() && callDispatchListenerEvent
+        && partitionedRegion.shouldDispatchListenerEvent())
+        || CacheClientNotifier.singletonHasClientProxies();
+    if (!needsPrEvent) {
+      return true;
+    }
+    return false;
+  }
+
+    if (skipPrEvent(event, callDispatchListenerEvent)) {
+      return;
+    }
+
-    final EntryEventImpl prevent = createEventForPR(event);
+    final EntryEventImpl prEvent = createEventForPR(event);
-      partitionedRegion.invokeTXCallbacks(eventType, prevent,
+      partitionedRegion.invokeTXCallbacks(eventType, prEvent,
-      prevent.release();
+      prEvent.release();
+
+    if (skipPrEvent(event, callDispatchListenerEvent)) {
+      return;
+    }
+
-    final EntryEventImpl prevent = createEventForPR(event);
+    final EntryEventImpl prEvent = createEventForPR(event);
-      partitionedRegion.invokeDestroyCallbacks(eventType, prevent,
+      partitionedRegion.invokeDestroyCallbacks(eventType, prEvent,
-      prevent.release();
+      prEvent.release();
+
+    if (skipPrEvent(event, callDispatchListenerEvent)) {
+      return;
+    }
+
-    final EntryEventImpl prevent = createEventForPR(event);
+    final EntryEventImpl prEvent = createEventForPR(event);
-      partitionedRegion.invokeInvalidateCallbacks(eventType, prevent,
+      partitionedRegion.invokeInvalidateCallbacks(eventType, prEvent,
-      prevent.release();
+      prEvent.release();
+    if (skipPrEvent(event, callDispatchListenerEvent)) {
+      return;
+    }
+
-    final EntryEventImpl prevent = createEventForPR(event);
+    final EntryEventImpl prEvent = createEventForPR(event);
-      partitionedRegion.invokePutCallbacks(eventType, prevent,
+      partitionedRegion.invokePutCallbacks(eventType, prEvent,
-      prevent.release();
+      prEvent.release();

INS31 INS83 INS39 INS42 INS44 INS44 INS8 INS83 INS43 INS42 INS83 INS39 INS42 INS25 INS60 INS25 INS41 INS25 INS60 INS54 INS25 INS25 INS25 MOV60 MOV54 INS42 INS38 INS8 INS39 INS59 INS38 INS8 INS9 INS32 INS8 INS78 INS83 INS43 INS59 INS8 INS8 INS32 INS8 INS32 INS8 INS32 INS8 INS32 INS41 INS42 INS27 INS42 INS41 INS42 INS42 INS42 INS41 INS42 INS42 INS42 INS32 INS21 INS21 INS42 INS42 INS42 INS41 UPD42 INS42 INS42 INS42 INS41 MOV42 MOV42 UPD42 UPD42 MOV42 UPD42 MOV42 INS42 INS41 UPD42 INS42 INS42 INS9 INS36 INS32 INS9 INS42 INS42 INS32 INS32 MOV42 MOV42 INS27 INS42 INS42 INS42 INS42 INS42 INS42 INS27 INS42 INS42 UPD42 UPD42 UPD42 MOV27 UPD42 UPD42 UPD42 INS9 UPD42 INS32 INS42 INS32 INS32 INS42 MOV32 MOV42 INS42 INS42 INS42 INS42 INS42 INS42 MOV42 MOV42 DEL83 DEL83 DEL42 DEL59 DEL60 DEL42 DEL42 DEL9 DEL32 DEL21 DEL8 DEL42 DEL42 DEL32 DEL21 DEL8 DEL54