Revert "GEODE-6804: add tab completion for deploy --jar= and --dir= (#3860)"

This reverts commit 81da5bf9bc993d13eb4dd0f13383eb8da1c58dc5.

This commit fails on Windows.

-import java.util.Objects;
-import java.util.stream.Stream;
-
-    return Stream.of(currentFile)
-        .map(File::list)
-        .filter(Objects::nonNull)
-        .flatMap(Stream::of)
-        .map(s -> prefix + s)
-        .collect(Collectors.toList());
+    return Arrays.stream(currentFile.list()).map(s -> prefix + s).collect(Collectors.toList());
-      getRoots().forEach(path -> completions.add(new Completion(path)));
+      getRoots().stream().forEach(path -> completions.add(new Completion(path)));
-        .forEach(path -> completions.add(new Completion(path)));
+        .forEach(path -> {
+          completions.add(new Completion(path));
+        });

MOV32 INS8 UPD42 MOV42 UPD42 MOV42 INS32 INS32 INS21 MOV42 INS42 MOV32 INS42 INS32 MOV42 MOV42 MOV14 DEL40 DEL26 DEL40 DEL26 DEL32 DEL42 DEL42 DEL42 DEL90 DEL42 DEL42 DEL42 DEL90 DEL32 DEL42 DEL42 DEL42 DEL90 DEL32 DEL32