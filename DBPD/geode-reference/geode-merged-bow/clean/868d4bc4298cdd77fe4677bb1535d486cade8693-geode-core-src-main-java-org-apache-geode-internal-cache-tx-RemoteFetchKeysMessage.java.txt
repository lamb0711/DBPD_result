GEODE-8330: Structural Improvements to Versioning

Follow-on to GEODE-8240, this commit eliminates redundancy in 
versioning code and provides a single way to convert a short
to a version and a single way to acquire a known version from
a (possibly unknown) version.
+import org.apache.geode.internal.serialization.Version;
+import org.apache.geode.internal.serialization.Versioning;
-          InitialImageOperation.CHUNK_SIZE_IN_BYTES + 2048, recipient.getVersionObject());
+          InitialImageOperation.CHUNK_SIZE_IN_BYTES + 2048, Versioning
+              .getKnownVersionOrDefault(recipient.getVersionOrdinalObject(), Version.CURRENT));

INS26 INS26 INS40 INS40 INS32 INS42 INS42 INS32 INS40 MOV42 UPD42 MOV42 DEL32