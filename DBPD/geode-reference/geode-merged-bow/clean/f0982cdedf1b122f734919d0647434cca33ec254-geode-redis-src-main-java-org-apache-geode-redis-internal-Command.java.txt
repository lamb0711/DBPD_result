Revert "GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4745)" (#4780)

This reverts commit 2f6bf013368df5a4b5efe68162a4953f9a88bbf2.
+ *
+ *
-    if (commandElems == null || commandElems.isEmpty()) {
+    if (commandElems == null || commandElems.isEmpty())
-    }
-    String commandName = null;
+
-      commandName = Coder.bytesToString(charCommand).toUpperCase();
+      String commandName = Coder.bytesToString(charCommand).toUpperCase();
-    if (response == null) {
+    if (response == null)
-    }
-    if (response.getByte(0) == Coder.ERROR_ID) {
+    if (response.getByte(0) == Coder.ERROR_ID)
-    }
-      } else if (this.key == null) {
+      } else if (this.key == null)
-      }
-    } else {
+    } else
-    }
-      if (this.bytes == null) {
+      if (this.bytes == null)
-      }
-    } else {
+    } else
-    }

MOV8 MOV53 MOV41 MOV41 MOV41 MOV27 MOV41 INS60 INS25 MOV41 MOV43 INS59 MOV27 MOV21 INS42 MOV32 MOV21 DEL8 DEL42 DEL33 DEL59 DEL60 DEL42 DEL7 DEL21 DEL8 DEL8 DEL8 DEL8 DEL8 DEL25 DEL8