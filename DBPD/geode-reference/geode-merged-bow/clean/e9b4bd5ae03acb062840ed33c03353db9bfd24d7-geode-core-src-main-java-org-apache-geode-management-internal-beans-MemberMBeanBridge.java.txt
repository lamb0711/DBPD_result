GEODE-4442: Move abort of backups to separate message (#1381)


+import java.util.Collections;
-          successfulDataStores = cache.getBackupService().doBackup(abort);
+          if (abort) {
+            cache.getBackupService().abortBackup();
+            successfulDataStores = Collections.emptySet();
+          } else {
+            successfulDataStores = cache.getBackupService().doBackup();
+          }

INS26 INS40 INS8 INS25 INS42 MOV8 INS8 INS21 INS21 MOV32 INS7 INS7 UPD42 INS42 INS32 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 DEL42 DEL42 DEL7