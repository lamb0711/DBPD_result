Revert "GEODE-5535: Upgrade FastClasspathScanner to 4.0.6 (#2290)"

This reverts commit c2d3c1e16c49aac021a26dca65b57996be7d94a2.

+import java.lang.reflect.Modifier;
+import java.util.HashSet;
-import io.github.classgraph.ClassGraph;
-import io.github.classgraph.ClassInfoList;
-import io.github.classgraph.ScanResult;
+import io.github.lukehutch.fastclasspathscanner.FastClasspathScanner;
+
-    ScanResult scanResult = new ClassGraph().whitelistPackages(packagesToScan).enableClassInfo()
-        .enableAnnotationInfo().scan();
+    Set<Class<?>> classesImplementing = new HashSet<>();
+    new FastClasspathScanner(packagesToScan)
+        .matchClassesImplementing(implementedInterface, classesImplementing::add).scan();
-    ClassInfoList classInfoList = scanResult.getClassesImplementing(implementedInterface.getName())
-        .filter(ci -> !ci.isAbstract() && !ci.isInterface() && ci.isPublic());
-
-    return classInfoList.loadClasses().stream().collect(toSet());
+    return classesImplementing.stream().filter(ClasspathScanLoadHelper::isInstantiable)
+        .collect(toSet());
-    ScanResult scanResult = new ClassGraph().whitelistPackages(packagesToScan).enableClassInfo()
-        .enableAnnotationInfo().scan();
-    ClassInfoList classInfoList = scanResult.getClassesWithAnnotation(annotation.getName());
-
-    return classInfoList.loadClasses().stream().collect(toSet());
+    Set<Class<?>> classesWithAnnotation = new HashSet<>();
+    new FastClasspathScanner(packagesToScan)
+        .matchClassesWithAnnotation(annotation, classesWithAnnotation::add).scan();
+    return classesWithAnnotation;
+  private static boolean isInstantiable(Class<?> klass) {
+    int modifiers = klass.getModifiers();
+
+    return !Modifier.isAbstract(modifiers) && !Modifier.isInterface(modifiers)
+        && Modifier.isPublic(modifiers);
+  }

MOV26 UPD40 UPD40 UPD40 INS31 INS31 MOV83 MOV83 MOV74 MOV42 MOV44 MOV44 INS8 INS83 INS83 INS39 INS42 INS44 INS8 INS21 INS60 INS21 INS41 INS74 INS42 INS60 MOV41 INS74 INS59 INS32 INS74 INS59 INS32 INS42 INS43 INS76 INS39 INS59 INS27 INS43 INS74 INS42 INS14 INS32 UPD42 MOV42 INS43 INS74 INS42 INS14 MOV32 MOV42 UPD42 MOV42 INS42 INS32 INS38 INS38 INS32 UPD42 MOV42 INS43 INS76 INS74 INS14 UPD42 MOV42 MOV42 INS90 UPD42 INS90 UPD42 MOV42 INS43 INS76 INS74 MOV14 UPD42 MOV42 UPD42 MOV42 INS90 UPD42 MOV42 UPD42 MOV42 INS32 INS32 UPD42 MOV42 INS42 INS42 UPD42 MOV42 INS43 INS43 INS42 INS42 INS42 UPD42 UPD42 INS42 INS42 INS42 INS43 UPD43 INS42 INS42 INS42 INS42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 INS42 UPD42 MOV42 INS42 UPD42 DEL43 DEL42 DEL43 DEL14 DEL42 DEL42 DEL32 DEL42 DEL32 DEL42 DEL32 DEL42 DEL32 DEL59 DEL43 DEL42 DEL42 DEL42 DEL32 DEL32 DEL42 DEL42 DEL59 DEL42 DEL32 DEL38 DEL42 DEL42 DEL32 DEL38 DEL42 DEL42 DEL32 DEL27 DEL86 DEL32 DEL59 DEL60 DEL32 DEL42 DEL32 DEL42 DEL32 DEL32 DEL32 DEL32 DEL43 DEL42 DEL32 DEL59 DEL60 DEL43 DEL42 DEL42 DEL42 DEL32 DEL32 DEL59 DEL60 DEL8 DEL31