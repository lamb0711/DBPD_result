GEODE-1654: Fix Javadoc warnings

- * The implementation will guard client/server, jmx, pulse, gfsh commands
+ * The implementation will guard client/server, JMX, Pulse, GFSH commands
-   * @param props:
+   * @param props
-
-
-   * close any resources used by the SecurityManager, called when a cache is closed.
+   * Close any resources used by the SecurityManager, called when a cache is closed.

UPD66 INS42 UPD66 DEL66