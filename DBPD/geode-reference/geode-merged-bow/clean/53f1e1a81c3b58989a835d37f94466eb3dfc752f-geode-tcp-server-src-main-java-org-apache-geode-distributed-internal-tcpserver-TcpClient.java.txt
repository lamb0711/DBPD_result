GEODE-7852: Adding a SocketFactory configuration for client Pools (#4830)

Adding the ability to provide a SocketFactory to a client pool for use in
creating sockets. Adding an implementation of this SocketFactory that
configures the pool to use an SNI proxy.

Adding an automated test of the SniSocketFactory that uses docker compose
to use a real SNI proxy and geode servers that are not reachable without
going through the proxy.

Co-authored-by: Bill Burcham <bburcham@pivotal.io>
+  private final TcpSocketFactory socketFactory;
-      final ObjectDeserializer objectDeserializer) {
+      final ObjectDeserializer objectDeserializer, TcpSocketFactory socketFactory) {
+    this.socketFactory = socketFactory;
-        socketCreator.forCluster().connect(addr, (int) newTimeout, null);
+        socketCreator.forCluster().connect(addr, (int) newTimeout, null, socketFactory);
-      sock = socketCreator.forCluster().connect(addr, timeout, null);
+      sock = socketCreator.forCluster().connect(addr, timeout, null, socketFactory);

INS23 INS83 INS83 INS43 INS59 INS44 INS42 INS42 INS43 INS42 INS21 INS42 INS7 INS22 INS42 INS52 INS42 INS42 INS42