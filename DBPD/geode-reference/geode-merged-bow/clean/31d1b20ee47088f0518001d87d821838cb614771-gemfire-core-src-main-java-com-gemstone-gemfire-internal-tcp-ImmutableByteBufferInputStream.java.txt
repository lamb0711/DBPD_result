Initial import of geode-1.0.0.0-SNAPSHOT-2.
All the new sub-project directories (like jvsd) were not imported.
A diff was done to confirm that this commit is exactly the same as
the open directory the snapshot was made from.

+import com.gemstone.gemfire.internal.offheap.SimpleMemoryAllocatorImpl.Chunk;
+
-    ByteBuffer bb = existing.slice();
-    bb.limit(length);
-    setBuffer(bb);
+    setBuffer(existing.slice(length));
+  public ImmutableByteBufferInputStream(Chunk blob) {
+    super(blob);
+  }

INS26 INS40 INS31 INS83 INS42 INS44 INS8 INS21 INS43 INS42 INS46 INS32 INS42 INS42 INS42 INS32 MOV42 MOV42 MOV42 DEL42 DEL43 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL21 DEL42 DEL42 DEL32 DEL21