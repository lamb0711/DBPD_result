GEODE-6133: Use System.lineSeparator() for line feeds (#4152)

Cleanup any classes and tests touched for line feeds.
-import static org.apache.commons.lang3.SystemUtils.LINE_SEPARATOR;
+import static java.lang.System.lineSeparator;
-          .map(line -> line + LINE_SEPARATOR).forEach(this.inputListener::notifyInputLine);
+          .map(line -> line + lineSeparator()).forEach(inputListener::notifyInputLine);

UPD40 INS42 INS32 INS42 DEL42 DEL52 DEL42 DEL22