GEODE-3540: unable to gfsh destroy gateway-sender (#1414)

   GW sender bean is not unregistered during destroy command which
   is why it appears when you list gateways after destroy. This fix is to 
   unregistered the MX bean from SystemManagementService
+      case GATEWAYSENDER_REMOVE:
+        GatewaySender removedSender = (GatewaySender) resource;
+        adapter.handleGatewaySenderRemoved(removedSender);
+        break;

MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 MOV10 INS49 INS60 INS21 INS10 INS42 INS43 INS59 INS32 INS42 INS42 INS11 INS42 INS42 INS42 INS43 INS42 INS42