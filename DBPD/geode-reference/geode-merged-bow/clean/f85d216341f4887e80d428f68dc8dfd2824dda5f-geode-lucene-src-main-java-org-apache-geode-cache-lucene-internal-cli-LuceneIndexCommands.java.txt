GEODE-2928: get rid of the isGfshVM static variable

* consolidate the availability indicators
* remove the isGfshVM and isGfshVM() method
* enhance the MultiStepCommand to include info on shellOnly commands to enhance command validation
* remove the SUPPORT_MULTIPLE_GFSH static flag and properly remove the gfsh instance at the end of each test

-    return (!CliUtil.isGfshVM() || (getGfsh() != null && getGfsh().isConnectedAndReady()));
+    Gfsh gfsh = Gfsh.getCurrentInstance();
+
+    // command should always be available on the server
+    if (gfsh == null) {
+      return true;
+    }
+
+    // if in gfshVM, only when gfsh is connected and ready
+    return gfsh.isConnectedAndReady();

INS60 INS25 INS41 INS43 INS59 INS27 INS8 INS32 INS42 INS42 INS32 INS42 MOV33 INS41 UPD42 MOV42 MOV42 UPD42 MOV42 UPD42 MOV42 INS9 DEL32 DEL38 DEL42 DEL32 DEL27 DEL32 DEL32 DEL27 DEL36 DEL27 DEL36 DEL41