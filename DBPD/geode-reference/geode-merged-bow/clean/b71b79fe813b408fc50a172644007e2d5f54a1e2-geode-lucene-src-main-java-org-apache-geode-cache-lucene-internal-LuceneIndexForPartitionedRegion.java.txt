GEODE-3926: handle new lucene indexing exception

	* Ignored test till LUCENE_REINDEX flag is removed
	* Indexing in progress exception will not be thrown if LUCENE_REINDEX flag is not enabled

-    return fileAndChunkRegion.get(IndexRepositoryFactory.APACHE_GEODE_INDEX_COMPLETE, id) != null;
+    return (fileAndChunkRegion.get(IndexRepositoryFactory.APACHE_GEODE_INDEX_COMPLETE, id) != null
+        || !LuceneServiceImpl.LUCENE_REINDEX);

INS36 INS27 MOV27 INS38 INS40