GEODE-5212: Ignore case when comparing paths. (#2268)

   LocatorLauncher and ServerLauncher should ignore 
   while validating workingDir path.

-        if (!CURRENT_DIRECTORY.equals(getWorkingDirectory())) {
+        if (!CURRENT_DIRECTORY.equalsIgnoreCase(getWorkingDirectory())) {

UPD42