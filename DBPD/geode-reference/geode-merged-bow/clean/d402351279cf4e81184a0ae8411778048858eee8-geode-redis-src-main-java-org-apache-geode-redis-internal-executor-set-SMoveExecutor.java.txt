GEODE-8114: Refactor RedisSet and RedisHash (#5100)

* broke up the RedisSet and RedisHash classes by moving the static methods into separate non-static classes.
* made canonical empty classes, so we don't have to do as much null-checking.

-import java.util.concurrent.atomic.AtomicBoolean;
-          RedisSet.srem(region, source, new ArrayList<>(Collections.singletonList(member)),
-              new AtomicBoolean()) == 1;
+          new RedisSetInRegion(region).srem(source,
+              new ArrayList<>(Collections.singletonList(member)),
+              null) == 1;
-          RedisSet.sadd(region, destination, new ArrayList<>(Collections.singletonList(member)));
+          new RedisSetInRegion(region).sadd(destination,
+              new ArrayList<>(Collections.singletonList(member)));

INS14 UPD42 INS42 INS33 INS43 INS42 INS42 INS14 INS43 INS42 INS42 DEL40 DEL26 DEL42 DEL42 DEL42 DEL42 DEL43 DEL14 DEL42 DEL42