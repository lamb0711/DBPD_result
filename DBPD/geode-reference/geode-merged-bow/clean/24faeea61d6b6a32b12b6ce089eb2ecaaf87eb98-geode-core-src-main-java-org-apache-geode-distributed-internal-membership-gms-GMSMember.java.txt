GEODE-7196 Simplify ClusterDistributionManage

remove distribution ID collections from ClusterDistributionManager

move Executors out of DistributionManager implementations

make membership views immutable - we shouldn't install a new view
and then modify it because it's now used directly by other code.

-    String uuid = formatUUID();
-    sb.append("GMSMember[name=").append(name)
-        .append(";addr=").append(inetAddr).append(";port=").append(udpPort)
+    sb.append("GMSMember[");
+    if (name != null && name.length() > 0) {
+      sb.append("name=").append(name);
+    }
+    sb.append(";addr=").append(inetAddr).append(";port=").append(udpPort)
-        .append(";viewId=").append(vmViewId)
-        .append(";version=").append(versionOrdinal).append(uuid)
-        .append("]");
+        .append(";viewId=").append(vmViewId);
+    if (versionOrdinal != Version.CURRENT_ORDINAL) {
+      sb.append(";version=").append(versionOrdinal);
+    }
+    sb.append("]");

INS21 INS25 INS21 INS25 INS21 INS32 INS27 INS8 INS32 INS27 INS8 INS32 INS42 INS42 INS45 INS27 INS27 INS21 MOV32 MOV42 MOV42 INS42 INS40 INS21 UPD42 MOV42 MOV42 MOV45 INS42 INS33 INS32 INS34 INS32 INS32 UPD42 MOV42 INS42 INS32 INS42 INS42 INS32 MOV42 MOV42 INS42 INS42 INS45 INS42 MOV42 MOV45 MOV42 MOV42 DEL45 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL43 DEL42 DEL32 DEL59 DEL60 DEL32 DEL32 DEL32 DEL42 DEL32 DEL32 DEL21