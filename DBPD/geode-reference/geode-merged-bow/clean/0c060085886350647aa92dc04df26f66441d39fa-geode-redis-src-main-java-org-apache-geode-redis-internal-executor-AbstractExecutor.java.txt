GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4781)

* Revert "Revert "GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4745)" (#4780)"

This reverts commit f0982cdedf1b122f734919d0647434cca33ec254.

* Fix sporadic test failures for concurrent HSetNX
- *
- *
-   *
+   * <p>
-   * a
-   * {@link RuntimeException} is thrown
+   * a {@link RuntimeException} is thrown
-    if (type == null || type == RedisDataType.REDIS_PROTECTED)
-      return false;
+
-    if (size < 0)
+    if (size < 0) {
-    if (index >= 0)
+    }
+    if (index >= 0) {
-    else
+    } else {
+    }
-    if (size < 0)
+    if (size < 0) {
-    if (index >= 0)
+    }
+    if (index >= 0) {
-    else
+    } else {
+    }
-    if (size < 0L)
+    if (size < 0L) {
-    if (index >= 0L)
+    }
+    if (index >= 0L) {
-    else
+    } else {
+    }
-    if (size < 0L)
+    if (size < 0L) {
-    if (index >= 0L)
+    }
+    if (index >= 0L) {
-    else
+    } else {
+    }

INS66 UPD66 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 MOV53 MOV41 MOV41 MOV53 MOV41 MOV41 MOV53 MOV41 MOV41 MOV53 MOV41 MOV41 DEL42 DEL33 DEL27 DEL42 DEL40 DEL27 DEL27 DEL9 DEL41 DEL25