Merge branch 'release/1.8.0'

+import org.apache.geode.internal.logging.LoggingThread;
-        Thread t = new Thread(new Runnable() {
-          @Override
-          public void run() {
-            try {
-              Thread.sleep(SLEEP_TIME_BEFORE_DISCONNECT_DS);
-            } catch (InterruptedException ignore) {
-            }
-            ConnectionTable.threadWantsSharedResources();
-            if (ids.isConnected()) {
-              ids.disconnect();
-            }
+        Thread t = new LoggingThread("ShutdownAllRequestDisconnectThread", false, () -> {
+          try {
+            Thread.sleep(SLEEP_TIME_BEFORE_DISCONNECT_DS);
+          } catch (InterruptedException ignore) {
+          }
+          ConnectionTable.threadWantsSharedResources();
+          if (ids.isConnected()) {
+            ids.disconnect();

INS26 INS40 MOV31 MOV78 UPD83 INS42 MOV44 INS8 MOV60 MOV21 INS25 INS42 INS8 MOV60 INS25 MOV32 INS8 INS60 MOV21 MOV43 INS59 INS42 INS14 INS43 INS45 INS9 INS86 INS42 MOV8 DEL42 DEL78 DEL42 DEL83 DEL39 DEL42 DEL42 DEL42 DEL43 DEL42 DEL42 DEL43 DEL1 DEL14 DEL14 DEL59 DEL60 DEL8 DEL25 DEL8 DEL25 DEL8 DEL31