GEode-5317: Add null check around reconnectDS

-        reconnectDS.disconnect();
+        if (reconnectDS != null) {
+          reconnectDS.disconnect();
+        }

INS8 INS25 INS27 MOV8 INS42 INS33