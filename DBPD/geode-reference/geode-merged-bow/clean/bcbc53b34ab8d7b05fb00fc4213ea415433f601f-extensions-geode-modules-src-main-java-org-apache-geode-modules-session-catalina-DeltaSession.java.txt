Merge pull request #5110 from metatype/develop

GEODE-7896:  Update tomcat version
-    super.setAttribute(name, value, false); // don't do notification since this is a replication
+    if (this.manager == null) {
+      // Name cannot be null
+      if (name == null) {
+        throw new IllegalArgumentException(sm.getString("standardSession.setAttribute.namenull"));
+      }
+
+      // Null value is the same as removeAttribute()
+      if (value == null) {
+        removeAttribute(name);
+        return;
+      }
+
+      // Validate our current state
+      if (!isValidInternal()) {
+        throw new IllegalStateException(
+            sm.getString("standardSession.setAttribute.ise", getIdInternal()));
+      }
+
+      // Replace or add this attribute
+      getAttributes().put(name, value);
+    } else {
+      super.setAttribute(name, value, false); // don't do notification since this is a replication
+    }

INS8 INS25 INS27 INS8 MOV8 INS22 INS33 INS25 INS25 INS25 INS21 INS52 INS42 INS27 INS8 INS27 INS8 INS38 INS8 INS32 INS42 INS33 INS53 INS42 INS33 INS21 INS41 INS32 INS53 INS32 INS42 INS42 INS42 INS14 INS32 INS42 INS14 INS42 INS43 INS32 INS42 INS42 INS43 INS32 INS42 INS42 INS42 INS45 INS42 INS42 INS42 INS45 INS32 INS42