GEODE-7006: Fixes function execution by id with transactions. (#3840)

* Fixes test to force and wait for PR metadata to update.
+    boolean inTransaction = TXManagerImpl.getCurrentTXState() != null;
-    if (pool.getPRSingleHopEnabled()) {
+    if (pool.getPRSingleHopEnabled() && !inTransaction) {

INS60 INS39 INS59 INS27 INS42 INS27 MOV32 INS38 INS32 INS33 INS42 INS42 INS42