Feature/geode 8144 (#5164)

* modified SocketCreator to look for a hostname if one is not present and endpoint verification is enabled

This fixes some problems when running in docker containers

* removed test shell script

* modified a test to use LocalHostUtil to get localhost

* remove use of InetAddress.getLocalHost & add a sanIpAddress if there is no name for the localhost address
+import org.apache.commons.validator.routines.InetAddressValidator;
+
+   * Returns true if the given hostname is an IPv4 or IPv6 numeric address
+   */
+  public static boolean isIPLiteral(String hostName) {
+    return InetAddressValidator.getInstance().isValid(hostName);
+  }
+
+  /**

INS26 INS40 INS31 INS29 INS83 INS83 INS39 INS42 INS44 INS8 INS65 INS43 INS42 INS41 INS66 INS42 INS32 INS32 INS42 INS42 INS42 INS42