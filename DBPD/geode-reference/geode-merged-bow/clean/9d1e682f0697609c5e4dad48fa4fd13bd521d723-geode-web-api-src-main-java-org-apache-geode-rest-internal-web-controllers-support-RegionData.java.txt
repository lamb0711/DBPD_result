GEODE-7869: Cleanup warnings in geode-web-api

-  private final List<T> data = new ArrayList<T>();
+  private final List<T> data = new ArrayList<>();
-    Assert.state(StringUtils.hasText(this.regionNamePath),
+    Assert.state(StringUtils.hasText(regionNamePath),
-  public RegionData<T> add(final T... data) {
+  @SafeVarargs
+  public final RegionData<T> add(final T... data) {
-    return this.data.isEmpty();
+    return data.isEmpty();
-    return Collections.unmodifiableList(this.data);
+    return Collections.unmodifiableList(data);
-    return this.data.size();
+    return data.size();

INS78 INS83 INS42 UPD74 INS42 INS42 INS42 INS42 DEL42 DEL43 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22