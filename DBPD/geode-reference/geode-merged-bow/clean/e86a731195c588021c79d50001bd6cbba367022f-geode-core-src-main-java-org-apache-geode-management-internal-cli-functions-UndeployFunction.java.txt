Merge branch 'release/1.3.0'

-import java.util.StringTokenizer;
+import org.apache.commons.lang.ArrayUtils;
-import org.apache.geode.cache.CacheFactory;
-  private InternalCache getCache() {
-    return (InternalCache) CacheFactory.getAnyInstance();
-  }
-
-      final String jarFilenameList = (String) args[0]; // Comma separated
-      InternalCache cache = getCache();
+      final String[] jarFilenameList = (String[]) args[0]; // Comma separated
+      InternalCache cache = (InternalCache) context.getCache();
-      if (jarFilenameList == null || jarFilenameList.equals("")) {
+      if (ArrayUtils.isEmpty(jarFilenameList)) {
-        StringTokenizer jarTokenizer = new StringTokenizer(jarFilenameList, ",");
-        while (jarTokenizer.hasMoreTokens()) {
-          String jarFilename = jarTokenizer.nextToken().trim();
+        for (String jarFilename : jarFilenameList) {

MOV26 UPD40 INS5 MOV43 INS32 MOV43 INS85 INS11 INS42 INS42 MOV42 INS70 INS5 MOV43 INS32 INS44 INS42 MOV8 MOV43 INS85 INS42 MOV42 MOV43 INS42 DEL40 DEL26 DEL83 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL11 DEL41 DEL8 DEL31 DEL32 DEL42 DEL33 DEL27 DEL42 DEL45 DEL32 DEL27 DEL42 DEL42 DEL42 DEL32 DEL42 DEL32 DEL59 DEL60 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL45 DEL14 DEL59 DEL60 DEL42 DEL42 DEL32 DEL61