GEODE-7869: Cleanup warnings in geode-gfsh

-import java.util.Iterator;
-public class ContinuousQueryFunction implements InternalFunction {
+public class ContinuousQueryFunction implements InternalFunction<String> {
-  public void execute(FunctionContext context) {
+  public void execute(FunctionContext<String> context) {
-      String clientID = (String) context.getArguments();
+      String clientID = context.getArguments();
-              Iterator<CacheClientProxy> it = cacheClientProxySet.iterator();
-              while (it.hasNext()) {
+              for (CacheClientProxy ccp : cacheClientProxySet) {
-                CacheClientProxy ccp = it.next();
+                    final String id = cache.getDistributedSystem().getDistributedMember().getId();
-                        (isPrimary == true
-                            ? cache.getDistributedSystem().getDistributedMember().getId() : ""),
-                        (isPrimary == false
-                            ? cache.getDistributedSystem().getDistributedMember().getId() : ""));
+                        (isPrimary ? id : ""),
+                        (!isPrimary ? id : ""));
-              if (foundClientinCCP == false) {
+              if (!foundClientinCCP) {

INS74 MOV43 INS43 INS42 INS74 MOV43 INS43 INS42 MOV43 MOV32 INS70 INS44 INS42 MOV8 INS38 MOV43 INS42 INS42 INS60 INS83 MOV43 INS59 INS42 MOV32 INS42 INS42 INS38 INS42 INS42 DEL40 DEL26 DEL11 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL9 DEL27 DEL42 DEL9 DEL27 DEL42 DEL42 DEL32 DEL42 DEL32 DEL42 DEL32 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL42 DEL32 DEL61 DEL42 DEL9 DEL27