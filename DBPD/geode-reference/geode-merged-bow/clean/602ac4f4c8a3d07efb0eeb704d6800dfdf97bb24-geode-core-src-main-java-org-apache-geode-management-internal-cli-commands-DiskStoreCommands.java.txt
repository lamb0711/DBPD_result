GEODE-2261: do not use remote function calls to update the shared configuration

+import java.util.concurrent.atomic.AtomicReference;
-import org.apache.geode.management.internal.configuration.SharedConfigurationWriter;
-      XmlEntity xmlEntity = null;
+      AtomicReference<XmlEntity> xmlEntity = new AtomicReference<>();
-          if (xmlEntity == null) {
-            xmlEntity = result.getXmlEntity();
+          if (xmlEntity.get() == null) {
+            xmlEntity.set(result.getXmlEntity());
-      if (xmlEntity != null) {
-        result
-            .setCommandPersisted((new SharedConfigurationWriter()).addXmlEntity(xmlEntity, groups));
+      if (xmlEntity.get() != null) {
+        persistClusterConfiguration(result,
+            () -> getSharedConfiguration().addXmlEntity(xmlEntity.get(), groups));
-      XmlEntity xmlEntity = null;
+      AtomicReference<XmlEntity> xmlEntity = new AtomicReference<>();
-          if (xmlEntity == null) {
-            xmlEntity = result.getXmlEntity();
+          if (xmlEntity.get() == null) {
+            xmlEntity.set(result.getXmlEntity());
-      if (xmlEntity != null) {
-        result.setCommandPersisted(
-            (new SharedConfigurationWriter()).deleteXmlEntity(xmlEntity, groups));
+      if (xmlEntity.get() != null) {
+        persistClusterConfiguration(result,
+            () -> getSharedConfiguration().deleteXmlEntity(xmlEntity.get(), groups));

MOV26 UPD40 INS74 INS74 INS43 MOV43 INS14 INS32 INS43 MOV43 INS14 INS32 INS42 INS74 INS42 INS42 INS42 INS74 INS42 INS42 INS43 INS42 INS86 INS43 INS42 INS86 INS42 INS32 INS42 INS32 INS32 MOV42 INS32 MOV42 INS32 MOV42 INS32 MOV42 INS32 INS42 MOV42 INS42 INS32 INS42 MOV42 INS42 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 MOV32 INS42 INS42 MOV32 DEL33 DEL42 DEL42 DEL7 DEL42 DEL42 DEL42 DEL43 DEL14 DEL36 DEL32 DEL33 DEL42 DEL42 DEL7 DEL42 DEL42 DEL42 DEL43 DEL14 DEL36 DEL32