GEODE-8147: change redis DELETE and EXISTS to use Function+Delta (#5128)

Co-authored-by: Sarah <mboxwala@pivotal.io>
Co-authored-by: Sarah <sabbey@pivotal.io>
-import org.apache.geode.redis.internal.RedisConstants.ArityDef;
-    if (commandElems.size() < 2) {
-      command.setResponse(Coder.getErrorResponse(context.getByteBufAllocator(), ArityDef.EXISTS));
-      return;
-    }
+    RedisKeyCommands redisKeyCommands =
+        new RedisKeyCommandsFunctionExecutor(context.getRegionProvider().getDataRegion());
-        .filter(key -> context.getKeyRegistrar().isRegistered(key))
+        .filter(key -> redisKeyCommands.exists(key))

INS60 INS43 INS59 INS42 INS42 INS14 INS43 INS32 INS42 MOV32 INS42 UPD42 UPD42 MOV42 UPD42 DEL40 DEL26 DEL42 DEL42 DEL32 DEL34 DEL27 DEL42 DEL42 DEL42 DEL42 DEL40 DEL32 DEL32 DEL21 DEL41 DEL8 DEL25 DEL42 DEL32