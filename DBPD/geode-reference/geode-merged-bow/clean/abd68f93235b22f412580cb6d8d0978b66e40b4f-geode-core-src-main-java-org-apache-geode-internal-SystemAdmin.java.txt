GEODE-6028: Remove checks that are not necessary (#2827)


-        if (sleepCount > maxSleepCount && !quiet) {
-          System.out.println(
-              "Locator process has terminated.");
-        } else if (OSProcess.exists(pid)) {
+        if (OSProcess.exists(pid)) {
+        } else if (!quiet) {
+          System.out.println(
+              "Locator process has terminated.");

MOV25 INS25 MOV38 MOV8 DEL42 DEL42 DEL27 DEL27 DEL25