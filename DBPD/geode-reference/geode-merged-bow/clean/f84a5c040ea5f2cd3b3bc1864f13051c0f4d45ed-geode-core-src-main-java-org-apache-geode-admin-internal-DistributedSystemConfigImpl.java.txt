Merge branch 'develop' into GEODE-5407

-              (array == null || array.isEmpty()) ? null : array.get(array.size() - 1);
+              array.isEmpty() ? null : array.get(array.size() - 1);

MOV32 INS33 DEL42 DEL33 DEL27 DEL27 DEL36 DEL33