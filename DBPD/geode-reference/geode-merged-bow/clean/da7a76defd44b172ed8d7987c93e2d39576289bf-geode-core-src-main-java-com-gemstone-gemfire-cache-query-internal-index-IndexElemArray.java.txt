Merge branch 'develop' into feature/GEODE-17-2

-    if (o == null) {
-      for (int index = 0; index < size; index++)
-        if (elementData[index] == null) {
-          fastRemove(index);
-          return true;
-        }
-    } else {
-      for (int index = 0; index < size; index++)
-        if (o.equals(elementData[index])) {
-          fastRemove(index);
-          return true;
-        }
+    synchronized (lock) {
+      if (o == null) {
+        for (int index = 0; index < size; index++)
+          if (elementData[index] == null) {
+            fastRemove(index);
+            return true;
+          }
+      } else {
+        for (int index = 0; index < size; index++)
+          if (o.equals(elementData[index])) {
+            fastRemove(index);
+            return true;
+          }
+      }
-    if (numMoved > 0)
+    if (numMoved > 0) {
-
-    synchronized (lock) {
-      elementData = newArray;
-      --size;
+    elementData = newArray;
+    --size;

INS8 INS51 MOV41 MOV21 MOV21 INS42 MOV8 INS8 MOV21 DEL42 DEL8 DEL51