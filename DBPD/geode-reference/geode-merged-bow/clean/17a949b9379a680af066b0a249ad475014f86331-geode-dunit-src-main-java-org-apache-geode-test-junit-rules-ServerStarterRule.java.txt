GEODE-6559: PdxInstance.getObject() is using class from older jar in case of Reconnect (#3353)

- TypeRegistry needs to be cleared during reconnect
- Fix ServerStarterRule to not blindly disable some PDX attributes on server start
- Currently configuring PDX, via gfsh, does not work correctly in
  conjunction with this rule.

Authored-by: Jens Deppe <jdeppe@pivotal.io>

+  private boolean pdxPersistentUserSet = false;
+  private boolean pdxReadSerializedUserSet = false;
+    pdxPersistentUserSet = true;
+    pdxReadSerializedUserSet = true;
-    cf.setPdxPersistent(pdxPersistent);
-    cf.setPdxReadSerialized(pdxReadSerialized);
+    if (pdxPersistentUserSet) {
+      cf.setPdxPersistent(pdxPersistent);
+    }
+    if (pdxReadSerializedUserSet) {
+      cf.setPdxReadSerialized(pdxReadSerialized);
+    }

INS23 INS23 INS83 INS39 INS59 INS83 INS39 INS59 INS42 INS9 INS42 INS9 INS21 INS21 INS25 INS25 INS7 INS7 INS42 INS8 INS42 INS8 INS42 INS9 INS42 INS9 MOV21 MOV21