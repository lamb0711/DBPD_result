GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4781)

* Revert "Revert "GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4745)" (#4780)"

This reverts commit f0982cdedf1b122f734919d0647434cca33ec254.

* Fix sporadic test failures for concurrent HSetNX
- *
- *
-    if (commandElems == null || commandElems.isEmpty())
+    if (commandElems == null || commandElems.isEmpty()) {
+    }
-
+    String commandName = null;
-      String commandName = Coder.bytesToString(charCommand).toUpperCase();
+      commandName = Coder.bytesToString(charCommand).toUpperCase();
-    if (response == null)
+    if (response == null) {
+    }
-    if (response.getByte(0) == Coder.ERROR_ID)
+    if (response.getByte(0) == Coder.ERROR_ID) {
+    }
-      } else if (this.key == null)
+      } else if (this.key == null) {
+      }
-    } else
+    } else {
+    }
-      if (this.bytes == null)
+      if (this.bytes == null) {
+      }
-    } else
+    } else {
+    }

INS60 INS8 MOV43 INS59 INS8 INS8 INS8 INS8 MOV53 INS42 INS33 INS21 MOV41 MOV41 MOV41 MOV41 INS7 INS8 INS42 MOV32 INS8 MOV21 MOV21 DEL42 DEL59 DEL60