GEODE-6828: check for debug/trace early(#3684)

Now debug or trace must be true for getNormalDistributionManagerIds (which does a synchronize) to be called.
-          if (mgr.getNormalDistributionManagerIds().size() > 1) {
-            // suppress this msg if we are the only member.
-            if (logger.isTraceEnabled()) {
-              logger.trace("<No Recipients> {}", this);
-            }
-          } else {
-            // suppress this msg if we are the only member.
-            if (logger.isDebugEnabled()) {
-              logger.debug("<No Recipients> {}", this);
+          if (logger.isDebugEnabled() || logger.isTraceEnabled()) {
+            if (mgr.getNormalDistributionManagerIds().size() > 1) {
+              // suppress this msg if we are the only member.
+              if (logger.isTraceEnabled()) {
+                logger.trace("<No Recipients> {}", this);
+              }
+            } else {
+              // suppress this msg if we are the only member.
+              if (logger.isDebugEnabled()) {
+                logger.debug("<No Recipients> {}", this);
+              }

INS8 INS25 INS27 MOV8 INS32 INS32 INS42 INS42 INS42 INS42