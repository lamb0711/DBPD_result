GEODE-3075: Add an integration test for the ServerConnectionFactory

the current implementation is bogus, but does test that the flow works.

+import java.io.IOException;
+  /**
+   * Bogus, but it lets us write an integration test so that nobody breaks our flow.
+   */
-      InternalCache cache) {}
+      InternalCache cache) throws IOException {
+    outputStream.write(inputStream.read());
+  }

INS26 INS40 INS29 INS43 INS65 INS42 INS21 INS66 INS32 INS42 INS42 INS32 INS42 INS42