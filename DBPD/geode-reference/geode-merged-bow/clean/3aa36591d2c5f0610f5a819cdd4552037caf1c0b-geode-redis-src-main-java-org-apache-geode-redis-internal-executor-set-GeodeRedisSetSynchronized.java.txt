GEODE 8014: delete redis sets and hashes when empty (#4989)


+import org.apache.geode.redis.internal.RedisDataType;
+
+
+    if (members().isEmpty()) {
+      RedisDataType type = context.getKeyRegistrar().getType(key);
+      if (type == RedisDataType.REDIS_SET) {
+        context.getRegionProvider().removeKey(key, type);
+      }
+    }
+

INS26 INS40 INS25 INS32 INS8 INS32 INS42 INS60 INS25 INS42 INS43 INS59 INS27 INS8 INS42 INS42 INS32 INS42 INS40 INS21 INS32 INS42 INS42 INS32 INS42 INS42 INS32 INS42 INS42 INS42 INS42 INS42