GEODE-6544: Handle unauthorised accesses to Pulse (#3362)

- Fixed minor warnings.
- Added a new 'access Denied' page to be used whenever an already
authenticated operator tries to access a resource for which not enough
permissions are granted.
-  private void login() {
+  public void login(String username, String password) {
-    WebElement userNameOnPulsePage =
-        (new WebDriverWait(driver, 30, 1000)).until(
-            (ExpectedCondition<WebElement>) d -> d.findElement(By.id("userName")));
+    WebElement userNameOnPulsePage = (new WebDriverWait(driver, 30, 1000))
+        .until((ExpectedCondition<WebElement>) d -> d.findElement(By.id("userName")));
+  private void login() {
+    login(username, password);
+  }
+

INS31 UPD83 INS44 INS44 INS83 INS39 INS42 INS8 INS43 INS42 INS43 INS42 INS21 INS42 INS42 INS32 INS42 INS42 INS42