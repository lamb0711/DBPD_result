GEODE-4414: Support explicit nulls in geode's protobuf protocol (#1437)


-    return serializationService.decode(request.getArguments());
+    if (request.hasArguments()) {
+      return serializationService.decode(request.getArguments());
+    } else {
+      return null;
+    }

INS8 INS25 INS32 MOV8 INS8 INS42 INS42 INS41 INS33