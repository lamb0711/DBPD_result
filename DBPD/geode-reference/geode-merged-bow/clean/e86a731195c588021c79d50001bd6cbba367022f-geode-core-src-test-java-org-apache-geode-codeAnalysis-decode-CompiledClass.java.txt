Merge branch 'release/1.3.0'

+import org.apache.geode.internal.ExitCode;
-      exit(3);
+      System.exit(3);
-      exit(3);
+      System.exit(3);
-      exit(3);
+      System.exit(3);
-      exit(3);
+      System.exit(3);
-    exit(0);
-  }
-
-  private static void exit(int exitCode) {
-    int b;
-    // if (false) {
-    // if (exitCode == 0)
-    // System.out.println("Done - press Enter to exit: ");
-    // else
-    // System.out.println("Press Enter to exit:");
-    // try {
-    // b = System.in.read();
-    // } catch (java.io.IOException e) {};
-    // }
-    System.exit(exitCode);
+    ExitCode.NORMAL.doSystemExit();

INS26 INS40 INS40 UPD42 INS42 INS42 INS42 INS42 DEL34 DEL83 DEL83 DEL39 DEL42 DEL39 DEL42 DEL44 DEL39 DEL42 DEL59 DEL60 DEL42 DEL42 DEL42 DEL32 DEL21 DEL8 DEL31