GEODE-7803: provide undeprecated internal region create (#4722)

You can now use InternalRegionFactory to create a region configured with InternalRegionArguments. No need to use the deprecated AttributesFactory.
InternalRegionFactory used to be named RegionFactoryImpl.

-    this.attrsFactory = new AttributesFactory<>(regionFactory.attrsFactory.create());
-    this.cache = regionFactory.cache;
+    this.attrsFactory = new AttributesFactory<>(regionFactory.getRegionAttributes());
+    this.cache = regionFactory.getCache();
-   * Returns the Region attributes used by this Region factory.
+   * Returns the attributes used by this factory to create a region.
-    return this.attrsFactory.create();
+    return attrsFactory.create();
-  @SuppressWarnings("unchecked")
+  @SuppressWarnings("deprecation")
-    @SuppressWarnings("deprecation")
-    RegionAttributes<K, V> ra = this.attrsFactory.create();
-    return getCache().createRegion(name, ra);
+    return getCache().createRegion(name, getRegionAttributes());
-  @SuppressWarnings("unchecked")
+  @SuppressWarnings({"deprecation", "unchecked"})
-    @SuppressWarnings("deprecation")
-    RegionAttributes<K, V> ra = this.attrsFactory.create();
-    return ((InternalRegion) parent).createSubregion(name, ra);
+    return ((InternalRegion) parent).createSubregion(name, getRegionAttributes());

MOV79 INS4 UPD66 INS45 INS45 INS32 INS42 INS32 INS32 INS42 INS42 UPD42 MOV42 UPD42 MOV42 UPD42 INS42 DEL40 DEL40 DEL52 DEL42 DEL22 DEL42 DEL45 DEL79 DEL42 DEL43 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL52 DEL42 DEL22 DEL42 DEL32 DEL59 DEL60 DEL45 DEL42 DEL45 DEL79 DEL42 DEL43 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL52 DEL42 DEL22 DEL42 DEL32 DEL59 DEL60