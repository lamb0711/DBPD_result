GEODE-2811: close OffHeapEvictor when cache is closed

Rejected executions are now ignored if shutting down.
execute now used instead of submit.
Close logic on HeapEvictor improved to prevent race conditions and NPEs.

+  /** Used by test to inject an evictor */
+  void setOffHeapEvictor(OffHeapEvictor evictor) {
+    this.offHeapEvictor = evictor;
+  }
+
+  /** Used by test to inject an evictor */
+  void setHeapEvictor(HeapEvictor evictor) {
+    this.heapEvictor = evictor;
+  }
+
-          HeapEvictor he = this.heapEvictor;
-          if (he != null) {
-            he.close();
-          }
+          closeHeapEvictor();
+          closeOffHeapEvictor();
+  private void closeOffHeapEvictor() {
+    OffHeapEvictor evictor = this.offHeapEvictor;
+    if (evictor != null) {
+      evictor.close();
+    }
+  }
+
+  private void closeHeapEvictor() {
+    HeapEvictor evictor = this.heapEvictor;
+    if (evictor != null) {
+      evictor.close();
+    }
+  }
+

INS31 INS31 INS31 INS31 INS29 INS39 INS42 INS44 INS8 INS29 INS39 INS42 INS44 INS8 INS83 INS39 INS42 INS8 INS83 INS39 INS42 INS8 INS65 INS43 INS42 INS21 INS65 INS43 INS42 INS21 INS60 INS25 MOV60 INS25 INS66 INS42 INS7 INS66 INS42 INS7 INS43 INS59 INS27 INS8 INS27 INS8 INS22 INS42 INS22 INS42 INS42 INS42 INS22 INS42 INS33 INS21 UPD42 INS42 INS33 INS21 INS52 INS42 INS52 INS42 INS52 INS42 INS32 INS32 INS42 INS42 INS42 INS42 INS21 MOV21 INS32 INS42 UPD42 DEL42 DEL42 DEL33 DEL27 DEL8 DEL25