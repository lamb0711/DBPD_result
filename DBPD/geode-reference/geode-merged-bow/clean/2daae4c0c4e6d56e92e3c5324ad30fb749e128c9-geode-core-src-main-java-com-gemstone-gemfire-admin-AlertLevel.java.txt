GEODE-1744: Fix probable bugs from == use

This closes #230

-		if (this.name != that.name &&
-	  		!(this.name != null &&
-	  		this.name.equals(that.name))) return false;
+		if (this.name != null &&
+	  		!this.name.equals(that.name)) return false;

MOV27 INS38 MOV32 DEL52 DEL42 DEL22 DEL40 DEL27 DEL36 DEL38 DEL27