Merge remote-tracking branch 'upstream/develop' into develop

-    if (filteredRecipients == null || filteredRecipients.size() == 0)
+    if (filteredRecipients.size() == 0)

MOV27 DEL42 DEL33 DEL27 DEL27