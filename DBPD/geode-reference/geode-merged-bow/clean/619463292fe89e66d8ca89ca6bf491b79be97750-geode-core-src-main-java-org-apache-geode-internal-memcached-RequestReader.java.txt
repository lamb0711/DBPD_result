Merge pull request #3229 from upthewaterspout/feature/make-immutable-GEODE-6380

GEODE-6380: Fixing a number of static fields to be immutable
-import org.apache.geode.annotations.internal.MakeImmutable;
-  @MakeImmutable
-  private static byte[] cleanByteArray;
+  @Immutable
+  private static final byte[] cleanByteArray = createCleanByteArray();
-  private byte[] getCleanByteArray() {
-    if (cleanByteArray != null) {
-      return cleanByteArray;
-    }
-    cleanByteArray = new byte[RESPONSE_HEADER_LENGTH];
+  private static byte[] createCleanByteArray() {
+    byte[] cleanByteArray = new byte[RESPONSE_HEADER_LENGTH];
+  private byte[] getCleanByteArray() {
+    return cleanByteArray;
+  }
+

INS31 INS83 INS83 UPD42 INS83 INS5 INS42 MOV8 UPD42 INS32 INS60 INS39 INS85 INS42 INS5 INS59 INS39 INS85 INS42 MOV3 DEL40 DEL26 DEL42 DEL33 DEL27 DEL25 DEL42 DEL7 DEL21