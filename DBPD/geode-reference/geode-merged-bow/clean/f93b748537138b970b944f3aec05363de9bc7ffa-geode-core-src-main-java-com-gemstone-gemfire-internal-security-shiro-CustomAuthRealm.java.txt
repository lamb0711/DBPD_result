GEODE-1712: introduce SecurityService interface for mocking

* create Unit Tests for client commands that use security

-    return new SimpleAuthenticationInfo(principal, authToken.getPassword(), REALM_NAME);
+    try {
+      return new SimpleAuthenticationInfo(principal, authToken.getPassword(), REALM_NAME);
+    } catch (IllegalArgumentException e) {
+      throw new IllegalArgumentException("failed for " + username + " " + password, e);
+    }

INS54 INS8 INS12 MOV41 INS44 INS8 INS43 INS42 INS53 INS42 INS14 INS43 INS27 INS42 INS42 INS45 INS42 INS45 INS42