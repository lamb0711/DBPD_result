GEODE-8072: check the null and stop the on-going query function when … (#5053)

* GEODE-8072: check the null and stop the on-going query function when cache is closing

+    if (region.isDestroyed()) {
+      throw new FunctionException("Region is destroyed");
+    }
+

INS25 INS32 INS8 INS42 INS42 INS53 INS14 INS43 INS45 INS42