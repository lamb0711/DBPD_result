GEODE-2619: Lucene APIs unwrap function exception and throw the cause.

	* This is done so that users do not encounter Function Exceptions while executing Lucene APIs.

	This closes #434

-      } else {
-        e.printStackTrace();
-        throw e;
+      } else if (e.getCause() instanceof RuntimeException) {
+        throw (RuntimeException) e.getCause();
+      throw e;

MOV53 INS25 INS62 MOV8 INS32 INS43 INS53 INS42 INS42 INS42 INS11 INS43 INS32 INS42 MOV42 UPD42 MOV42 DEL32 DEL21