Merge branch 'develop' into feature/GEODE-17-2

-import com.gemstone.gemfire.internal.offheap.StoredObject;
-      if (data instanceof StoredObject && !((StoredObject) data).isSerialized()) {
+      CachedDeserializable cd = (CachedDeserializable) data;
+      if (!cd.isSerialized()) {
-        data = ((StoredObject) data).getDeserializedForReading();
+        data = cd.getDeserializedForReading();
-        data = ((CachedDeserializable)data).getValue();
+        data = cd.getValue();
-      if (data instanceof StoredObject) {
-        // it is off-heap so do not unwrap it.
-        if (!((StoredObject) data).isSerialized()) {
-          isObject = false;
-        }
-      } else {
-        data = ((CachedDeserializable)data).getValue();
+      CachedDeserializable cd = (CachedDeserializable) data;
+      isObject = cd.isSerialized();
+      if (cd.usesHeapForStorage()) {
+        data = cd.getValue();

INS60 INS25 INS43 INS59 INS38 MOV8 MOV8 INS42 INS42 MOV11 MOV32 INS42 INS60 MOV21 INS25 INS43 INS59 INS32 MOV8 INS42 INS42 UPD42 MOV42 INS42 MOV11 INS32 INS42 INS42 INS42 INS42 INS42 DEL40 DEL26 DEL42 DEL43 DEL42 DEL11 DEL36 DEL42 DEL43 DEL42 DEL11 DEL36 DEL36 DEL42 DEL42 DEL43 DEL62 DEL38 DEL27 DEL25 DEL9 DEL36 DEL42 DEL43 DEL62 DEL42 DEL43 DEL42 DEL11 DEL36 DEL42 DEL32 DEL38 DEL8 DEL25 DEL8 DEL25