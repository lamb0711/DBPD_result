GEODE-4072: Fixed race condition in AckThreadReader shutdown.

	* The global connection variable may be set to null by another thread.
	* Assign the local variable to the connection variable and then perform all the null checks on that local variable.

-      if (connection != null) {
-        Connection conn = connection;
+      Connection conn = connection;
+      if (conn != null) {

MOV60 UPD42