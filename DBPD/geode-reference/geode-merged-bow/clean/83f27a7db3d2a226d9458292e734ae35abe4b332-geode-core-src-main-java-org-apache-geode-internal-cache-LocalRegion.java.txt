GEODE-3543: refactor ExpiryTask to not call GemFireCacheImpl.getInstance

+    if (!isEntryExpiryPossible()) {
+      return;
+    }
-      try {
-        if (isEntryExpiryPossible()) {
-          ExpiryTask.setNow();
-        }
+      ExpiryTask.doWithNowSet(this, () -> {
-      } finally {
-        ExpiryTask.clearNow();
-      }
+      });

INS25 INS38 INS8 MOV32 INS41 MOV21 UPD42 INS52 INS86 MOV8 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25 DEL8 DEL54