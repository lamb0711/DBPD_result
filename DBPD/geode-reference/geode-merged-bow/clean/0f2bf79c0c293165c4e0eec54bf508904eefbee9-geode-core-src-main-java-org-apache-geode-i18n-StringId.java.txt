GEODE-6380: Annotating static fields

Marking static fields as immutable or with an annotation that indicates
how the field should be fixed/removed.

+import org.apache.geode.annotations.Immutable;
+import org.apache.geode.annotations.internal.MakeImmutable;
+  @MakeImmutable("This appears to never be modified at runtime")
+  @MakeImmutable("This appears to never be modified at runtime")
+  @MakeImmutable("This appears to never be modified at runtime")
+  @Immutable

INS26 INS26 INS40 INS40 INS79 INS79 INS79 INS78 INS42 INS45 INS42 INS45 INS42 INS45 INS42