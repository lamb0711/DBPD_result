GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4781)

* Revert "Revert "GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4745)" (#4780)"

This reverts commit f0982cdedf1b122f734919d0647434cca33ec254.

* Fix sporadic test failures for concurrent HSetNX
-import org.apache.geode.cache.Region;
+/**
+ * <pre>
+ * Implementation of the HMGET command.
+ * Returns values associated with the specified fields in the hash stored for a given key.
+ *
+ * Examples:
+ *
+ * redis> HSET myhash field1 "Hello"
+ * (integer) 1
+ * redis> HSET myhash field2 "World"
+ * (integer) 1
+ * redis> HMGET myhash field1 field2 nofield
+ * 1) "Hello"
+ * 2) "World"
+ * 3) (nil)
+ *
+ * </pre>
+ */
-    Region<ByteArrayWrapper, ByteArrayWrapper> keyRegion = getRegion(context, key);
+    Map<ByteArrayWrapper, ByteArrayWrapper> map = getMap(context, key);
+
-    if (keyRegion == null) {
+    if (map == null) {
-    Map<ByteArrayWrapper, ByteArrayWrapper> results = keyRegion.getAll(fields);
-
-    for (ByteArrayWrapper field : fields)
-      values.add(results.get(field));
+    for (ByteArrayWrapper field : fields) {
+      values.add(map.get(field));
+    }

INS29 INS65 MOV60 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS66 INS8 UPD42 UPD42 INS21 UPD42 UPD42 UPD42 MOV32 UPD42 DEL40 DEL26 DEL42 DEL43 DEL42 DEL43 DEL42 DEL43 DEL74 DEL42 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL21