GEODE-6946: update the configuration objects to use non-primitive jav… (#3919)

The v2 management configuration classes now use non-primitive attributes so that "null" can be used to detect if an attribute has been set by the user calling the settor. This will allow partial update/patch operations to be supported in the future.
-    if (xmlObject.isReadSerialized() != null) {
-      pdx.setReadSerialized(xmlObject.isReadSerialized());
-    }
+
+    pdx.setReadSerialized(xmlObject.isReadSerialized());
-    if (xmlObject.isIgnoreUnreadFields() != null) {
-      pdx.setIgnoreUnreadFields(xmlObject.isIgnoreUnreadFields());
-    }
-    if (xmlObject.isPersistent() != null) {
-      pdx.setPersistent(xmlObject.isPersistent());
-    }
+    pdx.setIgnoreUnreadFields(xmlObject.isIgnoreUnreadFields());
+    pdx.setPersistent(xmlObject.isPersistent());

MOV21 MOV21 MOV21 DEL42 DEL42 DEL32 DEL33 DEL27 DEL8 DEL25 DEL42 DEL42 DEL32 DEL33 DEL27 DEL8 DEL25 DEL42 DEL42 DEL32 DEL33 DEL27 DEL8 DEL25