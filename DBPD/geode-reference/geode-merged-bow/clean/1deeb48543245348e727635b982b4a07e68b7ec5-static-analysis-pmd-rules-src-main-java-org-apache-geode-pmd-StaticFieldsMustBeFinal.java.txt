GEODE-6380: Renaming pmd project to pmd-rules

The gradle pmd plugin looks for a jar named pmd-X.X.X.jar to determine
the pmd version. On the release branch, when we remove the SNAPSHOT from
the version, our jar matches. Renaming the project so that our jar no
longer matches the plugin's regex.


