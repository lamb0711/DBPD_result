GEODE-7869: Cleanup warnings in geode-gfsh

+import java.nio.file.Files;
-@SuppressWarnings("rawtypes")
-  private HeadlessGfshShell shell = null;
-  private LinkedBlockingQueue queue = new LinkedBlockingQueue<>();
-  private long timeout = 20;
+  private HeadlessGfshShell shell;
+  private LinkedBlockingQueue<Object> queue = new LinkedBlockingQueue<>();
+  private long timeout;
-      throws ClassNotFoundException, IOException {
+      throws IOException {
-      throws ClassNotFoundException, IOException {
+      throws IOException {
-  @SuppressWarnings("unchecked")
-  LinkedBlockingQueue getQueue() {
+  LinkedBlockingQueue<Object> getQueue() {
-    private ResultHandler handler = null;
+    private ResultHandler handler;
-        throws ClassNotFoundException, IOException {
+        throws IOException {
-        while (true) {
-          try {
-            endOfShell.await();
-          } catch (InterruptedException e) {
-            // e.printStackTrace();
-          }
-          this.exitShellRequest = ExitShellRequest.NORMAL_EXIT;
-          setShellStatus(Status.SHUTTING_DOWN);
-          break;
+        try {
+          endOfShell.await();
+        } catch (InterruptedException ignore) {
+        this.exitShellRequest = ExitShellRequest.NORMAL_EXIT;
+        setShellStatus(Status.SHUTTING_DOWN);
-    private String name;
-    public HeadlessGfshConfig(String name, String parentDir) {
-      this.name = name;
+    public HeadlessGfshConfig(String name, String parentDir) throws IOException {
-      if (isDUnitTest(this.name)) {
-        fileNamePrefix = this.name;
+      if (isDUnitTest(name)) {
+        fileNamePrefix = name;
-      this.parentDir.mkdirs();
+      Files.createDirectories(this.parentDir.toPath());

INS26 INS40 INS74 INS74 MOV43 INS43 MOV43 INS43 MOV8 INS43 INS42 INS42 MOV21 INS54 INS42 MOV8 MOV8 INS32 INS42 INS42 INS42 MOV32 UPD42 INS42 UPD42 DEL42 DEL45 DEL79 DEL33 DEL34 DEL42 DEL43 DEL42 DEL43 DEL42 DEL45 DEL79 DEL33 DEL42 DEL43 DEL10 DEL9 DEL61 DEL54 DEL8 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22