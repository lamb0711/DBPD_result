GEODE-944: also check loopback address. This fixes the unit test
failure on ubuntu.

+      else if (((InetAddress) host).isLoopbackAddress()) {
+        return true;
+      }

INS25 INS32 INS8 MOV8 INS36 INS42 INS41 INS11 INS9 INS43 INS42 INS42