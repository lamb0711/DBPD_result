GEODE-2298: Executing a Function via Swagger without parameters throws an exception

-
+        if (typeValue == null)
+          return (T) new JSONObject();
-        if (NumberUtils.isPrimitiveOrObject(typeValue.toString())) {
+        if (NumberUtils.isPrimitiveOrObject(typeValue)) {

INS25 INS27 INS41 INS42 INS33 INS11 MOV42 INS43 INS14 INS42 INS43 INS42 DEL42 DEL32