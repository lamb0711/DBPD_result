GEODE-6058: recordVersion should allow update higher local version if for non-persistent region (#2851)


-            && region.isInitialized()) {
+            && region.isInitialized() && region.getDataPolicy().withPersistence()) {

INS27 MOV27 INS32 INS32 INS42 INS42 INS42