Merge branch 'release/1.3.0'

+import org.apache.geode.GemFireConfigException;
-
-      try {
-        if (encrypt == null) {
-          synchronized (this) {
-            if (encrypt == null) {
-              encrypt = GMSEncrypt.getEncryptCipher(dhSKAlgo, secretBytes);
-            }
+      if (encrypt == null) {
+        synchronized (this) {
+          if (encrypt == null) {
+            encrypt = GMSEncrypt.getEncryptCipher(dhSKAlgo, secretBytes);
-      } catch (Exception ex) {
-        throw ex;

INS26 INS40 MOV8 MOV41 DEL42 DEL43 DEL42 DEL44 DEL42 DEL53 DEL8 DEL12 DEL54 DEL8