GEODE-8166: reimplement redis expiration to use new data model (#5174)

* Added timestamp field to RedisData and implemented serialization and delta for it.
Both active and passive expiration have been implemented.

* RedisHash, RedisSet, and RedisString now subclass AbstractRedisData
* type command now uses function
* getset, incr, decr, incrby, and decrby now uses function and is atomic

-    if (cause instanceof RedisDataTypeMismatchException) {
+    if (cause instanceof NumberFormatException) {
+      response = RedisResponse.error(cause.getMessage());
+    } else if (cause instanceof ArithmeticException) {
+      response = RedisResponse.error(cause.getMessage());
+    } else if (cause instanceof RedisDataTypeMismatchException) {

INS25 INS62 INS8 INS25 INS42 INS43 INS21 INS62 INS8 MOV25 INS42 INS7 INS42 INS43 INS21 INS42 INS32 INS42 INS7 INS42 INS42 INS32 INS42 INS32 INS42 INS42 INS42 INS42 INS32 INS42 INS42