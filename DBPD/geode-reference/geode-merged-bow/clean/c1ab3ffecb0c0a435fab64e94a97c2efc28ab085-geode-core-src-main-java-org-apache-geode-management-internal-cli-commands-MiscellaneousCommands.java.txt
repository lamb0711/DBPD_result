GEODE-2977: make group/name option values consistent

* this closes #536

-      help = CliStrings.EXPORT_STACKTRACE__HELP) String memberNameOrId,
+      help = CliStrings.EXPORT_STACKTRACE__HELP) String[] memberNameOrId,
-          help = CliStrings.EXPORT_STACKTRACE__GROUP) String group,
+          help = CliStrings.EXPORT_STACKTRACE__GROUP) String[] group,
+
+    if (fileName == null) {
+      fileName = filePrefix.append("_").append(System.currentTimeMillis()).toString();
+    }
+    final File outFile = new File(fileName);
-      if (fileName == null) {
-        fileName = filePrefix.append("_").append(System.currentTimeMillis()).toString();
-      }
-      final File outFile = new File(fileName);
+
-      Set<DistributedMember> targetMembers = null;
-
-      if ((group == null || group.isEmpty())
-          && (memberNameOrId == null || memberNameOrId.isEmpty())) {
-        targetMembers = CliUtil.getAllMembers(cache);
-      } else {
-        targetMembers = CliUtil.findMembersOrThrow(group, memberNameOrId);
+      Set<DistributedMember> targetMembers = CliUtil.findMembers(group, memberNameOrId);
+      if (targetMembers.isEmpty()) {
+        return ResultBuilder.createUserErrorResult(CliStrings.NO_MEMBERS_FOUND_MESSAGE);
-    } catch (CommandResultException crex) {
-      return crex.getResult();
-    } catch (Exception ex) {
+    } catch (IOException ex) {

MOV8 INS5 INS5 MOV60 MOV60 INS54 MOV41 MOV43 INS85 MOV43 INS85 INS8 INS12 MOV25 MOV60 MOV60 MOV60 MOV60 MOV60 INS25 MOV60 MOV60 MOV70 MOV60 MOV21 MOV21 MOV21 INS44 MOV8 INS32 INS8 INS43 INS42 MOV21 INS32 UPD42 MOV42 MOV42 INS41 INS42 INS42 INS42 INS42 INS42 INS32 UPD42 UPD42 MOV42 UPD42 MOV42 INS40 MOV42 UPD42 MOV27 UPD42 MOV42 DEL33 DEL42 DEL33 DEL27 DEL32 DEL27 DEL36 DEL42 DEL33 DEL27 DEL32 DEL27 DEL36 DEL27 DEL42 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL8 DEL8 DEL25 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL32 DEL41 DEL8 DEL12 DEL42 DEL43 DEL42 DEL44 DEL12 DEL54 DEL8