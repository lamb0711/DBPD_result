GEODE-5983: use equals() instead of == (#2777)

* For boxed primitive types equals() is used instead of ==
-    if (index != opp)
+    if (index != null && (!index.equals(opp)))

UPD27 INS27 INS36 MOV42 INS33 INS38 INS32 INS42 INS42 INS42 DEL42