Merge branch 'release/1.3.0'

+    if (value instanceof byte[]) {
+      // already blobified
+      return;
+    }
-      value = session.getNativeSession().getAttribute(name);
+      value = session.getAttribute(name);

INS25 INS62 INS8 INS42 INS5 INS41 INS39 INS85 MOV42 DEL42 DEL32