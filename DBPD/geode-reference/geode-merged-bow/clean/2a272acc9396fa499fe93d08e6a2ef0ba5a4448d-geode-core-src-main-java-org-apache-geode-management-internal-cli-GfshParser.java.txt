GEODE-2880: fix help, hint, regionName, filePath value completions

+import org.apache.logging.log4j.Logger;
-import java.util.logging.Logger;
-  // Make use of LogWrapper
-  private static final LogWrapper logWrapper = LogWrapper.getInstance();
-  private static final org.apache.logging.log4j.Logger logger = LogService.getLogger();
+  private static final Logger logger = LogService.getLogger();
-  /**
-   * Used for warning messages
-   */
-  private Logger consoleLogger;
+
-    if (CliUtil.isGfshVM()) {
-      consoleLogger = Logger.getLogger(this.getClass().getCanonicalName());
-    } else {
-      consoleLogger = logWrapper.getLogger();
-    }
-
-    } else {
+    }
+    // if the completed values are option, and the userInput doesn't ends with an "=" sign,
+    else if (!userInput.endsWith("=")) {
-
-  @Override
-  public void obtainHelp(String command) {
-    if (StringUtils.isBlank(command))
-      super.obtainHelp(command);
-    else
-      consoleLogger.info(commandManager.obtainHelp(command));
-  }
-
-  public void obtainHint(String topic) {
-    consoleLogger.info(commandManager.obtainHint(topic));
-  }

MOV26 UPD40 MOV43 INS25 INS38 MOV8 INS32 INS42 INS42 INS45 DEL83 DEL83 DEL83 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL23 DEL40 DEL66 DEL65 DEL29 DEL83 DEL42 DEL59 DEL23 DEL42 DEL42 DEL32 DEL42 DEL42 DEL42 DEL52 DEL42 DEL32 DEL42 DEL32 DEL32 DEL7 DEL21 DEL8 DEL42 DEL42 DEL42 DEL32 DEL7 DEL21 DEL8 DEL25 DEL42 DEL78 DEL83 DEL39 DEL42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL48 DEL21 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL21 DEL25 DEL8 DEL31 DEL83 DEL39 DEL42 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL21 DEL8 DEL31