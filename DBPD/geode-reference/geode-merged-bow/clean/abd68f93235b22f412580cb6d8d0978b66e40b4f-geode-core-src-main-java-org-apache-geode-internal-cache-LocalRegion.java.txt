GEODE-6028: Remove checks that are not necessary (#2827)


-      if (numBS > 0) {
-        if (logger.isDebugEnabled()) {
-          logger.debug("{}: notifying {} cache servers of event: {}", this.getName(), numBS,
-              event);
-        }
+      if (logger.isDebugEnabled()) {
+        logger.debug("{}: notifying {} cache servers of event: {}", this.getName(), numBS,
+            event);

MOV25 DEL42 DEL34 DEL27 DEL8 DEL25