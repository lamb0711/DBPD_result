Initial import of geode-1.0.0.0-SNAPSHOT-2.
All the new sub-project directories (like jvsd) were not imported.
A diff was done to confirm that this commit is exactly the same as
the open directory the snapshot was made from.

-    final EntryEventImpl event = new EntryEventImpl(
+    final EntryEventImpl event = EntryEventImpl.create(
+    try {
+
+    } finally {
+      event.release();
+    }

INS54 MOV43 INS59 INS8 INS8 INS42 INS32 MOV25 MOV25 MOV21 MOV21 MOV60 MOV60 MOV21 MOV25 MOV41 INS21 INS42 INS42 INS42 MOV32 INS42 INS33 MOV32 INS9 INS42 INS9 INS9 INS32 INS42 INS42 DEL42 DEL43 DEL42 DEL42 DEL42 DEL33 DEL9 DEL42 DEL9 DEL9 DEL14 DEL59