GEODE-1648: Introduce security-enabled-components and tests

-      try {
-        InternalDistributedSystem newSystem =
-          InternalDistributedSystem.newInstance(config);
-        addSystem(newSystem);
-        return newSystem;
-
-      }
-      catch (GemFireSecurityException ex) {
-        ex.fillInStackTrace(); // Make it harder to figure out where jgroups code lives.
-        throw ex;
-      }
+      InternalDistributedSystem newSystem =
+        InternalDistributedSystem.newInstance(config);
+      addSystem(newSystem);
+      return newSystem;

MOV60 MOV21 MOV41 DEL8 DEL42 DEL43 DEL42 DEL44 DEL42 DEL42 DEL32 DEL21 DEL42 DEL53 DEL8 DEL12 DEL54