GEODE-2632: change dependencies on GemFireCacheImpl to InternalCache

* misc cleanup of code where possible

-
-import org.apache.geode.cache.Cache;
+import org.apache.geode.internal.cache.InternalCache;
- * 
-
-      Cache cache = CacheFactory.getAnyInstance();
+      InternalCache cache = getCache();
+  private InternalCache getCache() {
+    return (InternalCache) CacheFactory.getAnyInstance();
+  }
-      Cache cache = CacheFactory.getAnyInstance();
+      InternalCache cache = getCache();
-
-            if (object instanceof Exception) {
-              LogWrapper.getInstance().warning(
-                  "Exception in Describe Client " + ((Throwable) object).getMessage(),
-                  ((Throwable) object));
-              continue;
-            } else if (object instanceof Throwable) {
+            if (object instanceof Throwable) {
-
-

MOV26 UPD40 INS31 INS83 INS43 INS42 INS8 INS42 INS41 INS11 INS43 MOV32 UPD43 INS42 UPD43 UPD42 UPD42 INS32 UPD42 INS42 MOV25 DEL42 DEL42 DEL42 DEL43 DEL62 DEL42 DEL42 DEL32 DEL42 DEL45 DEL42 DEL43 DEL42 DEL11 DEL36 DEL42 DEL32 DEL27 DEL42 DEL43 DEL42 DEL11 DEL36 DEL32 DEL21 DEL18 DEL8 DEL25