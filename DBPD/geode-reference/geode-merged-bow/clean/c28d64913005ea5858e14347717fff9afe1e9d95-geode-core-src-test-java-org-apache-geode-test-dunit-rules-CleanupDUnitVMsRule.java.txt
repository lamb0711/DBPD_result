Merge branch 'release/1.4.0'

-
-import org.apache.geode.test.dunit.VM;
-import org.junit.After;
-import org.junit.rules.ExternalResource;
+import static org.apache.geode.test.dunit.Host.getHostCount;
+import org.junit.After;
+import org.junit.rules.ExternalResource;
+
+import org.apache.geode.test.dunit.VM;
+
+  public void before() {
+    if (getHostCount() > 0) {
+      getHost(0).getAllVMs().forEach(VM::bounce);
+    }
+  }
+
+  @Override

MOV26 MOV26 INS26 INS40 INS31 INS78 INS83 INS39 INS42 INS8 INS42 INS25 INS27 INS8 INS32 INS34 INS21 INS42 INS32 INS32 INS42 INS90 INS32 INS42 INS42 INS42 INS42 INS34