Revert "GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4745)" (#4780)

This reverts commit 2f6bf013368df5a4b5efe68162a4953f9a88bbf2.
-import java.util.Set;
-  private static final int EXISTS = 1;
+  private final int EXISTS = 1;
-  private static final int NOT_EXISTS = 0;
+  private final int NOT_EXISTS = 0;
-    if (commandElems.size() != 3) {
+    if (commandElems.size() < 3) {
-    if (!context.getKeyRegistrar().isRegistered(key)) {
-      command.setResponse(Coder.getIntegerResponse(context.getByteBufAllocator(), NOT_EXISTS));
-      return;
-    }
-
-    Region<ByteArrayWrapper, Set<ByteArrayWrapper>> region = this.getRegion(context);
+    checkDataType(key, RedisDataType.REDIS_SET, context);
+    @SuppressWarnings("unchecked")
+    Region<ByteArrayWrapper, Boolean> keyRegion =
+        (Region<ByteArrayWrapper, Boolean>) context.getRegionProvider().getRegion(key);
-    Set<ByteArrayWrapper> set = region.get(key);
-
-    if (set == null) {
+    if (keyRegion == null) {
-    if (set.contains(member)) {
+    if (keyRegion.containsKey(member))
-      // save key for next quick lookup
-      context.getKeyRegistrar().register(key, RedisDataType.REDIS_SET);
-    } else {
+    else
-    }
+

MOV21 INS60 UPD27 INS79 UPD74 MOV74 MOV59 MOV21 MOV21 UPD42 UPD42 INS40 MOV42 INS42 INS45 UPD43 MOV43 UPD43 UPD42 INS11 UPD42 UPD42 UPD42 UPD42 UPD42 UPD74 MOV74 INS32 UPD43 MOV43 INS32 UPD42 MOV42 MOV42 UPD42 INS42 UPD42 MOV42 DEL40 DEL26 DEL83 DEL83 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL38 DEL41 DEL8 DEL25 DEL42 DEL42 DEL42 DEL32 DEL42 DEL32 DEL74 DEL32 DEL42 DEL52 DEL42 DEL42 DEL32 DEL59 DEL60 DEL60 DEL42 DEL42 DEL32 DEL42 DEL42 DEL40 DEL32 DEL21 DEL8 DEL8