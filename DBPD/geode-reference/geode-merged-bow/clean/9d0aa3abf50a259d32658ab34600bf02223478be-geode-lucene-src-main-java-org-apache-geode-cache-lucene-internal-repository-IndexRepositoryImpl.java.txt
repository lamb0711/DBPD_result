GEODE-3273: catch serializer's exception for current event,
             not to interrupt other events.

+import java.util.Collections;
+    Collection<Document> docs = Collections.emptyList();
-      Collection<Document> docs = serializer.toDocuments(index, value);
-      docs.forEach(doc -> SerializerUtil.addKey(key, doc));
-      writer.addDocuments(docs);
+      try {
+        docs = serializer.toDocuments(index, value);
+      } catch (Exception e) {
+        stats.incFailedEntries();
+        logger.info("Failed to add index for " + value + " due to " + e.getMessage());
+      }
+      if (!docs.isEmpty()) {
+        docs.forEach(doc -> SerializerUtil.addKey(key, doc));
+        writer.addDocuments(docs);
+      }
+    Collection<Document> docs = Collections.emptyList();
-      Collection<Document> docs = serializer.toDocuments(index, value);
-      docs.forEach(doc -> SerializerUtil.addKey(key, doc));
-      Term keyTerm = SerializerUtil.toKeyTerm(key);
-      writer.updateDocuments(keyTerm, docs);
+      try {
+        docs = serializer.toDocuments(index, value);
+      } catch (Exception e) {
+        stats.incFailedEntries();
+        logger.info("Failed to update index for " + value + " due to " + e.getMessage());
+      }
+      if (!docs.isEmpty()) {
+        docs.forEach(doc -> SerializerUtil.addKey(key, doc));
+        Term keyTerm = SerializerUtil.toKeyTerm(key);
+        writer.updateDocuments(keyTerm, docs);
+      }

INS26 INS40 INS60 INS60 MOV74 INS59 INS8 MOV74 INS59 INS8 INS42 INS32 INS54 INS25 INS42 INS32 INS54 INS25 INS42 INS42 INS8 INS12 INS38 MOV8 INS42 INS42 INS8 INS12 INS38 MOV8 INS21 INS44 INS8 INS32 INS21 INS44 INS8 INS32 INS7 INS43 INS42 INS21 INS21 INS42 INS42 INS7 INS43 INS42 INS21 INS21 INS42 INS42 INS42 MOV32 INS42 INS32 INS32 INS42 MOV32 INS42 INS32 INS32 INS42 INS42 INS42 INS42 INS27 INS42 INS42 INS42 INS42 INS27 INS45 INS42 INS45 INS32 INS45 INS42 INS45 INS32 INS42 INS42 INS42 INS42 DEL42 DEL59 DEL60 DEL42 DEL59 DEL60