GEODE-7150: Use Long in OQL COUNT and AVG (#3994)

- Use 'long' instead of 'int' to compute OQL COUNT result.
- Use 'long' instead of 'int' to compute OQL AVG intermediate results.
+import static org.apache.geode.cache.query.internal.aggregate.AbstractAggregator.downCast;
+
-  private int count = 0;
+  private long count = 0;
-  int getCount() {
+  long getCount() {
-    return count;
+    return downCast(count);

INS26 INS40 UPD39 UPD39 INS32 INS42 INS42 DEL42