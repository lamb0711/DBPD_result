GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4781)

* Revert "Revert "GEODE-7828: Convert backing store for Redis Hashes and Sets to single regions (#4745)" (#4780)"

This reverts commit f0982cdedf1b122f734919d0647434cca33ec254.

* Fix sporadic test failures for concurrent HSetNX
- *
- *
-   *
+   * <p>
-    if (buffer == null)
+    if (buffer == null) {
-    if (!buffer.isReadable())
+    }
+    if (!buffer.isReadable()) {
+    }
-    if (firstB != arrayID)
+    if (firstB != arrayID) {
+    }
-    if (!parseArray(commandElems, buffer))
+    if (!parseArray(commandElems, buffer)) {
+    }
-    if (arrayLength == Integer.MIN_VALUE || !parseRN(buffer))
+    if (arrayLength == Integer.MIN_VALUE || !parseRN(buffer)) {
-    if (arrayLength < 0 || arrayLength > 1000000000)
+    }
+    if (arrayLength < 0 || arrayLength > 1000000000) {
+    }
-      if (!buffer.isReadable())
+      if (!buffer.isReadable()) {
+      }
-        if (newBulkString == null)
+        if (newBulkString == null) {
+        }
-      } else
+      } else {
+      }
-    if (bulkStringLength == Integer.MIN_VALUE)
+    if (bulkStringLength == Integer.MIN_VALUE) {
-    if (bulkStringLength > MAX_BULK_STRING_LENGTH)
+    }
+    if (bulkStringLength > MAX_BULK_STRING_LENGTH) {
-    if (!parseRN(buffer))
+    }
+    if (!parseRN(buffer)) {
+    }
-    if (!buffer.isReadable(bulkStringLength))
+    if (!buffer.isReadable(bulkStringLength)) {
+    }
-    if (!parseRN(buffer))
+    if (!parseRN(buffer)) {
+    }
-      if (!buffer.isReadable())
+      if (!buffer.isReadable()) {
+      }
-    if (!buffer.isReadable(2))
+    if (!buffer.isReadable(2)) {
+    }
-    if (b != rID)
+    if (b != rID) {
+    }
-    if (b != nID)
+    if (b != nID) {
+    }

INS66 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 INS8 MOV53 MOV41 MOV53 MOV41 MOV41 MOV53 MOV41 MOV53 MOV41 MOV41 MOV41 MOV41 MOV53 MOV53 INS8 INS8 INS33 MOV33 MOV33 INS8 MOV41 MOV53 MOV41 INS8 MOV41