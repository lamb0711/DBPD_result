GEODE-2485: fix leak in tx suspend/resume

test now closes the cache it creates

-          ((SystemTimerTask) task).cancel();
+          if (((SystemTimerTask) task).cancel()) {
+            _cache.purgeCCPTimer();
+          }
-      task.cancel();
+      if (task.cancel()) {
+        _cache.purgeCCPTimer();
+      }

INS25 MOV32 INS8 MOV21 INS32 INS42 INS42 INS25 MOV32 INS8 MOV21 INS32 INS42 INS42