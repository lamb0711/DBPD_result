GEODE-2182: try-catch around size estimation for index look ups

* Fix possible race condition where entry is destroyed

-   * @param newKey
-   * @param entry
-   * @throws TypeMismatchException
-
-   * 
-      if (re == null)
+      if (re == null) {
+      }
-   * 
+      if (IndexManager.testHook != null) {
+        IndexManager.testHook.hook(201);
+      }

INS25 INS8 INS27 INS8 MOV53 INS40 INS33 INS21 INS32 INS40 INS42 INS34 DEL42 DEL65 DEL42 DEL65 DEL42 DEL65