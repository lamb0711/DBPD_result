Merge remote-tracking branch 'origin/develop' into feature/GEODE-1162

-          writer.beforeCommit(getEvent());
+          TXEvent event = getEvent();
+          if (!event.hasOnlyInternalEvents()) {
+            writer.beforeCommit(event);
+          }
-          writer.beforeCommit(getEvent());
+          TXEvent event = getEvent();
+          if (!event.hasOnlyInternalEvents()) {
+            writer.beforeCommit(event);
+          }
-    return true;
+    return !getEvent().hasOnlyInternalEvents();

INS38 INS32 INS32 INS42 INS42 INS8 INS8 MOV21 INS60 INS25 MOV21 INS60 INS25 INS43 INS59 INS38 INS8 INS43 INS59 INS38 INS8 INS42 INS42 MOV32 INS32 MOV21 INS42 INS42 MOV32 INS32 MOV21 INS42 INS42 INS42 INS42 INS42 INS42 DEL8 DEL8 DEL9