GEODE-5830: Remove NONE enum for SSL configuration

The 'NONE' enum constant doesn't work and it's not used, so it's
entirely removed by this commit. Updated documentation and tests.
+
-        SecurableCommunicationChannel.NONE)) {
-      return false;
-    }
-    if (ArrayUtils.contains(distributionConfig.getSecurableCommunicationChannels(),
+
-        sslEnabledComponent) ? true : false;
+        sslEnabledComponent);

MOV32 DEL42 DEL42 DEL42 DEL42 DEL32 DEL40 DEL32 DEL9 DEL41 DEL8 DEL25 DEL9 DEL9 DEL16