GEODE-3286: Failing to cleanup connections from ConnectionTable

Merge branch 'feature/GEODE-3286' into develop

This closes #657

-import java.net.SocketAddress;
-  /**
-   * Stats from the delegate
-   */
-  DMStats stats;
+
+  private DMStats stats;
-    if (this.stats == null) {
+    if (this.getStats() == null) {
-    if (this.stats == null) {
+    if (this.getStats() == null) {
-            this.stats.incFailedAccept();
+            this.getStats().incFailedAccept();
-      getConTable().acceptConnection(othersock);
+      getConTable().acceptConnection(othersock, new PeerConnectionFactory());
-        this.stats.incFailedAccept();
+        this.getStats().incFailedAccept();
-        this.stats.incFailedAccept();
+        this.getStats().incFailedAccept();
-          this.stats.incFailedAccept();
+          this.getStats().incFailedAccept();
-          this.stats.incReconnectAttempts();
+          this.getStats().incReconnectAttempts();
+  /**
+   * Stats from the delegate
+   */
+  public DMStats getStats() {
+    return stats;
+  }
+

INS31 INS83 MOV29 INS83 INS43 INS42 INS8 INS42 INS41 INS42 INS32 INS32 INS52 INS42 INS52 INS42 INS14 INS43 INS42 INS32 INS32 INS52 INS42 INS52 INS42 INS32 INS32 INS52 INS42 INS52 INS42 INS32 INS52 INS42 DEL40 DEL26 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22 DEL52 DEL42 DEL22