GEODE-77 fixing uncaught exception in view creator

check for inclusion in the new view before trying to set failure detection
port

-            tempView.setFailureDetectionPort(mbr, newView.getFailureDetectionPort(mbr));
+            if (tempView.contains(mbr)) {
+              tempView.setFailureDetectionPort(mbr, newView.getFailureDetectionPort(mbr));
+            }

INS8 INS25 INS32 MOV8 INS42 INS42 INS42