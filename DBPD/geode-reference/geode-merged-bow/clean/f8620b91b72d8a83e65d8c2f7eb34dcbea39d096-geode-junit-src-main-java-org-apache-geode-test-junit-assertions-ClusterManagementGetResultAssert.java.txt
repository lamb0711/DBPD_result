GEODE-7699: use ConfigurationInfo to wrap the get result (#4596)

* GEODE-7699: use ConfigurationInfo to wrap the get result

Co-authored-by: Dale Emery <demery@pivotal.io>

-    return actual.getResult();
+    return actual.getResult().getConfigurationByGroup().get(0);
-    return assertThat(getActual().getConfigResult());
+    return assertThat(getActual().getResult().getConfigurationByGroup().get(0).getConfiguration());
-    return assertThat(getActual().getRuntimeResult());
+    return assertThat(getActual().getResult().getConfigurationByGroup().get(0).getRuntimeInfo());

INS32 INS32 INS32 INS32 INS42 INS34 INS42 INS32 INS42 INS32 MOV32 INS42 INS32 INS42 INS32 INS42 MOV32 MOV42 INS42 INS34 MOV32 MOV42 INS42 INS34 UPD42 UPD42 UPD42 UPD42