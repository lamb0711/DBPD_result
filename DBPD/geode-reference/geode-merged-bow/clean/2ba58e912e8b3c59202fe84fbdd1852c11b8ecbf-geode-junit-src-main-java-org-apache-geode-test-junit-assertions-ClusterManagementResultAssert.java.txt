GEODE-6811: view the runtime info of gateway receivers (#3764)

* add the GWR realizer to retrieve the runtime information
* be able to set member name in RuntimeInfo
* add unit test for gatewayreceiverRealizer

-  public ListAssert<ConfigurationResult<T, R>> hasListResult() {
-    return assertThat(actual.getResult());
+  public List<ConfigurationResult<T, R>> getResult() {
+    return actual.getResult();
+  };
+
+  public ListAssert<T> hasConfigurations() {
+    return assertThat(getActual().getConfigResult());
-  public ConfigurationResult<T, R> getResult(int index) {
-    return getActual().getResult().get(index);
+  public ListAssert<R> hasRuntimeInfos() {
+    return assertThat(getActual().getRuntimeResult());

INS31 UPD74 UPD42 INS83 INS74 INS42 MOV8 UPD74 INS42 INS8 INS43 MOV43 INS43 UPD43 INS41 INS42 MOV32 INS42 MOV32 UPD42 INS32 UPD42 INS42 INS32 UPD42 INS32 INS42 INS42 DEL42 DEL32 DEL42 DEL42 DEL43 DEL42 DEL39 DEL42 DEL44