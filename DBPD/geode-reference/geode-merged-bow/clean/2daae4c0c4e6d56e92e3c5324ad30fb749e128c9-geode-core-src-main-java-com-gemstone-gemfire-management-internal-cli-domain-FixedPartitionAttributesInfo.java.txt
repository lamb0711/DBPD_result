GEODE-1744: Fix probable bugs from == use

This closes #230

+import io.netty.util.internal.StringUtil;
+import org.apache.commons.lang.StringUtils;
+
-					this.partitionName == fpaInfo.getPartitionName() &&
+						 StringUtils.equals(this.partitionName, fpaInfo.getPartitionName()) &&

INS26 INS26 INS40 INS40 MOV27 UPD27 MOV27 MOV27 INS32 INS42 INS42 MOV22 MOV32 DEL27