GEODE-7902: fix flaky test (#4845)

* wait for a second before restarting the server for the file to get synced to the file system.
* unify the file copy operation
-import java.nio.file.Files;
-      Files.copy(stagedJar.toPath(), deployedFile);
+      FileUtils.copyFile(stagedJar, deployedFile.toFile());

UPD42 UPD42 MOV42 INS32 UPD42 MOV42 UPD42 MOV42 DEL40 DEL26 DEL32