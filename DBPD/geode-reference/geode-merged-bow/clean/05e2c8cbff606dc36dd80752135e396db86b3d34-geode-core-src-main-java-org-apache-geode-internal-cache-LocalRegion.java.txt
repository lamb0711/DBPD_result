GEODE-4573 set JTAPaused flag even if JTA is not yet bootstrapped when pauseTransaction is executed. (#1403)

* This will prevent discoverJTA to bootstrap a supposedly paused transaction.
+  private boolean isJTAPaused() {
+    TXManagerImpl txMgr = (TXManagerImpl) getCache().getCacheTransactionManager();
+    return txMgr.isJTAPaused();
+  }
+
-          if (isTransactionPaused()) {
+          if (isTransactionPaused() || isJTAPaused()) {

INS31 INS83 INS39 INS42 INS8 INS60 INS41 INS43 INS59 INS32 INS42 INS42 INS11 INS42 INS42 INS43 INS32 INS42 INS32 INS42 INS42 INS27 MOV32 INS32 INS42