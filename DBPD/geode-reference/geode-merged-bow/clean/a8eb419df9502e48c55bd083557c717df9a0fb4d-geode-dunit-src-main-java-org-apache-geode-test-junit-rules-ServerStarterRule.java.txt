GEODE-5971: describe member failed to display multiple cache servers (#3409)

* add support for multiple cache servers in ServerStartupRule



+import java.util.ArrayList;
+import java.util.List;
-  private transient CacheServer server;
+  private transient List<CacheServer> servers = new ArrayList<>();
-  private boolean noCacheServer = false;
+  // By default we start one server per jvm
+  private int serverCount = 1;
-    return server;
+    return servers.get(0);
+  }
+
+  public List<CacheServer> getServers() {
+    return servers;
+    for (CacheServer server : servers) {
+      server.stop();
+    }
-    server = null;
+    servers.clear();
-    this.noCacheServer = true;
+    this.serverCount = 0;
+    return this;
+  }
+
+  public ServerStarterRule withServerCount(int serverCount) {
+    this.serverCount = serverCount;
-    if (!noCacheServer) {
-      server = cache.addCacheServer();
+    if (serverCount > 1 && memberPort != 0) {
+      throw new IllegalStateException("can't specify a member port when you have multiple port");
+    }
+
+    for (int i = 0; i < serverCount; i++) {
+      CacheServer server = cache.addCacheServer();
-      server.setPort(memberPort);
+      if (serverCount == 1) {
+        server.setPort(memberPort);
+      } else {
+        server.setPort(0);
+      }
+      // if this member has multiple cache servers, the memberPort will be the last server's port
+      // started.
+      servers.add(server);

INS26 INS26 INS40 INS40 INS31 INS31 INS74 UPD39 INS83 INS74 INS42 INS8 INS83 INS43 INS42 INS44 INS8 INS43 MOV43 UPD42 INS14 UPD42 INS34 INS43 INS43 INS41 INS70 INS21 INS42 INS39 INS42 INS21 INS41 INS24 INS42 INS74 INS32 INS42 INS42 INS42 INS44 INS42 INS8 INS32 INS7 INS52 INS27 INS8 INS58 INS27 INS37 MOV8 INS43 INS42 INS42 INS34 INS43 INS42 MOV21 INS42 INS42 INS34 INS22 INS42 INS27 INS27 INS53 INS39 INS59 INS42 INS42 INS42 INS60 INS25 INS21 INS42 INS42 INS32 UPD42 INS52 INS42 INS42 INS34 INS42 INS34 INS14 INS42 INS34 INS43 INS59 INS27 INS8 INS8 INS32 INS42 INS42 INS43 INS45 INS42 INS42 MOV32 INS42 INS34 MOV21 MOV21 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS34 DEL9 DEL42 DEL42 DEL33 DEL7 DEL9 DEL42 DEL38 DEL42 DEL7