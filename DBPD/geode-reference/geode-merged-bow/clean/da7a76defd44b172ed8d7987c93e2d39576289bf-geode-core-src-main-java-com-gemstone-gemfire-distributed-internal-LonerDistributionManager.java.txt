Merge branch 'develop' into feature/GEODE-17-2

-    this.lonerPort = newPort;
-    this.getId().setPort(this.lonerPort);
+    this.lonerPort = newPort;
+    this.getId().setPort(this.lonerPort);

MOV21