fixing a possible memory leak

GMSMembershipManager thought it was destroying artifacts for shunned
members who had expired but it wasn't.

+    Set removedMembers = new HashSet();
+    
-      if (ll >= deathTime)
+      if (ll >= deathTime) {
+      }
+      
-      if (logger.isDebugEnabled())
+      if (logger.isDebugEnabled()) {
-    } // while
+      }
+      
+      removedMembers.add(mm);
+    }
+    // removed timed-out entries from the shunned-members collections
+    if (removedMembers.size() > 0) {
+      it = removedMembers.iterator();
+      while (it.hasNext()) {
+        InternalDistributedMember idm = (InternalDistributedMember)it.next();
+        endShun(idm);
+      }
+    }

INS60 INS25 INS43 INS59 INS27 INS8 INS42 INS42 INS14 INS21 INS32 INS34 INS21 INS61 INS43 INS8 INS8 INS32 INS42 INS42 INS7 INS32 INS8 INS42 INS18 MOV21 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS60 INS21 INS42 INS42 INS43 INS59 INS32 INS42 INS42 INS11 INS42 INS42 INS43 INS32 INS42 INS42 INS42 DEL18