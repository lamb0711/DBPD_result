GEODE-2161: avoid unnecessary byte copy of hdos.

-      byte[] myData = hdos.toByteArray();
-      DataSerializer.writeByteArray(myData, out);
+      if (out instanceof HeapDataOutputStream) {
+        ((HeapDataOutputStream) out).writeAsSerializedByteArray(hdos);
+      } else {
+        byte[] myData = hdos.toByteArray();
+        DataSerializer.writeByteArray(myData, out);
+      }
-        byte[] myData = hdos.toByteArray();
-        DataSerializer.writeByteArray(myData, out);
+        if (out instanceof HeapDataOutputStream) {
+          ((HeapDataOutputStream) out).writeAsSerializedByteArray(hdos);
+        } else {
+          byte[] myData = hdos.toByteArray();
+          DataSerializer.writeByteArray(myData, out);
+        }

INS25 INS62 INS8 INS8 INS42 INS43 INS21 MOV60 MOV21 INS25 INS42 INS32 INS62 INS8 INS8 INS36 INS42 INS42 INS42 INS43 INS21 MOV60 MOV21 INS11 INS42 INS32 INS43 INS42 INS36 INS42 INS42 INS42 INS11 INS43 INS42 INS42