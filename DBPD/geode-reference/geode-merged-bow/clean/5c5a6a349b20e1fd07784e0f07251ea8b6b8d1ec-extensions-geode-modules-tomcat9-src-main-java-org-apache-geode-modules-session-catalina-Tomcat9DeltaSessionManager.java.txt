GEODE:6784 Added unit tests to classes in the tomcat module (#4061)

Co-authored-by: Benjamin Ross <bross@pivotal.io>
Co-authored-by: Donal Evans <doevans@pivotal.io>
-    super.startInternal();
+    startInternalBase();
-    this.setState(LifecycleState.STARTING);
+    setLifecycleState(LifecycleState.STARTING);
+  }
+
+  void setLifecycleState(LifecycleState newState) throws LifecycleException {
+    this.setState(newState);
+  }
+
+  void startInternalBase() throws LifecycleException {
+    super.startInternal();
-    super.stopInternal();
+    stopInternalBase();
-    super.destroyInternal();
+    destroyInternalBase();
-    this.setState(LifecycleState.STOPPING);
+    setLifecycleState(LifecycleState.STOPPING);
+  void stopInternalBase() throws LifecycleException {
+    super.stopInternal();
+  }
+
+  void destroyInternalBase() throws LifecycleException {
+    super.destroyInternal();
+  }
+

INS31 INS31 INS31 INS31 INS39 INS42 INS44 INS43 INS8 INS39 INS42 INS43 INS8 INS39 INS42 INS43 INS8 INS39 INS42 INS43 INS8 INS21 INS43 INS42 INS42 INS21 INS42 MOV21 INS21 INS21 INS42 MOV21 INS42 MOV21 INS32 INS42 INS32 INS32 INS32 INS42 UPD42 INS52 INS42 INS42 INS42 INS42 UPD42 DEL52 DEL52