GEODE-3175 backward-compatibility tests fail with bad classpath

Attempt restart of a VM if it is unavailable.

-      return (VM) vms.get(n);
+      VM vm = (VM) vms.get(n);
+      vm.makeAvailable();
+      return vm;

INS60 INS21 INS43 INS59 INS32 INS42 INS42 INS42 MOV11 INS42 INS42