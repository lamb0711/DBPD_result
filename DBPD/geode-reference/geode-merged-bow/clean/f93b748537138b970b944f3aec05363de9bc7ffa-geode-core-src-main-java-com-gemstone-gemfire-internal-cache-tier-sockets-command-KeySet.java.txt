GEODE-1712: introduce SecurityService interface for mocking

* create Unit Tests for client commands that use security

-import com.gemstone.gemfire.internal.security.GeodeSecurityUtil;
-
-  private KeySet() {
-  }
-
-    CachedRegionHelper crHelper = servConn.getCachedRegionHelper();
-    LocalRegion region = (LocalRegion) crHelper.getRegion(regionName);
+    LocalRegion region = (LocalRegion) servConn.getCache().getRegion(regionName);
-    GeodeSecurityUtil.authorizeRegionRead(regionName);
+    try {
+      this.securityService.authorizeRegionRead(regionName);
+    } catch (NotAuthorizedException ex) {
+      writeChunkedException(msg, ex, false, servConn);
+      servConn.setAsTrue(RESPONDED);
+      return;
+    }

INS54 MOV43 INS8 INS12 MOV21 INS44 INS8 MOV43 INS43 INS42 INS21 INS21 INS41 INS32 INS22 INS42 INS32 INS32 UPD42 MOV42 INS42 INS52 INS42 INS42 INS42 INS42 INS9 INS42 INS42 INS42 INS42 DEL40 DEL26 DEL83 DEL42 DEL8 DEL31 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42