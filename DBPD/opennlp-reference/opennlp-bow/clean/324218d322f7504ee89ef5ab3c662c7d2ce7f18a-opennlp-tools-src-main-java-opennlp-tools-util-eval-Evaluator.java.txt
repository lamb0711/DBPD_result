OPENNLP-226 Now Evaluator has only one constructor that receives a variable length param.

git-svn-id: https://svn.apache.org/repos/asf/incubator/opennlp/trunk@1160806 13f79535-47bb-0310-9956-ffa450edef68

-import java.util.Arrays;
-import java.util.LinkedList;
-import java.util.List;
-  private List<EvaluationMonitor<T>> listeners;
-  
-  public Evaluator() {
-    this.listeners = null;
-  }
-  
-  public Evaluator(List<? extends EvaluationMonitor<T>> listeners) {
-    if(listeners != null) {
-      this.listeners = new LinkedList<EvaluationMonitor<T>>(listeners);
-    }
-  }
+  private EvaluationMonitor<T>[] listeners;
-    if(listeners != null) {
-      this.listeners = Arrays.asList(listeners);
-    }
+    this.listeners = listeners;
