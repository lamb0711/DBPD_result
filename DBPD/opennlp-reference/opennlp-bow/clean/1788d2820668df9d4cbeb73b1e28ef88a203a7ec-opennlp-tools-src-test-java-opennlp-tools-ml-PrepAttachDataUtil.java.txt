OPENNLP-947: Organize imports according to new order

This closes #75

-      for (int i = 1; i < ocs.length; i++)
-        if (ocs[i] > ocs[best])
+      for (int i = 1; i < ocs.length; i++) {
+        if (ocs[i] > ocs[best]) {
+        }
+      }
