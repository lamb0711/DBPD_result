OPENNLP-722 Ordering of features is removed from the Comparable Event. The feature odering should not depend on the ids assigned to them.

git-svn-id: https://svn.apache.org/repos/asf/opennlp/trunk@1633461 13f79535-47bb-0310-9956-ffa450edef68

-    if (values == null) {
-      Arrays.sort(pids);
-    } else {
-      sort(pids, values);
-    }
+//    if (values == null) {
+//      Arrays.sort(pids);
+//    } else {
+//      sort(pids, values);
+//    }
