OPENNLP-288 Now tokens are lower cased when old tag dictionary format is used and case sensitive is set to false.

git-svn-id: https://svn.apache.org/repos/asf/incubator/opennlp/trunk@1176832 13f79535-47bb-0310-9956-ffa450edef68

-      dictionary.put(parts[0], tags);
+      if (caseSensitive)
+        dictionary.put(parts[0], tags);
+      else
+        dictionary.put(parts[0].toLowerCase(), tags);
