OPENNLP-507 Added the getCoveredText method.

git-svn-id: https://svn.apache.org/repos/asf/opennlp/trunk@1334907 13f79535-47bb-0310-9956-ffa450edef68

+  public String getCoveredText() {
+    return text.substring(span.getStart(), span.getEnd());
+  }
+  
+  /**
+   * Represents this parse in a human readable way.  
+   */
-    return (text.substring(span.getStart(), span.getEnd()));
+    return getCoveredText();
