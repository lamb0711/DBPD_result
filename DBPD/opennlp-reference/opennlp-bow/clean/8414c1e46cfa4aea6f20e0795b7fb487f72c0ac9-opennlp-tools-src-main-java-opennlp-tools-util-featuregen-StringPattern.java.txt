OPENNLP-1197: fix bug, recognizing symbols (including #, %. & etc) (#322)


-        if (ch == ',' || ch == '.' || ch == '?' || ch == '!') {
+        if (ch != '・' && ch != 'ー' && ch != '〜')
-        }
