OPENNLP-994: Remove deprecated methods from the Document Categorizer, this closes apache/opennlp#133

+import java.util.ArrayList;
+import java.util.Arrays;
+import java.util.List;
- * exactly the same tokenization during testing and training.
+ * exactly the same tokenization during testing and training.ø
-
-
-    StringBuilder sampleText = new StringBuilder();
+    List<String> tokensList = new ArrayList<>();
-      for (int i = 1; i < tokens.length; i++) {
-        sampleText.append(tokens[i]);
-        sampleText.append(' ');
-      }
+      tokensList.addAll(Arrays.asList(tokens).subList(1, tokens.length));
-
-    if (sampleText.length() > 0) {
-      return new DocumentSample(language, sampleText.toString());
+    if (tokensList.size() > 0) {
+      return new DocumentSample(language, tokensList.toArray(new String[tokensList.size()]));

INS26 INS26 INS26 INS40 INS40 INS40 UPD66 INS74 INS43 INS43 UPD42 INS21 INS42 UPD42 MOV42 INS74 INS32 UPD42 UPD42 INS43 INS42 INS42 INS32 UPD42 MOV42 INS32 UPD42 MOV42 INS34 INS40 UPD42 INS42 INS3 UPD42 MOV42 UPD42 MOV42 INS42 INS5 INS32 INS43 INS85 INS42 INS42 INS42 DEL43 DEL43 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL42 DEL42 DEL2 DEL32 DEL21 DEL42 DEL13 DEL32 DEL21 DEL8 DEL24 DEL42