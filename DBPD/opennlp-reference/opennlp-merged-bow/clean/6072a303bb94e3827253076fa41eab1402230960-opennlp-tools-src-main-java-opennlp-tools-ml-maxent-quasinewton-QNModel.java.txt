Fix bug in pred label mapping

The pred labels are mapping the features from
string to int representation. The bug introduced in
this issue randomized this mapping when the model
is saved. This commit is not fixing this and maps
the features again correctly.

See issue OPENNLP-830

-    
-    if (predicate == null) throw new RuntimeException("ASDASFAS");
-    if (pmap == null) throw new RuntimeException("ASDASFAXXXXXXXS");
-    
-    pmap.keySet().toArray(pmapArray);
+    for (String pred : pmap.keySet()) {
+      pmapArray[pmap.get(pred)] = pred;
+    }

INS70 INS44 MOV32 INS8 INS43 INS42 MOV21 INS42 INS7 INS2 INS42 INS42 INS32 UPD42 MOV42 INS42 UPD42 MOV42 DEL42 DEL33 DEL27 DEL42 DEL43 DEL45 DEL14 DEL53 DEL25 DEL42 DEL33 DEL27 DEL42 DEL43 DEL45 DEL14 DEL53 DEL25 DEL32