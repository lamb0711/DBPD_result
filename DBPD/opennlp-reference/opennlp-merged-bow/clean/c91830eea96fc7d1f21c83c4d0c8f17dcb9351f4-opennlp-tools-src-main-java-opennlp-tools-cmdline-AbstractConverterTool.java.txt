Use try with resources

See issue OPENNLP-872

-      ObjectStream<T> sampleStream = streamFactory.create(formatArgs);
-
-      try {
+      try (ObjectStream<T> sampleStream = streamFactory.create(formatArgs)) {
-      finally {
-        if (sampleStream != null)
-          try {
-            sampleStream.close();
-          } catch (IOException e) {
-            // sorry that this can fail
-          }
-      }

INS58 MOV74 MOV59 DEL60 DEL42 DEL33 DEL27 DEL42 DEL42 DEL32 DEL21 DEL8 DEL42 DEL43 DEL42 DEL44 DEL8 DEL12 DEL54 DEL25 DEL8