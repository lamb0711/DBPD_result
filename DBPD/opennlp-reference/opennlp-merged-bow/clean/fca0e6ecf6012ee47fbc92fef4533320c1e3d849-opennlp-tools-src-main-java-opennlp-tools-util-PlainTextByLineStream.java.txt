OPENNLP-1149: remove unused member in PlainTextByLineStream and change the type of encoding (#280)

OPENNLP-1149: remove unused member in PlainTextByLineStream, change the type of encoding and add null-check for inputStreamFactory
-import java.nio.channels.Channels;
-import java.nio.channels.FileChannel;
+import java.util.Objects;
-  private final FileChannel channel;
-  private final String encoding;
+  private final Charset encoding;
-    this.inputStreamFactory = inputStreamFactory;
-    this.channel = null;
-    this.encoding = charset.name();
+    this.inputStreamFactory =
+        Objects.requireNonNull(inputStreamFactory, "inputStreamFactory must not be null!");
+    this.encoding = charset;
-    if (inputStreamFactory != null) {
-      in = new BufferedReader(new InputStreamReader(inputStreamFactory.createInputStream(),
+    in = new BufferedReader(new InputStreamReader(inputStreamFactory.createInputStream(),
-    } else if (channel == null) {
-      in.reset();
-    } else {
-      channel.position(0);
-      in = new BufferedReader(Channels.newReader(channel, encoding));
-    }
-    if (in != null && channel == null) {
+    if (in != null) {
-    } else if (channel != null) {
-      channel.close();

MOV26 UPD40 UPD43 MOV59 MOV8 UPD42 MOV27 INS32 INS42 INS42 INS42 INS42 INS45 DEL40 DEL26 DEL42 DEL59 DEL83 DEL83 DEL42 DEL43 DEL23 DEL42 DEL52 DEL42 DEL22 DEL33 DEL7 DEL21 DEL42 DEL42 DEL32 DEL42 DEL33 DEL27 DEL42 DEL33 DEL27 DEL42 DEL42 DEL32 DEL21 DEL8 DEL42 DEL42 DEL34 DEL32 DEL21 DEL42 DEL42 DEL43 DEL42 DEL42 DEL42 DEL42 DEL32 DEL14 DEL7 DEL21 DEL8 DEL25 DEL25 DEL8 DEL42 DEL33 DEL27 DEL27 DEL42 DEL33 DEL27 DEL42 DEL42 DEL32 DEL21 DEL8 DEL25