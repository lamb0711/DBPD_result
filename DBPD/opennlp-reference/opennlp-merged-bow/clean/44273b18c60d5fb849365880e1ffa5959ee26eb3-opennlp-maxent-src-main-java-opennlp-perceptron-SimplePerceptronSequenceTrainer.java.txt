OPENNLP-369 Now using for each loop. Thanks to Aliaksandr Autayeu for providing patch.

git-svn-id: https://svn.apache.org/repos/asf/incubator/opennlp/trunk@1200823 13f79535-47bb-0310-9956-ffa450edef68

-        for (int ei=0;ei<taggerEvents.length;ei++) {
-          String[] contextStrings = taggerEvents[ei].getContext();
-          float values[] =taggerEvents[ei].getValues();
-          int oi = omap.get(taggerEvents[ei].getOutcome());
-          for (int ci=0;ci<contextStrings.length;ci++) {
+        for (Event taggerEvent : taggerEvents) {
+          String[] contextStrings = taggerEvent.getContext();
+          float values[] = taggerEvent.getValues();
+          int oi = omap.get(taggerEvent.getOutcome());
+          for (int ci = 0; ci < contextStrings.length; ci++) {

INS70 INS44 INS42 MOV8 INS43 INS42 INS42 INS42 INS42 INS42 DEL42 DEL42 DEL2 DEL42 DEL42 DEL2 DEL42 DEL42 DEL2 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL40 DEL27 DEL42 DEL37 DEL24