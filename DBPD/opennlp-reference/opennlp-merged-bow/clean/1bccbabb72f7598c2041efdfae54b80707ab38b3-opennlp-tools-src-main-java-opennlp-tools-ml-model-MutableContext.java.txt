Correct indentation and white spaces

This closes #30

See issue OPENNLP-914

-    parameters[outcomeIndex]=value;
+    parameters[outcomeIndex] = value;
-    parameters[outcomeIndex]+=value;
+    parameters[outcomeIndex] += value;

