OPENNLP-368 Now using for each loop. Thanks to Aliaksandr Autayeu for providing patch.

git-svn-id: https://svn.apache.org/repos/asf/incubator/opennlp/trunk@1200836 13f79535-47bb-0310-9956-ffa450edef68

-      for (int nameIndex = 0; nameIndex < names.size(); nameIndex++) {
-        if (names.get(nameIndex).getStart() == tokenIndex) {
+      for (Span name : names) {
+        if (name.getStart() == tokenIndex) {
-          if (names.get(nameIndex).getType() == null) {
+          if (name.getType() == null) {
-            result.append(NameSampleDataStream.START_TAG_PREFIX).append(names.get(nameIndex).getType()).append("> ");
+            result.append(NameSampleDataStream.START_TAG_PREFIX).append(name.getType()).append("> ");
-        if (names.get(nameIndex).getEnd() == tokenIndex) {
+        if (name.getEnd() == tokenIndex) {
-    
-    for (int nameIndex = 0; nameIndex < names.size(); nameIndex++) {
-      if (names.get(nameIndex).getEnd() == sentence.size()) {
+
+    for (Span name : names) {
+      if (name.getEnd() == sentence.size()) {

MOV8 MOV60 MOV25 MOV25 INS70 MOV41 MOV58 MOV27 MOV37 INS8 INS44 INS42 INS8 INS70 MOV21 INS43 INS42 INS25 INS44 INS42 MOV8 MOV42 INS42 INS27 MOV8 INS43 INS42 INS42 INS13 INS32 MOV32 INS42 UPD42 MOV42 INS42 INS42 UPD42 MOV42 MOV42 UPD42 MOV42 INS42 INS42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL32 DEL27 DEL42 DEL37 DEL24 DEL39 DEL42 DEL34 DEL59 DEL58 DEL42 DEL42 DEL42 DEL32 DEL27 DEL42 DEL37 DEL42 DEL42 DEL42 DEL32 DEL42 DEL32 DEL27 DEL25 DEL8 DEL24 DEL8