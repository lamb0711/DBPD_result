OPENNLP-947: Organize imports according to new order, this closes apache/opennlp#74

-    for (int i = 0, il = tokens.length; i < il; i++) {
-      Span s = tokens[i];
+    for (Span s : tokens) {
-      }
-      else if (useAlphaNumericOptimization() && alphanumeric.matcher(tok).matches()) {
+      } else if (useAlphaNumericOptimization() && alphanumeric.matcher(tok).matches()) {
-      }
-      else {
+      } else {
-            model.eval(cg.getContext(tok, j - origStart));
+              model.eval(cg.getContext(tok, j - origStart));

INS70 INS44 INS42 MOV8 MOV43 INS42 DEL42 DEL42 DEL42 DEL2 DEL59 DEL60 DEL39 DEL42 DEL34 DEL59 DEL42 DEL40 DEL59 DEL58 DEL42 DEL42 DEL27 DEL42 DEL37 DEL24