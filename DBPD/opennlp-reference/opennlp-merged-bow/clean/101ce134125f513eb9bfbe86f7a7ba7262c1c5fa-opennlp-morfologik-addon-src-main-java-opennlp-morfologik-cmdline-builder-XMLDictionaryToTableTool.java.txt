OPENNLP-910: Add checkstyle, this closes apache/opennlp#37

-          if(valid(word, tag)) {
+          if (valid(word, tag)) {
-    return "" + SEPARATOR +// base
+    return "" + SEPARATOR + // base
-

