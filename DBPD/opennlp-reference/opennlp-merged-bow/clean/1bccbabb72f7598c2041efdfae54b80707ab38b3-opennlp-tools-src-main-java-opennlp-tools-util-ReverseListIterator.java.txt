Correct indentation and white spaces

This closes #30

See issue OPENNLP-914

-    index = list.size()-1;
-    this.list=list;
+    index = list.size() - 1;
+    this.list = list;
-    return index >=0;
+    return index >= 0;

