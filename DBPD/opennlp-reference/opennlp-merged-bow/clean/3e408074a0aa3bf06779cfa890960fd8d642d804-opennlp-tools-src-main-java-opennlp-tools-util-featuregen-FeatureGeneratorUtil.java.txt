OPENNLP-1014: Add more tests for featuregen

This closes #151

-    else if (pattern.isAllCapitalLetter() && token.length() == 1) {
-      feat = "sc";
-    }
-      feat = "ac";
+      if (token.length() == 1) {
+        feat = "sc";
+      }
+      else {
+        feat = "ac";
+      }

MOV25 MOV32 MOV8 MOV27 MOV8 MOV27 MOV8 MOV32 MOV8 INS8 INS25 MOV27 MOV8 MOV8 DEL42 DEL42 DEL32 DEL27 DEL25