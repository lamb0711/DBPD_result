Use try with resources

See issue OPENNLP-872

-    InputStream versionIn = Version.class.getResourceAsStream("opennlp.version");
-
-    if (versionIn != null) {
-      try {
+    try (InputStream versionIn = 
+        Version.class.getResourceAsStream("opennlp.version")) {
+      if (versionIn != null) {
-      } catch (IOException e) {
-        // ignore error
-      finally {
-        try {
-          versionIn.close();
-        } catch (IOException e) {
-          // ignore error
-        }
-      }
+    } catch (IOException e) {
+      // ignore error

INS54 INS58 INS8 MOV12 MOV43 MOV59 MOV25 MOV8 DEL42 DEL42 DEL32 DEL21 DEL8 DEL42 DEL43 DEL42 DEL44 DEL8 DEL12 DEL54 DEL8 DEL54 DEL8 DEL60