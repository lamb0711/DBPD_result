OPENNLP-910: Add checkstyle, this closes apache/opennlp#29

-          if (valid(word,tag)) {
+          if(valid(word, tag)) {
-    return "" + SEPARATOR + // base
-        word + SEPARATOR +
-        tag;
+    return "" + SEPARATOR +// base
+        word + SEPARATOR + tag;

