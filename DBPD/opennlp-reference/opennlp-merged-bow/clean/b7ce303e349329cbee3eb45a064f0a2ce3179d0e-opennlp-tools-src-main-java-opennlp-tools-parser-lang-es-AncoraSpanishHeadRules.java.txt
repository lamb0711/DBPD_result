OPENNLP-922: Refactor equals and hashCode, add missing hashCode

This closes #42

Also adds a checkstyle rule to enforce presence
of both hashCode and equals

+import java.util.Objects;
+    public int hashCode() {
+      return Objects.hash(leftToRight, Arrays.hashCode(tags));
+    }
+
+    @Override
-      else if (obj instanceof HeadRule) {
+
+      if (obj instanceof HeadRule) {
-      else {
-        return false;
-      }
+
+      return false;
+  public int hashCode() {
+    return Objects.hash(headRules, punctSet);
+  }
+
+  @Override
-    else if (obj instanceof AncoraSpanishHeadRules) {
+
+    if (obj instanceof AncoraSpanishHeadRules) {
-      return rules.headRules.equals(headRules) &&
-          rules.punctSet.equals(punctSet);
+      return rules.headRules.equals(headRules)
+          && rules.punctSet.equals(punctSet);
-    else {
-      return false;
-    }
+
+    return false;

INS26 INS40 INS31 INS31 INS78 INS83 INS39 INS42 INS8 INS78 INS83 INS39 INS42 INS8 INS42 INS41 MOV25 MOV41 INS42 INS41 MOV25 MOV41 INS32 INS32 INS42 INS42 INS42 INS42 INS42 INS42 INS42 INS32 INS42 INS42 INS42 DEL8 DEL8