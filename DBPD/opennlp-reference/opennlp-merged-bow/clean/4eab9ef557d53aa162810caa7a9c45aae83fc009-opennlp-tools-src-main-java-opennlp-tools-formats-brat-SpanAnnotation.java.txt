OPENNLP-646 Applied patch to add a getCoveredText method to the SpanAnnotation. And updated AnnotationConfiguration to always use UTF-8 to read in the config file. Thanks to Peter Thygesen for providing a patch.

git-svn-id: https://svn.apache.org/repos/asf/opennlp/trunk@1568932 13f79535-47bb-0310-9956-ffa450edef68

-  private final String coveredText;
-  
-  SpanAnnotation(String id, String type, Span span, String coveredText) {
+
+  SpanAnnotation(String id, String type, Span span) {
-    this.coveredText = coveredText;
-  
-  public String getCoveredText() {
-    return coveredText;
+
+    // or change to CharSequence like opennlp Span
+  public String getCoveredText(String text) {
+    return getSpan().getCoveredText(text).toString();
-    return super.toString() + " " + span.getStart() + " " + span.getEnd() + " " + coveredText;
+    return super.toString() + " " + span.getStart() + " " + span.getEnd();

MOV44 UPD42 INS32 INS32 INS42 INS32 INS42 INS42 INS42 DEL83 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL42 DEL45 DEL42