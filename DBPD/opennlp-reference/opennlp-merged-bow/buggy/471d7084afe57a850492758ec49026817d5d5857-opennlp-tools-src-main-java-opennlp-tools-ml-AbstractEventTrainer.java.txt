OPENNLP-581 Now the trainers abstract implementations stores a type parameter. Also, they call the isValid method, so each trainer don't have to do that anymore.

git-svn-id: https://svn.apache.org/repos/asf/opennlp/trunk@1490391 13f79535-47bb-0310-9956-ffa450edef68

+    
+    if (!isValid()) {
+      throw new IllegalArgumentException("trainParams are not valid!");
+    }
+    
-    addToReport("Training-Eventhash", hses.calculateHashSum().toString(16));
+    addToReport("Training-Eventhash", hses.calculateHashSum().toString(16)); 
+    addToReport(AbstractTrainer.TRAINER_TYPE_PARAM, EventTrainer.EVENT_VALUE); 

INS25 INS21 INS38 INS8 INS32 INS32 INS53 INS42 INS40 INS40 INS42 INS14 INS43 INS45 INS42