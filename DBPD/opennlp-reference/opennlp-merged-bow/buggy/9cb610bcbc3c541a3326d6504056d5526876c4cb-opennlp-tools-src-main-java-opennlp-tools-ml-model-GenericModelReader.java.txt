OPENNLP-871: Clean up code base for release

This closes #20

-    if (modelType.equals("Perceptron")) {
-      delegateModelReader = new PerceptronModelReader(this.dataReader);
-    }
-    else if (modelType.equals("GIS")) {
-      delegateModelReader = new GISModelReader(this.dataReader);
-    }
-    else if (modelType.equals("QN")) {
+    switch (modelType) {
+      case "Perceptron":
+        delegateModelReader = new PerceptronModelReader(this.dataReader);
+        break;
+      case "GIS":
+        delegateModelReader = new GISModelReader(this.dataReader);
+        break;
+      case "QN":
-    }
-    else if (modelType.equals("NaiveBayes")) {
+        break;
+      case "NaiveBayes":
-    }
-    else {
-      throw new IOException("Unknown model format: "+modelType);
+        break;
+      default:
+        throw new IOException("Unknown model format: " + modelType);

INS50 INS42 INS49 MOV21 INS10 INS49 MOV21 INS10 INS49 MOV21 INS10 INS49 MOV21 INS10 INS49 MOV53 INS45 INS45 INS45 INS45 DEL42 DEL42 DEL45 DEL32 DEL8 DEL42 DEL42 DEL45 DEL32 DEL8 DEL42 DEL42 DEL45 DEL32 DEL8 DEL42 DEL42 DEL45 DEL32 DEL8 DEL8 DEL25 DEL25 DEL25 DEL25