OPENNLP-226 CrossValidators should receive a list/one listener in constructor, the same way is done with evaluators

git-svn-id: https://svn.apache.org/repos/asf/incubator/opennlp/trunk@1160122 13f79535-47bb-0310-9956-ffa450edef68

+import opennlp.tools.util.TrainingParameters;
+    opennlp.tools.util.TrainingParameters mlParams = CmdLineUtil
+        .loadTrainingParameters(params.getParams(), false);
+    
-    ChunkerCrossValidator validator = new ChunkerCrossValidator(
-        params.getLang(), params.getCutoff(), params.getIterations());
-    
+    
+    if (mlParams == null) {
+      mlParams = new TrainingParameters();
+      mlParams.put(TrainingParameters.ALGORITHM_PARAM, "MAXENT");
+      mlParams.put(TrainingParameters.ITERATIONS_PARAM,
+          Integer.toString(params.getIterations()));
+      mlParams.put(TrainingParameters.CUTOFF_PARAM,
+          Integer.toString(params.getCutoff()));
+    }
+
+    ChunkerCrossValidator validator = new ChunkerCrossValidator(
+        params.getLang(), mlParams, listeners);
-      validator.evaluate(sampleStream, params.getFolds(), listeners);
+      validator.evaluate(sampleStream, params.getFolds());

INS26 INS40 MOV60 INS60 INS25 INS43 INS59 INS27 INS8 INS40 INS42 INS32 INS42 INS33 INS21 INS21 INS21 INS21 INS42 INS42 INS32 INS9 INS7 INS32 INS32 INS32 INS42 INS42 INS42 INS42 INS42 INS14 INS42 INS42 INS40 INS45 INS42 INS42 INS40 INS32 INS42 INS42 INS40 INS32 INS43 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 DEL42