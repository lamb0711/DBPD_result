- fix ant tasks
- disable cycle detection (warn instead)
- show version in dep trail

git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@192966 13f79535-47bb-0310-9956-ffa450edef68

+import java.util.LinkedList;
+                catch ( CyclicDependencyException e )
+                {
+                    // would like to throw this, but we have crappy stuff in the repo
+                    // no logger to use here either just now
+                    fireEvent( ResolutionListener.OMIT_FOR_CYCLE, listeners,
+                               new ResolutionNode( e.getArtifact(), child ) );
+                }
+                case ResolutionListener.OMIT_FOR_CYCLE:
+                    listener.omitForCycle( node.getArtifact() );
+                    break;
+        private final ResolutionNode parent;
+
+            this.parent = null;
+            this.parent = parent;
-                        throw new CyclicDependencyException( "The dependency is present in a cycle", a );
+                        throw new CyclicDependencyException( "A dependency has introduced a cycle", a );
-            List path = new ArrayList( parents );
-            path.add( artifact.getId() );
+            List path = new LinkedList();
+            ResolutionNode node = this;
+            while ( node != null )
+            {
+                path.add( 0, node.getArtifact().getId() );
+                node = node.parent;
+            }

INS26 INS40 INS23 INS83 INS83 INS43 INS59 INS42 INS42 INS21 INS21 INS60 INS61 MOV10 MOV10 INS7 INS7 INS43 INS59 INS27 INS8 INS49 INS21 INS10 INS22 INS33 INS22 INS42 INS42 INS42 INS52 INS42 INS33 MOV21 INS21 INS40 INS32 INS52 INS42 INS52 INS42 UPD43 INS7 INS12 INS42 INS42 INS32 UPD42 INS34 INS42 INS40 INS44 INS8 INS42 INS42 INS32 INS43 INS42 INS21 INS42 UPD42 MOV42 INS42 INS32 INS42 INS40 INS42 INS14 UPD45 INS43 INS32 INS42 INS42 INS42 INS42 DEL42