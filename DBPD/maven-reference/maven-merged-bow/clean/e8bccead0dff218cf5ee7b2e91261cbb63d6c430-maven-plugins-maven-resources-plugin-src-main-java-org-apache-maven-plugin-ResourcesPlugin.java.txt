o make sure we account for times when we have resources entries like:

  <resource>
    <directory>src/main/resource</directory>
  </resource>

  We want to pick up everything within the specified directory so the the
  implicit include is **/**.


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@162613 13f79535-47bb-0310-9956-ffa450edef68

+        System.out.println( "outputDirectory = " + outputDirectory );
+
+        System.out.println( "resources = " + resources );
+
+            // If we only have a directory then we want to include
+            // everything we can find within that path.
+
+            String includes;
+
+            if ( resource.getIncludes().size() > 0 )
+            {
+                includes = listToString( resource.getIncludes() );
+            }
+            else
+            {
+                includes = "**/**";
+            }
+
-                                                 listToString( resource.getIncludes() ),
+                                                 includes,
-                ;

INS21 INS21 INS32 INS32 INS40 INS42 INS27 INS40 INS42 INS27 INS60 INS25 INS45 INS42 INS45 INS42 INS43 INS59 INS27 INS8 INS8 INS42 INS42 INS32 INS34 INS21 INS21 INS32 INS42 INS7 INS7 INS42 INS42 INS42 INS42 MOV32 INS42 INS45 DEL20