[MNG-3168] Correct error message when project descriptor not found

git-svn-id: https://svn.apache.org/repos/asf/maven/maven-3/trunk@902614 13f79535-47bb-0310-9956-ffa450edef68

-            request.setPom( resolveFile( new File( alternatePomFile ), workingDirectory ) );
+            File pom = resolveFile( new File( alternatePomFile ), workingDirectory );
+
+            request.setPom( pom );
-        else if ( request.getPom() != null && !request.getPom().isAbsolute() )
+        else
-            request.setPom( request.getPom().getAbsoluteFile() );
+            File pom = modelProcessor.locatePom( baseDirectory );
+
+            if ( pom.isFile() )
+            {
+                request.setPom( pom );
+            }
-        else if ( ( request.getPom() == null ) && ( request.getBaseDirectory() != null ) )
-        {
-            File pom = modelProcessor.locatePom( new File( request.getBaseDirectory() ) );
-
-            request.setPom( pom );
-        }
-        // TODO: Is this correct?
-        else if ( request.getBaseDirectory() == null )
-        {
-            request.setBaseDirectory( new File( System.getProperty( "user.dir" ) ) );
-        }
-            return new File( workingDirectory, file.getPath() );
+            return new File( workingDirectory, file.getPath() ).getAbsoluteFile();

INS25 MOV27 MOV8 INS8 INS60 MOV21 INS60 INS25 MOV43 INS59 MOV32 MOV43 INS59 INS32 MOV8 INS42 MOV32 MOV42 MOV42 INS42 INS32 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 UPD42 MOV42 INS42 INS32 UPD42 MOV42 MOV14 INS42 DEL42 DEL32 DEL42 DEL32 DEL32 DEL33 DEL27 DEL42 DEL32 DEL32 DEL38 DEL27 DEL25 DEL25 DEL42 DEL42 DEL32 DEL33 DEL27 DEL36 DEL42 DEL42 DEL32 DEL33 DEL27 DEL36 DEL27 DEL42 DEL42 DEL42 DEL42 DEL42 DEL32 DEL14 DEL32 DEL59 DEL60 DEL8 DEL42 DEL42 DEL32 DEL33 DEL27 DEL42 DEL42 DEL42 DEL43 DEL42 DEL42 DEL45 DEL32 DEL14 DEL32 DEL21 DEL8 DEL25 DEL25