[MNG-6855] Simplify some code

Closes #279

-            Validate.isTrue( size >= 0L && progressedSize <= size || size < 0L,
+            Validate.isTrue( size < 0L || progressedSize <= size,

UPD27 MOV27 UPD27 MOV42 MOV34 DEL27