Fix: IT 3746, 102, 0043 - Merged out some previous changes to PluginParameterExpressionEvaluator.

git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@704797 13f79535-47bb-0310-9956-ffa450edef68

-        
+
-        
+
-                
-            // We will attempt to get nab a system property as a way to specify a
-            // parameter to a plugins. My particular case here is allowing the surefire
-            // plugin to run a single test so I want to specify that class on the cli
-            // as a parameter.
+            // The CLI should win for defining properties
-            if ( properties != null )
+            if ( ( value == null ) && ( properties != null ) )
-                value = properties.getProperty( expression );                
-            }                    
-            
-            if ( project != null && project.getProperties() != null )
+                // We will attempt to get nab a system property as a way to specify a
+                // parameter to a plugins. My particular case here is allowing the surefire
+                // plugin to run a single test so I want to specify that class on the cli
+                // as a parameter.
+
+                value = properties.getProperty( expression );
+            }
+
+            if ( ( value == null ) && ( ( project != null ) && ( project.getProperties() != null ) ) )
+

INS27 INS27 INS36 INS36 INS36 INS36 INS27 MOV27 INS27 MOV27 INS42 INS33 INS42 INS33 INS36 INS36 MOV27 MOV27