Code cleanup - Various code simplifications (tests, loops, …)

-            if ( !compareObjects( getGoal(), other.getGoal() ) )
-            {
-                return false;
-            }
+            return compareObjects( getGoal(), other.getGoal() );
-            return true;

MOV41 MOV32 DEL9 DEL38 DEL8 DEL25 DEL9 DEL41