add a null check in the case where there is no parent and no version - validation will pick that up later


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@178603 13f79535-47bb-0310-9956-ffa450edef68

-        // currentVersion
+        // version
+        // TODO: I think according to the latest design docs, we don't want to inherit version at all
-            child.setVersion( child.getParent().getVersion() );
+            
+            if ( child.getParent() != null )
+            {
+                child.setVersion( child.getParent().getVersion() );
+            }
-}
+}

INS8 INS25 INS27 MOV8 INS32 INS33 INS42 INS42