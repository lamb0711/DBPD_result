Fix for urls being empty in the POM and causing an out of bounds exception

-            if ( path.length() > 0 )
+            if ( url.length() > 0 && path.length() > 0 )

INS27 INS27 MOV27 INS32 INS34 INS42 INS42