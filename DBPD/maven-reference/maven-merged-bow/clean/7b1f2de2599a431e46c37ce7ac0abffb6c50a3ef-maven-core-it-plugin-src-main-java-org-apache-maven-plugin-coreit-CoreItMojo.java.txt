can't be half and half! move to new plugin model


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@163644 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.maven.plugin.PluginExecutionRequest;
-import org.apache.maven.plugin.PluginExecutionResponse;
+import org.apache.maven.plugin.PluginExecutionException;
+import java.io.IOException;
-    public void execute( PluginExecutionRequest request, PluginExecutionResponse response )
-        throws Exception
+    public void execute()
+        throws PluginExecutionException
-        throws Exception
+        throws PluginExecutionException
-        if ( !dir.exists() )
+        try
-            dir.mkdirs();
+             if ( !dir.exists() )
+             {
+                 dir.mkdirs();
+             }
+             
+             File touch = new File( dir, file );
+     
+             FileWriter w = new FileWriter( touch );
+             
+             w.write( file );
+             
+             w.close();
-        
-        File touch = new File( dir, file );
-
-        FileWriter w = new FileWriter( touch );
-        
-        w.write( file );
-        
-        w.close();
+        catch ( IOException e )
+        {
+            throw new PluginExecutionException( "Error touching file", e );
+        }

UPD40 UPD40 INS43 INS43 INS8 UPD42 MOV42 INS42 INS54 MOV8 INS12 INS44 INS8 INS43 INS42 INS53 UPD42 MOV42 INS14 INS43 INS45 INS42 INS42 DEL43 DEL42 DEL44 DEL42 DEL43 DEL42 DEL44 DEL42 DEL43 DEL43