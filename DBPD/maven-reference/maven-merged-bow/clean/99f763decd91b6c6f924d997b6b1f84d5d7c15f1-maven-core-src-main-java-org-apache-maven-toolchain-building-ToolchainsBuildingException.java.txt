Add junit tests for DefaultToolchainsBuilder and ToolchainsBuildingException

-            writer.print( " @ " );
-            writer.println( problem.getLocation() );
+            String location = problem.getLocation();
+            if ( !location.isEmpty() )
+            {
+                writer.print( " @ " );
+                writer.print( location );
+            }
+            writer.println();

INS60 INS25 INS43 INS59 INS38 INS8 INS42 INS42 MOV32 INS32 MOV21 INS21 INS42 INS42 INS32 INS42 INS42 INS42