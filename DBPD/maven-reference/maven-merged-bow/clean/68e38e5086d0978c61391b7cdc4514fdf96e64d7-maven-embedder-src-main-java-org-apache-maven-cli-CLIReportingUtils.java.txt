Make it possible to run unit tests from an IDE

+import org.apache.commons.lang3.StringUtils;
-            if ( timestamp != null )
+            if ( StringUtils.isNotBlank( timestamp ) )

INS26 INS40 INS32 INS42 INS42 INS42 DEL42 DEL33 DEL27