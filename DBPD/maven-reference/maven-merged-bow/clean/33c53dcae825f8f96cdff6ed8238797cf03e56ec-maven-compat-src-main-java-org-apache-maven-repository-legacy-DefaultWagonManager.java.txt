Safer deletes

-                    temp.delete();
+                    if ( !temp.delete() )
+                    {
+                        temp.deleteOnExit();
+                    }
-            try
+            if ( !file.delete() )
-                file.delete();
-            }
-            catch ( Exception e )
-            {
-                logger.warn( "skip failed to delete temporary file : " + file.getAbsolutePath() + " , message "
-                    + e.getMessage() );
+                logger.warn( "skip failed to delete temporary file : " + file.getAbsolutePath() );
+                file.deleteOnExit();
-                    checksumFile.delete();
+                    checksumFile.delete(); // ignore if failed as we will overwrite
-                tempChecksumFile.delete();
+                if ( !tempChecksumFile.delete() )
+                {
+                    tempChecksumFile.deleteOnExit();
+                }

INS25 INS38 MOV8 MOV32 INS21 INS25 INS32 INS38 INS8 INS25 INS42 INS42 MOV32 MOV21 INS38 INS8 INS32 MOV32 MOV21 INS42 INS42 INS32 INS42 INS42 DEL45 DEL42 DEL42 DEL32 DEL21 DEL8 DEL42 DEL43 DEL42 DEL44 DEL12 DEL54