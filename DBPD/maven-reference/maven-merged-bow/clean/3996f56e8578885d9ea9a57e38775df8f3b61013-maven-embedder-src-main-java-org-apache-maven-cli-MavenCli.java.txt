[MNG-4824] multiple failures need additional whitespace


git-svn-id: https://svn.apache.org/repos/asf/maven/maven-3/trunk@998878 13f79535-47bb-0310-9956-ffa450edef68

-        String msg = indent + summary.getMessage();
+        String msg = summary.getMessage();
-                msg += '\n' + indent + "-> " + referenceKey;
+                msg += "\n-> " + referenceKey;
-        if ( showErrors || ( summary.getException() instanceof InternalErrorException ) )
+        String[] lines = msg.split( "(\r\n)|(\r)|(\n)" );
+
+        for ( int i = 0; i < lines.length; i++ )
-            logger.error( msg, summary.getException() );
-        }
-        else
-        {
-            logger.error( msg );
+            String line = indent + lines[i].trim();
+
+            if ( i == lines.length - 1 && ( showErrors || ( summary.getException() instanceof InternalErrorException ) ) )
+            {
+                logger.error( line, summary.getException() );
+            }
+            else
+            {
+                logger.error( line );
+            }

INS60 INS24 INS5 INS59 INS58 INS27 INS37 INS8 MOV32 INS43 INS85 INS42 INS32 INS39 INS59 INS42 INS40 INS42 INS60 MOV25 INS42 INS42 INS42 INS45 INS42 INS34 INS43 INS59 INS27 INS42 INS42 INS27 INS27 INS36 INS42 INS32 INS42 INS27 MOV27 INS2 INS42 INS40 INS34 UPD42 UPD42 UPD45 INS42 INS42 DEL42 DEL27 DEL13 DEL42