[MNG-6171] always show WARNING about usage of a non threadsafe plugin
-            if ( !unsafePlugins.isEmpty()  && logger.isDebugEnabled() )
+            if ( !unsafePlugins.isEmpty() )

MOV38 DEL42 DEL42 DEL32 DEL27