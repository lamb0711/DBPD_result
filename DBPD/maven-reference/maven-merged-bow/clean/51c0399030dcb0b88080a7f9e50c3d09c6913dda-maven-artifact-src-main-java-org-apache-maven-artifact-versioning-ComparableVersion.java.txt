[MNG-6967] Improve the command line output from maven-artifact.

+
+        /**
+         * Return the contents in the same format that is used when you call toString() on a List.
+         */
+        private String toListString()
+        {
+            StringBuilder buffer = new StringBuilder();
+            buffer.append( "[" );
+            for ( Item item : this )
+            {
+                if ( buffer.length() > 1 )
+                {
+                    buffer.append( ", " );
+                }
+                if ( item instanceof ListItem )
+                {
+                    buffer.append( ( (ListItem ) item ).toListString() );
+                }
+                else
+                {
+                    buffer.append( item );
+                }
+            }
+            buffer.append( "]" );
+            return buffer.toString();
+        }
-        System.out.println( "Display parameters as parsed by Maven (in canonical form) and comparison result:" );
+        System.out.println( "Display parameters as parsed by Maven (in canonical form and as a list of tokens) and"
+                                + " comparison result:" );
-            System.out.println( ( i++ ) + ". " + version + " == " + c.getCanonical() );
+            System.out.println( ( i++ ) + ". " + version + " -> " + c.getCanonical() + "   " + c.items.toListString() );

INS31 INS29 INS83 INS43 INS42 INS8 INS65 INS42 INS60 INS21 INS70 INS21 INS41 INS66 INS43 INS59 INS32 INS44 INS52 INS8 INS32 INS32 INS27 INS42 INS42 INS14 INS42 INS42 INS45 INS43 INS42 INS25 INS25 INS42 INS42 INS45 INS42 INS42 INS45 INS45 INS43 INS42 INS27 INS8 INS62 INS8 INS8 MOV45 INS42 INS32 INS34 INS21 INS42 INS43 INS21 INS21 INS45 UPD45 INS32 INS42 INS42 INS32 INS42 INS32 INS32 INS40 INS42 INS42 INS42 INS45 INS42 INS42 INS32 INS42 INS42 INS42 INS36 INS42 INS11 INS43 INS42 INS42 DEL45