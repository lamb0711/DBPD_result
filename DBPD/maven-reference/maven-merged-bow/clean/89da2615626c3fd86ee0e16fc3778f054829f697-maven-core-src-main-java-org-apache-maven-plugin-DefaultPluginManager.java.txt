put back deprecation message


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@188649 13f79535-47bb-0310-9956-ffa450edef68

-            // Make sure the parameter is either editable/configurable, or else is NOT specified in the POM
-            if ( !parameter.isEditable() && value != null )
+            if ( value != null )
-                StringBuffer errorMessage = new StringBuffer().append( "ERROR: Cannot override read-only parameter: " );
-                errorMessage.append( key );
-                errorMessage.append( " in goal: " ).append( goal.getFullGoalName() );
+                // Make sure the parameter is either editable/configurable, or else is NOT specified in the POM
+                if ( !parameter.isEditable() )
+                {
+                    StringBuffer errorMessage = new StringBuffer().append(
+                        "ERROR: Cannot override read-only parameter: " );
+                    errorMessage.append( key );
+                    errorMessage.append( " in goal: " ).append( goal.getFullGoalName() );
-                throw new PluginConfigurationException( errorMessage.toString() );
+                    throw new PluginConfigurationException( errorMessage.toString() );
+                }
+
+                String deprecated = parameter.getDeprecated();
+                if ( StringUtils.isNotEmpty( deprecated ) )
+                {
+                    getLogger().warn( "DEPRECATED [" + parameter.getName() + "]: " + deprecated );
+                }

INS25 MOV27 INS8 MOV25 INS60 INS25 MOV38 INS43 INS59 INS32 INS8 INS42 INS42 INS32 INS42 INS42 INS42 INS21 INS42 INS42 INS32 INS32 INS42 INS27 INS42 INS45 INS32 INS45 INS42 INS42 INS42 DEL27