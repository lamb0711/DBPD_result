o rolling back to r617947


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@618044 13f79535-47bb-0310-9956-ffa450edef68

-import org.apache.maven.artifact.handler.manager.ArtifactHandlerManager;
-    // This should not be in here and neither should any of artifact listing methods that pick off artifacts
-    // based on their scope as that forces MavenProject to know about all scopes that are available which
-    // makes the addition of new scopes unscalable. Another component should take as input a MavenProject and
-    // spit out the list of artifacts based on scope. jvz.
-    private ArtifactHandlerManager artifactHandlerManager;
-
-    // This is temporary until we get the scope related elements out of here. jvz.
-    public void setArtifactHandlerManager( ArtifactHandlerManager artifactHandlerManager )
-    {
-        this.artifactHandlerManager = artifactHandlerManager;
-    }
-
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
-            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
+            if ( a.getArtifactHandler().isAddedToClasspath() )
+    /**
+     * Default toString
+     */
+    public String toString()
+    {
+        StringBuffer sb = new StringBuffer(30);
+        sb.append( "MavenProject: " );
+        sb.append( this.getGroupId() );
+        sb.append( ":" );
+        sb.append( this.getArtifactId() );
+        sb.append( ":" );
+        sb.append( this.getVersion() );
+        sb.append( " @ " );
+        
+        try 
+        {
+            sb.append( this.getFile().getPath() );
+        }
+        catch (NullPointerException e)
+        {
+            //don't log it.
+        }
+        
+        return sb.toString();        
+    }
+    

INS31 INS29 INS83 INS43 INS42 INS8 INS65 INS42 INS60 INS21 INS21 INS21 INS21 INS21 INS21 INS21 INS54 INS41 INS66 INS43 INS59 INS32 INS32 INS32 INS32 INS32 INS32 INS32 INS8 INS12 INS32 UPD42 MOV42 INS42 INS14 INS42 INS42 INS45 INS42 INS42 INS32 INS42 INS42 INS45 INS42 INS42 INS32 INS42 INS42 INS45 INS42 INS42 INS32 INS42 INS42 INS45 MOV21 INS44 INS8 INS42 INS42 INS43 INS34 INS52 INS42 INS52 INS42 INS52 INS42 INS32 INS43 INS42 MOV32 MOV32 MOV32 MOV32 MOV32 MOV32 MOV32 MOV32 INS42 INS42 INS42 INS32 INS42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 INS32 INS42 INS52 INS42 DEL40 DEL26 DEL83 DEL43 DEL42 DEL59 DEL23 DEL83 DEL39 DEL42 DEL42 DEL43 DEL42 DEL44 DEL8 DEL31 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL52 DEL42 DEL22 DEL42 DEL7