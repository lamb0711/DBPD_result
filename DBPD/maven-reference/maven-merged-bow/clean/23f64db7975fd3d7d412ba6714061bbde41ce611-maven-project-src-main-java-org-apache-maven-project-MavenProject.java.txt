Removed code duplication

git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@421968 13f79535-47bb-0310-9956-ffa450edef68

-                    String refId = getProjectReferenceId( a.getGroupId(), a.getArtifactId(), a.getVersion() );
-                    MavenProject project = (MavenProject) projectReferences.get( refId );
-                    if ( project != null )
-                    {
-                        list.add( project.getBuild().getOutputDirectory() );
-                    }
-                    else
-                    {
-                        File file = a.getFile();
-                        if ( file == null )
-                        {
-                            throw new DependencyResolutionRequiredException( a );
-                        }
-                        list.add( file.getPath() );
-                    }
+                    addArtifactPath( a, list );
-                    String refId = getProjectReferenceId( a.getGroupId(), a.getArtifactId(), a.getVersion() );
-                    MavenProject project = (MavenProject) projectReferences.get( refId );
-                    if ( project != null )
-                    {
-                        list.add( project.getBuild().getOutputDirectory() );
-                    }
-                    else
-                    {
-                        File file = a.getFile();
-                        if ( file == null )
-                        {
-                            throw new DependencyResolutionRequiredException( a );
-                        }
-                        list.add( file.getPath() );
-                    }
+                    addArtifactPath( a, list );
+    
+    private void addArtifactPath(Artifact a, List list) throws DependencyResolutionRequiredException
+    {
+        String refId = getProjectReferenceId( a.getGroupId(), a.getArtifactId(), a.getVersion() );
+        MavenProject project = (MavenProject) projectReferences.get( refId );
+        if ( project != null )
+        {
+            list.add( project.getBuild().getOutputDirectory() );
+        }
+        else
+        {
+            File file = a.getFile();
+            if ( file == null )
+            {
+                throw new DependencyResolutionRequiredException( a );
+            }
+            list.add( file.getPath() );
+        }
+    }

MOV31 INS31 INS83 MOV43 INS42 MOV43 MOV8 UPD83 INS39 UPD42 INS44 INS44 INS43 MOV8 INS43 INS42 INS43 INS42 INS42 INS42 INS42 MOV25 MOV32 MOV27 INS8 INS21 INS32 UPD42 UPD42 UPD42 MOV42 INS42 INS42 INS42 DEL42 DEL43 DEL42 DEL42 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL42 DEL42 DEL32 DEL32 DEL59 DEL60 DEL42 DEL43 DEL42 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL11 DEL59 DEL60 DEL42 DEL33 DEL27 DEL42 DEL32 DEL42 DEL32 DEL42 DEL43 DEL42 DEL42 DEL42 DEL32 DEL59 DEL60 DEL42 DEL33 DEL27 DEL42 DEL43 DEL42 DEL14 DEL53 DEL8 DEL25 DEL42 DEL42 DEL42 DEL42 DEL32 DEL32 DEL21 DEL8 DEL8 DEL25