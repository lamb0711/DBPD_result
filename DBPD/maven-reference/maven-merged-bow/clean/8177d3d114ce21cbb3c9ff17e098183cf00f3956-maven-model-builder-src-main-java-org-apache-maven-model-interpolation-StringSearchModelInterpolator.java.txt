[MNG-6819] Fix also NPE in StringSearchModelInterpolator


-                if ( !interpolated.equals( value ) )
+                if ( interpolated != null && !interpolated.equals( value ) )
-                        if ( !interpolated.equals( value ) )
+                        if ( interpolated != null && !interpolated.equals( value ) )

INS27 INS27 MOV38 INS42 INS33 INS27 INS27 MOV38 INS42 INS33