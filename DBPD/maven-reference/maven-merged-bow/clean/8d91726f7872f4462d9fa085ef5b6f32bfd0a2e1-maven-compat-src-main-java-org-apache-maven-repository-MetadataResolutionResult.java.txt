[MNG-5934] String handling issues identified by PMD

-        StringBuilder sb = new StringBuilder();
+        if ( artifacts == null )
+            return "";
-        if ( artifacts != null )
+        StringBuilder sb = new StringBuilder( 256 );
+        int i = 1;
+        sb.append( "---------\n" );
+        sb.append( artifacts.size() ).append( '\n' );
+        for ( Artifact a : artifacts )
-            int i = 1;
-            sb.append( "---------" ).append( "\n" );
-            sb.append( artifacts.size() ).append( "\n" );
-            for ( Artifact a : artifacts )
-            {
-                sb.append( i ).append( " " ).append( a ).append( "\n" );
-                i++;
-            }
-            sb.append( "---------" ).append( "\n" );
+            sb.append( i ).append( ' ' ).append( a ).append( '\n' );
+            i++;
-
+        sb.append( "---------\n" );

MOV8 INS25 MOV60 INS21 MOV41 INS27 INS41 MOV43 INS32 INS32 INS42 INS33 INS45 MOV42 MOV42 UPD45 MOV45 INS13 MOV42 MOV42 UPD45 MOV45 UPD42 MOV42 MOV43 INS34 INS13 INS13 DEL32 DEL42 DEL45 DEL32 DEL45 DEL45 DEL45 DEL32 DEL45 DEL32 DEL21 DEL42 DEL33 DEL27 DEL25 DEL8