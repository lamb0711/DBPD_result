PR: MNG-122
cleanup formatting

git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@320812 13f79535-47bb-0310-9956-ffa450edef68

-                line();
-
-
-                line();
+
+        if ( !showErrors )
+        {
+            getLogger().info( "For more information, run Maven with the -e switch" );
+
+            line();
+        }
+
+
+        if ( !showErrors )
+        {
+            getLogger().info( "For more information, run Maven with the -e switch" );
+
+            line();
+        }
+
+        if ( !showErrors )
+        {
+            getLogger().info( "For more information, run Maven with the -e switch" );
+
+            line();
+        }
-            getLogger().info( "Trace", t );
+            getLogger().debug( "Trace", t );
-        else
-        {
-            getLogger().info( "For more information, run Maven with the -e switch" );
-        }
+            getLogger().info( "" );
+            getLogger().info( "" );
+
-
-            getLogger().info( "" );
-            getLogger().info( "" );
+            line();

INS25 INS25 INS25 INS38 INS8 INS38 INS8 INS38 INS8 MOV21 MOV21 MOV21 MOV21 INS42 INS21 MOV21 INS42 INS21 INS21 INS42 MOV21 INS21 MOV21 INS32 INS32 INS32 INS32 INS32 INS42 INS45 INS32 INS42 INS45 INS42 INS42 UPD42 INS42 INS42 DEL8