Safer deletes

-                            file.delete();
+                            if ( !file.delete() )
+                            {
+                                // sleep for 10ms just in case this is windows holding a file lock
+                                try
+                                {
+                                    Thread.sleep( 10 );
+                                }
+                                catch ( InterruptedException ie )
+                                {
+                                    // ignore
+                                }
+                                file.delete(); // if this fails, forget about it
+                            }
-                file.delete();
+                if ( !file.delete() )
+                {
+                    // sleep for 10ms just in case this is windows holding a file lock
+                    try
+                    {
+                        Thread.sleep( 10 );
+                    }
+                    catch ( InterruptedException ie )
+                    {
+                        // ignore
+                    }
+                    file.delete(); // if this fails, forget about it
+                }

INS25 MOV32 INS8 MOV25 INS38 INS8 INS32 INS54 MOV21 INS42 INS42 INS8 INS12 INS25 INS21 INS44 INS8 MOV32 INS8 INS32 INS43 INS42 MOV25 INS42 INS42 INS34 INS42 INS38 INS8 INS32 INS54 MOV21 INS42 INS42 INS8 INS12 INS21 INS44 INS8 INS32 INS43 INS42 INS42 INS42 INS34 INS42 DEL8 DEL8