[MNG-6855] Simplify some code

Closes #279

-        else if ( !source.equals( other.source ) )
+        else
-            return false;
+            return source.equals( other.source );
-        return true;

MOV8 MOV32 DEL9 DEL38 DEL25 DEL9 DEL41