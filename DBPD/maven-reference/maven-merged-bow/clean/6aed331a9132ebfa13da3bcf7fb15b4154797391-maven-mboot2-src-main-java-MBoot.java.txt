mboot needed to be smarter about its parsing - was picking up <version> from anywhere
removed unused "local" parsing


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@163426 13f79535-47bb-0310-9956-ffa450edef68

+        int depth = 0;
+
-        private Local local = new Local();
-
-        private boolean insideLocal = false;
-
-        public Local getLocal()
-        {
-            return local;
-        }
-
-            else if ( rawName.equals( "repository" ) && !insideLocal )
+            else if ( rawName.equals( "repository" ) )
-            else if ( rawName.equals( "local" ) )
-            {
-                insideLocal = true;
-            }
+            depth ++;
-            else if ( rawName.equals( "local" ) )
-            {
-                insideLocal = false;
-            }
-            else if ( rawName.equals( "artifactId" ) )
+            else if ( depth == 2 )
-                artifactId = getBodyText();
-            }
-            else if ( rawName.equals( "version" ) )
-            {
-                version = getBodyText();
-            }
-            else if ( rawName.equals( "groupId" ) )
-            {
-                groupId = getBodyText();
-            }
-            else if ( rawName.equals( "type" ) )
-            {
-                type = getBodyText();
-            }
-            else if ( rawName.equals( "repository" ) )
-            {
-                if ( insideLocal )
+                if ( rawName.equals( "artifactId" ) )
-                    local.repository = getBodyText();
+                    artifactId = getBodyText();
-                else
+                else if ( rawName.equals( "version" ) )
+                {
+                    version = getBodyText();
+                }
+                else if ( rawName.equals( "groupId" ) )
+                {
+                    groupId = getBodyText();
+                }
+                else if ( rawName.equals( "type" ) )
+                {
+                    type = getBodyText();
+                }
+                else if ( rawName.equals( "repository" ) )
+
+            depth --;
-
-    public static class Local
-        implements Serializable
-    {
-        private String repository;
-
-        private String online;
-
-        public String getRepository()
-        {
-            return this.repository;
-        }
-
-        public void setRepository( String repository )
-        {
-            this.repository = repository;
-        }
-
-        public String getOnline()
-        {
-            return this.online;
-        }
-
-        public void setOnline( String online )
-        {
-            this.online = online;
-        }
-    }

MOV23 UPD39 UPD42 INS34 MOV25 INS21 MOV25 MOV21 MOV32 MOV8 INS37 MOV32 MOV8 INS37 MOV32 MOV8 INS42 MOV32 MOV8 INS42 MOV32 MOV8 MOV32 MOV8 UPD42 MOV8 INS42 MOV8 MOV25 INS42 MOV8 MOV25 MOV27 MOV8 INS27 INS42 INS34 INS25 MOV32 MOV8 INS25 MOV32 MOV8 INS25 MOV32 MOV8 INS25 MOV32 MOV8 MOV25 MOV32 DEL83 DEL9 DEL83 DEL42 DEL43 DEL42 DEL42 DEL43 DEL14 DEL59 DEL23 DEL83 DEL42 DEL43 DEL42 DEL42 DEL41 DEL8 DEL31 DEL42 DEL38 DEL27 DEL42 DEL42 DEL45 DEL32 DEL42 DEL9 DEL7 DEL21 DEL8 DEL25 DEL42 DEL42 DEL42 DEL40 DEL42 DEL32 DEL7 DEL21 DEL8 DEL25 DEL25 DEL42 DEL42 DEL45 DEL32 DEL8 DEL25 DEL25 DEL25 DEL42 DEL9 DEL7 DEL83 DEL83 DEL42 DEL42 DEL43 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL83 DEL42 DEL43 DEL42 DEL59 DEL23 DEL83 DEL42 DEL43 DEL42 DEL52 DEL42 DEL22 DEL41 DEL8 DEL31 DEL83 DEL39 DEL42 DEL42 DEL43 DEL42 DEL44 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL8 DEL31 DEL83 DEL42 DEL43 DEL42 DEL52 DEL42 DEL22 DEL41 DEL8 DEL31 DEL83 DEL39 DEL42 DEL42 DEL43 DEL42 DEL44 DEL52 DEL42 DEL22 DEL42 DEL7 DEL21 DEL8 DEL31 DEL55