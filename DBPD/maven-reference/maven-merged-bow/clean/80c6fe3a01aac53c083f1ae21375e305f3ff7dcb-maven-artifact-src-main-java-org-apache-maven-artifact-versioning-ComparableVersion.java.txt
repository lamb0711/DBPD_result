[MNG-6643] - Version comparison CLI does not work anymore

-import org.apache.commons.lang3.StringUtils;
-
-        String strippedBuf = StringUtils.stripStart( buf, "0" );
-        if ( strippedBuf.isEmpty() )
+        if ( buf == null || buf.isEmpty() )
-        return strippedBuf;
+        for ( int i = 0; i < buf.length(); ++i )
+        {
+            char c = buf.charAt( i );
+            if ( c != '0' )
+            {
+                return buf.substring( i );
+            }
+        }
+        return buf;

INS25 INS24 INS27 MOV8 INS58 INS27 INS38 INS8 UPD42 INS27 INS32 INS39 INS59 INS42 INS32 INS42 INS60 MOV25 INS42 INS33 INS42 INS42 INS42 INS34 INS42 INS42 INS39 MOV59 INS27 INS8 UPD42 INS42 INS13 INS41 UPD42 UPD42 UPD42 INS32 INS42 UPD42 MOV42 UPD42 MOV42 DEL40 DEL26 DEL45 DEL32 DEL42 DEL43 DEL60