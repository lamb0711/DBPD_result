o updating for the changes I just made to maven-artifact


git-svn-id: https://svn.apache.org/repos/asf/maven/components/trunk@617948 13f79535-47bb-0310-9956-ffa450edef68

+import org.apache.maven.artifact.handler.manager.ArtifactHandlerManager;
+    // This should not be in here and neither should any of artifact listing methods that pick off artifacts
+    // based on their scope as that forces MavenProject to know about all scopes that are available which
+    // makes the addition of new scopes unscalable. Another component should take as input a MavenProject and
+    // spit out the list of artifacts based on scope. jvz.
+    private ArtifactHandlerManager artifactHandlerManager;
+
+    // This is temporary until we get the scope related elements out of here. jvz.
+    public void setArtifactHandlerManager( ArtifactHandlerManager artifactHandlerManager )
+    {
+        this.artifactHandlerManager = artifactHandlerManager;
+    }
+
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-            if ( a.getArtifactHandler().isAddedToClasspath() )
+            if ( artifactHandlerManager.getArtifactHandler( a.getType() ).isAddedToClasspath() )
-    /**
-     * Default toString
-     */
-    public String toString()
-    {
-        StringBuffer sb = new StringBuffer(30);
-        sb.append( "MavenProject: " );
-        sb.append( this.getGroupId() );
-        sb.append( ":" );
-        sb.append( this.getArtifactId() );
-        sb.append( ":" );
-        sb.append( this.getVersion() );
-        sb.append( " @ " );
-        
-        try 
-        {
-            sb.append( this.getFile().getPath() );
-        }
-        catch (NullPointerException e)
-        {
-            //don't log it.
-        }
-        
-        return sb.toString();        
-    }
-    

INS26 INS40 INS23 INS31 INS83 INS43 INS59 INS83 INS39 INS42 INS44 INS8 UPD42 MOV42 INS42 INS43 INS42 MOV21 INS42 INS7 INS22 INS42 INS52 INS42 INS32 INS32 INS32 INS32 INS32 INS32 INS32 INS32 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 INS42 MOV32 INS42 INS42 MOV32 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 UPD42 DEL42 DEL42 DEL52 DEL42 DEL32 DEL42 DEL32 DEL32 DEL66 DEL65 DEL29 DEL83 DEL42 DEL43 DEL42 DEL43 DEL42 DEL42 DEL43 DEL34 DEL14 DEL59 DEL60 DEL42 DEL42 DEL45 DEL32 DEL21 DEL42 DEL42 DEL52 DEL42 DEL32 DEL32 DEL21 DEL42 DEL42 DEL45 DEL32 DEL21 DEL42 DEL42 DEL52 DEL42 DEL32 DEL32 DEL21 DEL42 DEL42 DEL45 DEL32 DEL21 DEL42 DEL42 DEL52 DEL42 DEL32 DEL32 DEL21 DEL42 DEL42 DEL45 DEL32 DEL21 DEL8 DEL42 DEL43 DEL42 DEL44 DEL8 DEL12 DEL54 DEL42 DEL42 DEL32 DEL41 DEL8 DEL31