code formatting
-            messageBuffer.append( "Inside the definition for plugin \'");
+            messageBuffer.append( "Inside the definition for plugin \'" );
-            if( isArray || isCollection )
+            if ( isArray || isCollection )
-                messageBuffer.append(  '\n' );
+                messageBuffer.append( '\n' );
-                messageBuffer.append(  '\n' );
+                messageBuffer.append( '\n' );
-                messageBuffer.append(  '\n' );
+                messageBuffer.append( '\n' );
-            if( isArray || isCollection )
+            if ( isArray || isCollection )
