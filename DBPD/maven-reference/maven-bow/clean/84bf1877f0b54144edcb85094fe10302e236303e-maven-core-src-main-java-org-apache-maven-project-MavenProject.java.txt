[MNG-6700] Equal compile source roots are added multiple times

This closes #266

+                else if ( ".".equals( path ) )
+                {
+                    path = getBasedir().getAbsolutePath();
+                }
