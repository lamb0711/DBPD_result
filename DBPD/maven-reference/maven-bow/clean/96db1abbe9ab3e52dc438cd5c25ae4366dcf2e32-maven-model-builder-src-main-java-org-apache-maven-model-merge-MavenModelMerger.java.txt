Code cleanup - Maven requires Java 5+ : Remove unnecessary boxing

-                indices.add( Integer.valueOf( i ) );
+                indices.add( i );
-                    indices.add( Integer.valueOf( ~i ) );
+                    indices.add( ~i );
