code formatting
-        for ( ProfileActivator activator : activators ) {
-            if ( activator.presentInConfig( profile, context, problems ) ) {
+        for ( ProfileActivator activator : activators )
+        {
+            if ( activator.presentInConfig( profile, context, problems ) )
+            {
